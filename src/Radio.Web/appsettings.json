{
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://0.0.0.0:5002"
      }
    }
  },
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ApiBaseUrl": "http://localhost:5000",
  "RotaryPhone": {
    "HubUrl": "http://radio:5004/hub",
    "ApiBaseUrl": "http://radio:5004",
    "Gv": {
      "SendEnabled": false,
      "MarkReadEnabled": false,
      "StatusPollSeconds": 10,
      "AuthKey": ""
    }
  },
  "Devices": {
    "Aliases": {
      "CABLE Input (VB-Audio Virtual Cable)": "VB-Audio Cable In",
      "CABLE In 16ch (VB-Audio Virtual Cable)": "VB-Audio Cable 16ch",
      "Realtek Digital Output (Realtek USB Audio)": "Realtek USB \u00b7 S/PDIF"
    }
  },
  "Database": {
    "RootPath": "./data",
    "ConfigurationSubdirectory": "config",
    "ConfigurationFileName": "configuration.db"
  },
  "DataProtection": {
    "//": "Key ring backing Blazor Server's interactive-component markers and antiforgery tokens. Explicit + deploy-safe: it lives under the persistent data root (Deploy-ToLinux.ps1's rsync --delete only wipes api/ and web/) and is derived without reference to HOME, so radio-web.service's ProtectHome=true cannot strand it in a read-only /home. Resolves relative to the process working directory -> /opt/radio-console/data/keys-web on the box, which is inside the unit's ReadWritePaths. Deliberately separate from the API's ./data/keys so the secrets ring holds only API-created keys. See design/plans/SECRET-KEYRING-INVESTIGATION.md.",
    "KeysPath": "./data/keys-web"
  },
  "Serilog": {
    "//": "Read by Program.cs via ReadFrom.Configuration. Before LOG-1 the logger hardcoded MinimumLevel.Debug() and never called ReadFrom.Configuration, so this block did not exist and the Logging block below was read by nothing: 106 Debug sites stayed live and the file sink had no retention cap at all, measured at 65 MB/day on an appliance that runs for weeks inside a cabinet. Heavy log volume on that box correlates with audio distortion, so this is not housekeeping. ⚠ Unlike Radio.API, this Console sink has NO restrictedToMinimumLevel, so under systemd every Information line here reaches `journalctl -u radio-web` as well as the file. LOG-11 restricted the API's console sink in code (Radio.API/Program.cs:48-53) and did not touch this one. Anything logged at Information in Radio.Web is therefore in the journal — see plan PHN-5 C-93 before adding one.",
    "Using": [
      "Serilog.Sinks.Console",
      "Serilog.Sinks.File",
      "Serilog.Sinks.Async"
    ],
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Information",
        "Microsoft.AspNetCore": "Warning",
        "System.Net.Http.HttpClient": "Warning"
      }
    },
    "WriteTo": [
      {
        "Name": "Async",
        "Args": {
          "configure": [
            {
              "Name": "Console",
              "Args": {
                "outputTemplate": "[{Timestamp:HH:mm:ss} {Level:u3}] {SourceContext}: {Message:lj}{NewLine}{Exception}"
              }
            }
          ]
        }
      },
      {
        "Name": "Async",
        "Args": {
          "configure": [
            {
              "Name": "File",
              "Args": {
                "path": "logs/web-.txt",
                "rollingInterval": "Day",
                "retainedFileCountLimit": 7,
                "fileSizeLimitBytes": 52428800,
                "rollOnFileSizeLimit": true,
                "outputTemplate": "{Timestamp:yyyy-MM-dd HH:mm:ss.fff zzz} [{Level:u3}] [{SourceContext}] {Message:lj}{NewLine}{Exception}"
              }
            }
          ]
        }
      }
    ],
    "Enrich": [
      "FromLogContext"
    ]
  }
}
