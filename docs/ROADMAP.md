# Roadmap

Forward-looking catalog of major upcoming work, organized by maturity stage. New items typically enter as **research arcs**, get **scoped into plans**, then **implemented as PRs**.

Companion docs:
- [`design/WORK-LOG.md`](../design/WORK-LOG.md) — chronological record of what's been done
- [`design/FUTURE-WORK.md`](../design/FUTURE-WORK.md) — stubbed features pending implementation
- [`design/DECISION-LOG.md`](../design/DECISION-LOG.md) — architectural decisions
- [`docs/plans/`](plans/) — scoped plans ready for implementation
- [`docs/research/`](research/) — research output (not yet committed work)

---

## Research arcs (research output; not yet planned implementation)

Research arcs explore a problem space deeply enough that *if* the team decides to act, the work is informed by data rather than guesswork. Each arc carries an explicit **non-goal of producing implementation work** — the research output is consumed later by a separate plan, with the team choosing which ideas (if any) to act on.

### Cast stutter comparison + BT audio stabilization (2026-05-21 → 2026-05-22)

**Branch**: `research/cast-bt-comparison` → merged to `main`
**Docs**:
- [`docs/research/2026-05-21-cast-stutter-comparison.md`](research/2026-05-21-cast-stutter-comparison.md) — Cast (HttpMp3 + DirectChannel) vs SoundCloud + Plex reference systems
- [`docs/research/2026-05-22-bt-audio-stabilization.md`](research/2026-05-22-bt-audio-stabilization.md) — RTest BT-as-source vs PW-stock + bluez-alsa + AOSP-BT

**Output**:
- **2 docs**, ~700 lines combined
- **8 failure modes for Cast** × 4 systems = 32-cell matrix (filled)
- **11 failure modes for BT** × 4 systems = 44-cell matrix (filled)
- **10 + 11 pipeline rows** (filled across all systems)
- **6 + 6 synthesis patterns** identifying where RTest differs from the reference cluster
- **11 + 5 speculative ideas** (8 Cast-specific + 3 system-isolation shared + 5 BT-specific), each with full 5-block measurement methodology so a debug agent can demonstrably show a change is an *actual* improvement (Evidence → Baseline probe → Post-change probe → Success criterion → Verification steps)
- **Concurrent-load discipline** baked in: every probe captures `PROBE-SYS-LOAD` (vmstat / iostat / pidstat / log-rate / SSH-session-count) alongside the audio probe; success criteria must hold under both light-load and heavy-load scenarios

**Status**: research complete. No implementation commitments. Live-inspection probes pending LAN access (Plex receiver source walk, MSE buffered-range readouts, `pw-top` + `btmon` captures, 72 h soak runs). Probe scripts in `scripts/research/` declared as research deliverables, not yet authored.

**How this becomes work**: when the team decides to act on any idea, the next step is a scoped plan in [`docs/plans/`](plans/) that consumes that idea's 5-block measurement structure as its acceptance criteria. The plan owns implementation; the research doc retains the rationale + measurement contract.

---

## Planning queue (scoped, ready for implementation)

### Cast/BT research — Phase 1 + Phase 2 (selected 2026-05-22)

Consumed from the cast/BT research arc per embedded-audio-engineering prioritization (transcript record). Sequenced and scoped in [`docs/plans/2026-05-22-cast-bt-phase-1-2-arc.md`](plans/2026-05-22-cast-bt-phase-1-2-arc.md).

**Phase 1 — stop the bleeding** (3 plans, ~280 LOC; can run in any order or parallel):

| Plan | Idea | Addresses | Branch |
|---|---|---|---|
| [`2026-05-22-bt-capture-watchdog.md`](plans/2026-05-22-bt-capture-watchdog.md) | #12 Watchdog on `OnProcess` interval | FM-BT-3 (known long-uptime quiescence bug) | `feat/bt-capture-watchdog` |
| [`2026-05-22-bt-autoswitch-gate.md`](plans/2026-05-22-bt-autoswitch-gate.md) | #13 Gate `autoSwitchOnConnect` on PW node existence | FM-BT-1 (autoSwitch retries on missing PW node) | `feat/bt-autoswitch-gate` |
| [`2026-05-22-bt-codec-observability.md`](plans/2026-05-22-bt-codec-observability.md) | #15 Surface negotiated codec + bitpool as observable metric | FM-BT-6 (codec quality degradation currently invisible) | `feat/bt-codec-observability` |

**Phase 2 — isolate the audio path** (2 plans, ~165 LOC; ships immediately after Phase 1 completes; CPU affinity before PW event subscription):

| Order | Plan | Idea | Addresses | Branch |
|---|---|---|---|---|
| 1 | [`2026-05-22-audio-thread-isolation.md`](plans/2026-05-22-audio-thread-isolation.md) | #9 Pin radio-api to dedicated CPU cores + SCHED_FIFO | FM2 + FM8 + FM-BT-11 (load contention) | `feat/audio-thread-isolation` |
| 2 | [`2026-05-22-pw-event-subscription.md`](plans/2026-05-22-pw-event-subscription.md) | #14 Replace pw-cli scraping with PipeWire event subscription | FM-BT-1 + FM-BT-2 (eliminates scrape race) | `feat/pw-event-subscription` |

**Shared scaffolding** (research-tier code in `scripts/research/`, declared per-plan in Task 0): `sysload_capture.sh`, `sysload_correlate.py`, `bt_stall_detect.py`, `bt_autoswitch_audit.py`, `bt_codec_observability_probe.sh`, `heavy_load_harness.sh`, `bt_pair_cycle_harness.sh`, plus the per-plan `*_compare.py` PASS/FAIL gates.

**Phase 3+ ideas remain in research** until Phase 1+2 measurement points the finger.

### Active follow-up arc — BT clock-skew "underwater" audio (selected 2026-05-22)

PR #400's observability surfaced 217–391 ppm clock skew between the BT phone clock and the local speaker clock during BT → Cast playback — ~10–20× over BT A2DP spec. The skew is unfixable in software (two crystals cannot be sync'd); the goal is to make the compensation that masks it less audible.

- **Research**: [`docs/research/2026-05-22-bt-clock-skew-measurement.md`](research/2026-05-22-bt-clock-skew-measurement.md) — measurement methodology, architectural analysis, mitigation menu
- **Plan in flight**: [`docs/plans/2026-05-22-bt-drift-compensation-refinement.md`](plans/2026-05-22-bt-drift-compensation-refinement.md) — Path C: smaller, more-frequent compensation events with cosine-ramp crossfade smoothing. Branch `feat/bt-drift-compensation-refinement`.
- **Path D shipped** (PR #404 / commit `781a455`). UAT showed objective metrics REGRESSED across all 3 criteria — but diagnosis revealed this was a measurement artifact from a previously-undiagnosed dual-audio-path issue, not a Path D failure.

### Active follow-up arc — BT dual-routing fix + output picker UI (selected 2026-05-22)

`pactl list sink-inputs` on radio revealed **two independent audio paths** feeding the local soundbar simultaneously:
1. **Path A (designed)**: BT → Radio.API → audio engine → outputs
2. **Path B (rogue)**: BT → PipeWire stream-restore → default sink directly (bypassing Radio.API entirely)

The dual-path explains both Mark's "audio from both soundbar AND Cast" complaint AND the Path D regression (the resampler added latency to Path A while Path B remained delay-free, creating comb-filter "underwater" artifacts).

- **Research**: [`docs/research/2026-05-22-bt-dual-routing-investigation.md`](research/2026-05-22-bt-dual-routing-investigation.md)
- **Plan: WP rule (Part 1, critical-path)**: [`docs/plans/2026-05-22-wp-bt-route-exclusivity.md`](plans/2026-05-22-wp-bt-route-exclusivity.md) — WirePlumber config that prevents BT A2DP from auto-routing to default sink. Without this, Path D can't be measured cleanly. ~30 LOC infra config.
- **Plan: Output picker UI (Part 2, UX)**: [`docs/plans/2026-05-22-output-picker-ui.md`](plans/2026-05-22-output-picker-ui.md) — replaces the `MainLayout.razor:636-641` stub (currently `NavigationManager.NavigateTo("/devices")`) with a real popover. ~150-200 LOC mirroring `CastDeviceDropdown.razor`.

### GV Messages — Voicemail + SMS UI on `/phone` (selected 2026-06-20)

Consumes the Designer handoff (`docs/design-handoffs/HANDOFF-phone-messages-voicemail-sms.md`) and Architect ADR-022 (`design/decisions/2026-06-20-gvbridge-voicemail-sms-integration.md`). Restructures `/phone` into a unified Messages feed (voicemail + texts + recent calls, segmented filter, "More ▸" rail) consuming RotaryPhone's `gvbridge` API at `radio:5004`. **Status corrected 2026-07-31 — this section previously claimed "read experience ships fully." It does not.** The **client** work (GV-1→GV-4) is merged and correct, but **the server has never returned read data**: RotaryPhone's `PositionalGvThreadParser.ThreadsArray()` expects a JSON object root with a `"threads"` property while the `alt=protojson` request returns arrays, so every real response yields 0 items behind a clean HTTP 200. Voicemail and SMS read have **never** worked against live Google Voice. Defect is in the deployed tree (`D:\prj\rp-deploy`); a capture-then-fix cycle is underway there, and **no Radio Console change is required**. Honest status: **read UI complete, read data blocked upstream.** SMS send is built behind `RotaryPhone:Gv:SendEnabled=false`; **GV-5 📋** reconciles the client against the as-built contract ([ADR-028](../design/decisions/2026-07-30-gv-sms-send-contract.md)) and gates thread reply-ability. _A "wrong tree" concern briefly blocked it and was **disproven** — `D:\prj\rp-deploy` is a git **worktree** of `D:\prj\RotaryPhone`, sharing one object store, so the ADR was derived from the deployed objects all along._ **GV-7 📋** covers rendering non-dialable senders. **Update 2026-07-31 (later the same day) — the upstream parser fix (`627b928`) landed and read data IS now flowing**, superseding the "read data blocked upstream" status recorded above: a [live UAT](uat/2026-07-31-gv-live-data/REPORT.md) rendered 20 real SMS threads and 20 voicemails with real transcripts. That pass then found the *read experience* carries three defects on top of working data, and a [root-cause pass](uat/2026-07-31-gv-live-data/F-1-DIAGNOSIS.md) split them across two repos. **Ours is GV-8 📋** — the client collapses every non-2xx to `null` and renders it as an empty conversation, so a 502 is byte-identical to a genuinely empty thread; it ships **independently of the two RotaryPhone items and is deliberately not blocked on them** (ours makes the failure honest, theirs makes it rare). **Theirs** are a `%2F` thread-id decode — every group/MMS conversation is currently unreadable — and a ~9-minute GV auth blackout every 20 minutes; both are routed via `D:/prj/RotaryPhone/docs/prompts/`. **GV-9 📋** and **GV-10 📋** carry the LOW findings. **GV-7's "do not design blind" warning is retired** — the observations exist. _Standing note for anyone testing this surface: until the blackout is fixed, **record wall-clock time**. The 2026-07-31 pass hypothesised throttling precisely because it did not, and the logs falsified that hypothesis — 401 never 429, wall-clock not request volume._ Queued for Builder in [`docs/BUILDER_QUEUE.md`](BUILDER_QUEUE.md). **Update 2026-07-31 (post-merge) — GV-8 shipped as PR #461, and its Polisher + Tester passes produced four follow-ups, triaged rather than queued wholesale** (three of the four are not GV-shaped): **one new cross-cutting row — `UX-1` 📋, skeleton shimmer amplitude.** A **design-token** question, not a bug: the shimmer measurably animates (`Animation.currentTime` 0 → 117 ms; **14.5 %** of pixels change between frames 116 ms apart against a **0 %** static control), but its gradient is a **6/255** delta (`--surface-raised #141416` → `--surface-overlay #1A1A1D`) and reads as static to the eye. `.skeleton-loading` backs **every** skeleton in the app — 27 `<Skeleton>` call sites across 6 pages plus 38 raw nodes — so any change lands everywhere at once; the row is **gated on a Designer answer before a plan is written**. **Two fold-ins:** the deploy script's wrong-architecture default (`Deploy-ToLinux.ps1:51` ships `linux-arm64` to an `x86_64` box) → **OPS-1 (c)**, same file and same "deploy reports success without proving what it did" failure class; and a missing `== null` guard in `PhoneTextsPanel`'s dead thread-list branch → **GV-9**, which is therefore **no longer CSS-only**. **One deliberate non-row:** Playwright MCP is unusable in this WSL instance — tooling, not product, with no PR to claim; the working substitute is recorded in the queue's ordering notes. **⚠ One item from PR #461 remains live-unverified — `M-1`** (an inbound SMS arriving on an open-but-failed thread). It is unit-covered and its branch ordering was observed, but the transition itself never was — no inbound traffic arrived and a synthetic message was correctly declined. **Tracked as outstanding verification, not counted as a pass.**

| # | Plan | Scope | Depends on | Branch |
|---|---|---|---|---|
| GV-1 | [`pr1-foundation-ia-shell.md`](superpowers/plans/2026-06-20-gv-messages-pr1-foundation-ia-shell.md) | DTOs, `GvBridgeApiService` reads + absolute audio URL, `PhoneHubService` GV events, `GvBridgeStatusService`, auth-handler seam (OFF), config/DI, Messages-feed IA shell (calls folded in, missed-call badge). | — | `feat/gv-messages-pr1-foundation` |
| GV-2 | [`pr2-voicemail-surface.md`](superpowers/plans/2026-06-20-gv-messages-pr2-voicemail-surface.md) | Voicemail rows + inline seekable player + transcript states + new-arrival + UI-local/flagged mark-read seam. | GV-1 | `feat/gv-messages-pr2-voicemail` |
| GV-3 | [`pr3-texts-surface.md`](superpowers/plans/2026-06-20-gv-messages-pr3-texts-surface.md) | Thread list + conversation bubbles + `GvSmsReceived` + compose/keyboard (all flag-gated send) + open-thread-to-RotaryPhone deliverable. | GV-1 | `feat/gv-messages-pr3-texts` |
| GV-4 | [`pr4-mark-read.md`](superpowers/plans/2026-06-20-gv-messages-pr4-mark-read.md) | Durable GV read-state (GV write-through, [ADR-024](../design/decisions/2026-06-20-gv-mark-read-durable-readstate.md)): `MarkVoicemailReadAsync` + SMS-thread mark-read wired to RotaryPhone's `read` routes, `ReadStateChanged` SignalR subscription, idempotent reconcile keyed by `(id-or-threadId + isRead)`. **✅ SHIPPED — PR #441**, behind `RotaryPhone:Gv:MarkReadEnabled=false`. No longer on hold: RotaryPhone's routes are shipped and dark by config only, so lighting up is a two-side config flip. | GV-2, GV-3 | `feat/gv-messages-pr4-mark-read` |
| GV-5 | [`pr5-send-contract.md`](superpowers/plans/2026-07-30-gv-messages-pr5-send-contract.md) | Reconcile the SMS **send** contract against RotaryPhone's as-built endpoint ([ADR-028](../design/decisions/2026-07-30-gv-sms-send-contract.md)): fix the request shape (our missing `ToNumber` makes **every** send fail `400 invalid_number` today), adopt the nine-code `Code` taxonomy with per-code UI treatment, subscribe to the `SmsSent` outbound echo, and add the idempotent `OutboundSmsReconciler` (exact `Id`, then `(Outbound, counterparty, text, ≤120s)`). Also gates **thread reply-ability** (ADR-028 §8) — ~a third of inbound SMS is from non-dialable senders (short codes, opaque IDs), so compose is gated client-side before the POST rather than surfacing their `400 invalid_number` as a failed send. Ships with `SendEnabled` still `false`. **📋 Queued** — a "wrong tree" concern was raised and **disproven** (`rp-deploy` is a git *worktree* of `D:\prj\RotaryPhone`, same object store). **Plan reconciled against post-GV-8 `main` 2026-07-31 — refresh, not re-plan: no ADR-028 decision moved.** All Chunk 5/6/6b line anchors re-sited (`PhonePage.razor` +109, `PhoneTextsPanel.razor` +51, `PhoneMessagesPanel.razor` +12); **new Chunk 0** because `GvResult<T>` nulls `Value` on `HttpError` (a test-pinned invariant GV-6 relies on) while GV-5 maps off the **`Code` in the body** and needs two fields off a non-2xx — a separate `FailureBody` is added rather than widening `Value`. **⚠ `GvBridgeApiService.ReadErrorCodeAsync` must NOT be reused for send:** it tries `error` before `code`, which is right for the read routes and GV-6 but returns this endpoint's human prose, silently collapsing all nine codes into one. **🔵 One decision is open for the owner** — whether the composer stays reachable on a conversation that failed to load (GV-8 UAT `O-2`); the plan recommends disabling it with a reason, because leaving it live lets one optimistic append flip the pane out of GV-8's error branch and render a single bubble as the whole conversation. **9 chunks / 9 tasks.** | GV-3 | `feat/gv-messages-pr5-send-contract` |
| GV-7 | _plan TBD (design-led)_ | Render non-dialable SMS senders: thread-list row, conversation header, and contact resolution for **numeric short codes** and **opaque 36-char sender IDs**. There is **no `fromName` in the GV payload**, so names can only come from local contact resolution — which cannot resolve these senders. Display counterpart to GV-5's send-side gate. **📋 Queued — live observations now EXIST** ([UAT § Findings for GV-7](uat/2026-07-31-gv-live-data/REPORT.md), G-1…G-9); the "do not design blind" warning is retired. Design against **G-1** (the `Texts 2` badge is an *unread count* — there are **20** threads) and **G-3** (no opaque 36-char ID is reachable from this surface, so that case is *untested against real data*, not proven absent). **G-4 de-risked the layout concern:** a 36-char ID measured **safe at 1920×720** — no truncation strategy needed. Also folds in **G-6** (header duplicates the identifier when no name resolves), **G-8** (MMS preview sender prefix) and **F-3** (composer rendered-but-disabled with no stated reason — a design decision for this row, not a prescription). | GV-3 | `feat/gv-messages-pr7-nondialable-senders` |

| GV-8 | [`plan`](superpowers/plans/2026-07-31-gv-texts-load-error-state.md) | **Distinguish a failed conversation load from an empty one.** UAT F-1 (HIGH): `GvBridgeApiService.GetSmsThreadMessagesAsync` collapses every non-2xx/timeout/deserialization error to `null`, then `PhonePage.razor:632` does `?? new()` — so a 502 renders as **"Start the conversation below."** with no error, no spinner, no retry. Adds outcome-aware client results, `_openThreadError`/`_openThreadLoading` page state threaded through `PhoneMessagesPanel` (whose `Loading`/`Error` parameters already exist but are never passed), and the missing error branch in `PhoneTextsPanel`, reusing the thread list's existing `cloud_off` + "Couldn't load messages." + `Retry` pattern one level down. Folds in F-2 (empty copy must mean *genuinely empty*). **Not blocked on RotaryPhone's two defects.** **✅ Shipped — PR #461.** [UAT 10/10, 0 HIGH/MEDIUM](uat/2026-07-31-gv8-error-state/REPORT.md), verified under a genuine upstream 502 during a natural auth blackout. C7 confirmed a genuinely empty group thread still reads as **empty**, so the opposite lie was not introduced. One user-visible delta by design: **a failed open no longer marks the thread read**, and a successful `Retry` performs the read-marking the failed open skipped. | GV-3 | `fix/gv-texts-load-error-state` |
| GV-9 | _plan TBD (small; **no longer CSS-only**)_ | **Texts-surface polish.** F-4 — `.texts-conv-number` lacks `text-overflow: ellipsis` and clips mid-character past ~60 chars (*hardening only; no live data triggers it, and a 36-char ID measured safe*). F-7 — unread rows sit 20px out of alignment because the unread dot displaces the text instead of occupying a reserved gutter, so every row jumps when marked read. **Third item folded in 2026-07-31 from the GV-8 cycle:** `PhoneTextsPanel.razor:162`'s thread-list branch still has a bare `else if (Error)` where the canonical form is `Error && <collection> == null` (`PhoneMessagesPanel.razor:110`/`:78`), which GV-8 shipped one branch over at `:61`. Dead code in production and correctly left out of PR #461 — fixed here because the file's own comment keeps this branch *"in sync so it isn't the next thing someone copies."* The behaviour is **unasserted in both directions** (no existing test sets `Error` in thread-list mode), so the fix must add a test. **📋 Queued.** | GV-3 | `fix/gv-texts-polish-overflow-unread-align` |
| GV-10 | _plan TBD (investigate first)_ | **Confirm-or-close: bubbles may render the list snippet rather than the full body** (UAT F-5). **Unproven** — the plausible mechanism is upstream (per-thread messages are derived by filtering the whole SMS folder list, whose entries carry snippets), in which case this closes with no Radio Console change. First task is one `curl`, run inside a healthy auth window. **📋 Queued, lowest priority.** | GV-3 | `fix/gv-texts-bubble-full-body` |

### Audio-pipeline + hygiene follow-ups from the 2026-08-10 "no audio" session (selected 2026-08-10)

One user report — *"no audio from the soundbar"* — decomposed into **five distinct bugs**, which produced three PRs the same day: **#467 ✅** (systemd restart limiter made functional; `StartLimitIntervalSec` was inherited-and-unreachable on both units, plus crash-forensics groundwork), **#469 ✅** (cross-source metadata contamination — `TrackIdentified` was broadcast to every source, not just the active one — and a BT state clobber where deferred capture demoted a `Playing` source to `Ready` and silently killed fingerprinting), and **#468 ✅ merged `8b1ce0a`** (confirm the Cast output before leaving local output muted; make source teardown state-independent). **This section is the deferred remainder**, queued in [`docs/BUILDER_QUEUE.md`](BUILDER_QUEUE.md) — scoped, not re-brainstormed. _(Eight rows as first queued; **nine as of 2026-08-11** after `AUD-5` was added from PR #473's review, and **eleven** after `AUD-6`/`AUD-7` were added from the 2026-08-11 production deploy — see the second- and third-pass notes below. **⚠ Note the section title is now slightly narrower than its contents:** the last three rows did not come from the 2026-08-10 session at all. They are kept here because they are the same audio-pipeline theme and share its ranking, not because they share its provenance — each row states its own.)_

**⚠ Updated 2026-08-11 (status-accuracy pass, no re-scoping).** That day produced **four** merged PRs, not three — **#470 ✅ `dea1e02`** landed the eight queue rows themselves — and **#468 was extended before it merged**, which changes what this section asserted. **`AUD-3` is ✅ SHIPPED, not blocked.** The owner directed the Cast connect/teardown race be closed *inside* #468 rather than deferred, and commits `47da897` + `1fded0c` did it: `GoogleCastOutput` gained a lifecycle lock and a connection generation, the resolved receiver moved from a field to a local (which is what actually eliminates the NRE), and `SoundFlowAudioEngine` gained `BeginCastConnectAsync`/`TryCommitCastConnectAsync` over an epoch bumped under `_activeOutputLock` — so the commit re-checks *and* tears down under one lock acquisition. **Three things it did not do, recorded on the queue row: the hardware UAT the row demanded never happened** (verification is desk-only — #468 added no `docs/` files at all), **the service-level commit call is untested** (those tests use `Mock<IAudioEngine>` while the epoch capture gates on the concrete `SoundFlowAudioEngine`), and **closing the race test gap cost two new `internal` seams.** **`AUD-4` also gained a problem nobody anticipated:** #468 rewrote `AudioSourceBase.StopAsync` to stop gating teardown on `State`, and its comment claims `StopCoreAsync` is *"the only code that detaches the component"* — which competes with `AUD-4`'s claim that the real detach is `SoundFlowPlaybackService.StopAsync` → `RemoveComponent`. **There are three layers there, not two, and reconciling that is now `AUD-4`'s first task.** **🚧 PR #471** (`fix/serialize-native-device-enumeration` — native `pa_mainloop` enumeration + device-lifecycle gating) **is still in flight**; no row is queued for it until it lands, but it touches `SoundFlowAudioEngine.cs`, `SoundFlowDeviceManager.cs` and `FilePlayerAudioSource.cs`, so expect `AUD-1`/`AUD-2`/`AUD-4` anchors to move again. **`TEST-1` remains first; the owner's ranking is unchanged.**

**⚠ Updated again 2026-08-11 (second pass — one row added, three staleness corrections). The tranche is now NINE rows.** **New: `AUD-5`** — a genuine **pre-existing** defect surfaced by PR #473's review, where a Cast connection that is no longer current can persist its volume as the system master volume. It is placed **after `AUD-1` and before `AUD-2`**; **`TEST-1` stays first and no existing row moved.** It outranks `AUD-2` because its mechanism is confirmed by construction rather than an unverified inference, it is the most directly user-visible row in the tranche, and it is the only one that can silently write wrong state to **persistent** config; it sits below `AUD-1` because `AUD-1` is an observed, currently-firing defect while `AUD-5`'s production firing is still a hypothesis. **Corrections: (1) PR #471 has MERGED (`61187bb`)** — it is no longer in flight, and the anchor churn predicted just above **did not materialize**: every anchor in `AUD-1`, `AUD-2` and `AUD-4` re-verifies byte-exact on `0870410`, including `FilePlayerAudioSource.cs:1988`/`:2112`, which survived #471's edits to that very file, and #471 touched **none** of the files `AUD-2` or `AUD-4` cite. **(2) PR #472 (`97f1655`) and PR #473 (`0870410`) also merged**; #473 shifted every anchor below `:32` in `GoogleCastOutput.cs` by exactly **+66**, so `AUD-3`'s citations in that file are pre-#473 and must be re-sited before reuse. **(3) `AUD-3`'s closing observation that the class comment "asserts an invariant the code does not hold" is FIXED** — that is exactly what #473 corrected, and the corrected comment now documents `AUD-5`'s exposure directly. **⚠ Separately, and deliberately not a queue row: `docs/BUILDER_PROMPT.md` has never existed, and the "Handoff Protocol at the top of `docs/BUILDER_QUEUE.md`" that the Builder role definition cites is not there either** — so every Builder is dispatched against two documents that do not exist, and has been silently reconstructing the loop from the queue's shape. Owner/Planner authors both; a Builder must not write its own operating instructions. Reasoning and the concrete deliverable are in [`docs/BUILDER_QUEUE.md`](BUILDER_QUEUE.md) § Dependency / ordering notes. **Also recorded there: PR #473's pre-merge checklist item landed in `CLAUDE.md` under a new `## Pre-Merge Review` section**, chosen because this repo has no `CONTRIBUTING.md` or PR template and per-plan checklists die with their plans.

**⚠ Updated 2026-08-11 (third pass — one defect from the production deploy, queued as two rows). The tranche is now ELEVEN rows.** **New: `AUD-6` and `AUD-7`**, from a defect that surfaced during today's deploy restart and was measured directly on the box. **The measurement:** MiniAudio enumerated `[0] AB13X USB Audio Analog Stereo` / `[1] Built-in Audio Analog Stereo (IsDefault=True)` on **Aug 10**, and the **reverse order** on **Aug 11**. The persisted value in the config store is the string **`playback-1`** on both days — **on Aug 10 that meant the Soundbar, on Aug 11 it means USB Audio Out. Nothing in the store changed; the meaning did.** **Two independent causes, deliberately split into two rows.** **`AUD-6` — unstable identity:** output ids are minted straight off the enumeration index (`SoundFlowDeviceManager.cs:517`) and that string is what reaches storage, while resolution back to hardware is **pure string parsing, not a lookup** (`GetDeviceIndexById` is an `int.TryParse` and nothing else). **A stable, name-keyed identity already exists and is already trusted** — `AudioOutput:DeviceDisplay:DeviceFriendlyNames` survived the reordering correctly, which is why the friendly names in the live payload are right while the id underneath them is wrong. Needs a **migration**: every existing store holds ordinals, and on the very deploy that ships the fix the legacy ordinal may already mean the wrong device. **`AUD-7` — the reported active output diverges from actual routing:** `/api/devices/output` reported `playback-1` "USB Audio Out" as `isActive`, while PipeWire had the app's stream on the **built-in analog sink** (the Soundbar) with the USB sink `SUSPENDED`. **Investigating sharpened the diagnosis in a way that changes the fix:** it is not that two notions drift apart, it is that **the native device switch (`SwitchPlaybackDevice`) has exactly one caller — the interactive HTTP path — and startup never reaches it**; the method the engine's own comment nominates as performing the swap does no such thing. **So `AUD-7` is deterministic, not intermittent** — it fires on every restart where the preference is not the system default, with or without a reorder. **⚠ Ordering is load-bearing: `AUD-6` first.** `AUD-7` makes startup *act* on the persisted preference, and acting on an unstable key is worse than ignoring it — shipping it alone would convert a **mis-report** into a **mis-route**. **⚠ Do not reopen the 2026-08-10 question: this fragility was raised then as a hypothesis and CORRECTLY FALSIFIED as the cause of the soundbar silence** (the app had opened the right device; the cause was the Cast startup mute, shipped in #468). **That falsification stands.** What is new is that the fragility itself has now been observed firing — a different claim. **Placement: after `AUD-1`, above `AUD-5`; `TEST-1` untouched at the top and no existing row moved** — though a case that `AUD-7` outranks `AUD-1` outright is defensible and needs only the owner's word. **Thematically the siblings of `AUD-2`/`AUD-4`:** those are **source** key identity, these are **device** key identity — **four rows now turn on an identifier that is not stable being used as if it were**, and in every one of them the mismatch fails silently while something logs or reports success anyway.

**Two things are worth carrying out of this session even if none of the rows is claimed soon.** **(1) The suite lied, and the lie cost a diagnosis.** Four tests failed under timing/load pressure and passed on retry; the failure was read as a real regression before it was recognised as noise. The root is not the four tests — it is that **unit tests can reach an ambient `localhost:5000`**, so a result can depend on whether `radio-api` happens to be running on the self-hosted runner. `TEST-1` is ranked first for that reason: it is what makes the other seven rows' verification mean anything. **(2) Two separate bugs here exist because a log line asserted a success it never verified.** `SoundFlowMasterMixer.RemoveSource` logs *"Removed audio source … from mixer"* while removing only a `List<IAudioSource>` entry — which is why commit `03a6fea` was applied one layer too high and **did nothing for months**; `SoundFlowPlaybackService.SetGainOffset` logs *"Applied gain offset …"* after a write whose matching lookup may have hit nothing. Neither log is wrong about what it did; both are wrong about what they imply.

**⚠ One trap is prominent enough to belong in the roadmap, not just the queue: do not "fix" `UseShazamForAllSources` by setting it `false`.** The flag conflates *always fingerprint BT* — **necessary**, because AVRCP cannot supply album art on this box (BlueZ 5.72 ships no BIP/cover-art implementation; 7 days of data show **0 AVRCP-sourced art against 2,560 SongRec-sourced**) — with *let SongRec overwrite AVRCP title/artist/album*, which is not necessary and rewrites `Enter Sandman (Remastered)` to `Enter Sandman`. Turning it off removes BT album art entirely.

| # | Plan | Scope | Depends on | Branch |
|---|---|---|---|---|
| TEST-1 | _plan TBD_ | **Stop the suite producing false CI signals.** A bUnit test asserting across a real `await` (`WaitForAssertion`; five files already use the idiom), three `Radio.API.Tests` controller timeouts under parallel load (a *different* failure mode — and the three are **not named in the session record**, so re-identify before scoping), and underneath both **the real defect: unit tests can reach an ambient `localhost:5000`**. Stub the hub/config clients so no unit test can open a socket to the box. **📋 Queued — highest priority of the tranche; claim before the other seven.** | — | `fix/test-rig-hermetic-and-flaky-waits` |
| AUD-1 | _plan TBD_ | **Split `UseShazamForAllSources` into two decisions** — *always fingerprint BT* (keep) and *let SongRec overwrite AVRCP metadata* (drop). The wanted behaviour already exists at `BluetoothAudioSource.cs:893-905`, on the branch the flag never takes. **Caveat likely forcing a per-source split:** the same flag governs `FilePlayerAudioSource`, where ID3 tags are authoritative differently — decide FilePlayer's side deliberately rather than dragging it along. **📋 Queued.** | — | `fix/split-shazam-fingerprint-vs-overwrite` |
| AUD-6 | _plan TBD (medium)_ | **Output device identity is an enumeration ordinal, so a persisted output preference silently changes which physical device it means.** `SoundFlowDeviceManager.cs:517` mints `Id = $"playback-{i}"` off the raw MiniAudio loop index, that string is what reaches `AudioPreferences:CurrentOutput`, and resolution back to hardware is **pure string parsing, not a lookup** — `GetDeviceIndexById` is an `int.TryParse` with no name comparison anywhere on the path. **Measured across today's restart:** enumeration order flipped between Aug 10 and Aug 11 while the stored value stayed `playback-1`; it meant Soundbar, then USB Audio Out. **⚠ The codebase already wrote a guard for exactly this (`AudioEngineInitializationService.cs:294-330`, comment and all) and it CANNOT FIRE** — "expected" and "actual" are the same lookup by the same key against the same cache, so it confirms the wrong device against itself. **The stable key already exists and is already trusted:** `AudioOutput:DeviceDisplay:DeviceFriendlyNames` is name-keyed and survived the reorder correctly; `AudioDeviceInfo.RawName` already carries it internally. Fix direction: persist a stable identifier, keep `playback-N` as a transient view-model concern that never reaches storage. **⚠ Migration required** — existing stores hold ordinals, there are **two** read paths (config store and `IOptionsMonitor`), and the key is polymorphic (it also holds `google-cast`/`http-stream`). **⚠ The 2026-08-10 falsification of this as the cause of the soundbar silence STANDS — this row is the different claim that the fragility has now been observed firing.** **📋 Queued — claim before AUD-7; the order is load-bearing.** | — | `fix/stable-output-device-identity` |
| AUD-7 | _plan TBD (medium)_ | **The reported active output diverges from where audio physically goes — the output selector cannot be trusted.** `/api/devices/output` reported `playback-1` "USB Audio Out" as `isActive`, while PipeWire had the app's stream on the built-in analog sink (the Soundbar) and the USB sink `SUSPENDED`. **Mechanism, sharper than "unreconciled at startup":** `SoundFlowAudioEngine.SwitchPlaybackDevice:838` is the only code that opens a different playback device and has **exactly one caller** — `DevicesController.SetOutputDevice`, the interactive HTTP path. **Startup never calls it**, and `SoundFlowDeviceManager.SetOutputDeviceAsync`, which the gate's own comment (`SoundFlowAudioEngine.cs:222-224`) nominates as performing the swap, **touches no audio device** — it validates an id, assigns a string, and persists. So audio stays on whatever `InitializeAsync` picked: the system default. **Therefore DETERMINISTIC, not intermittent — every restart where the preference is not the system default, no reordering needed.** `isActive` is a string compare against that same bookkeeping field, and `_currentDeviceIndex` — the only thing that knows the truth — is **private with no accessor**, so the API cannot report it today even if it wanted to. **Four separate notions of "the output device" exist and only one is real**; a third silent repoint hides in the recovery path (`:805-809` re-inits index 0 unconditionally). **⚠ Half (a) — actually routing to the preference — WITHOUT AUD-6 is a regression, not a partial fix:** it converts a mis-report into a mis-route. Ship the reporting half alone, or claim AUD-6 first. **⚠ The live evidence is a snapshot as of `0b2ebcb`** — the owner's UAT will re-select "Soundbar" and make the UI truthful again; **that is not a fix, and only a restart re-tests it.** **📋 Queued — after AUD-6.** | — _(claim AUD-6 first)_ | `fix/reconcile-reported-and-actual-output-device` |
| AUD-5 | _plan TBD (small)_ | **A Cast connection that is no longer current can persist its volume as the system master volume.** `GoogleCastOutput.SyncInitialVolumeAsync` is a *read* of device status, but its success path fires `CastVolumeChanged` — whose **sole subscriber** unconditionally sets **and persists** `AudioManager.MasterVolume` (the setter schedules a debounced write to the config store, so wrong state survives a restart). **Nothing re-checks `_connectionGeneration` between the status response and the event fire**, and `IsInitialSync` — which exists on the event for exactly this distinction — is **never branched on**, only interpolated into log messages. **⚠ Two constraints on the fix, both settled: (1) widening `_lifecycleLock` will NOT close this** — the exposure is the *event fire*, not the field read, and widening it would hold a lock across a SharpCaster network call, re-creating the output-picker hang AUD-3 was built to avoid; the correct shape is a **generation re-check immediately before the fire**. **(2) It PREDATES #468** (`b420edc`, 2026-02-11) — **not** a regression from the Cast synchronization work; do not fix it by unpicking that. **Also recorded on the queue row: what is NOT wrong** — the three unsynchronized sibling `_client` reads were assessed **safe** and PR #473 wrote that reasoning into the file; this defect is *downstream* of them. **A hypothesis, not a diagnosis:** if a superseded Cast connection can persist master volume, that is a plausible source of *"my volume changed by itself"* reports — check journald for `"Synced volume from Cast device … (initial: True)"` against Cast churn; **a negative result does not close the row.** **📋 Queued — placed after AUD-1, before AUD-2; TEST-1 untouched at the top.** | — | `fix/cast-initial-volume-sync-generation-check` |
| AUD-2 | _plan TBD (investigate first)_ | **Confirm-or-close: is SDR gain/ducking silently dead?** SDR mints `sdr-radio-<guid>` as its playback key while `AudioManager` addresses the source as `Radio-<guid>`. **Unverified code-read inference** — but if it holds, **TTS and event audio never duck the radio**, silently, because no gain/duck setter checks membership. Ducking is a documented core pattern in `CLAUDE.md`. **📋 Queued — claim before AUD-4; it decides the key.** | — | `fix/sdr-playback-id-ducking-gain` |
| OPS-2 | _plan TBD_ | **Pin six floating package versions** (`Radzen.Blazor`, `Serilog.AspNetCore`, `Serilog`, `Serilog.Extensions.Logging`, `SoundFlow`, `Scalar.AspNetCore`) across seven `PackageReference` lines in three projects. **Pin to what resolves; upgrade nothing.** ⚠ A floating-version theory was investigated on 2026-08-10 and **disproved** as the cause of a CI failure — this is reproducibility hygiene, not a bug fix. **📋 Queued.** | — | `chore/pin-floating-package-versions` |
| OPS-3 | _plan TBD_ | **`BindsTo=` for `radio-web`.** #467 made the restart limiter functional but `Requires=radio-api.service` does **not** propagate spontaneous failure — only an explicit stop/restart — so when `radio-api` trips its limiter, `radio-web` keeps serving a UI whose backend is gone. The reasoning is already written into `deploy/common/radio-web.service:12-22`. **⚠ Real change to production service coupling: own PR, owner review, exempt from auto-merge-on-green.** **📋 Queued.** | — | `fix/systemd-bindsto-radio-web` |
| AUD-3 | _no plan — shipped inside #468 at the owner's direction_ | **Close the Cast connect/teardown data race.** The connect path touched `GoogleCastOutput`'s unsynchronised `_client`/`_connectedReceiver` without holding `SoundFlowAudioEngine._activeOutputLock`, concurrently with teardown. **✅ SHIPPED in PR #468, merge commit `8b1ce0a`** (`47da897` + `1fded0c`) — the race itself, not just its observable outcome: a lifecycle lock + connection generation in `GoogleCastOutput`, the receiver resolved into a local instead of a field, and `BeginCastConnectAsync`/`TryCommitCastConnectAsync` on the engine over an epoch bumped under `_activeOutputLock`. **⚠ The earlier "#468's guard closes the outcome, not the race — do not read its merge as closing this" note is SUPERSEDED: it was true of #468 as first written, and false of what merged.** **Three residues, all recorded on the queue row and none re-queued: the demanded hardware UAT never happened** (desk-only verification), **the service-level commit call is untested** (`Mock<IAudioEngine>` vs an epoch capture gated on the concrete engine), and **two new `internal` test seams** (folded into `TEST-2` as a pattern). | — _(was: PR #468 — merged)_ | `fix/cast-connect-teardown-race` _(unused; shipped on `fix/cast-startup-mute-rollback-and-source-teardown`)_ |
| AUD-4 | _plan TBD_ | **Unify the two source-removal layers and rename `SoundFlowMasterMixer`** — a `List<IAudioSource>`, not the audio mixer; the real detach is `SoundFlowPlaybackService.StopAsync` → `RemoveComponent`. Its log line **actively misled a previous fix attempt** (`03a6fea`, applied one layer too high, silently inert for months). ⚠ Trap: do **not** sweep by `oldSource.Id` — SDR registers under a different key; sweep `_activeComponents`. **⚠ Amended 2026-08-11: there are THREE layers, not two.** PR #468 rewrote `AudioSourceBase.StopAsync` to stop gating teardown on `State` — because sources were holding attached, audible components while their state read Ready/Stopped — and its comment claims `StopCoreAsync` is *"the only code that detaches the component,"* competing directly with this row's claim. **Reconciling that is now this row's first task, before any code moves**; unifying two layers while a third has just shifted underneath them is exactly how `03a6fea` went wrong. **📋 Queued — same root as AUD-2; prefer AUD-2 first.** | — | `refactor/unify-source-removal-and-rename-mixer` |
| TEST-2 | _plan TBD (feasibility first)_ | **Deferred-capture branch-dispatch coverage gap** left by #469. The test reaches the acquisition through the internal `ApplyDeferredCaptureState()` seam, not the `capture is SoundComponent` / `capture is AudioCaptureDevice` branches — constructing either needs a native SoundFlow `AudioEngine`, a constraint already recorded in the method's own doc comment. The state decision is pinned; the dispatch around it is not. **May legitimately close as "still infeasible" — do not close it with a mock that re-asserts the seam.** **📋 Queued, lowest priority.** | — | `test/bt-capture-branch-dispatch-coverage` |

_One approved follow-up from this session is **not** a queue row: two boundary-doc Change Log entries sit uncommitted in `D:\prj\RotaryPhone` (branch `diag/gv-srtp-receive`), recording that `systemd-coredump` replaced apport machine-wide — which affects RotaryPhone's crash retrieval too. Routed to [`BUILDER_QUEUE.md`](BUILDER_QUEUE.md) § Cross-repo handoffs #7. **The 2026-07-16 entry that established the "commit it there separately" precedent is itself still uncommitted**, so this is a recurring protocol miss rather than a one-off._

### CI infrastructure — RTest appserver runner migration

Selected as a follow-up after the cast/BT Phase 1+2 tranche revealed that the project's monthly GH Actions minutes pool can be a real blocker. Plan modeled on FamilyWorkspace's 2026-05-17 migration which already provisioned the runner.

| Plan | Branch | Scope |
|---|---|---|
| [`2026-05-22-rtest-ci-appserver-migration.md`](plans/2026-05-22-rtest-ci-appserver-migration.md) | `chore/rtest-ci-appserver-migration` | Flip 4 Linux workflows from `runs-on: ubuntu-latest` to `runs-on: [self-hosted, linux, x64, appserver]`. `audio-uat.yml` (Windows) stays. Advisory-mode CI; no branch-protection change. Mark verifies first run before merge. |

---

## Queued (intent recorded, not yet scoped)

### Voice control — Home Assistant Assist integration (queued 2026-08-09)

**Goal**: hands-free voice control of the console ("play vinyl", "tune to 98.5", "cast to the living room", "what song is this?") with spoken responses routed through the console's own ducking/TTS event pipeline. The touchscreen stays owned by the Blazor UI — voice state (listening/processing/responding) renders as Blazor overlay components we build, not a separate voice UI.

**Origin**: 2026-08-09 evaluation of [voice-satellite-card-integration](https://github.com/jxlarrea/voice-satellite-card-integration) (browser-based HA voice satellite). Verdict: it is a Home Assistant *frontend* engine — it only runs on HA-served pages, cannot embed in a non-HA app, and its browser-resident wake-word inference is a poor fit for radio's memory budget. Its architecture (client-side wake word → HA Assist pipeline → intents → device actions) is the template; the implementation here should use HA + Wyoming protocol services instead. Licensing note: that project is AGPL-3.0 and this repo is Apache-2.0 — reference its ideas, do not lift its code.

**Host constraints (verified against `homelab` 2026-08-09)**:
- **radio** (`.50`, N100, 3.6 GiB RAM) is memory-starved — 2.9 GiB used + 2.4 GiB swap at last capture; homelab's standing rule is "don't add anything resident casually." No Docker. The gdm desktop session is load-bearing (PipeWire capture + the GV Chrome session). Anything added on radio must be tiny (≈50 MB resident, no browser, no HA).
- **No Home Assistant deployment exists anywhere in the homelab** (remote-access spec 2026-07-24 states this explicitly) — standing one up is part of this work, not a prerequisite someone else owns.
- **appserver** is the natural HA/STT host: the Docker host, with a GTX 1660 SUPER (6 GiB VRAM, currently serving Ollama) for GPU speech-to-text. Runner container limits alone (16+16+24 GB) imply substantial RAM, but exact free RAM/VRAM is not captured in homelab — verify `free -h` / `nvidia-smi` before install, and account for Ollama VRAM contention.

**Conflict analysis vs the GV Chrome / rotary-phone stack (verified against homelab 2026-08-09)** — the design below is shaped to *not touch* the load-bearing pieces documented in `radio/services/rotary-phone.md` and `radio-base.md`:
- **No browser anywhere in this design.** The GV trunk depends on the logged-in desktop Chrome (CDP `:9224`, cookie refresh cron every 20 min, `mute-gv-browser.py` driving its audio). Voice capture runs as a backend process, so nothing competes with or restarts that Chrome — this is also the decisive reason the browser-based Voice Satellite engine / HA-iframe alternative stays rejected.
- **Microphone isolation**: voice uses a **dedicated USB mic, addressed by its PipeWire node name — never `default`**. PipeWire tolerates shared capture, but pinning the node means that even if GV Chrome ever opens a mic, the two never contend for the same device, and `radio-bt-setup.service`'s PipeWire defaults are left alone.
- **Session plumbing**: capture rides the same `mmack` seat0 PipeWire socket radio-api already uses for BT capture (`/run/user/1000/pipewire-0`, `Linger=no`, `radio-pipewire-access` ACL) — a known, already-mitigated pattern, not a new one. A headless rebuild breaks voice capture the same way it breaks BT capture; same DR note covers both.
- **Ports**: rotary-phone owns SIP `udp/5060`/`5061`, GV-bridge RTP `5070`, RTP `49000+`, SCO `49100+`, CDP `:9224`; payload HTTP is `5000`/`5002`/`5004`. The satellite's default Wyoming port (`tcp/10700`, Option A only) collides with none of these; Option B adds no listener at all.
- **Memory/CPU**: wake word and STT run on appserver, never on radio. Radio's added load is PCM capture + streaming (~32 kB/s at 16 kHz mono) — no inference. Option B adds zero resident processes; Option A adds one (~30–60 MB).

**Proposed architecture (to be validated by a research arc before planning)** — two variants for the radio side; both keep wake word + STT + TTS + intent handling on appserver and the UX purely in Blazor:
1. **appserver — new containers**: Home Assistant + Wyoming services: faster-whisper (STT, GPU), piper (TTS), openWakeWord (wake word). Wired together as an HA Assist pipeline.
2. **radio — mic capture, two options**:
   - **Option A — off-the-shelf satellite**: a `wyoming-satellite` systemd unit streams the mic to HA; wake detection stays on appserver (`--wake-uri` pointed at the openWakeWord container). Fastest path to a working pipeline; one new resident process; Blazor state arrives via HA's websocket or a radio-api relay.
   - **Option B — native (preferred end-state): radio-api *is* the satellite.** Radio.Infrastructure already captures PipeWire audio for the BT source; add a mic capture service that streams to HA's **Assist Pipeline WebSocket API** (`assist_pipeline/run`, binary audio frames, pipeline started at the wake stage so detection happens server-side). Pipeline stage events (wake → stt → intent → tts) fan out through the existing SignalR hubs to the Blazor overlay. **Zero new processes on radio, no new listener, voice becomes a first-class Radio Console feature** with its config in the normal appsettings/secrets stores. Cost: more dev work than A; the research arc should validate continuous-stream-over-WiFi behavior (or gate the stream on local VAD) before committing.
   - Sensible sequence: stand the pipeline up with A to validate end-to-end latency/accuracy, then fold capture into radio-api as B and retire the extra unit.
3. **Intent → action**: HA custom sentences → `rest_command` → radio-api REST endpoints (sources, tuning, volume, Cast targets, fingerprinting/play-history for "what's playing").
4. **Blazor overlay**: Radio.Web renders listening/processing/responding state — under Option B this is just another SignalR event stream from radio-api, keeping the UI's dependency surface unchanged (no direct Blazor→HA coupling).
5. **Responses through the cabinet**: HA automations POST to radio-api's TTS/audio-event endpoints so the existing priority-ducking speaks the reply and fades the music back — no satellite speaker path exists in either option.

**Setup references** (for whoever picks this up):
- HA container install: <https://www.home-assistant.io/installation/linux#docker-compose>
- Assist pipelines overview: <https://www.home-assistant.io/voice_control/voice_remote_local_assistant/>
- Wyoming integration (HA side): <https://www.home-assistant.io/integrations/wyoming/>
- Wyoming services: <https://github.com/rhasspy/wyoming-satellite> (Option A) · <https://github.com/rhasspy/wyoming-faster-whisper> · <https://github.com/rhasspy/wyoming-piper> · <https://github.com/rhasspy/wyoming-openwakeword>; for GPU STT the `lscr.io/linuxserver/faster-whisper` image has a CUDA variant
- Custom sentences (intents): <https://www.home-assistant.io/voice_control/custom_sentences/> · `rest_command`: <https://www.home-assistant.io/integrations/rest_command/>
- Assist Pipeline WebSocket API (Option B's contract): <https://developers.home-assistant.io/docs/voice/pipelines/> · auth/websocket basics: <https://developers.home-assistant.io/docs/api/websocket/>

**Alternative kept on the table**: embedding an HA iframe running Voice Satellite inside the kiosk page (buys its skins/timers/overlay UI for free) — rejected as primary due to radio's memory budget, the HTTPS/mixed-content burden on a LAN-HTTP stack, and the AGPL boundary.

**Hardware**: one USB microphone (or mic array) positioned in/on the cabinet. Everything else already exists.

**Homelab touchpoints — must land with the integration** (recorded here so the config work isn't forgotten when this is picked up):
- `appserver/`: new compose stack(s) + service docs (`home-assistant.md`, wyoming services), `DASHBOARDS.md` rows, `capture.sh` re-run to pick up the containers, `SECRETS.template.md` entries (HA admin login, long-lived access token used for the radio-api bridge)
- `radio/`: mic device notes (PipeWire node name, udev), captured-state refresh, DR restore-order update (voice capture depends on the seat0 session, same as BT); **Option A only**: new satellite systemd unit → `radio/services/` doc. Option B changes radio-api config/appsettings instead — captured via the existing appsettings redact/capture flow
- `monitoring/`: Beszel/Homepage rows for HA + wyoming services
- `network/`: HA needs a stable name/address; record the new radio→appserver runtime dependency (voice dies if appserver is down — degrade gracefully in the Blazor overlay)
- homelab `docs/ROADMAP.md`: a project row for the HA deployment itself

**First step when picked up**: read off appserver free RAM/VRAM; then a short research arc benchmarking faster-whisper on the 1660 SUPER (vs CPU, vs Ollama contention), measuring continuous 16 kHz streaming over radio's WiFi (Option B viability), and confirming the GV Chrome never holds a mic node (one `pw-top`/`pactl list source-outputs` read-off during a rotary call — expected: it doesn't, call audio is SIP/RTP); then scope plans in `docs/plans/` (HA deploy · capture path A→B · Blazor overlay · intent catalog).

---

## Active in-progress

*Empty.*

When work is in active development on a feature branch, reference it here with the branch name, current state, and outstanding gates.

---

## Workflow notes

- **Research → Plan → Implementation**: research arcs don't directly produce code. A separate plan document in `docs/plans/` scopes the actual work, with acceptance criteria derived from the research's measurement-discipline blocks.
- **Branch hygiene**: every implementation lives on a short-lived branch (`feat/`, `fix/`, `docs/`) and merges via PR to `main`.
- **Measurement-driven implementation**: ideas with full 5-block measurement methodology (per the Cast + BT research arc) require the debug-agent verification step to actually run as part of PR acceptance — a change that doesn't measurably move its target metric is not merged.
