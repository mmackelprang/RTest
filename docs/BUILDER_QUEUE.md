# Builder Queue

> Work items queued by **Planner** for **Builder** to clear one PR per cycle.
> Planner appends rows + spec/plan links; Builder claims a 📋 row whose dependencies are all met, ships it as a PR, then marks it ✅.
>
> **Last updated:** 2026-09-09 (Coordinator) — ⭐ **`UX-1` IS DEPLOYED AND LIVE on `radio`** — verified via both `/api/health/version` endpoints, the token in the SERVED CSS (`--skeleton-shimmer-highlight: #38383F`), and kiosk connection count. ⛔ **BUT THE SHIMMER IS EFFECTIVELY INVISIBLE IN NORMAL OPERATION, AND NOBODY HAD MEASURED THAT.** The panels it decorates load their data in **3–7 ms** (measured on the box: `/api/playhistory` 0.0030–0.0051 s, `/api/queue` 0.0051–0.0067 s). One animation cycle is **1500 ms** and the first **~390 ms** of it is the `ease` dead pause — so the skeleton is on screen for roughly **0.3% of one cycle and NO SWEEP IS EVER PAINTED, at 56, at 36, or at the old 6.** ⭐ **The entire `UX-1` A/B was judged in a harness against a loading state this appliance never holds** — the row shipped a correct token for a surface nobody sees. ⚠ **Reduced-motion was FALSIFIED as the cause** (`enable-animations: true`; `design-system.css:1779` would have killed the sweep outright but is not firing). ⭐ **Follow-up decision needed and NOT yet queued:** leave it (it still covers genuinely slow paths — cold start, stalled API, Cast), add a **minimum dwell** so it is visible when it appears (at the cost of making fast loads feel slower), or **drop the skeletons** on panels that resolve in single-digit ms. ⛔ **`OPS-12` FILED (P0) — THE DEPLOY THAT SHIPPED IT WAS THE SECOND ATTEMPT; THE FIRST TOOK THE APPLIANCE DOWN.** An `rsync.cmd` shim + persistent PATH change — ⛔ **an OWNER DECISION for RotaryPhone deploy safety, NOT "unrelated" as first recorded** — silently flipped `Deploy-ToLinux.ps1` onto a transport branch that had **NEVER executed once**; it failed at [3/4] AFTER [2/4] had stopped both services. **THREE stacked defects, each revealing the next** (path form → ssh transport → ssh identity), so fixing only the first causes a SECOND outage. ⭐ **And the decision was never achievable in EITHER repo** — defects 2 and 3 are transport-level, so MSYS2 rsync cannot reach the box from this workstation at all; it gave RotaryPhone "failed rsync then tar" and gave us an outage. **Owner has ruled: leave the shim disabled.** ⭐ **The independent and larger defect: the transport is never proven reachable BEFORE the step that stops services** — that ordering turns any sync error into an outage regardless of transport. ⭐ **`OPS-9` is why this was diagnosable** — its per-call exit-code capture said `API sync failed!` honestly instead of claiming success. —  2026-09-09 (Builder) — ⭐ **`AUD-2` SHIPPED as [#642](https://github.com/mmackelprang/RTest/pull/642) — four primary source types registered under a key they minted while `AudioManager` addressed them by `IAudioSource.Id`, so gain and ducking wrote to a key nothing read.** ⭐ **THE OWNER'S EAR IS WHY THIS SHIPPED NOW** — the row had sat 📋 since 2026-08-11 on a code read plus one log line; the `PHN-2` `SOUND` sitting turned it into a heard failure on two independent paths. ⛔ **The suite was green throughout, and still is** — `_duckingMultipliers[sourceId] = multiplier` is an **indexer**, so writing to the wrong key throws nothing and returns nothing. ⭐ **THE TESTS THEREFORE ASSERT AN EFFECT, NOT THE ABSENCE OF AN EXCEPTION:** injecting the defect at the third party turns them red at **expected 0.2000, was 1.0000**. Six mutations run; every one bit. ⚠ **Bluetooth had this exact bug and was fixed alone by `2bbd0eb5` six months ago — that fix pinned nothing, which is why it survived in four more source types.** ⛔ **UAT IS NOT DISCHARGED and the PR says so plainly**: unit tests and a lint cannot hear anything, so the audible gate is `PHN-2` §3 U1 re-run at the cabinet. ⚠ **Fixing this makes `PHN-10` QUIETER without making it less real** — do not read a post-fix re-listen as evidence it is fixed. ⭐ **BOTH REVIEWERS FOUND THE SAME IRONY INDEPENDENTLY: the PR shipped two overclaiming comments inside the PR about overclaiming comments**, and a third appeared while fixing them — the corrected line anchors had already shifted again, because the same commit was editing those files. Self-referential anchors are now symbol names. ⚠ **Merged, NOT deployed** — explicitly out of scope. **Counted from the instrument, validated against the previous 26/55:** **27** live, **55** archived, 27 + 55 = **82** total. **All four count sites re-derived and in agreement.** ⚠ **This count excludes the coordinator's `OPS-12` row, which was uncommitted in the shared tree when this was written; with it the live figure is 26 and the total 81.** —  2026-09-09 (Coordinator) — ⛔ **THE OWNER RAN `PHN-2`'s NINE `SOUND` CHECKS AT THE CABINET: 5 pass, 3 FAIL, 1 deferred. `PHN-2` DOES NOT CLOSE.** [Record](uat/2026-09-09-phn2-sound-uat/RESULT.md). ⭐ **`AUD-2` is now CONFIRMED BY EAR on two independent paths** — voicemail ducking and TTS ducking, with the radio audibly playing. It had been sitting 📋 with a finished plan and no dependencies; it is the obvious next build. **Two new rows filed from the same fifteen minutes:** `AUD-24` (the seek bar does not move the audio, while `Time` advances correctly — a silent no-op wearing the costume of a working control) and `PHN-10` (two voicemails play at once, **and `PHN-1f` already SHIPPED the queue that was supposed to prevent it**). ⚠ **The console was MUTED when the sitting began** — every `SOUND` check would have failed for the wrong reason. ⭐ **Nine automated checks would have reported nothing**, exactly as the plan warned: *"a green 'it completed' is the least trustworthy evidence available on this row."* **Three defects in fifteen minutes, against a green suite.** —  2026-09-09 (Builder) — ⭐ **`UX-1` SHIPPED as [#641](https://github.com/mmackelprang/RTest/pull/641) — the shimmer highlight gets its OWN token at the owner's `56` (`#38383F`, delta 36, 6× the shipping delta), and `--surface-overlay` was not touched.** ⭐ **THE OWNER'S EYE OVERTURNED THE DESIGNER'S CENTRAL CLAIM AND THE ROW GOT CHEAPER FOR IT** — the design answer said *amplitude is the smaller half, geometry is the lever*; the owner could see only the full-width shipping ramp and found even that *"very dim and not easy to see in a dark room"*, and the Designer's seed of **36 sat below the dark-room threshold**. ⭐ To its credit the Designer **refused to endorse a landing value** (*"a seed for the A/B, not an answer"*), which is why this was A/B'd rather than built. Daylight wanted no more than night, so the **ambient-adaptation branch did not fire**. ⛔ **THE CENSUS EVERYONE QUOTED WAS SCOPED TOO NARROWLY** — *"20 consumers in `design-system.css`"* is true of that file and **three short of the app**: re-derived on `main` @ `143d678e` the file holds 1 declaration + 1 prose mention + **20 consumers** (one being the shimmer → 19 others), **plus three inline `style=` consumers at `PlayHistoryPage.razor:207,272,296`** — **real blast radius 22**. ⭐ *A count is only as good as the tree it was taken over* — same shape as `KIOSK-3`'s *"zero consumers"* grep scoped to `src/` while the consumer was a shell script, and `UI-11`'s mutation run under a `--filter` and reported as *"the entire suite"*. ⭐ **THE GATE WAS A DEMONSTRATED DIFFERENCE PAIRED WITH A CONTROL, and the control is the half that matters:** computed middle stop `rgb(26,26,29)` → **`rgb(56,56,63)`** while **`.surface-overlay` and `.rz-dialog`, two DIRECT consumers of the shared token, both held `rgb(26,26,29)`** — a gate checking only the subject would pass just as happily on the re-theme the row exists to avoid. ⭐ **Instrument validated first** — 859 rules actually loaded, `prefers-reduced-motion` **false** so the flat-fill override was not answering instead, and ⭐ **the reading was PROVEN animation-invariant rather than assumed**: sampled 420 ms apart while running, `background-position` moved `-136.261%` → `84.8031%` while `background-image` was byte-identical, so `UI-8`'s mid-`transition` trap is inapplicable **by establishment, not by hope**. ⭐ **Framebuffer too, and it calibrates on the published baseline exactly** — amplitude **6.0 → 35.83 levels**, max 26.0 → 55.83; ⭐ **`C-221`'s trap DEMONSTRATED rather than cited** (adjacent-pixel differencing moves only **0.5 → 0.667** where the real change is 6×). ⚠ **The plan's own §4.2 gate is internally inconsistent** — it defines `S` as a *peak* windowed slope but calibrates V0 against **0.034**, the *average* slope; the measured peak is **0.052**, so its *"do not proceed past a V0 that disagrees"* **would have halted a correct instrument**. ⛔ **THE GEOMETRY HALF OF THE PLAN DID NOT SHIP, AND THAT IS A DECISION** — the only experiment ever run on the narrowed ramp was the `v1` harness, later found unable to measure what it claimed; `56` was chosen at the **shipping** geometry, and shipping the ramp would put an unvalidated change under a validated one. ⛔⛔ **AND PRE-MERGE REVIEW CAUGHT THE BUILDER PROPAGATING A WRONG NUMBER INTO A PERMANENT ON-PAGE BANNER — the instrument built to condemn an instrument had the same defect it was condemning.** `NIGHT-SITTING.md`'s *"band on the element only ~0.49 s, two-thirds of every cycle static"* is **swapped, and its arithmetic counts one tile** — the harness sets `background` as a **shorthand**, which resets `background-repeat` to `repeat`, so the 2W tile recurs and the band crosses **twice** per cycle. **Measured in Chromium: ON ~1.01 s, OFF ~0.49 s.** ⭐ **The real mechanism is `ease`** — the animation declares no timing function, and the sweep decelerates to **2.1 %/s against a median of 221.7 %/s**, a **~390 ms dead pause at every cycle boundary** plus ~110 ms mid-cycle, which a narrow band sits out and a full-width ramp cannot. ⭐ *The conclusion survived; the reason did not* — and the Builder's own comment **contradicted itself eight lines later**, deriving the correct two-passes-per-cycle tiling that its duty-cycle figure ignored. **Corrected in nine artifacts, including the source record.** Plan now banner'd at the top and at both tasks. ⛔ **A LIVE FALSE CLAIM WAS FOUND IN THE ROW'S OWN EVIDENCE DIRECTORY** — `uat/2026-09-08-ux1-shimmer-variants/REPORT.md` still read *"The harness works and is reusable"*, falsified the next day, while `index.html` carried **no warning at all**, so **the one file someone would open in a browser was the one with nothing on it**. Struck; the banner is deliberately **dim** because that page is judged in a dark room. ⚠ **THREE DEFECTS IN THE BUILDER'S OWN COMMENT, found by re-deriving it** — *"two `color-mix` recipes"* is **three**; *"answers yes and yes in both conditions"* **overclaimed** (calmness was asked **only at night**; daylight *argues* rather than establishes it); *"measured"* → *"judged"*, because an owner looked at a ladder. ⚠ **And a commit message was wrong** — `62549d87` claimed all four tests are RED gates; **three are**, the fourth correctly passes both sides as a forward guard. ⚠ **TWO THINGS ARE OPEN**: whether a steeper ramp would allow a *lower* amplitude, and ⛔ **the plan's Task 0 dwell-time pre-check, which was NEVER RUN** — one pass is 750 ms, so if production skeletons dwell for less **no shape completes a pass at any amplitude**, and the owner judged a harness that animates continuously and cannot answer it. **The amplitude is right; whether skeletons are on screen long enough to show it is not established.** Both in `design/FUTURE-WORK.md`. ⚠ **No test here can see this change** — the four new tests assert CSS **source text**, so a green suite is **not** UAT for this row; measured both ways, reverting the stylesheet gives **3 failed / 5 passed**. Build **47 / 0** (`--no-incremental`), suite **3,868 passed / 4 failed**, all documented Windows-known-failing. ⚠ **Merged, NOT deployed** — the coordinator deploys. **Counted from the instrument, validated against the previous 25/53:** **26** live, **54** archived, 26 + 54 = **80** total. **All four count sites re-derived and in agreement.** — **Prior entry (Builder):** ⭐ **`UI-8` SHIPPED as [#640](https://github.com/mmackelprang/RTest/pull/640) — the owner's NO GLOW decision landed as *delete*, and neither token was declared.** ⛔ **A ZERO-VISUAL-CHANGE ROW IS A GATE-DESIGN PROBLEM, and that is the transferable part.** The two tokens were never declared, so all three `box-shadow`s were already invalid at computed-value time and resolved to `none` — **a test asserting "the glow is absent" proves nothing, and a UAT claiming to have observed the change would have been false.** The dark-room sitting was cancelled because it **had no subject**. The honest gate is **equality**: computed `box-shadow` on all three sites is `none` **before and after**, measured in Chromium against the real stylesheet. ⭐ **The instrument was validated before the result was believed** — `.nav-badge` (which consumes the *declared* amber glow) held `rgba(240,168,48,0.25) 0 0 6px` throughout, and the backgrounds resolved to `--signal-green`/`--signal-red`, proving the rules **match**; without that, `none` would have been vacuous. ⚠ **Two instrument bugs were caught first** — a CSSOM walker that treated every `CSSStyleRule` as a nesting container, and a reading taken **mid-`transition`**. ⛔⛔ **PRE-MERGE REVIEW CAUGHT AN OVERCLAIM INSIDE THE BUILDER'S OWN CORRECTION — the fourth time this repo has paid for that shape, and it arrived *inside a fix for exactly that defect*.** The rewritten `ENC-12` comment claimed resurrecting the tokens would "re-import a dual-source bug" on the hero dot; **false on either reading** — the rule no longer references either token, and `PhoneStatusHero.razor:15-16` sets `box-shadow` inline every render, which outranks any normal author declaration. ⛔ **The dossier's own correction #1 has the same defect**, asserting both *"inline overrides unconditionally"* and *"making it reachable would paint the dot green during an amber `Ringing` state"* — **which cannot both be true**; the Builder inherited the weaker half. ⭐ *A correction is not exempt from the standard it is enforcing.* ⭐ **The `⛔ do not declare` note was moved from 5,300 lines away to beside `--signal-amber-glow`**, where someone adding the tokens would actually go. ⚠ **The token lint was RUN in BOTH directions — 2 violations against `main` at the three cited lines, 0 against the branch — and it CORRECTS the dossier: the allowlist of six is missing `--bubble-accent`** (9 sites, injected inline by `SourceBubble.razor:15`/`MainLayout.razor:57,71`), so *"exactly the two tokens"* holds only with a **seventh** entry. **Committing the lint is left to Planner as new scope.** ⚠ **This diff falsified an XML `<remarks>` block sitting in still-`📋` `UX-1`'s plan** — a Builder pasting it would have shipped a false comment; amended there and at §5.3, **touching no `UX-1` decision, task or scope**. ⚠ **An incremental Release build reported `0 Warning(s)` and that is NOT the gate passing** — no C# project recompiled, so the figure is *unmeasured*, not *better*; `--no-incremental` restored the real **47 / 0**. Suite **3,863 passed / 5 failed**, all documented Windows-known-failing (the four `SrcVariableResamplerTests` plus `CoverArtArchive_ReturnsValidUrl_ForKnownRecording`, which **passed** in an earlier run the same cycle). ⚠ **Merged, NOT deployed** — explicitly out of scope. **Counted from the instrument, validated against the previous 26/52:** **25** live, **53** archived, 25 + 53 = **78** total. **All four count sites re-derived and in agreement.** — **Prior entry (Coordinator):** ⭐ **`AUD-20` FILED: the SDR source misses its callback deadline 2–4× more often since 09-08 ~13:00 and NO CAUSE IS ESTABLISHED.** ⛔ **Its value is the FIVE falsified hypotheses** — SongRec churn, the 09-08 deploy, RF quality, a changed threshold, and a GC regime change (that last one **inverted on measurement**: 09-07 is the outlier, not the elevated days). **Do not re-run them.** ⚠ **And the fourth count site had drifted AGAIN** — the *"All N of them"* line read **51** against an archive heading of **52**; it is the same site found stale at 45-vs-47 this morning and it is the one that keeps getting missed. — ⛔⛔ **`AUD-12` SHIPPED as [#623](https://github.com/mmackelprang/RTest/pull/623) — and its UAT is DEFERRED, NOT DISCHARGED.** The owner authorised the merge with the gate outstanding; **that moved the gate, it did not satisfy it.** **No phone was connected, no A2DP source played, no pause/resume performed, and the appliance was never touched.** The plan's §5 still has to be run, and its four-way vacuity table read first — **three of the four make the UAT lie in the dangerous direction**, including the UAT's own step 4, where **pressing the transport button clears the stall**. ⭐ **THIS WAS A RE-GATE, NOT A HAND-MERGE, AND THAT IS THE WHOLE POINT.** The PR had been open six days with gates green against a `main` **33 commits** older. Integrated by **merge, not rebase** — one conflict instead of seven, no force-push onto a `git push` that fails over HTTP/2 while printing `Everything up-to-date`, and the body's four evidence shas stay resolvable. **Exactly one conflict:** this banner line, resolved to `main`'s side because the branch's version led with *"`AUD-12` … will STAY 🚧: deliberately unmerged"* — falsified by the authorisation. ⭐ **`main` WAS GATED FIRST, which is the only reason the result is attributable:** `main` @ `17e56719` = 47 warnings / 0 errors, **3,854 passed / 4 failed**; merged = 47 / 0, **3,864 passed / 4 failed** — *the same four* `SrcVariableResamplerTests` **by name**, **+10 passing**, per-project equality everywhere else. **Zero regressions.** ⭐ **Mutations `M1` and `M8` re-run IN THE MERGED TREE at WHOLE-PROJECT scope — 1,556 cases, no `--filter`** — killing exactly 3 and exactly 1 as predicted. **A positive control validates the INSTRUMENT, never the SEARCH SPACE**, re-earned one row after `UI-11` paid for it. ✅ **The adjacency the re-gate existed to check came back CLEAN, and the negative result is the deliverable:** `UI-13`'s cache-advance reordering cannot interact because `AudioStateUpdateService` **never subscribes to `IAudioSource.StateChanged`** — promotions reach the wire only through the 500 ms poll; `UI-12`/`UI-14`'s null gates cover events carrying no BT playback state, and the posited null is **not representable** (`AudioDtoMapper` coalesces every field); `UI-11` and `GV-12` are different assemblies. ⛔⛔ **`AUD-12` IS A COLLIDING ID, and this merge is where that starts to bite.** `HANDOFF-GA-PUNCH-LIST.md:1060` carries a **different, still-open `AUD-12`** — *"PipeWire event subscription in place of polling"* (2026-05-22) — also named at `:1365` and `:1431`, **so "`AUD-12` ✅ shipped" is true of the queue row and false of that one.** The schemes diverged from `AUD-10` onward: `AUD-10` and `AUD-11` collide the same way; `AUD-1`/`AUD-2`/`AUD-4`/`AUD-5` still agree. **Three colliding ids. Planner to reconcile — nothing was renumbered.** ⛔ **The PR's own body claimed the opposite** (*"has no punch-list row"*) — right conclusion, reason refuted by one grep, wrong in the direction that hides the collision. ⚠ **Second adversarial review, run AFTER the merge on the resolved diff: no HIGH, and every finding was in a COMMENT.** ⛔ ***"Stopped has TWO provenances" was a false census — there are THREE***; `AudioSourceBase.StopAsync` was unnamed, and the guard refuses it for a reason the comment never gave (`StopCoreAsync` nulls all three disjuncts first). ⭐ *A comment that says "TWO" is a claim about the whole program, not about the two it names.* ⛔ **The one new line citation this branch added had ALREADY ROTTED before merge** — `:184`, pushed down by **the branch's own +60-line field remark**; this file's citations have now been invalidated **twice by this row's own diff**. ⛔ **The `volatile` remark narrated an interleaving its own stated guarantee forbids** — the field is written *before* the switch, so no stale read is possible; the hazard is the helper's read-then-act. ⚠ **The disconnect clear's guarantee was one-sided and is now scoped, not fixed:** clearing before the first `await` guarantees the clear *runs*, not that the bit *stays* clear — a late `PlaybackStatusChanged(Playing)` during the `async void` teardown re-arms it. **That window is `AUD-14`'s to close** (it is reachable only through `C-174`'s stale-watcher leak) and a second clear is a live-audio-path change this row still cannot UAT. ⚠ **Three claims in the row's OWN dossier were false** — *"the only exit is an AVRCP edge"* (`AudioSourceBase` leaves `Ready` at `:96`/`:127`/`:139`), *"BlueZ signals only on change, so no edge is coming"* (a **fresh** `AttachMediaPlayerAsync` re-reads `Status`; the silence is a **re-attach** hitting its dedup, `C-174`), and *"`AudioController.cs:576` is a pure projection"* (it is `:575`, and a **conjunction** with `_audioEngine.State == Running`). **All three were already corrected in the CODE by the first review and never propagated to the prose beside it.** ⭐ **Every conclusion survived; none of the reasons did.** ⚠ **CI `build` and `llm-review` were both still `queued` on the self-hosted runner at merge time and were not waited on** — `main` protection is off and local gates are the merge-blocking truth (`CLAUDE.md` § *The merge gate*); GitGuardian passed. **Counted from the instrument, validated against the previous 26/51:** **26** live, **52** archived, 26 + 52 = **78** total. — **Prior entry:** ⛔⛔ **`UI-11` SHIPPED as [#637](https://github.com/mmackelprang/RTest/pull/637), and the sharpest finding is that THE BUILDER'S OWN CORRECTION WAS FALSE — pre-merge review caught it before the PR opened.** Mid-cycle the Builder struck the plan's §6.3 (*"a wider pattern is how the console goes black"*) on a mutation reading **20/20 green**, and wrote that into **four places**. Re-run over the **whole project: 7 failed / 1,197 passed** — all four `StaticAssetPipelineTests` cases RED, `/css/design-system.css` returning `NotFound`. **§6.3 is TRUE**; the strike was reversed. ⛔ **The cause was SCOPE, not method, one row after `KIOSK-3` recorded the identical lesson:** the mutation ran under `--filter "FullyQualifiedName~ApiNotFoundPipelineTests"` and was reported as *"the entire suite"* — **20 is exactly that one class's case count**, and the guard sat in the same project, in **the very file this row's own Task 1 had moved an hour earlier**. Two earlier mutations had each failed exactly their own test, and that is what made the third feel safe. ⭐ **Re-earned verbatim: a positive control validates the INSTRUMENT, never the SEARCH SPACE.** ⭐ **The mechanism is an asymmetry worth carrying — page routes are endpoints, static files are not**: a real `@page` endpoint competes with the fallback and wins **on precedence first** (⚠ so `Order = int.MaxValue` is NOT what protects it, and prose saying so makes an `Order`-only change look safe), while static files never compete at all — routing selects the fallback and `StaticFileMiddleware` stands down because *some* endpoint was selected. ⛔ **THE ROW'S OWN HEADLINE WAS ALSO FALSE:** `Radio.Web` has **no SPA fallback and never had one** (`git log -S MapFallback --all -- src/` = 0 commits, no `index.html`, twelve plain `@page` routes); both cited incidents emitted from **RotaryPhone's `:5004`**, and **our own archive line 99 said so the whole time**. ⭐ **A misattribution surviving two hops — they said "your trap", we recorded "ours", and neither side re-derived which server sent the bytes.** What survived was that our 404 was **correct by accident and bodyless**, which is what cost them a probe. ⭐ **The plan's one unmeasured assumption was MEASURED and HELD — 18 passed / 2 failed exactly as predicted**, so its stop-and-re-plan condition did not fire; two refinements recorded (the RED is one assertion earlier than predicted — `Content-Type` is `null` outright — and `/Error` renders fine, so all twelve routes stayed in the guard). ⚠ **Review also found the test host had been dialling the LIVE APPLIANCE** for the whole of `OPS-5` and this row: `RadioWebFactory` claimed hosted-service removal stopped the background polls, but `PhoneHubService`/`GvTrunkHubService` are `AddSingleton` started fire-and-forget outside that mechanism and opened real SignalR connections to `radio:5004` on a 10/30/60 s ladder. Now pointed at `127.0.0.1:1`. ⚠ **`git log -S` citations are falsified by the commit that writes them** — corrected in four places; **a citation can be true when written and false when merged.** ⭐ **The wire check earned its place twice:** `Content-Type` carries **no `charset`** (`.MediaType` strips parameters, so a charset would have passed silently), and the `PHN-5` no-logging claim was **falsified rather than trusted** — four requests carrying a phone number and a token produced **zero** log lines. ⭐ **`UseStatusCodePages` measured in BOTH directions and it is why Option B beat Option A:** with the terminal rule removed the middleware injects `404 text/plain` into a response the API contract owns; with it present the response is untouched. **Counted from the instrument, validated against the previous 27/50:** **26** live, **51** archived, 26 + 51 = **77** total. ⚠ **A fourth count site was found stale and fixed** — the archive's own `## Shipped rows (47)` heading, adrift by three. ⚠ **`build` and `llm-review` both PASSED (1m32s / 2m49s)** — no runner stall, twice running. — **Prior entry:** ⛔ **`KIOSK-3` SHIPPED as [#635](https://github.com/mmackelprang/RTest/pull/635), and it does NOT reach the box on a deploy.** `/usr/local/bin/radio-console-open` is installed by `setup-kiosk.sh`, which `Deploy-ToLinux.ps1` never runs — the live copy was still dated **Aug 18** and still read `psidtsAgeSeconds`. **The box also has no checkout**, so the kiosk directory must be copied over first; both install paths are in the PR body. ⭐ **The field lied in BOTH directions**, reproduced live at 14:39Z: `psidtsAgeSeconds: 34` inside its own "healthy" band while `cookiesValid:false, degraded:true` in the same payload. ⛔ **Review caught a false comment that had been INHERITED, not invented** — the 120 s gate cited `MeasuredSixtySecondCadence_StaysHealthy` as pinning the cadence; that test is a `FakeTimeProvider` and a literal `AddSeconds(-60)`, so **nothing detects a cadence slip**, and the same false claim in the test's own `<remarks>` was fixed too. ⭐ *A reason inherited from another file is not thereby verified.* — **Prior entry (Coordinator):** ⭐ **THREE OWNER DECISIONS LANDED AT THE PANEL, and two of them made rows CHEAPER rather than more expensive.** **`UX-1` = `56`** (`#38383F`, delta 36), confirmed in daylight AND dark — **the ambient-light-adaptation branch did NOT fire**, so it stays a one-token build. **`UI-8` = NO GLOW** — the Designer is overruled, the row flips to *delete the dead references*, **the dark-room sitting is CANCELLED** (the edit is a literal zero-visual-change cleanup) and it becomes **auto-mergeable**. **`PHN-9` CONFIRMED** against its own stated kill condition, with unplanned corroboration: the badge read **14 → absent across the deploy → 15 after the owner tapped PHONE**. ⚠ **Both sightings nearly recorded the WRONG answer for the same reason** — a positional harness (`"A"`, *"the ones on top"*) whose mapping the coordinator held stale; caught both times by re-querying the live instrument. **Ask for the VALUE, not the position.** — **`UI-13` shipped as [#634](https://github.com/mmackelprang/RTest/pull/634) and archived.** **Counted from the instrument, validated against the previous 28/49:** **27** live, **50** archived, 27 + 50 = **77** total — **`KIOSK-3` shipped as [#635](https://github.com/mmackelprang/RTest/pull/635) and archived.** ⛔ **`KIOSK-3` is DEPLOY-GATING and was found by re-deriving a claim we had asserted three times**: the desktop launcher reads `psidtsAgeSeconds`, which RotaryPhone's #79 deletes. **Our "zero consumers" grep was scoped to `src/`; the consumer is a shell script.** ⭐ **A positive control validates the INSTRUMENT, never the SEARCH SPACE.** ⛔ **THE ROW'S HEADLINE WAS FALSE AND THE PLAN SAID SO** — `SendAsync` cannot throw on a failed broadcast (`HubConnectionContext.cs:361` catches everything else, aborts the **connection**, returns a **successful** `FlushResult`; 129 sends to a hard-killed socket faulted none). Only cancellation escapes, that token is always `stoppingToken`, and `ExecuteAsync` catches exactly that as shutdown and breaks — **the stranded cache dies with the process.** ⭐ **So the deliverable was the written-down PRECONDITION, not a fix**, recorded in the code above the cache-field block, in **ADR-034**, and in the test remarks: a backplane or any different token makes this live **and silent**. ⭐ **RED was 6 failed / 2 passed on `Expected: 2 / Actual: 1`** — the defect stated numerically. ⭐ **`M1'` — six INDEPENDENT per-site reverts, each failing exactly its own test** — is what proves *"fix the shape, not the instance"* rather than asserting it; **`C1` stayed 8/8 green**, so the tests pin behaviour, not line position. ⛔ **`M3` AS PLANNED CANNOT BE RUN** — deleting an assignment is `CS0649` + warnings-as-errors, so the **compiler** gates it before any test; same shape as `UI-14`'s two non-compiling mutations, one cycle later. ⭐ **Pre-merge review found a MEASURED four-site test gap**: one guard covered `CheckVolumeAsync` alone, and mutating `_lastPlaybackState` to `null` left **all 8 tests green** while re-broadcasting an unchanged state twice a second forever. Four guards added, each verified to fail only its own mutation. ⚠ **A cancellation test structurally CANNOT detect a never-advanced cache** — it only invokes twice. ⛔ **The precondition comment invalidated its own line citation in the commit that wrote it** (`:196` → `:223`, moved by its own insertion), and ⛔ **"records *the clients have this* as a fact before it is one" was refuted by the evidence eleven lines below it** — a completed `SendAsync` is **not** proof of delivery. ⛔ **"The eight `async void` handlers" is nine**, traceable to an in-tree comment that correctly says *"eight siblings"* being read as the total. **`_lastQueue` was DELETED rather than moved**, against the plan (struck in three places): `d465cdd2` deleted its only reader in the same commit that orphaned the write. ⛔ **Do not file the follow-up row — it is done.** ⭐ **`UI-15` FILED from review finding `L5`, and unlike `UI-13` it is a LIVE defect**: `CheckSourceChangedAsync` treats `null` as both *"never observed"* and *"no source"*, so every `null → non-null` transition is suppressed as a first poll and a real source change is **never broadcast, with nothing logged**. ⚠ **No UAT and none is possible** — before and after are identical on every reachable path; nothing was deployed. ⚠ **CI `build` never ran** — it sat `queued` on the self-hosted `appserver` runner; merged on local gates per `CLAUDE.md` § *The merge gate*, which makes CI advisory. ⚠ **`llm-review` PASSED in 2m17s this cycle**, contradicting the expectation that it stalls indefinitely. _**Prior entry — `UI-14` (Builder):**_ ⭐ **`M0` MEASURED THE HOLE BEFORE THE FIX, and that measurement is the row's real deliverable:** the *realistic* forbidden refactor — keep `AcceptPayload` **and** add a "defensive" null check to `NotifyAsync<T>` — was **green across the entire solution** (`Radio.Web.Tests` 1,172/1,172) while silently dropping every `NowPlayingChanged(null)`. The *naive* form fails only 4, **so the gate `UI-12` left behind looked real and was not.** `M1`, the same mutation after, is RED on exactly the three new tests. ⭐ **`M7` is the shape-only control `UI-12`'s `M4` failed to be** — 1,180/1,180 green with the fan-out lint run under it. ⛔ **SIX premises fell, and the row's own load-bearing claim was one of them.** The PERMANENCE argument (*"once `_lastNowPlaying` records the silence… never re-sent"*) is **refuted by the function it cites**: `_lastNowPlaying` cannot hold a null, and `HasNowPlayingChanged` returns `true` unconditionally when either side is null, so a cached null would re-broadcast on the next **500 ms** poll. The real mechanism is that the **server** caches a non-null snapshot while the **client** drops a null off the wire. ⭐ **Right conclusion, wrong reason — for the THIRD row running** (`UI-12`, `UI-14`'s planning, and now `UI-14` itself). ⛔ **"`AudioStateUpdateService` only broadcasts on a change" is FALSE of the service** — `EventPlaybackChanged` has no change comparison anywhere in its path. ⛔ **This row's claim that `UI-12` falsified `AudioStateHubServiceTests.cs:148-158` is wrong on both halves** — `UI-12` never touched that event, and the comment's `HubConnection`-handler-table claim is still literally true; **`UI-14` falsifies it, by a different route**. ⚠ **Two of the plan's mutations DO NOT COMPILE** (`CS8620`, `CS0067`) — reported as measured rather than adjusted to fit; for `M5` the **compiler** is the gate, before any test. ⚠ **ADR-033's third counting slip fixed**: *"seven payload-carrying events"* → **eight**, seven of them reference-typed. A reflection census now stops a fourth. ⚠ **Not demonstrable on the appliance and the PR says so** — but a browser-driven circuit did prove `OnVolumeMessageAsync` executing 3× on real broadcasts. ⭐ **`PHN-9` was found on the appliance, not in the code**: the PHONE unread badge read 14 before a redeploy and vanished after it, and the disappearance was investigated instead of explained away. **Two plausible first guesses were both wrong** — not a regression from that morning's four PRs, and not the 30 s hub timeout sitting right there in the log. ⛔ **`AUD-19` was CITED BEFORE IT EXISTED.** `AUD-1.md:113` claimed since 2026-09-08 that it was *"filed … documented in `design/FUTURE-WORK.md`"* — **neither was true**, while `AUD-1`'s Builder was told to cite it in its PR body before merge, so that Builder would have had to stop or invent the reference. The row exists now and `AUD-1` carries a struck correction. **The coordinator wrote that citation; `UI-12`'s Builder caught it.** ⚠ **This also corrected a stale figure below** — the *"All N of them"* line read **45** against an archive of **47**, so two of the three count sites disagreed with the instrument. ⭐ **All four hub events declared `Func<T, Task>` now reject a null payload in ONE shared, logged helper ABOVE the fan-out**, and the `On<T>` type arguments became nullable — the type finally agreeing with what a JSON deserializer across a process boundary can produce. ⛔ **NOT in `NotifyAsync<T>`**: `T` is erased there, so a check would silently drop every `NowPlayingChanged(null)` — which means *"nothing is playing"* — stranding the dock on the previous track forever (ADR-033). ⚠ **The change is UNOBSERVABLE in production and the PR says so**: a null cannot arrive, so no UAT can demonstrate the guard, and a UAT report claiming otherwise would be wrong. ⛔ **Four premises fell, one of them the row's own stated reason** — it refused a guard because one *"would drop a broadcast the UI handles today"*, and the UI does **not** handle it: `RadioControlPanel.razor:1053` throws an NRE (swallowed since `UI-7`) while three other surfaces wipe their cached radio state. ⭐ **The conclusion survived; the reason did not.** The `Encoder*` guards cited as precedent were **copy-propagated boilerplate from an event since deleted**, and `db132192` did the identical transformation on `RadioStateChanged` adding no guard — neither shape was ever reasoned about. ⛔ **The plan's control mutation could not pass and its own `M2` row said why** — it varied behaviour *and* shape; **a control that cannot pass is not a control**, and the corrected `M4b` is 8/8 green. ⚠ **The plan's `grep -cE "warning"` gate returns 94, not 47** (MSBuild emits each warning once per project-graph pass) — same defect in `OPS-2:536`. ⚠ **Pre-merge review found no HIGH and all seven findings in COMMENTS**, the sharpest being a seam justified by *"a test cannot do this"* that `RadioControlPanelBandSyncTests.cs:167-173` does today — `CLAUDE.md` item 4 verbatim, **arriving inside a correction**. ⭐ **`UI-14` is the deferred half and the sharper row**: this cycle gated the four events where a regression is *unobservable* and left the three where it is *user-visible* ungated — the realistic form of the forbidden refactor **passes the entire suite** while dropping every `NowPlayingChanged(null)`. ⛔ **`AUD-19` DOES NOT EXIST** — `queue/AUD-1.md:113` says it was "filed … documented in `design/FUTURE-WORK.md`"; it is in neither queue file, has no dossier, and is absent from `FUTURE-WORK.md`. `AUD-1`'s Builder is told to name it in a PR body before merge. **Planner to reconcile.**
>
> ⚠ **Planner: append new rows here as one line.** The Item cell is a short title plus a link to `queue/<ID>.md`; the detail belongs in the dossier. The old shape put 12,946 characters on one physical line, which put the file past what any agent could read in a single pass.

---

## Status legend

| Mark | Meaning |
|---|---|
| 📋 | Queued — ready to claim (all dependencies met) |
| 🔒 | Blocked — waiting on a dependency row |
| ⛔ | On hold — blocked on an **external** dependency (another repo/team must ship first); do not claim |
| 🚫 | **Parked — refused by an owner decision. Never claim.** ⭐ **NEW 2026-09-05, and it exists because the alternative was a false status.** A refused row is not ⛔: ⛔ says *another team must ship first*, which promises the work resumes when they do. `GV-5` is not waiting on anybody — `D31` says the thing it unblocks is not wanted. Marking it ⛔ would have made this queue assert something untrue, which is the defect class `CLAUDE.md` § *Pre-Merge Review* exists for, one layer up from a comment. **The row stays in the table rather than being deleted**, because the reasoning is the asset and a deleted row gets re-proposed. **The reason always lives in [`HANDOFF-GA-PUNCH-LIST.md`](HANDOFF-GA-PUNCH-LIST.md) §6**, not here. |
| 🚧 | In flight — a Builder cycle is working it on a branch |
| ✅ | Done — merged to `main` |

---

## ⛔ ID namespace — check BOTH files before taking a number

**There is ONE `AUD-*` namespace, shared with
[`HANDOFF-GA-PUNCH-LIST.md`](HANDOFF-GA-PUNCH-LIST.md).** Until 2026-09-09 the two documents numbered
independently: `AUD-1`–`AUD-9` happened to agree, but **`AUD-10`, `AUD-11` and `AUD-12` meant three
different things in each**, so *"`AUD-12` shipped"* was true of this file and **false of the punch-list
item of the same name.** Found by `AUD-12`'s own Builder during Docs Impact; one grep refuted the PR
body's claim that no punch-list row existed.

**The punch-list renumbered, because this side could not** — these IDs carry dossiers, branches, merged
PRs and archive entries. Its `AUD-10`/`11`/`11a`/`12` became **`AUD-21`/`22`/`22a`/`23`**.

⚠ **`AUD-21`, `AUD-22`, `AUD-22a` and `AUD-23` are RESERVED by the punch list. The next free `AUD`
number is `AUD-24`.**

✅ **AUDITED 2026-09-09 — `AUD` was the only collision.** `GV`, `UI`, `TEST`, `OPS`, `PHN`, `ENC` and
`KIOSK` were each checked by comparing the subject both documents give every shared ID, run twice
independently. **No other prefix diverges.**

**Next free number in each namespace** — take these, and update this line when you do:
`AUD-24` · `GV-13` · `UI-16` · `TEST-9` · `OPS-11` · `PHN-10` · `ENC-22` · `KIOSK-4`.
⚠ `GV-11` and `PHN-6` were **rejected candidates, never minted** — they are genuinely free, not gaps
that mean something.

⭐ **Why `AUD` drifted and the others did not, because the mechanism is reusable:** the archive rows
cite the punch list by section for their own IDs, and the punch list cites back. **`AUD` was the one
prefix where neither document ever referenced the other's numbering.** The collision was *the absence
of a cross-citation*, not bad luck — so **a new row that cites its counterpart section is a row that
cannot silently diverge.**

⚠ **A number is still not free because one file has not used it.** Check both.

## Queue

| # | Item | Status | Plan | Spec / handoff | Depends on | Branch |
|---|------|--------|------|----------------|-----------|--------|
| GV-5 | 🚫 **PARKED 2026-09-05 BY OWNER DECISION `D31` — NEVER CLAIM.** — [detail](queue/GV-5.md) | 🚫 | [`plans/.../pr5-send-contract.md`](superpowers/plans/2026-07-30-gv-messages-pr5-send-contract.md) _(kept for reconstruction; do not execute)_ | [ADR-028](../design/decisions/2026-07-30-gv-sms-send-contract.md) · [handoff §send/bubble states](design-handoffs/HANDOFF-phone-messages-voicemail-sms.md) | **GV-3** (`GvBridgeSendService`, compose/reply, `MessageBubble`, optimistic-append seam). _No external blocker: `POST /api/gvbridge/sms/send` is **shipped**. Their `GVBridge:EnableSmsSend=false` does **not** block — a dark server returns `409 send_disabled`, which this PR handles as a first-class state. **Merges as a user-visible no-op** (our flag off → compose unreachable, no new calls); the only always-on delta is the inert `SmsSent` subscription._ | `feat/gv-messages-pr5-send-contract` |
| GV-7 | 🔄 **RE-SCOPED BY `D31` 2026-09-05 — the display half survives, the gating half is deleted, and the `GV-5` dependency DISSOLVES.** — [detail](queue/GV-7.md) | 📋 | _plan TBD (design-led; consult the Designer handoff first)_ | [ADR-028 §8](../design/decisions/2026-07-30-gv-sms-send-contract.md) · [handoff Screen C/D](design-handoffs/HANDOFF-phone-messages-voicemail-sms.md) · [**UAT § Findings for GV-7**](uat/2026-07-31-gv-live-data/REPORT.md) | **GV-3** (texts surface). _The "wait for observations" caveat is **discharged** — they exist and are linked in the row. Coordinate with GV-5 if both are in flight: GV-5 adds `GvCounterparty` to `ApiModels.cs` and this row consumes it. **Coordinate with GV-8 too, and prefer GV-8 first:** GV-8 rewrites the conversation pane's state branches (`PhoneTextsPanel.razor:36-68`), which this row also touches — and designing the header/empty treatment on top of a pane that **cannot express "failed"** would bake the F-1 confusion into the new design._ | `feat/gv-messages-pr7-nondialable-senders` |
| GV-10 | ❌ **FALSIFIED 2026-09-06 as a Radio Console defect — bubbles already render the full body verbatim; the residual is upstream-only. Status left 📋 because nothing shipped; whether it closes is the owner's call.** — [detail](queue/GV-10.md) | 📋 | _no plan — the "investigate first" instruction was DISCHARGED; the answer is the diagnosis linked at right_ | ❌ **[F-5 diagnosis 2026-09-06 — read first, it supersedes the row's inference](uat/2026-07-31-gv-live-data/F-5-DIAGNOSIS.md)** · [UAT F-5](uat/2026-07-31-gv-live-data/REPORT.md) · [root-cause § Scope-affecting notes](uat/2026-07-31-gv-live-data/F-1-DIAGNOSIS.md) | **GV-3** (texts surface). _Lowest priority of the three UAT rows; do not schedule ahead of GV-8._ | `fix/gv-texts-bubble-full-body` |
| AUD-1 | **Split `UseShazamForAllSources` into the two independent decisions it currently conflates.** — [detail](queue/AUD-1.md) | 📋 | [`AUD-1-split-the-fingerprint-gate-from-the-overwrite.md`](../design/plans/AUD-1-split-the-fingerprint-gate-from-the-overwrite.md) · **0.75 d** (F1) / **0.5 d** (F2) · ⛔ **NOT auto-mergeable** — live audio path, user-visible metadata, UAT needs a phone · ✅ **UNBLOCKED BY OWNER DECISION 2026-09-08 — and the rule is simpler than the F1/F2 split.** *"When metadata is available from the audio source, use the source metadata (song name, album name, album art). When one or more is missing, use fingerprinting to augment the missing data."* **Precedence is per FIELD, not per track**, and the same rule applies to **both** sources — so FilePlayer follows BT (F1), but by making one rule true everywhere rather than by imitation. Keep the always-fingerprint gate at `:837`; ⛔ **CORRECTED 2026-09-08** — replace the overwrite at `:867-891` with per-field fill-if-missing. ⚠ **NOT "the behaviour already on the preserve branch at `:893-905`"**, as this cell previously said: that branch fills **cover art only** and never touches Title/Artist/Album, so an empty AVRCP title would end up with no title at all. The preserve path must GAIN per-field text fill — new behaviour, not behaviour made reachable. ⚠ Treat an empty string from the source as **missing**, not as an authoritative blank. ⚠ **`AUD-17` makes this self-correcting on Bluetooth** — AVRCP never supplies art there, so art is always "missing" and fingerprinting always fills it, which is exactly today's working ~99% · _Planner's prior recommendation, now superseded but its evidence still stands:_ `PlayHistory` shows **44 of 52 file plays had their tags overwritten**, two fabricated titles, a **U+2010 non-breaking hyphen** corrupting `blink‐182`, and nine competing cover-art hashes for a track whose embedded art has one. 79% of files carry embedded art and `:2126-2128` replaces it unconditionally · ⚠ **THE FLAG MUST NOT BE RENAMED.** The SQLite store outranks both JSON layers and already holds `fingerprinting:useShazamForAllSources|true`; a rename orphans that row, the new key falls through to `appsettings.json`'s `false`, and **BT album art dies** — the one outcome this row forbids. Not theoretical: `fingerprinting:fpcalcPath` is already orphaned there from the AcoustID→SongRec rename · ⚠ Anchor drift: `appsettings.json:91` → **`:93`**_ | _no spec doc — the diagnosis is in this row_ · provenance: 2026-08-10 debugging session; PR #469 is the adjacent merged fix | — _(no row dependency. **Touches the same file and method region as PR #469** (`BluetoothAudioSource.OnTrackIdentified`), which is merged — rebase, don't re-derive. **Also touches `FilePlayerAudioSource.cs`, which #468 changed on 2026-08-11** — the anchors above are already re-sited, but rebase rather than trusting any earlier copy.)_ | `fix/split-shazam-fingerprint-vs-overwrite` |
| AUD-5 | **A Cast connection that is no longer current can persist its volume as the system master volume.** — [detail](queue/AUD-5.md) | 📋 | [`AUD-5-stale-cast-volume-persists-as-master.md`](../design/plans/AUD-5-stale-cast-volume-persists-as-master.md) · **0.5 d** · **both halves** (generation re-check + subscriber ignoring `IsInitialSync`) · ⚠ **run the plan's Task 1 forensic log read BEFORE the code lands** — Task 3 retires the message it greps for | _no spec doc — the diagnosis is in this row_ · **provenance: PR #473's pre-merge review**, which found it while checking whether #473's own comment was true · the mechanism is now documented in-tree at [`GoogleCastOutput.cs:95-104`](../src/Radio.Infrastructure/Audio/Outputs/GoogleCastOutput.cs) · origin commit `b420edc` (2026-02-11) | — _(no row dependency; claimable now. **⚠ Touches `GoogleCastOutput.cs`, which PR #473 (`0870410`) reflowed: its header comment grew by exactly +66 lines, so EVERY anchor below `:32` in that file moved +66.** The citations in **this** row are already post-#473 and verified; any citation copied from the **`AUD-3`** row is pre-#473 and is not. Also touches `AudioStateUpdateService.cs`, which no other row claims. **No file overlap with `AUD-1`, `AUD-2` or `AUD-4`**, so it can run alongside any of them.)_ | `fix/cast-initial-volume-sync-generation-check` |
| OPS-3 | **`BindsTo=` for `radio-web` — make joint failure actually joint.** — [detail](queue/OPS-3.md) | 📋 | [`OPS-3-bindsto-for-radio-web.md`](../design/plans/OPS-3-bindsto-for-radio-web.md) · **0.5 d + ~20 min supervised box session** · ⚠ **RE-SCOPE REQUIRED — the rehearsal (2026-09-08) disproved the row as specified.** `BindsTo=` alone fires during `radio-api`'s ordinary 10 s back-off AND the console never returns (`inactive/dead/success`), so a *transient* crash darkens it permanently — the opposite of the purpose the punch list filed it for (`C-195`). ⭐ **`BindsTo=` on web PLUS `Upholds=` on api gives both directions and was measured working.** Merging is inert regardless: `Deploy-ToLinux.ps1` does not install unit files (`C-191`) · ✅ **CLAIMABLE — scope NARROWED by owner decision 2026-09-08 (second decision, superseding the first the same day): ship `BindsTo=` + `Upholds=` ONLY. Readiness is split out to `OPS-10`.** ⚠ **The first decision was made on a false premise I supplied.** This row asserted that `Upholds=` starting web early leaves the SignalR hub *"dead for the lifetime of the radio-web process"*, quoting `Deploy-ToLinux.ps1:445-451`. **That comment describes half of what shipped** — "Fix A" (`c1fba27a`, #386) was **two** fixes, and the client-side retry loop is **still on `main`** (`AudioVisualizationHubService.cs:187-193`, `:208-218`, back-off `{2,5,10,30}`s, replays subscriptions). **So an early start costs 2–30 s of inert console that self-heals, not a permanent outage** — the comment's own trailing clause said *"regardless of hub-service code resilience"* and was quoted twice while being read backwards. Readiness keeps four independent justifications (⭐ including one nobody had noticed: **the boot path runs no ordering poll at all**), but it is a **worthwhile improvement, not a blocker** — hence its own row. ⭐ **Narrowing to unit files preserves `C-191`** — merging *and* deploying stay inert — which is most of why the split is cheap. Estimate returns to **~0.5 d + ~20 min supervised**. ⚠ The owner's first instinct was to drop `Upholds=`; it was reversed once the measured cost was put beside it — **`BindsTo=` alone fires during the ordinary 10 s back-off and the console never returns**, turning a transient crash into permanent darkness, which is worse than the bug the row fixes. ⚠ **Re-rehearse the readiness change before rollout**; the container fixture has now caught two things the reasoning missed · _Second rehearsal on systemd 255.4 (the appliance's exact version), 2026-09-08:_ `C-199` **closed**: the 249 result transfers unchanged. But `C-200` is **CONFIRMED and re-diagnosed** — the hazard is **not** the deploy's `rsync --delete` (that framing was wrong; `:170` stops web then immediately stops api, and `BindsTo=` takes web down again long before any file operation). **It is the START path.** `Deploy-ToLinux.ps1:452` starts api, polls `/hubs/visualization/negotiate` until it answers, and only then starts web — because, per its own comment at `:445-451`, starting web early leaves the SignalR hub *"dead for the lifetime of the radio-web process."* **`Upholds=` starts web the moment api is *active*, which is at exec, not at listener-bound**, so the poll is bypassed and every deploy reintroduces the pre-Fix-A bug it exists to prevent. Needs an owner decision on readiness (`Type=notify`/`ExecStartPost=` on api is the option that fixes the cause) · ⚠ **Instrument correction: `NRestarts` cannot see this.** `Upholds=` issues `start`, not `restart`, so the counter reads 0 whether or not web is cycled — the earlier no-wedge derivation rested on it and was **vacuous**. The property holds structurally instead: web's start count can never exceed api's failure count, and **api latches `failed` at 5 failures in 300 s** (measured, cycle 5 of 6 — both services then down until `systemctl reset-failed`)_ | [`deploy/common/radio-web.service:12-22`](../deploy/common/radio-web.service) (the gap, already documented by #467) · [`deploy/common/radio-api.service`](../deploy/common/radio-api.service) (limiter rationale) | — _(no row dependency; **PR #467 is merged**, so both `StartLimit*` blocks are on `main` to build on.)_ | `fix/systemd-bindsto-radio-web` |
| AUD-4 | **Unify the two source-removal layers, and rename `SoundFlowMasterMixer` — it is not a mixer.** — [detail](queue/AUD-4.md) | 📋 | [`AUD-4-unify-source-removal-and-rename-the-mixer.md`](../design/plans/AUD-4-unify-source-removal-and-rename-the-mixer.md) · **2 d** minimal / **3 d** split · ⛔ **not auto-mergeable — Task 4 changes engine-stop behaviour on the live audio path** | _no spec doc — the diagnosis is in the dossier_ · commit `03a6fea` is the provenance for the "one layer too high" trap · **PR #468 (`8b1ce0a`) is the provenance for the third layer** | — _(**no dependency. ⚠ The "prefer `AUD-2` first" note that stood here was FALSIFIED 2026-09-06 and is removed, not softened.** [`ORDERING-NOTES.md`](queue/ORDERING-NOTES.md)'s claim that `AUD-2` and `AUD-4` are "two symptoms of ONE root cause" and that `AUD-2` "decides the key" is **false**: per-source teardown is key-symmetric — `SDRRadioAudioSource.cs:915` mints `_playbackId` and `:1027`/`:1059` stop with the same field — and this row's roster is keyed by **object reference**, not by string, so there is no key here to decide. The row's prescribed "sweep `_activeComponents` instead" is unnecessary and also points at dead code: `SoundFlowPlaybackService.StopAll()` has **zero callers in the tree**. Plan: `design/plans/AUD-4-unify-source-removal-and-rename-the-mixer.md` §0.4, `C-148`, `C-150`. **Still rebase past #468.**)_ | `refactor/unify-source-removal-and-rename-mixer` |

| AUD-11 | ⭐ **NEW 2026-09-06, observed live — the BT capture stream silently re-links to the built-in line-in when its target disappears, and every indicator stays green.** — [detail](queue/AUD-11.md) | 📋 | [`AUD-11-the-capture-that-recorded-the-wrong-jack.md`](../design/plans/AUD-11-the-capture-that-recorded-the-wrong-jack.md) · **1.5 d + a box session** · ⛔ **NOT auto-mergeable** · ⚠ **the `PW_ID_ANY` attribution was WRONG**: the mechanism is `node.autoconnect = true` (an instruction with no failure mode), so the fix is ADDITIVE (`node.dont-reconnect`) with no trade-off — `PW_ID_ANY` stays | _no spec doc — measured on `radio` 2026-09-06_ · `LinuxBluetoothService.cs:1985-1987` (the same behaviour, documented and guarded TWICE on the old `pw-record` path — this is a regression the #262 native migration introduced) | — _(no row dependency. Makes `AUD-10` silent rather than loud; neither blocks the other.)_ | — |
| AUD-10 | ⭐ **NEW 2026-09-06, observed live — pausing on the phone destroys the A2DP transport and resume never restores it, so BT playback is effectively single-use.** — [detail](queue/AUD-10.md) | 📋 | _plan TBD — **two-phone test first**; this row may legitimately close as "upstream / not ours"_ | _no spec doc — measured on `radio` 2026-09-06, reproduced twice_ | — _(no row dependency. ⚠ `audio-gateway` is the CORRECT profile — the number that matters is `sources:`. Profile cycling does nothing; both were tried.)_ | — |

| AUD-13 | ⭐ **NEW 2026-09-06 — `USBPort: ""` matches every device via `string.Contains("")`, so Radio, Vinyl and GenericUSB bind to whatever enumerates first, and the warning written to catch exactly this is unreachable.** — [detail](queue/AUD-13.md) | 📋 | [`AUD-13-the-fallback-that-picks-the-first-jack.md`](../design/plans/AUD-13-the-fallback-that-picks-the-first-jack.md) · **0.5 d + two `curl`s** · ⛔ **NOT auto-mergeable** — it deliberately converts a working state into a failing one on a live appliance, and no gate can observe which jack made a sound · ⭐ **OWNER DECISION DISCHARGED 2026-09-08, and it dissolved the question rather than answering it.** ⚠ **The row's named defect is UNREACHABLE in production**: `USBPort: ""` never reaches `Contains("")` at `USBAudioSourceBase.cs:199` because `AudioSourceFactory.cs:158-162`/`:202-206` refuse a blank port, `RadioFactory.cs:336-339` returns false on one, and `SoundFlowDeviceManager.cs:250` throws `ArgumentException.ThrowIfNullOrEmpty` **22 lines earlier**. It read as true only because `USBAudioSourceTests.cs:58-59` **mocks the device manager away**, removing the backstop exactly where a reader watches the code run · ⚠ **The Radio half is do-nothing**: empty `Devices:Radio:USBPort` is correct, documented configuration — `SystemConfigPage.razor:280` already says *"Leave empty if using RTL-SDR"* · **The real defect is the same fallback reached by a NON-empty port** (`:207-214`): unmatched pattern → `captureDevices[0]`, ambiguous pattern → first match silently, zero devices → **system default** silently_ | _no spec doc — confirmed by code reading 2026-09-06 while planning `AUD-11`_ · `USBAudioSourceBase.cs:198-203` · `src/Radio.API/appsettings.json:51`/`:54` | — _(no row dependency. **Touches `USBAudioSourceBase.cs`, shared by three sources.** Same area as `AUD-11`; neither blocks the other but expect anchors to move if both are in flight.)_ | — |
| AUD-14 | ⭐ **NEW 2026-09-06 — a stale AVRCP watcher survives player re-attach and can raise `Playing` against a torn-down source.** The dedup at `:2539` returns *above* the `Dispose()` at `:2542`, and `OnInterfaceRemoved` never nulls `_mediaPlayer`. — [detail](queue/AUD-14.md) | 📋 | _plan TBD_ · ⛔ **NOT auto-mergeable** — live audio path on a device event; UAT needs the owner's phone | _no spec doc — found by the `AUD-12` investigation, argued for its own row in that plan's §7.1_ · `LinuxBluetoothService.cs:2536-2542`, `:929-932` · a 2026-07-16 note already records the missing `InterfacesRemoved` cleanup | — _(no row dependency, and **`AUD-12` must NOT wait for it.** This is *why* `AUD-12` ships `(State == Stopped && HasCapturePath)` rather than bare `Stopped`; if this lands first that guard becomes belt-and-braces rather than load-bearing.)_ | — |

| AUD-15 | ⭐ **NEW 2026-09-07 — the BT audio ring buffer runs EMPTY (`buffer: 0/384000`), 55 underruns and climbing, substituting silence into playback.** PipeWire `OnProcess` execution reaches **14.2 ms** against a 10.67 ms quantum. — [detail](queue/AUD-15.md) | 📋 | [`AUD-15-the-buffer-that-runs-empty.md`](../design/plans/AUD-15-the-buffer-that-runs-empty.md) · **2 d + a ≥30 min box session with the owner present** (onset is ~12 min) · ⛔ **NOT auto-mergeable** — the fix continuously changes the sample rate of live audio and a wrong gain or sign detunes the music rather than failing loudly · ⚠ **the plan DISPROVED this row's headline**: the lock is a red herring (375-quantum ring), `buffer: 0/384000` is a tautology, and the cause is an open-loop resampler whose `SetRatio` has zero callers | _no spec doc — measured on `radio` 2026-09-07; log evidence in the dossier_ · `CLAUDE.md` § PipeWire Quantum Tuning | — _(no row dependency. ⚠ **NOT the same defect as `AUD-10`** (transport dies on pause) **nor the RotaryPhone HFP disconnect** found in the same logs — same day, same journal, three different problems. Touches the live audio path; **not auto-mergeable**.)_ | — |

| AUD-16 | ⭐ **NEW 2026-09-08 — retire the deprecated USB radio path. ⚠ The deprecation itself is ALREADY implemented, so this is a removal decision, not a fix.** — [detail](queue/AUD-16.md) | 📋 | _plan TBD — ⚠ **"keep it, documented" is an acceptable close**; the greenfield rule makes removal permitted, not required. Scope first: is the `RaddyRF320BT/` submodule in or out, and what happens to an orphaned SQLite store row?_ | _no spec doc — owner input 2026-09-07: "The Radio USB connection has been deprecated so that the SDRRTL radio is really the only one supported"_ · **already live** at `RadioFactory.cs:147` (defaults to `RTLSDRCore`), `:336-339` (blank port ⇒ RF320 never offered), `SystemConfigPage.razor:280` (ships the copy) | — _(no row dependency. **Not `AUD-13`** — that row's real defect is the wrong-jack fallback on a NON-empty port and must not be folded in.)_ | `chore/aud-16-retire-usb-radio-path` |
| AUD-17 | ⛔ **RETRACTED AND REWRITTEN 2026-09-08, the same night it was filed — AVRCP album art has NEVER worked, and this was never a regression.** `LinuxBluetoothService.cs:2761-2765` reads the MPRIS names `ArtUrl`/`mpris:artUrl` off a proxy on `org.bluez.MediaPlayer1`, which publishes **`ImgHandle`** — so `CacheAvrcpArtAsync` has never executed. — [detail](queue/AUD-17.md) | 📋 | _plan TBD — ⚠ **the first question is whether to fix it at all.** `ImgHandle` is an OBEX BIP handle and **BlueZ 5.72 ships no BIP client**, so the options are implement BIP, **delete the dead path** (probably right — SongRec already supplies art on ~99% of rows), or upgrade BlueZ. Whatever is chosen, **fix the comment and the `:339`/`:358` log strings**, which claim MPRIS about a BlueZ interface_ · ⛔ **NOT auto-mergeable** if the path is changed rather than deleted — needs the owner's phone | _no spec doc — measured read-only on `radio` 2026-09-08: **`file://` is ZERO across all 45,210 `TrackMetadata` rows, every source**, and `PlayHistoryTracker.cs:733-737` writes any BT-supplied URL raw at row creation, so one would have appeared. Of 66 `Avrcp` rows with art, **26 share a content-addressed filename with a `Shazam` row** (`AlbumArtCacheService.cs:46`/`:80` hash the image bytes, so a shared name means byte-identical), 1 with `Manual`_ · ⛔ **Do NOT "correct" `queue/AUD-1.md:26` or `ROADMAP.md:133`** — their conclusion is right, only their mechanism is incomplete | — _(no row dependency. ⚠ **`Source` records the TITLE's provenance, not the art's** — `UpdateRecentPlayHistoryCoverArtAsync` (`:1070-1112`) writes `CoverArtUrl` without touching `Source`, and its only live caller is the SongRec path. Any future row reasoning from `Source='Avrcp'` about **art** is reasoning from the wrong column.)_ | `fix/aud-17-avrcp-art-dead-path` |
| UI-10 | ⭐ **NEW 2026-09-08 — the Blazor circuit times out every ~30 s.** `System.TimeoutException: Server timeout (30000.00ms)` repeatedly, plus a `JSDisconnectedException`. **Ours entirely** — RotaryPhone touched neither service. — [detail](queue/UI-10.md) | 📋 | _plan TBD — ⚠ **cheapest thing first**: check `ServerTimeout ≥ 2 × KeepAliveInterval`, the commonest cause. Then establish whether it is the kiosk, the network or the server, and whether the reconnect overlay ever appears on the panel_ · ⚠ **a unit test cannot show a wall-clock symptom on a live box** — baseline `grep -c 'Server timeout'` per day BEFORE changing anything, so "it improved" is a comparison not an impression | _no spec doc — measured in `radio-web`'s log 2026-09-08; **continued after the upstream GV fix at 15:31**, so it is a standing condition rather than a symptom of that outage. 30000 ms is the SignalR client default, so this is the **Blazor Server circuit**, not a GV call_ | — _(no row dependency. ⚠ **May be upstream of `GV-12`** — a dead circuit cannot refetch. ⚠ **Check correlation with `AUD-18`** — same box, and `CLAUDE.md` records log volume correlating with audible distortion; both could be resource pressure. **Check, do not assume** — two rows blaming each other is worse than two independent rows.)_ | `fix/ui-10-blazor-circuit-timeout` |
| UI-15 | ⭐ **NEW 2026-09-09 — `isFirstRun` conflates "never seen a source" with "currently no source", so a real source change is silently NOT broadcast.** `CheckSourceChangedAsync` computes `isFirstRun` as `_lastActiveSourceType == null`, but `null` is also a legitimate observed value (`ActiveSource` is `IAudioSource?`), so **every `null → non-null` transition is suppressed as if it were the first poll**. — [detail](queue/UI-15.md) | 📋 | _plan TBD — ⚠ **establish reachability on the box FIRST**: if `radio-api` always restores a source before the first poll, the startup case never fires and this row may close as "unreachable, documented". The fix itself is small (a separate `_hasObservedSource` sentinel so `null` can be an ordinary value) — ⛔ **do not "fix" it by broadcasting on the true first run**, that suppression is deliberate_ · likely auto-mergeable — no hardware, unit-testable, and `AudioStateUpdateServiceCacheOrderingTests` already carries the harness | _no spec doc — found by the `UI-13` pre-merge review (finding `L5`) and confirmed by code reading 2026-09-09_ · ⛔ **NOT caused by `UI-13` and not worsened by it** — identical in both orderings, which is why it was filed rather than folded in · ⭐ **Same family as `AUD-12` / `GV-12` / `UI-13`: state that stops tracking reality with nothing reporting a fault** — the `LogInformation` sits inside the same suppressed branch, so nothing logs either | — _(no row dependency. ⚠ **Touches `CheckSourceChangedAsync`, which `UI-13` just rewrote** — rebase past it rather than re-deriving anchors, and **do not delete `TheFirstSourcePollEstablishesTheBaselineWithoutBroadcasting`**, which pins the genuine first run this row must not change. ⚠ **Two things the filer did NOT establish and must not be assumed**: whether `AudioManager._activeSource` can return to `null` after startup, and whether the missing broadcast is visible to the user who made the switch or only to other clients.)_ | `fix/ui-15-first-run-conflates-no-source` |
| PHN-7 | ⭐ **NEW 2026-09-08 — `BellHealthService` polls the transport that lies, so predictive-degrade cannot fire.** `SystemStatus` means two different things over SignalR vs REST, and we poll REST every 15 s. — [detail](queue/PHN-7.md) | 📋 | _plan TBD — ⛔ **needs an owner decision on one point**: their `acknowledged` flag does **NOT** survive a restart (`BellFailureTracker` is in-memory), so a nightly-restarting kiosk **does** resurrect a dismissed bell note. Treat the note as session-scoped and say so in the copy, or ask them to persist it_ · ⚠ **do NOT build the predictive-degrade rule against the REST signal meanwhile** — the rule is ours and correct, the signal is theirs and wrong until their convergence ships | _no spec doc — measured on the box 2026-09-08: two REST calls 10 ms apart returned `ht801LastCheckedUtc` of `…0867356Z` then `…0968027Z`, i.e. **`DateTime.UtcNow` every time**. Over SignalR the same field is a genuine 30 s probe of the **resolved registrar binding**; over REST it is a synchronous ICMP ping of the **configured** address_ · ⭐ **RotaryPhone's own doc says that configured-address ping "reported the CORRECT address throughout the entire 2026-07 outage while every INVITE went to a stale one"** — so predictive-degrade would not have fired during the incident it exists to prevent | — _(⚠ **Their convergence fix is ADR-written but not built**; it needs no change from us. Our half is deciding whether to wait or move onto the SignalR path we already hold open.)_ | `fix/phn-7-bell-health-transport` |
| PHN-8 | ⭐ **NEW 2026-09-08 by owner request — speak a text message from the phone DASHBOARD, without opening the thread.** ⚠ **NOT a duplicate of `PHN-3` and must not re-implement it**: that shipped a per-message play button on `MessageBubble.razor`, *inside* an open conversation. The **thread list has no speak affordance at all** (verified 2026-09-08 — `PhoneTextsPanel.razor`'s only speech matches are doc comments about the message-level feature). — [detail](queue/PHN-8.md) | 📋 | _plan TBD — ✅ **DESIGN GATE DISCHARGED BY OWNER 2026-09-08, both questions answered.** **(1) ADD, do not move** — despite the word *"rather than"*, `PHN-3`'s per-message button **stays** and nothing shipped is reverted. ⚠ The cost: **two places can now start speech**, so stop/state handling must be coherent across both and `PHN-3`'s state machine now needs to model *which origin*. ⚠ **Run a Polisher pass before merge** — two speak affordances on one surface is exactly that drift. **(2) Speak ALL UNREAD in that thread, oldest first**, falling back to the latest when nothing is unread; needs a queue and a stop control. ⚠ **"Unread" must mean what the unread dot means** — `GV-4`/`GV-6` own mark-read semantics, so do not mint a second definition. ⚠ **Whether speaking marks them read is NOT decided** — the plan must ask, and it interacts with `GV-6`'s dark-flag path. _Superseded framing, kept for the reasoning:_ `PHN-3`'s handoff already set the visual language for the per-message button and the topbar chip; extend it rather than inventing a second idiom_ | _no spec doc — ⭐ **the speech path is BUILT**: reuse `GvSpeechText.ForMessage`'s eight content rules and `EventPlaybackApiService.StartSpeechAsync`. This row is a surface/affordance question, not an audio one_ · ⚠ **the transport rejection token is `Transport`, NOT `Unreachable`** (`PhoneTextsPanel.razor:361` records an earlier draft getting this wrong) · ⚠ **never log message bodies or numbers** — `Radio.Web`'s Console sink is unrestricted, which is why `PHN-5` exists | — _(⚠ **`PHN-4` deleted the composer from this surface** and **`GV-9` shipped alignment work plus four structural tests on the conversation rows** — re-derive anchors and ⛔ **do not delete those tests as "odd assertions"**. **`GV-7` must not run concurrently**; `GV-12` touches the same panel's failure handling. ⚠ **Check `AUD-2` before judging by ear** — ducking is broken under event playback, so a speak test heard over un-ducked music reads as broken when it is not.)_ | `feat/phn-8-speak-from-dashboard` |
| PHN-9 | ⭐ **NEW 2026-09-09 — the topbar PHONE unread badge requires you to have already visited the page it points at.** `PhoneUnreadState` is a process-wide singleton starting at **0** (`Program.cs:431`) whose **only writer is `PhonePage` while mounted** (all 17 `Set` sites in `PhonePage.razor`); `MainLayout:402` seeds the badge by *reading that singleton, with no fetch*. **After every `radio-web` restart the badge is absent for the life of the process** — and deploys restart `radio-web`, so that is the normal state. — [detail](queue/PHN-9.md) | 📋 | _plan TBD — ⛔ **NOT a small fix: there is no server-side unread total to hydrate from.** `GvBridgeStatusService` exposes only `Current`/`IsAvailable`/`IsHealthy`; no count-bearing DTO or API method exists anywhere in `src/Radio.Web`, only per-item `HasUnread`/`IsRead` flags. Needs **either** a new server-side total **or** a hosted poller on the `BellHealthService` shape (`Program.cs:443-453`) — ⚠ **price the client-side option honestly: deriving it means three REST calls on every app start** on a resource-constrained box · ⚠ **CROSS-REPO — if the answer is a server-side total it is RotaryPhone's endpoint, not ours.** Follow the boundary protocol and **ask first**; do not build a Radio-side poller that duplicates a total they intend to serve_ · ⛔ **NOT auto-mergeable** — cross-repo question + user-visible surface | _no spec doc — ⭐ **the repo already names this defect, about a different badge**: `MainLayout.razor:406-408` reads *"Unlike PhoneUnread (which PhonePage publishes), BellHealthService polls on its own, **so the badge does not require the user to have already visited the page it points at**"*, and `:420-423` describes the identical failure mode as already fixed for the encoder badge. **Queue, Mute, Encoder and Bell all hydrate authoritatively; this one does not** · ⚠ **The latch is stale in BOTH directions** — `Dispose` never resets, so messages read on the handset never decrement it; the badge can be wrong high as easily as wrong low · ⭐ **Live verification needs NO code change**: tap PHONE, go Home — badge should appear, survive a kiosk reload, and die on a `radio-web` restart. **If it does not behave that way this row is wrong**_ · ⛔ **Two first guesses were both WRONG and are recorded so nobody re-runs them**: not a regression from the four PRs deployed that morning, and **not** the 30 s hub `TimeoutException` in the log — **no hub carries an unread count** (`PhoneHubService.cs:18-36` delivers individual items, never a total) and `MainLayout` injects no hub service at all | — _(no row dependency. ⚠ **Related but distinct from `PHN-7`** — that row is `BellHealthService` polling a transport that lies; this is a badge that polls nothing whatsoever.)_ | `fix/phn-9-unread-badge-hydration` |
| PHN-10 | ⛔ **NEW 2026-09-09 — two voicemails can play at once, and `PHN-1f` SHIPPED the queue that was supposed to prevent it.** Owner UAT: *"Two voicemails **can** play simultaneously."* Check #11 exists to verify *"the room never carries two voices."* **The room carried two voices.** — [detail](queue/PHN-10.md) · [UAT record](uat/2026-09-09-phn2-sound-uat/RESULT.md) | 📋 | _plan TBD — ⛔ **This is not "a queue we never built"; it is a queue that EXISTS and did not cover this path**, which is a different and more serious diagnosis. ⚠ **Establish which of three before designing:** the queue does not cover voicemail, it covers it and fails, or it serialises REQUESTS while two SOURCES still reach the mixer. **Three different fixes** · ⛔ **TWO BEHAVIOURS WERE REPORTED AND ONLY ONE IS CERTAINLY WRONG** — simultaneity is forbidden by check #11; a second TTS text **interrupting** the first may be intended preemption (`PHN-1d` §2.2 covers doorbell-preempts-voicemail as designed). **Find the intended contract per path before calling either a defect — simultaneity and preemption are opposite failures and a fix for one can create the other**_ · ⛔ **NOT auto-mergeable** — cannot be closed by a green suite | _no spec doc — ⚠ **the ducking half of the owner's report is `AUD-2`, not this row.** Do not fix ducking here; this row owns HOW MANY things play at once, `AUD-2` owns WHETHER THE RADIO DUCKS under them · ⭐ **But note the interaction, because it is why the owner heard it so clearly: with ducking broken, two simultaneous voices arrive at full level over an un-ducked radio.** Fixing `AUD-2` makes this defect QUIETER without making it less real — ⛔ **do not let a post-`AUD-2` re-listen be read as evidence this is fixed** · ⭐ **Assert the PRESENCE of serialisation** — the second waits, then plays_ | — _(no row dependency to build. ⚠ **Read `PHN-1f`'s ARCHIVED row first** — it shipped the queue, and its scope is the fastest route to which of the three diagnoses applies.)_ | `fix/phn-10-two-voices-at-once` |
| OPS-10 | ⭐ **NEW 2026-09-08 — give `radio-api` a readiness signal, so "active" means "the hub answers".** Split out of `OPS-3` when the premise that made it urgent turned out false. — [detail](queue/OPS-10.md) | 📋 | _plan TBD — ⚠ **the analysis already exists**: `design/plans/OPS-3-bindsto-for-radio-web.md`'s readiness sections are this row's inheritance and should move here. ⛔ **`Type=notify` was investigated and REJECTED — do not re-propose without reading the dossier**: .NET sends `READY=1` on `ApplicationStarted`, after every `IHostedService.StartAsync` returns, and `AudioEngineInitializationService.cs:107-187` awaits hardware inline and unbounded, so `READY=1` would arrive **later** than "the hub answers" and would **manufacture the very wedge `OPS-3` exists to reduce**_ · ⛔ **NOT auto-mergeable** — changes production service startup | _no spec doc — ⭐ **the real gap is the BOOT path**: `Deploy-ToLinux.ps1:452` already polls the negotiate endpoint before starting web, but **nothing polls on boot**, so this ordering has never been enforced outside a deploy_ · ⚠ **The harm was overstated and the correction is the point** — a client retry loop still on `main` heals an early start in 2–30 s, so this is a correctness improvement, not an outage fix. **Do not restore the stronger claim** | — _(**`OPS-3`** ships the coupling this makes safe. Claiming it first is possible but pointless. ⛔ **One number cannot be established without the box**: how long `radio-api` really takes from exec to the hub answering, cold — it is nowhere in the repo, and nobody has recorded whether the deploy poll has ever reached its 20th iteration.)_ | `feat/ops-10-radio-api-readiness` |
| AUD-18 | ⭐ **NEW 2026-09-08 — the fingerprint tap returned ZERO bytes for 11½ hours while the radio played, and nothing noticed.** ⭐ **First repo row for the capture-lifecycle failure** — `MEMORY.md` has carried it as HIGH PRIORITY for months and **no row existed**; `SoundFlowAudioTap` appears in no other row. — [detail](queue/AUD-18.md) | 📋 | _plan TBD — ⚠ **consider proposing the watchdog and the root cause as SEPARATE, sequenced deliverables**: eleven hours of a 15-second loop returning zero is detectable in under a minute, and a restart-after-N-empty-captures guard is unit-testable and can ship without finding the cause_ · ⚠ **the failure needs ~37 h of uptime to appear, so no test reproduces it and no green run proves the root-cause fix** — say so rather than implying otherwise · ⛔ **NOT auto-mergeable** — live audio path | _no spec doc — measured on `radio` 2026-09-08. **Onset 09-07 23:00 EDT, recovery 09-08 10:22:57, duration 11 h 23 m.** Hourly counts were **240/hour, i.e. one every 15 s — the full capture window failing back-to-back** — each with ~1,420 read attempts. **Independently confirmed in the fingerprint DB: ZERO `TrackMetadata` rows for the whole window**, resuming exactly when the warnings stop. Daily counts **0,0,0,0 → 48 → 112 → 2,490**, the last being **18.6% of every log line**, doubled because `BackgroundIdentificationService` logs a twin_ · ⭐ **Recovered on an RTL-SDR STREAM restart at 10:23:03 with `radio-api` uptime unbroken since 09-06** — so restarting the *source* suffices and the service need not be bounced, which refines `MEMORY.md`'s "restart fixes" and points below the engine | — _(no row dependency. ⚠ **Not `AUD-15`** — that is the BT ring buffer empty *during playback*; this is the tap reading zero *from the mixer*. ⚠ **Not `AUD-12`** — the source was `Playing` throughout. ⚠ **Not proven audio stopped being audible**: the tap is a separate path from playback and nobody was awake to hear it — what IS proven user-visible is album art and play history dead for 11½ hours.)_ | `fix/aud-18-capture-tap-returns-zero` |
| AUD-19 | ⭐ **NEW 2026-09-09 — after `AUD-1` ships, now-playing will obey the per-field metadata rule and History will not.** `PlayHistoryTracker` sets `MetadataSource = Fingerprinting` on *any* landed identification (`:563-573`, `:631-641`) without consulting which branch the source took. — [detail](queue/AUD-19.md) | 📋 | _plan TBD — ⚠ **the first question is whether History is even WRONG**, and it must be asked before it is assumed: there is a real argument that history records *what fingerprinting identified*, in which case this row closes as "correct, documented" and that is a legitimate outcome. If it IS wrong, it must call `AUD-1`'s shared per-field predicate rather than re-implement it — two implementations of one rule is how the rule stops being one rule_ · likely auto-mergeable once `AUD-1` lands — no hardware, unit-testable | _no spec doc — ⛔ **this row was CITED BY `AUD-1.md:113` BEFORE IT EXISTED**, as *"filed … documented in `design/FUTURE-WORK.md`"*; neither was true, and `AUD-1`'s Builder is told to name it in the PR body before merge. **Coordinator error, caught by `UI-12`'s Builder** · ⚠ **`MetadataSource` is provenance and is load-bearing elsewhere** — `AUD-17` was retracted for misreading exactly this column (`Source` records the TITLE's provenance, not the art's) · ⚠ **Beware a vacuous UAT**: with `AUD-12`'s stall or `AUD-18`'s zero-byte tap live, NO identification lands and a broken build looks correct_ · ⭐ **Why separate rather than folded in: different file, different lifecycle, and folding it in roughly doubles the blast radius of a change already on the live audio path** | **`AUD-1`** — ⛔ **do not claim first**; this row has no meaning until the per-field rule exists to diverge from | `fix/aud-19-history-per-field-precedence` |
| AUD-20 | ⭐ **NEW 2026-09-09 — `SDRRadioAudioSource` misses its callback deadline 2–4× more often since 09-08 ~13:00, and NO CAUSE IS ESTABLISHED.** Baseline was **1–11/hour**; since then **20–70/hour continuously, including 42–65/hour overnight with nobody touching the console**. **504 of 504 sampled lines are the SDR source** — not a general audio condition. — [detail](queue/AUD-20.md) | 📋 | _plan TBD — ⛔ **THE VALUE OF THIS ROW IS THE FIVE FALSIFIED HYPOTHESES; DO NOT RE-RUN THEM.** (1) **SongRec churn** — anticorrelated: 09-05 had 18 timeouts/264 misses, 09-09 had 2/531, overnight ran 42–65/hr with **zero** SongRec activity. (2) **The 09-08 deploy** — `radio-api` started **16:07:28**, the run began **13:00:51**. (3) **RF quality** — anticorrelated: `Block sync lost` was **13,680** on 09-05 at baseline misses vs **845** on 09-07; ⚠ **and the RDS "name flapping" is a red herring**, that is a station scrolling its name and track through the 8-char PS field. (4) **A changed threshold** — reported minimum is exactly **40.0ms every day** and the format is byte-identical, so **the increase is real, not more reporting**. (5) **A GC regime change** — ⭐ **inverted on measurement**: `Gen2/Gen0` was 0.970 / 0.875 / **0.008** / 0.618 / 0.922 across 09-05…09-09, so **09-07 is the outlier, not the elevated days**_ · ⭐ **MOST PROMISING UNTESTED LEAD: correlate with SDR STREAM UPTIME rather than date.** `MEMORY.md`'s long-standing capture-lifecycle item and `AUD-18`'s recovery-on-RTL-SDR-restart both point there, and **nobody has checked whether the stream has run continuously since 09-08** | _no spec doc — measured from the file sink, reproducible with `grep -c` per daily log · ⚠ **The station DID change** (97.7 → 92.3 on 09-08 → back to 97.7 on 09-09) **and does not explain it**: the elevation persisted through the change back. Owner confirms no physical changes for weeks · ⚠ **It survived a `radio-api` restart on a fresh process AND predates the 09-08 deploy** — neither accumulated state nor introduced code · ⛔ **NO USER-VISIBLE SYMPTOM DEMONSTRATED** — audio played correctly throughout, including all night, and nobody has established the audible consequence of a 43 ms miss on a buffered path. **That is why it is P3** · ⛔ **Do NOT "fix" it by raising the threshold or suppressing the warning** — that converts an unexplained signal into no signal · ⭐ **The first honest deliverable is an ANSWER, not a fix; closing with "cause not determined, here is what was excluded" is a legitimate outcome** | — _(no row dependency. ⚠ **Related to `AUD-18`** — same source, and that row recovered on an RTL-SDR stream restart.)_ | `fix/aud-20-sdr-callback-deadline-rate` |
| AUD-24 | ⛔ **NEW 2026-09-09 — the seek bar does not move the audio, and the clock reports a position the player is not at.** Owner UAT at the cabinet: *"Dragging the seek bar does **not** change the music in MP3 mode"* — while check #2 **PASSED** in the same sitting, so `Time` advances correctly. ⭐ **A silent no-op wearing the costume of a working control.** — [detail](queue/AUD-24.md) · [UAT record](uat/2026-09-09-phn2-sound-uat/RESULT.md) | 📋 | _plan TBD — ⚠ **first establish WHICH LAYER drops it**: the bar moving proves the UI handler ran and proves nothing below it. `PHN-2`'s check was written against `SoundPlayerBase.Seek` (from `PHN-1a` §2.2 (1), ADR §14 Q3), so the engine call was suspected before any UI existed · ⚠ **`CLAUDE.md` records this repo deliberately made a non-seekable `SeekAsync` THROW rather than no-op** — *"a caller that posts … gets `200`, and hears [nothing] has been misled."* **If that throw exists and nothing surfaces it, the swallow is the defect and the seek may be fine** · ⚠ **Do not assume it generalises** — BT and SDR are not seekable at all; voicemail is a third path_ · ⛔ **NOT auto-mergeable** — cannot be closed by a green suite | _no spec doc — deferred from `PHN-1a` §2.2 (1) since before `PHN-2` merged, carried through the whole ADR-029 arc as an unverified `SOUND` item, **and confirmed broken the first time a person actually dragged the control** · ⭐ **Assert the PRESENCE of repositioning, not the absence of an error** — a test asserting "seek did not throw" passes today_ | — _(no row dependency. ⚠ **Related to but NOT the same as `AUD-2`** — that is the ducking/gain key mismatch; both are silent and both were found in the same sitting, but the mechanisms differ. **Do not fold them together.**)_ | `fix/aud-24-seek-does-not-reposition` |
| OPS-12 | 🔴 **NEW 2026-09-09 — the rsync branch of `Deploy-ToLinux.ps1` has NEVER executed, and the first attempt TOOK THE APPLIANCE DOWN.** `Get-Command rsync` (`:178-185`) picks the transport; rsync had never been on PATH, so **every successful deploy in project history took the scp fallback**. ⛔ **NOT an unrelated shim — an OWNER DECISION for RotaryPhone deploy safety** (corrected; the `rsync.cmd` shim + persistent PATH change were installed so deploys would use rsync instead of the tar-pipe fallback). It silently selected a never-run branch. It failed at **[3/4]** — but **[2/4] had already stopped `radio-api` and `radio-web`**, so the console went dark until the units were restarted by hand. — [detail](queue/OPS-12.md) | 📋 | _plan TBD — ⛔ **THREE STACKED DEFECTS, MEASURED BY DRY RUN, NOT INFERRED — and EACH FIX REVEALS THE NEXT.** (1) `'D:\...\api/'` → **`The source and destination cannot both be remote`**: MSYS2 rsync parses `D:` as a **hostname**, and **the script does NO path conversion** — `:220`/`:250` pass `${ApiPublishDir}/` raw. (2) `'/d/.../api/'` → **`dup() in/out/err failed`**: MSYS2 rsync **cannot drive native Windows OpenSSH**. (3) `+ -e /usr/bin/ssh` → **`Host key verification failed`**: MSYS2 ssh has its **own** `known_hosts`/`config`/identity. ⛔ **A Builder who fixes only the path will re-run the deploy, stop the services, and fail at `dup()` — a SECOND outage caused by the fix** · ⭐ **STRONGLY CONSIDER DELETING THE BRANCH rather than fixing it** — scp is the only transport that has ever worked here and rsync has **zero** successful executions; deleting removes a trap instead of arming it better_ · ⛔ **NOT auto-mergeable** — production deploy path | _no spec doc — ⛔ **DO NOT VALIDATE THIS ROW BY RUNNING THE DEPLOY.** Step 2 stops services before step 3 is reached, so **every failed attempt is an outage**. Use `rsync -n` against a scratch remote path, as the three measurements above did · ⭐ **The INDEPENDENT defect, and arguably the bigger one: the transport is never proven reachable BEFORE the step that stops things.** That ordering converts any sync error into an outage and would bite the tar/scp paths identically — **fix it even if rsync is deleted** · ⭐ **General lesson: installing a tool ARMED an untested code path.** A capability probe that silently switches transports is a landmine wherever the alternative has never been exercised. ⭐ **`Get-Command rsync` ran, passed, and answered a DIFFERENT QUESTION than the one being asked** — it proved rsync EXISTS; it was read as "rsync WORKS here" · ⚠ **Mitigation in place is NOT a fix**: `C:\Users\mark\bin\rsync.cmd` renamed to `rsync.cmd.disabled-broken-transport` — **workstation-local, invisible to the repo, and re-armed by anyone who installs rsync by any route**_ | — _(no row dependency. ⚠ **Related to `OPS-9`** — that row's per-call `$LASTEXITCODE` capture is why this failure REPORTED HONESTLY (`API sync failed!`) instead of claiming success. ⭐ **`OPS-9` worked exactly as designed; it is the reason this was diagnosable at all.**)_ | `fix/ops-12-deploy-transport-preflight` |
| TEST-10 | 🟡 **NEW 2026-09-09 — `NodeArrivesAfterProbe_SwitchesViaEvent` races a wall clock against a wall clock, and it failed a real merge gate.** The test sleeps `Task.Delay(500)` and assumes the production probe loop (`probeMs: 200`) has since expired and subscribed. **No rendezvous between those two clocks.** Surfaced during `UX-1`'s gate on a branch containing **zero `Radio.Infrastructure` files**. — [detail](queue/TEST-10.md) | 📋 | _plan TBD — ⭐ **`CLAUDE.md` § Test Timing ALREADY DOCUMENTS THIS EXACT SHAPE, and `TEST-4`/`BluetoothCaptureWatchdogTests` is the worked precedent** (13/200 under CPU saturation, once red on `main` at `2a81f56`; the test-only fix parked the watchdog on entry to every poll so polls were **counted rather than timed** → 200/200). **Read that file's class-level `<remarks>` first** · ⛔ **DO NOT raise the delay** — `CLAUDE.md`: *"raising a timeout converts a flaky test into a SLOW flaky test; the failure rate drops but never reaches zero"* · ⚠ **Check the SIBLING tests in the same file** — the one above it does `await Task.Delay(150)` against the same probe loop. **Fixing one and leaving three is how this returns**_ · likely auto-mergeable — test-only, no hardware | _no spec doc — ⚠ **it fails in the DANGEROUS direction**: `CLAUDE.md` notes a timing dependency is only hazardous when starvation can flip the assertion to **fail**, and a bounded negative check that starvation merely weakens is safe. **This one flips to fail** · ⭐ **Why P2 and not P1**: it passed **3/3 in isolation** and on **three prior full-suite runs** of the same branch, and the branch that caught it touched only docs, CSS, `.gitattributes` and one `Radio.Web.Tests` regex — **it cannot have been caused by that change** · ⛔ **But it is NOT in the documented known-failing set**, so it must not decay into background noise the way a known flake does — **that is the whole reason this row exists** · ⭐ **Assert the fix by REPEAT COUNT UNDER LOAD, not one green run** — `TEST-4`'s standard was **200 iterations under CPU saturation**, before and after. **A single pass proves nothing: the test already passes most of the time, which IS the problem**_ | — _(no row dependency. ⚠ **Sibling of `TEST-4`**, same file family, same anti-pattern, same fix technique.)_ | `fix/test-10-autoswitch-probe-rendezvous` |

**Shipped rows are not listed here.** All 55 of them are in [`BUILDER_QUEUE_ARCHIVE.md`](BUILDER_QUEUE_ARCHIVE.md) — with their prose intact for the rows archived before dossiers existed, and as a link to the dossier for those archived since (the `OPS-2` convention).

---

## Carried risks (baked into the plans as explicit tasks)

1. **Absolute audio URL** — the DTO `AudioUrl` is server-relative and resolves against the Web origin (`:5002`), 404ing. `GvBridgeApiService.GetVoicemailAudioUrl` rebuilds it absolute against `radio:5004`. Unit-tested in GV-1; the most likely silent-failure point.
2. **GV SMS ≠ trunk SMS** — the GV handler is `GvSmsReceived` on `PhoneHubService`/`/hub`; the pre-existing `GvTrunkHubService.SmsReceived` on `/hubs/gvtrunk` is a different product. Kept namespaced apart; cross-referenced in code comments (GV-1).
3. **Open thread to RotaryPhone** (GV-3 deliverable): request GV mark-read be pulled forward (decision 4) — ✅ **closed**, shipped as GV-4; ~~keep the voicemail audio endpoint unauthenticated/token-in-query when `X-RotaryPhone-Auth` ships (native `<audio>` can't send the header)~~ — ✅ **DISSOLVED, not resolved, by `PHN-1b` ([#534](https://github.com/mmackelprang/RTest/pull/534)).** The constraint existed only because the **browser** was the thing fetching the recording, and a native `<audio>` element cannot attach a request header. Radio.API now fetches it **server-side** through `GvMediaClient`, which attaches `X-RotaryPhone-Auth` itself (`GvMediaAuthHandler.cs:26`) — so the endpoint is free to require the header and there is nothing to ask RotaryPhone for. The `PHN-1b` commit calls this *"the mechanism that closes carried risk #3"*. ⚠ **Do not re-raise it when `PHN-2` retires the `<audio>` element** — that PR is the one that finishes removing the browser from the fetch path, which makes this *more* closed, not less; and confirm `SendSmsResponse` before wiring send — ✅ **closed by GV-5/ADR-028**, which read the as-built controller directly rather than their docs (several RotaryPhone-side handoff docs are stale and still claim the send endpoint does not exist).
4. **Trust their source over their docs for gvbridge contracts.** `D:/prj/RotaryPhone/docs/handoffs/radioconsole-gv-voicemail-sms-ui-handoff.md` still states "No `POST /api/gvbridge/sms/send` endpoint exists yet." It does. ADR-028 was written against `GvSmsController.cs` / `GvBridgeReadDtos.cs` / `GvThreadPoller.cs` directly; do the same for any future contract work.

---

## Companion notes (moved out of this file)

These three sections were measured against the index's size budget and moved rather than inlined. Nothing in them was edited; each file opens with the section verbatim.

| Section | Size | Now at |
|---|---|---|
| Dependency / ordering notes — claim order, and why each ordering is load-bearing | 48.5 KB | [`queue/ORDERING-NOTES.md`](queue/ORDERING-NOTES.md) |
| Cross-repo handoffs (RotaryPhone — NOT claimable here) | 14.3 KB | [`queue/CROSS-REPO-HANDOFFS.md`](queue/CROSS-REPO-HANDOFFS.md) |
| Documented fast-follows (NOT in these PRs) | 9.0 KB | [`queue/FAST-FOLLOWS.md`](queue/FAST-FOLLOWS.md) |

⚠ **Read `ORDERING-NOTES.md` before claiming.** Several rows have a mandatory claim order (`AUD-2` before `AUD-4`, `AUD-6` before `AUD-7`) that this index does not restate.

---

## How other documents cite this one

The restructure moved content that other documents cite. Those citations were re-pointed in the same PR, **at the file rather than at a line number** — a line anchor cannot survive the next edit, a dossier path can. `design/plans/*`, `design/decisions/*`, `docs/design-handoffs/*`, `docs/superpowers/plans/*`, `ROADMAP.md`, `FUTURE-WORK.md`, `INTEGRATIONS.md` and `HANDOFF-GA-PUNCH-LIST.md` now link to `queue/<ID>.md`, `queue/ORDERING-NOTES.md`, `queue/CROSS-REPO-HANDOFFS.md`, `queue/FAST-FOLLOWS.md` or `BUILDER_QUEUE_ARCHIVE.md` as appropriate.

⚠ **Cite a row as `queue/<ID>.md`, not as `BUILDER_QUEUE.md:<line>`.** The old form is what the restructure had to go back and repair in 21 files.

Two citations were deliberately left alone, both recorded rather than guessed:

- **`design/plans/AUD-2-one-key-per-source.md:3`** — its sentence is *about* line numbers (*"`:129` on `main`, `:131` in this working tree"*), so re-pointing it needs a rewrite, not a path swap. It was **already wrong before this change** — `:129` was `AUD-7`'s row.
- **`src/Radio.Web/Services/ApiClients/GvResult.cs:41`** — an XML doc comment citing `§ Dependency / ordering notes`. Correcting it means touching `src/`, which this documentation change deliberately does not.

**Directional words inside row prose** (*above*, *below*, *this file*) were written when every row shared one document and were left verbatim; some now point across files.

