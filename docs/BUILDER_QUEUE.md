# Builder Queue

> Work items queued by **Planner** for **Builder** to clear one PR per cycle.
> Planner appends rows + spec/plan links; Builder claims a 📋 row whose dependencies are all met, ships it as a PR, then marks it ✅.
>
> **Last updated:** 2026-09-06 (Builder) — **the queue was restructured; no row's meaning changed.** This file is now an index of **live rows only** (16 of 52). Each live row's full prose moved verbatim to a dossier under [`queue/`](queue/), and the 36 shipped rows moved, prose intact, to [`BUILDER_QUEUE_ARCHIVE.md`](BUILDER_QUEUE_ARCHIVE.md) — which also holds this file's former `Last updated` session log and its resolved-blocker narrative.
>
> ⚠ **Planner: append new rows here as one line.** The Item cell is a short title plus a link to `queue/<ID>.md`; the detail belongs in the dossier. The old shape put 12,946 characters on one physical line, which put the file past what any agent could read in a single pass.

---

## Status legend

| Mark | Meaning |
|---|---|
| 📋 | Queued — ready to claim (all dependencies met) |
| 🔒 | Blocked — waiting on a dependency row |
| ⛔ | On hold — blocked on an **external** dependency (another repo/team must ship first); do not claim |
| 🚫 | **Parked — refused by an owner decision. Never claim.** ⭐ **NEW 2026-09-05, and it exists because the alternative was a false status.** A refused row is not ⛔: ⛔ says *another team must ship first*, which promises the work resumes when they do. `GV-5` is not waiting on anybody — `D31` says the thing it unblocks is not wanted. Marking it ⛔ would have made this queue assert something untrue, which is the defect class `CLAUDE.md` § *Pre-Merge Review* exists for, one layer up from a comment. **The row stays in the table rather than being deleted**, because the reasoning is the asset and a deleted row gets re-proposed. **The reason always lives in [`HANDOFF-GA-PUNCH-LIST.md`](HANDOFF-GA-PUNCH-LIST.md) §6**, not here. |
| 🚧 | In flight — a Builder cycle is working it on a branch |
| ✅ | Done — merged to `main` |

---

## Queue

| # | Item | Status | Plan | Spec / handoff | Depends on | Branch |
|---|------|--------|------|----------------|-----------|--------|
| GV-5 | 🚫 **PARKED 2026-09-05 BY OWNER DECISION `D31` — NEVER CLAIM.** — [detail](queue/GV-5.md) | 🚫 | [`plans/.../pr5-send-contract.md`](superpowers/plans/2026-07-30-gv-messages-pr5-send-contract.md) _(kept for reconstruction; do not execute)_ | [ADR-028](../design/decisions/2026-07-30-gv-sms-send-contract.md) · [handoff §send/bubble states](design-handoffs/HANDOFF-phone-messages-voicemail-sms.md) | **GV-3** (`GvBridgeSendService`, compose/reply, `MessageBubble`, optimistic-append seam). _No external blocker: `POST /api/gvbridge/sms/send` is **shipped**. Their `GVBridge:EnableSmsSend=false` does **not** block — a dark server returns `409 send_disabled`, which this PR handles as a first-class state. **Merges as a user-visible no-op** (our flag off → compose unreachable, no new calls); the only always-on delta is the inert `SmsSent` subscription._ | `feat/gv-messages-pr5-send-contract` |
| GV-6 | ✅ **ASSESSED AGAINST `D31` 2026-09-05 AND UNAFFECTED — claim it as written.** — [detail](queue/GV-6.md) | 📋 | _plan TBD (small)_ | [ADR-024 §3.3](../design/decisions/2026-06-20-gv-mark-read-durable-readstate.md) | **GV-4** | `fix/gv-markread-dark-409` |
| GV-7 | 🔄 **RE-SCOPED BY `D31` 2026-09-05 — the display half survives, the gating half is deleted, and the `GV-5` dependency DISSOLVES.** — [detail](queue/GV-7.md) | 📋 | _plan TBD (design-led; consult the Designer handoff first)_ | [ADR-028 §8](../design/decisions/2026-07-30-gv-sms-send-contract.md) · [handoff Screen C/D](design-handoffs/HANDOFF-phone-messages-voicemail-sms.md) · [**UAT § Findings for GV-7**](uat/2026-07-31-gv-live-data/REPORT.md) | **GV-3** (texts surface). _The "wait for observations" caveat is **discharged** — they exist and are linked in the row. Coordinate with GV-5 if both are in flight: GV-5 adds `GvCounterparty` to `ApiModels.cs` and this row consumes it. **Coordinate with GV-8 too, and prefer GV-8 first:** GV-8 rewrites the conversation pane's state branches (`PhoneTextsPanel.razor:36-68`), which this row also touches — and designing the header/empty treatment on top of a pane that **cannot express "failed"** would bake the F-1 confusion into the new design._ | `feat/gv-messages-pr7-nondialable-senders` |
| GV-9 | ✅ **ASSESSED AGAINST `D31` 2026-09-05 AND UNAFFECTED — all three items survive.** — [detail](queue/GV-9.md) | 📋 | _plan TBD (small; **no longer CSS-only** — two CSS fixes plus one `.razor` guard and its missing test)_ | [UAT F-4/F-7](uat/2026-07-31-gv-live-data/REPORT.md) · [GV-8 UAT (guard provenance)](uat/2026-07-31-gv8-error-state/REPORT.md) · [handoff](design-handoffs/HANDOFF-phone-dark-theme-and-scrollbars.md) | **GV-3** (texts surface). _No external dependency; claimable now. GV-8 is merged, so the guard's canonical form (`PhoneTextsPanel.razor:61`) is on `main` to copy from — read it before writing the thread-list version rather than re-deriving it. Same surface as GV-7: if both are in flight expect `PhoneTextsPanel` and the thread-list row markup to have moved._ | `fix/gv-texts-polish-overflow-unread-align` |
| GV-10 | ⬆ **ASSESSED AGAINST `D31` 2026-09-05 — content unchanged, ARGUMENT STRENGTHENED, tier deliberately NOT promoted, and its priority within the GV set goes from LOWEST to HIGHEST.** — [detail](queue/GV-10.md) | 📋 | _plan TBD (investigate first; scope depends on the answer)_ | [UAT F-5](uat/2026-07-31-gv-live-data/REPORT.md) · [root-cause § Scope-affecting notes](uat/2026-07-31-gv-live-data/F-1-DIAGNOSIS.md) | **GV-3** (texts surface). _Lowest priority of the three UAT rows; do not schedule ahead of GV-8._ | `fix/gv-texts-bubble-full-body` |
| UX-1 | **Skeleton shimmer amplitude — is a 6/255 gradient delta enough on the dark theme?** — [detail](queue/UX-1.md) | 📋 | _plan TBD — **do not write one until the Designer has answered**; scope depends entirely on whether the answer is "new token," "retune the existing pair," or "leave it"_ | [GV-8 UAT `L-1`](uat/2026-07-31-gv8-error-state/REPORT.md) · evidence: `uat/2026-07-31-gv8-error-state/screenshots/03-c2-frame-a-108ms.png` vs `04-c2-frame-b-224ms.png` | — _(no code dependency; it is gated on a design answer, not on a row)_ | `feat/ux-skeleton-shimmer-amplitude` |
| AUD-1 | **Split `UseShazamForAllSources` into the two independent decisions it currently conflates.** — [detail](queue/AUD-1.md) | 📋 | _plan TBD (small-to-medium; the behaviour already exists — the work is splitting the switch and deciding FilePlayer's side)_ | _no spec doc — the diagnosis is in this row_ · provenance: 2026-08-10 debugging session; PR #469 is the adjacent merged fix | — _(no row dependency. **Touches the same file and method region as PR #469** (`BluetoothAudioSource.OnTrackIdentified`), which is merged — rebase, don't re-derive. **Also touches `FilePlayerAudioSource.cs`, which #468 changed on 2026-08-11** — the anchors above are already re-sited, but rebase rather than trusting any earlier copy.)_ | `fix/split-shazam-fingerprint-vs-overwrite` |
| AUD-5 | **A Cast connection that is no longer current can persist its volume as the system master volume.** — [detail](queue/AUD-5.md) | 📋 | _plan TBD (small — a generation parameter, one await-free re-check, and an explicit decision on `IsInitialSync`; **the diagnosis is complete and must not be re-brainstormed**)_ | _no spec doc — the diagnosis is in this row_ · **provenance: PR #473's pre-merge review**, which found it while checking whether #473's own comment was true · the mechanism is now documented in-tree at [`GoogleCastOutput.cs:95-104`](../src/Radio.Infrastructure/Audio/Outputs/GoogleCastOutput.cs) · origin commit `b420edc` (2026-02-11) | — _(no row dependency; claimable now. **⚠ Touches `GoogleCastOutput.cs`, which PR #473 (`0870410`) reflowed: its header comment grew by exactly +66 lines, so EVERY anchor below `:32` in that file moved +66.** The citations in **this** row are already post-#473 and verified; any citation copied from the **`AUD-3`** row is pre-#473 and is not. Also touches `AudioStateUpdateService.cs`, which no other row claims. **No file overlap with `AUD-1`, `AUD-2` or `AUD-4`**, so it can run alongside any of them.)_ | `fix/cast-initial-volume-sync-generation-check` |
| AUD-2 | **Confirm-or-close: is SDR gain/ducking silently dead because the source is registered under one key and addressed by another?** — [detail](queue/AUD-2.md) | 📋 | _plan TBD (**investigate first**; scope depends entirely on the answer)_ | _no spec doc — the diagnosis is in this row_ · `CLAUDE.md` § Architecture (ducking as a core pattern) | — _(no row dependency. **Same root as AUD-4, seen from the ducking side rather than the teardown side — prefer claiming AUD-2 FIRST**: it is investigation-cheap and it decides the key identity that AUD-4's sweep depends on.)_ | `fix/sdr-playback-id-ducking-gain` |
| OPS-2 | **Pin the six floating package versions.** — [detail](queue/OPS-2.md) | 📋 | _plan TBD (small; scope is fully enumerated in this row)_ | _no spec doc — the diagnosis is in this row_ | — _(no dependency; claimable now. Best sequenced **after TEST-1**, because its only real gate is a green full-suite run and TEST-1 is what makes that signal trustworthy.)_ | `chore/pin-floating-package-versions` |
| OPS-3 | **`BindsTo=` for `radio-web` — make joint failure actually joint.** — [detail](queue/OPS-3.md) | 📋 | _plan TBD (small in diff, large in blast radius — the plan should be mostly test/rollout, not code)_ | [`deploy/common/radio-web.service:12-22`](../deploy/common/radio-web.service) (the gap, already documented by #467) · [`deploy/common/radio-api.service`](../deploy/common/radio-api.service) (limiter rationale) | — _(no row dependency; **PR #467 is merged**, so both `StartLimit*` blocks are on `main` to build on.)_ | `fix/systemd-bindsto-radio-web` |
| AUD-4 | **Unify the two source-removal layers, and rename `SoundFlowMasterMixer` — it is not a mixer.** — [detail](queue/AUD-4.md) | 📋 | _plan TBD (medium; the rename dominates the diff, the layer-unification dominates the risk — **and the first task is now the three-layer reconciliation above, not code**)_ | _no spec doc — the diagnosis is in this row_ · commit `03a6fea` is the provenance for the "one layer too high" trap · **PR #468 (`8b1ce0a`) is the provenance for the third layer** | — _(no hard row dependency. **Prefer AUD-2 first** — it decides the key identity this row's sweep must use, and doing them in the other order risks building the sweep around a mismatch that AUD-2 then removes. **Also rebase past #468**, which changed `AudioSourceBase.StopAsync`'s teardown gate — see the ⚠ above.)_ | `refactor/unify-source-removal-and-rename-mixer` |
| TEST-2 | **Close the deferred-capture branch-dispatch coverage gap left by PR #469 — if a native test harness ever becomes feasible.** — [detail](queue/TEST-2.md) | 📋 | _plan TBD — **feasibility check first**: if a native SoundFlow test harness is not practical, close the row and say so — **and if so, prefer closing it with the seam convention described above rather than with nothing**_ | _no spec doc — the diagnosis is in this row_ · PR #469 (merged) is where the seam and its tests landed · **PR #468 (`8b1ce0a`) is where the second and third seams landed** | — _(no dependency. **Touches the same file as AUD-1** (`BluetoothAudioSource.cs`); if both are in flight, expect line anchors to move.)_ | `test/bt-capture-branch-dispatch-coverage` |
| TEST-7 | **A `TimeProvider` seam for `NowPlayingPanel`'s two hardcoded debounce timers.** — [detail](queue/TEST-7.md) | 📋 | — | [punch list §4.6 `TEST-7`](HANDOFF-GA-PUNCH-LIST.md) | — | — |
| UI-6 | **`AudioStateStore` notifies N subscribers and awaits one.** — [detail](queue/UI-6.md) | 📋 | *to be written — small enough for a plan-in-the-row if the owner prefers* | [`PHN-1f` plan §6.2](../design/plans/PHN-1f-the-wait-then-play-queue.md), and § *What the two reviewers found* below | — | `fix/audio-state-store-multicast-notify` |
| PHN-3 | ⭐ **NEW ROW 2026-09-05 — Feature B, the eighth and last PR of the ADR-029 arc, and it never had a row in this file.** — [detail](queue/PHN-3.md) | 📋 | [`PHN-3-the-sms-speak-button.md`](../design/plans/PHN-3-the-sms-speak-button.md) | [handoff §B `:297-430` + §Cross-1…5 `:79-181`](design-handoffs/HANDOFF-phone-console-audio-and-canned-replies.md) · [ADR-029](../design/decisions/2026-08-03-gv-audio-through-engine.md) §4.2 / §9 (the amendment) · [`PHN-2` plan](../design/plans/PHN-2-retire-the-audio-element.md) §0.5 · punch list [§4.4](HANDOFF-GA-PUNCH-LIST.md) | ✅ **MET.** `O6` (*`PHN-1` before or with `PHN-2` / `PHN-3`*) is satisfied: `PHN-1a`…`PHN-1f` ✅ and `PHN-2` ✅ [#566](https://github.com/mmackelprang/RTest/pull/566) have all merged. **Not blocked by `PHN-4`** — they touch different regions of `PhoneTextsPanel.razor` (this row edits only the `<MessageBubble>` call in the list branch), but **both edit that one 442-line file, so claim them SEQUENTIALLY, never concurrently** — the same instruction already given for `GV-9` vs `PHN-4`. ⚠ **`GV-10` is an ordering PREFERENCE, not a dependency:** if the bubbles carry snippets rather than full bodies, this feature reads an ellipsis aloud — that is `GV-10`'s defect and **must not be worked around here**, since a workaround would hide it in the one feature where the difference is audible. | `feat/phn-3-speak-a-text` |

**Shipped rows are not listed here.** All 36 of them, with their prose intact, are in [`BUILDER_QUEUE_ARCHIVE.md`](BUILDER_QUEUE_ARCHIVE.md).

---

## Carried risks (baked into the plans as explicit tasks)

1. **Absolute audio URL** — the DTO `AudioUrl` is server-relative and resolves against the Web origin (`:5002`), 404ing. `GvBridgeApiService.GetVoicemailAudioUrl` rebuilds it absolute against `radio:5004`. Unit-tested in GV-1; the most likely silent-failure point.
2. **GV SMS ≠ trunk SMS** — the GV handler is `GvSmsReceived` on `PhoneHubService`/`/hub`; the pre-existing `GvTrunkHubService.SmsReceived` on `/hubs/gvtrunk` is a different product. Kept namespaced apart; cross-referenced in code comments (GV-1).
3. **Open thread to RotaryPhone** (GV-3 deliverable): request GV mark-read be pulled forward (decision 4) — ✅ **closed**, shipped as GV-4; ~~keep the voicemail audio endpoint unauthenticated/token-in-query when `X-RotaryPhone-Auth` ships (native `<audio>` can't send the header)~~ — ✅ **DISSOLVED, not resolved, by `PHN-1b` ([#534](https://github.com/mmackelprang/RTest/pull/534)).** The constraint existed only because the **browser** was the thing fetching the recording, and a native `<audio>` element cannot attach a request header. Radio.API now fetches it **server-side** through `GvMediaClient`, which attaches `X-RotaryPhone-Auth` itself (`GvMediaAuthHandler.cs:26`) — so the endpoint is free to require the header and there is nothing to ask RotaryPhone for. The `PHN-1b` commit calls this *"the mechanism that closes carried risk #3"*. ⚠ **Do not re-raise it when `PHN-2` retires the `<audio>` element** — that PR is the one that finishes removing the browser from the fetch path, which makes this *more* closed, not less; and confirm `SendSmsResponse` before wiring send — ✅ **closed by GV-5/ADR-028**, which read the as-built controller directly rather than their docs (several RotaryPhone-side handoff docs are stale and still claim the send endpoint does not exist).
4. **Trust their source over their docs for gvbridge contracts.** `D:/prj/RotaryPhone/docs/handoffs/radioconsole-gv-voicemail-sms-ui-handoff.md` still states "No `POST /api/gvbridge/sms/send` endpoint exists yet." It does. ADR-028 was written against `GvSmsController.cs` / `GvBridgeReadDtos.cs` / `GvThreadPoller.cs` directly; do the same for any future contract work.

---

## Companion notes (moved out of this file)

These three sections were measured against the index's size budget and moved rather than inlined. Nothing in them was edited; each file opens with the section verbatim.

| Section | Size | Now at |
|---|---|---|
| Dependency / ordering notes — claim order, and why each ordering is load-bearing | 48.5 KB | [`queue/ORDERING-NOTES.md`](queue/ORDERING-NOTES.md) |
| Cross-repo handoffs (RotaryPhone — NOT claimable here) | 14.3 KB | [`queue/CROSS-REPO-HANDOFFS.md`](queue/CROSS-REPO-HANDOFFS.md) |
| Documented fast-follows (NOT in these PRs) | 9.0 KB | [`queue/FAST-FOLLOWS.md`](queue/FAST-FOLLOWS.md) |

⚠ **Read `ORDERING-NOTES.md` before claiming.** Several rows have a mandatory claim order (`AUD-2` before `AUD-4`, `AUD-6` before `AUD-7`) that this index does not restate.

---

## ⚠ Citations of this file from elsewhere are now wrong

The restructure moved content this file's own readers cite. Nothing below was fixed — re-siting anchors is a separate reviewed pass, and doing it blind is how anchors drift in the first place. Recorded so the next reader knows the citations are stale by construction rather than by neglect:

- **14 line anchors** of the form `docs/BUILDER_QUEUE.md:NNN` across `design/plans/*` and `docs/design-handoffs/*` were **correct before the restructure and are not any more** (e.g. `GV-6`'s plan cites `:118`, `OPS-2`'s cites `:132` and `:447`). Three further anchors were already stale beforehand and remain so.
- **21 citations by section name** — `§ Dependency / ordering notes`, `§ Cross-repo handoffs`, `§ Documented fast-follows` — now name sections that live in [`queue/`](queue/), not here.
- **Directional words inside row prose** (*above*, *below*, *this file*) were written when every row shared one document, and were left verbatim. Some now point across files.

