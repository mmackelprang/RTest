# Builder Queue

> Work items queued by **Planner** for **Builder** to clear one PR per cycle.
> Planner appends rows + spec/plan links; Builder claims a 📋 row whose dependencies are all met, ships it as a PR, then marks it ✅.
>
> **Last updated:** 2026-09-09 (Builder) — **`UI-12` shipped as [#632](https://github.com/mmackelprang/RTest/pull/632) and archived; `UI-14` filed from its review.** **Counted, validated against the previous 26/47:** **28** live, **47** archived, 28 + 47 = **75** total — `AUD-19` and `PHN-9` filed, nothing archived. ⭐ **`PHN-9` was found on the appliance, not in the code**: the PHONE unread badge read 14 before a redeploy and vanished after it, and the disappearance was investigated instead of explained away. **Two plausible first guesses were both wrong** — not a regression from that morning's four PRs, and not the 30 s hub timeout sitting right there in the log. ⛔ **`AUD-19` was CITED BEFORE IT EXISTED.** `AUD-1.md:113` claimed since 2026-09-08 that it was *"filed … documented in `design/FUTURE-WORK.md`"* — **neither was true**, while `AUD-1`'s Builder was told to cite it in its PR body before merge, so that Builder would have had to stop or invent the reference. The row exists now and `AUD-1` carries a struck correction. **The coordinator wrote that citation; `UI-12`'s Builder caught it.** ⚠ **This also corrected a stale figure below** — the *"All N of them"* line read **45** against an archive of **47**, so two of the three count sites disagreed with the instrument. ⭐ **All four hub events declared `Func<T, Task>` now reject a null payload in ONE shared, logged helper ABOVE the fan-out**, and the `On<T>` type arguments became nullable — the type finally agreeing with what a JSON deserializer across a process boundary can produce. ⛔ **NOT in `NotifyAsync<T>`**: `T` is erased there, so a check would silently drop every `NowPlayingChanged(null)` — which means *"nothing is playing"* — stranding the dock on the previous track forever (ADR-033). ⚠ **The change is UNOBSERVABLE in production and the PR says so**: a null cannot arrive, so no UAT can demonstrate the guard, and a UAT report claiming otherwise would be wrong. ⛔ **Four premises fell, one of them the row's own stated reason** — it refused a guard because one *"would drop a broadcast the UI handles today"*, and the UI does **not** handle it: `RadioControlPanel.razor:1053` throws an NRE (swallowed since `UI-7`) while three other surfaces wipe their cached radio state. ⭐ **The conclusion survived; the reason did not.** The `Encoder*` guards cited as precedent were **copy-propagated boilerplate from an event since deleted**, and `db132192` did the identical transformation on `RadioStateChanged` adding no guard — neither shape was ever reasoned about. ⛔ **The plan's control mutation could not pass and its own `M2` row said why** — it varied behaviour *and* shape; **a control that cannot pass is not a control**, and the corrected `M4b` is 8/8 green. ⚠ **The plan's `grep -cE "warning"` gate returns 94, not 47** (MSBuild emits each warning once per project-graph pass) — same defect in `OPS-2:536`. ⚠ **Pre-merge review found no HIGH and all seven findings in COMMENTS**, the sharpest being a seam justified by *"a test cannot do this"* that `RadioControlPanelBandSyncTests.cs:167-173` does today — `CLAUDE.md` item 4 verbatim, **arriving inside a correction**. ⭐ **`UI-14` is the deferred half and the sharper row**: this cycle gated the four events where a regression is *unobservable* and left the three where it is *user-visible* ungated — the realistic form of the forbidden refactor **passes the entire suite** while dropping every `NowPlayingChanged(null)`. ⛔ **`AUD-19` DOES NOT EXIST** — `queue/AUD-1.md:113` says it was "filed … documented in `design/FUTURE-WORK.md`"; it is in neither queue file, has no dossier, and is absent from `FUTURE-WORK.md`. `AUD-1`'s Builder is told to name it in a PR body before merge. **Planner to reconcile.**
>
> ⚠ **Planner: append new rows here as one line.** The Item cell is a short title plus a link to `queue/<ID>.md`; the detail belongs in the dossier. The old shape put 12,946 characters on one physical line, which put the file past what any agent could read in a single pass.

---

## Status legend

| Mark | Meaning |
|---|---|
| 📋 | Queued — ready to claim (all dependencies met) |
| 🔒 | Blocked — waiting on a dependency row |
| ⛔ | On hold — blocked on an **external** dependency (another repo/team must ship first); do not claim |
| 🚫 | **Parked — refused by an owner decision. Never claim.** ⭐ **NEW 2026-09-05, and it exists because the alternative was a false status.** A refused row is not ⛔: ⛔ says *another team must ship first*, which promises the work resumes when they do. `GV-5` is not waiting on anybody — `D31` says the thing it unblocks is not wanted. Marking it ⛔ would have made this queue assert something untrue, which is the defect class `CLAUDE.md` § *Pre-Merge Review* exists for, one layer up from a comment. **The row stays in the table rather than being deleted**, because the reasoning is the asset and a deleted row gets re-proposed. **The reason always lives in [`HANDOFF-GA-PUNCH-LIST.md`](HANDOFF-GA-PUNCH-LIST.md) §6**, not here. |
| 🚧 | In flight — a Builder cycle is working it on a branch |
| ✅ | Done — merged to `main` |

---

## Queue

| # | Item | Status | Plan | Spec / handoff | Depends on | Branch |
|---|------|--------|------|----------------|-----------|--------|
| GV-5 | 🚫 **PARKED 2026-09-05 BY OWNER DECISION `D31` — NEVER CLAIM.** — [detail](queue/GV-5.md) | 🚫 | [`plans/.../pr5-send-contract.md`](superpowers/plans/2026-07-30-gv-messages-pr5-send-contract.md) _(kept for reconstruction; do not execute)_ | [ADR-028](../design/decisions/2026-07-30-gv-sms-send-contract.md) · [handoff §send/bubble states](design-handoffs/HANDOFF-phone-messages-voicemail-sms.md) | **GV-3** (`GvBridgeSendService`, compose/reply, `MessageBubble`, optimistic-append seam). _No external blocker: `POST /api/gvbridge/sms/send` is **shipped**. Their `GVBridge:EnableSmsSend=false` does **not** block — a dark server returns `409 send_disabled`, which this PR handles as a first-class state. **Merges as a user-visible no-op** (our flag off → compose unreachable, no new calls); the only always-on delta is the inert `SmsSent` subscription._ | `feat/gv-messages-pr5-send-contract` |
| GV-7 | 🔄 **RE-SCOPED BY `D31` 2026-09-05 — the display half survives, the gating half is deleted, and the `GV-5` dependency DISSOLVES.** — [detail](queue/GV-7.md) | 📋 | _plan TBD (design-led; consult the Designer handoff first)_ | [ADR-028 §8](../design/decisions/2026-07-30-gv-sms-send-contract.md) · [handoff Screen C/D](design-handoffs/HANDOFF-phone-messages-voicemail-sms.md) · [**UAT § Findings for GV-7**](uat/2026-07-31-gv-live-data/REPORT.md) | **GV-3** (texts surface). _The "wait for observations" caveat is **discharged** — they exist and are linked in the row. Coordinate with GV-5 if both are in flight: GV-5 adds `GvCounterparty` to `ApiModels.cs` and this row consumes it. **Coordinate with GV-8 too, and prefer GV-8 first:** GV-8 rewrites the conversation pane's state branches (`PhoneTextsPanel.razor:36-68`), which this row also touches — and designing the header/empty treatment on top of a pane that **cannot express "failed"** would bake the F-1 confusion into the new design._ | `feat/gv-messages-pr7-nondialable-senders` |
| GV-10 | ❌ **FALSIFIED 2026-09-06 as a Radio Console defect — bubbles already render the full body verbatim; the residual is upstream-only. Status left 📋 because nothing shipped; whether it closes is the owner's call.** — [detail](queue/GV-10.md) | 📋 | _no plan — the "investigate first" instruction was DISCHARGED; the answer is the diagnosis linked at right_ | ❌ **[F-5 diagnosis 2026-09-06 — read first, it supersedes the row's inference](uat/2026-07-31-gv-live-data/F-5-DIAGNOSIS.md)** · [UAT F-5](uat/2026-07-31-gv-live-data/REPORT.md) · [root-cause § Scope-affecting notes](uat/2026-07-31-gv-live-data/F-1-DIAGNOSIS.md) | **GV-3** (texts surface). _Lowest priority of the three UAT rows; do not schedule ahead of GV-8._ | `fix/gv-texts-bubble-full-body` |
| UX-1 | **Skeleton shimmer amplitude — is a 6/255 gradient delta enough on the dark theme?** — [detail](queue/UX-1.md) | 📋 | [`UX-1-the-shimmer-nobody-can-see.md`](../design/plans/UX-1-the-shimmer-nobody-can-see.md) · **0.5 d Builder across two sittings + one ~30 min owner session at the panel** · ⛔ **NOT auto-mergeable** — purely visual, no automated gate can judge it, and the Designer **declined to endorse a landing value** ("a seed for the A/B, not an answer"), so Phase A A/Bs five variants before anything lands · ⚠ **The verification trap this row must not fall into:** naive adjacent-pixel differencing reports **~1.0 levels/px on the CURRENT broken shimmer** — numerically *better* than the 0.30 the fix targets — because a 0.034 ramp quantises into six 1-level steps ~30 px apart, so an unwindowed metric **certifies the bug as fixed**. The gate is windowed spatial slope (±8 px) with a calibration step that refuses to proceed unless the unfixed baseline reads ≈0.034 · ⚠ **No theme parity work here** — this app has exactly one `:root`, `color-scheme: dark` at `:57` and Radzen pinned to `material-dark-base.css`; a light-theme variant would be unreachable CSS, which has already shipped one silent defect in this very file _✅ **Designer gate DISCHARGED 2026-09-07 — the answer is a new token, `--skeleton-shimmer` (36,36,41), and this row can now be planned.** ⭐ **Amplitude is the SMALLER half:** `background-size: 200% 100%` stretches the ramp across one full element width, so there is no edge to detect — the geometry fix is 3.3× steeper and costs nothing. ⚠ **Sequence the panel test so the FREE geometry change is tried before the token**, and measure real skeleton dwell time first: if it is under 500 ms no shape completes a 750 ms pass and the row may still close as "leave it". Confidence is high that 6/255 is too low, low-to-moderate that 36 is right — the panel gamma at code values 20–40 is unmeasured. Full answer in the dossier_ | [GV-8 UAT `L-1`](uat/2026-07-31-gv8-error-state/REPORT.md) · evidence: `uat/2026-07-31-gv8-error-state/screenshots/03-c2-frame-a-108ms.png` vs `04-c2-frame-b-224ms.png` | — _(no code dependency; it is gated on a design answer, not on a row)_ | `feat/ux-skeleton-shimmer-amplitude` |
| AUD-1 | **Split `UseShazamForAllSources` into the two independent decisions it currently conflates.** — [detail](queue/AUD-1.md) | 📋 | [`AUD-1-split-the-fingerprint-gate-from-the-overwrite.md`](../design/plans/AUD-1-split-the-fingerprint-gate-from-the-overwrite.md) · **0.75 d** (F1) / **0.5 d** (F2) · ⛔ **NOT auto-mergeable** — live audio path, user-visible metadata, UAT needs a phone · ✅ **UNBLOCKED BY OWNER DECISION 2026-09-08 — and the rule is simpler than the F1/F2 split.** *"When metadata is available from the audio source, use the source metadata (song name, album name, album art). When one or more is missing, use fingerprinting to augment the missing data."* **Precedence is per FIELD, not per track**, and the same rule applies to **both** sources — so FilePlayer follows BT (F1), but by making one rule true everywhere rather than by imitation. Keep the always-fingerprint gate at `:837`; ⛔ **CORRECTED 2026-09-08** — replace the overwrite at `:867-891` with per-field fill-if-missing. ⚠ **NOT "the behaviour already on the preserve branch at `:893-905`"**, as this cell previously said: that branch fills **cover art only** and never touches Title/Artist/Album, so an empty AVRCP title would end up with no title at all. The preserve path must GAIN per-field text fill — new behaviour, not behaviour made reachable. ⚠ Treat an empty string from the source as **missing**, not as an authoritative blank. ⚠ **`AUD-17` makes this self-correcting on Bluetooth** — AVRCP never supplies art there, so art is always "missing" and fingerprinting always fills it, which is exactly today's working ~99% · _Planner's prior recommendation, now superseded but its evidence still stands:_ `PlayHistory` shows **44 of 52 file plays had their tags overwritten**, two fabricated titles, a **U+2010 non-breaking hyphen** corrupting `blink‐182`, and nine competing cover-art hashes for a track whose embedded art has one. 79% of files carry embedded art and `:2126-2128` replaces it unconditionally · ⚠ **THE FLAG MUST NOT BE RENAMED.** The SQLite store outranks both JSON layers and already holds `fingerprinting:useShazamForAllSources|true`; a rename orphans that row, the new key falls through to `appsettings.json`'s `false`, and **BT album art dies** — the one outcome this row forbids. Not theoretical: `fingerprinting:fpcalcPath` is already orphaned there from the AcoustID→SongRec rename · ⚠ Anchor drift: `appsettings.json:91` → **`:93`**_ | _no spec doc — the diagnosis is in this row_ · provenance: 2026-08-10 debugging session; PR #469 is the adjacent merged fix | — _(no row dependency. **Touches the same file and method region as PR #469** (`BluetoothAudioSource.OnTrackIdentified`), which is merged — rebase, don't re-derive. **Also touches `FilePlayerAudioSource.cs`, which #468 changed on 2026-08-11** — the anchors above are already re-sited, but rebase rather than trusting any earlier copy.)_ | `fix/split-shazam-fingerprint-vs-overwrite` |
| AUD-5 | **A Cast connection that is no longer current can persist its volume as the system master volume.** — [detail](queue/AUD-5.md) | 📋 | [`AUD-5-stale-cast-volume-persists-as-master.md`](../design/plans/AUD-5-stale-cast-volume-persists-as-master.md) · **0.5 d** · **both halves** (generation re-check + subscriber ignoring `IsInitialSync`) · ⚠ **run the plan's Task 1 forensic log read BEFORE the code lands** — Task 3 retires the message it greps for | _no spec doc — the diagnosis is in this row_ · **provenance: PR #473's pre-merge review**, which found it while checking whether #473's own comment was true · the mechanism is now documented in-tree at [`GoogleCastOutput.cs:95-104`](../src/Radio.Infrastructure/Audio/Outputs/GoogleCastOutput.cs) · origin commit `b420edc` (2026-02-11) | — _(no row dependency; claimable now. **⚠ Touches `GoogleCastOutput.cs`, which PR #473 (`0870410`) reflowed: its header comment grew by exactly +66 lines, so EVERY anchor below `:32` in that file moved +66.** The citations in **this** row are already post-#473 and verified; any citation copied from the **`AUD-3`** row is pre-#473 and is not. Also touches `AudioStateUpdateService.cs`, which no other row claims. **No file overlap with `AUD-1`, `AUD-2` or `AUD-4`**, so it can run alongside any of them.)_ | `fix/cast-initial-volume-sync-generation-check` |
| AUD-2 | ✅ **CONFIRMED 2026-09-05 — one key per source: four primary sources register under a minted key and are addressed by `IAudioSource.Id`, so gain and ducking miss silently.** — [detail](queue/AUD-2.md) | 📋 | [`AUD-2-one-key-per-source.md`](../design/plans/AUD-2-one-key-per-source.md) · ⚠ **the investigation is DONE — do not re-run it** | _no spec doc — the diagnosis is in the dossier_ · `CLAUDE.md` § Architecture (ducking as a core pattern) | — _(no row dependency; claimable now. **⚠ The "same root as AUD-4 — prefer claiming AUD-2 FIRST" note that stood here was FALSIFIED 2026-09-06 and is removed, not softened** — they are unrelated bugs and **either may be claimed first**; reasoning in [`queue/AUD-2.md`](queue/AUD-2.md) and [`queue/ORDERING-NOTES.md`](queue/ORDERING-NOTES.md).)_ | `fix/sdr-playback-id-ducking-gain` |
| OPS-3 | **`BindsTo=` for `radio-web` — make joint failure actually joint.** — [detail](queue/OPS-3.md) | 📋 | [`OPS-3-bindsto-for-radio-web.md`](../design/plans/OPS-3-bindsto-for-radio-web.md) · **0.5 d + ~20 min supervised box session** · ⚠ **RE-SCOPE REQUIRED — the rehearsal (2026-09-08) disproved the row as specified.** `BindsTo=` alone fires during `radio-api`'s ordinary 10 s back-off AND the console never returns (`inactive/dead/success`), so a *transient* crash darkens it permanently — the opposite of the purpose the punch list filed it for (`C-195`). ⭐ **`BindsTo=` on web PLUS `Upholds=` on api gives both directions and was measured working.** Merging is inert regardless: `Deploy-ToLinux.ps1` does not install unit files (`C-191`) · ✅ **CLAIMABLE — scope NARROWED by owner decision 2026-09-08 (second decision, superseding the first the same day): ship `BindsTo=` + `Upholds=` ONLY. Readiness is split out to `OPS-10`.** ⚠ **The first decision was made on a false premise I supplied.** This row asserted that `Upholds=` starting web early leaves the SignalR hub *"dead for the lifetime of the radio-web process"*, quoting `Deploy-ToLinux.ps1:445-451`. **That comment describes half of what shipped** — "Fix A" (`c1fba27a`, #386) was **two** fixes, and the client-side retry loop is **still on `main`** (`AudioVisualizationHubService.cs:187-193`, `:208-218`, back-off `{2,5,10,30}`s, replays subscriptions). **So an early start costs 2–30 s of inert console that self-heals, not a permanent outage** — the comment's own trailing clause said *"regardless of hub-service code resilience"* and was quoted twice while being read backwards. Readiness keeps four independent justifications (⭐ including one nobody had noticed: **the boot path runs no ordering poll at all**), but it is a **worthwhile improvement, not a blocker** — hence its own row. ⭐ **Narrowing to unit files preserves `C-191`** — merging *and* deploying stay inert — which is most of why the split is cheap. Estimate returns to **~0.5 d + ~20 min supervised**. ⚠ The owner's first instinct was to drop `Upholds=`; it was reversed once the measured cost was put beside it — **`BindsTo=` alone fires during the ordinary 10 s back-off and the console never returns**, turning a transient crash into permanent darkness, which is worse than the bug the row fixes. ⚠ **Re-rehearse the readiness change before rollout**; the container fixture has now caught two things the reasoning missed · _Second rehearsal on systemd 255.4 (the appliance's exact version), 2026-09-08:_ `C-199` **closed**: the 249 result transfers unchanged. But `C-200` is **CONFIRMED and re-diagnosed** — the hazard is **not** the deploy's `rsync --delete` (that framing was wrong; `:170` stops web then immediately stops api, and `BindsTo=` takes web down again long before any file operation). **It is the START path.** `Deploy-ToLinux.ps1:452` starts api, polls `/hubs/visualization/negotiate` until it answers, and only then starts web — because, per its own comment at `:445-451`, starting web early leaves the SignalR hub *"dead for the lifetime of the radio-web process."* **`Upholds=` starts web the moment api is *active*, which is at exec, not at listener-bound**, so the poll is bypassed and every deploy reintroduces the pre-Fix-A bug it exists to prevent. Needs an owner decision on readiness (`Type=notify`/`ExecStartPost=` on api is the option that fixes the cause) · ⚠ **Instrument correction: `NRestarts` cannot see this.** `Upholds=` issues `start`, not `restart`, so the counter reads 0 whether or not web is cycled — the earlier no-wedge derivation rested on it and was **vacuous**. The property holds structurally instead: web's start count can never exceed api's failure count, and **api latches `failed` at 5 failures in 300 s** (measured, cycle 5 of 6 — both services then down until `systemctl reset-failed`)_ | [`deploy/common/radio-web.service:12-22`](../deploy/common/radio-web.service) (the gap, already documented by #467) · [`deploy/common/radio-api.service`](../deploy/common/radio-api.service) (limiter rationale) | — _(no row dependency; **PR #467 is merged**, so both `StartLimit*` blocks are on `main` to build on.)_ | `fix/systemd-bindsto-radio-web` |
| AUD-4 | **Unify the two source-removal layers, and rename `SoundFlowMasterMixer` — it is not a mixer.** — [detail](queue/AUD-4.md) | 📋 | [`AUD-4-unify-source-removal-and-rename-the-mixer.md`](../design/plans/AUD-4-unify-source-removal-and-rename-the-mixer.md) · **2 d** minimal / **3 d** split · ⛔ **not auto-mergeable — Task 4 changes engine-stop behaviour on the live audio path** | _no spec doc — the diagnosis is in the dossier_ · commit `03a6fea` is the provenance for the "one layer too high" trap · **PR #468 (`8b1ce0a`) is the provenance for the third layer** | — _(**no dependency. ⚠ The "prefer `AUD-2` first" note that stood here was FALSIFIED 2026-09-06 and is removed, not softened.** [`ORDERING-NOTES.md`](queue/ORDERING-NOTES.md)'s claim that `AUD-2` and `AUD-4` are "two symptoms of ONE root cause" and that `AUD-2` "decides the key" is **false**: per-source teardown is key-symmetric — `SDRRadioAudioSource.cs:915` mints `_playbackId` and `:1027`/`:1059` stop with the same field — and this row's roster is keyed by **object reference**, not by string, so there is no key here to decide. The row's prescribed "sweep `_activeComponents` instead" is unnecessary and also points at dead code: `SoundFlowPlaybackService.StopAll()` has **zero callers in the tree**. Plan: `design/plans/AUD-4-unify-source-removal-and-rename-the-mixer.md` §0.4, `C-148`, `C-150`. **Still rebase past #468.**)_ | `refactor/unify-source-removal-and-rename-mixer` |

| AUD-12 | ⭐ **NEW 2026-09-06, observed live — the BT source stalls at `Ready` while audio is playing, so fingerprinting is gated off and album art never resolves.** ✅ **INVESTIGATION CLOSED 2026-09-06: sibling path, NOT a recurrence of #469** — `git log -L` puts the handler's last edit five months before that PR. — [detail](queue/AUD-12.md) | 📋 | [`AUD-12-the-source-that-stalled-at-ready.md`](../design/plans/AUD-12-the-source-that-stalled-at-ready.md) · **0.5 d** · **both predicates** (the `Ready` catch-up **and** the guarded `Stopped` arm) · ⭐ **PLAN REPAIRED 2026-09-08 after `TEST-2` (#614) invalidated it** — anchors re-pointed (everything below `:445` moved **+3**), `C-177` re-pinned to a **stronger** test through the real event path, and ⭐ **Task 4 DELETED**: it rested on the same false native-`AudioEngine` premise `TEST-2` refuted, so this row now touches **one file, not two**. ⛔⛔ **`C-181` — the old UAT could DESTROY ITS OWN EVIDENCE**: `AudioSourceBase.cs:96` writes `State = Playing` unconditionally, so pressing the transport button **clears the stall**, and the old step 4 said to press it *then* check — both would have passed on a broken build. Step 4 is now ordered non-destructive-first. ⛔ **`C-173` was wrong in every clause** — wrong endpoint (`POST /api/audio`, not `/api/audio/start`), wrong branch condition, and it told a tester to grep for a log string **that cannot appear on that path**. ⚠ **`GET /api/audio/state` does not exist**; it is `GET /api/audio`. ⚠ **The UAT now names FOUR ways it can lie** — `AUD-10` (broken build passes), `AUD-18` (**correct** build fails; new step 0b baselines `grep -c "No audio data captured"` at 0), `AUD-1` (album art is not a clean criterion), and its own step 4. ⭐ **Album art is DEMOTED from headline criterion** — this row was filed on the art, so read that before judging a fix. ⚠ **`AUD-12` before `AUD-1`** — the stall gates fingerprinting off entirely, which is what makes `AUD-1`'s UAT possible · ⚠ **the investigation is DONE — do not re-run it** · ⛔ **NOT auto-mergeable: user-facing audio behaviour and UAT is BLOCKED on the owner's phone; §5 is written and deferred** | _no spec doc — measured on `radio` 2026-09-06; log evidence in the dossier_ · #469 (`9bfb7cbe`) is the adjacent merged fix and is **not** the cause | — _(no row dependency; claimable now. **⚠ `AUD-10` is what drives this source to `Stopped` routinely and also poisons the UAT — read the plan's `C-178` before testing: a reconnect between the pause and the check makes a BROKEN build pass.** Touches `BluetoothAudioSource.cs`, which **`AUD-1` and `TEST-2` also claim** — if any two are in flight, expect anchors to move. Also one line in `MockBluetoothService.cs`, which nothing else claims.)_ | `fix/aud-12-bt-source-stalled-at-ready` |
| AUD-11 | ⭐ **NEW 2026-09-06, observed live — the BT capture stream silently re-links to the built-in line-in when its target disappears, and every indicator stays green.** — [detail](queue/AUD-11.md) | 📋 | [`AUD-11-the-capture-that-recorded-the-wrong-jack.md`](../design/plans/AUD-11-the-capture-that-recorded-the-wrong-jack.md) · **1.5 d + a box session** · ⛔ **NOT auto-mergeable** · ⚠ **the `PW_ID_ANY` attribution was WRONG**: the mechanism is `node.autoconnect = true` (an instruction with no failure mode), so the fix is ADDITIVE (`node.dont-reconnect`) with no trade-off — `PW_ID_ANY` stays | _no spec doc — measured on `radio` 2026-09-06_ · `LinuxBluetoothService.cs:1985-1987` (the same behaviour, documented and guarded TWICE on the old `pw-record` path — this is a regression the #262 native migration introduced) | — _(no row dependency. Makes `AUD-10` silent rather than loud; neither blocks the other.)_ | — |
| AUD-10 | ⭐ **NEW 2026-09-06, observed live — pausing on the phone destroys the A2DP transport and resume never restores it, so BT playback is effectively single-use.** — [detail](queue/AUD-10.md) | 📋 | _plan TBD — **two-phone test first**; this row may legitimately close as "upstream / not ours"_ | _no spec doc — measured on `radio` 2026-09-06, reproduced twice_ | — _(no row dependency. ⚠ `audio-gateway` is the CORRECT profile — the number that matters is `sources:`. Profile cycling does nothing; both were tried.)_ | — |

| AUD-13 | ⭐ **NEW 2026-09-06 — `USBPort: ""` matches every device via `string.Contains("")`, so Radio, Vinyl and GenericUSB bind to whatever enumerates first, and the warning written to catch exactly this is unreachable.** — [detail](queue/AUD-13.md) | 📋 | [`AUD-13-the-fallback-that-picks-the-first-jack.md`](../design/plans/AUD-13-the-fallback-that-picks-the-first-jack.md) · **0.5 d + two `curl`s** · ⛔ **NOT auto-mergeable** — it deliberately converts a working state into a failing one on a live appliance, and no gate can observe which jack made a sound · ⭐ **OWNER DECISION DISCHARGED 2026-09-08, and it dissolved the question rather than answering it.** ⚠ **The row's named defect is UNREACHABLE in production**: `USBPort: ""` never reaches `Contains("")` at `USBAudioSourceBase.cs:199` because `AudioSourceFactory.cs:158-162`/`:202-206` refuse a blank port, `RadioFactory.cs:336-339` returns false on one, and `SoundFlowDeviceManager.cs:250` throws `ArgumentException.ThrowIfNullOrEmpty` **22 lines earlier**. It read as true only because `USBAudioSourceTests.cs:58-59` **mocks the device manager away**, removing the backstop exactly where a reader watches the code run · ⚠ **The Radio half is do-nothing**: empty `Devices:Radio:USBPort` is correct, documented configuration — `SystemConfigPage.razor:280` already says *"Leave empty if using RTL-SDR"* · **The real defect is the same fallback reached by a NON-empty port** (`:207-214`): unmatched pattern → `captureDevices[0]`, ambiguous pattern → first match silently, zero devices → **system default** silently_ | _no spec doc — confirmed by code reading 2026-09-06 while planning `AUD-11`_ · `USBAudioSourceBase.cs:198-203` · `src/Radio.API/appsettings.json:51`/`:54` | — _(no row dependency. **Touches `USBAudioSourceBase.cs`, shared by three sources.** Same area as `AUD-11`; neither blocks the other but expect anchors to move if both are in flight.)_ | — |
| AUD-14 | ⭐ **NEW 2026-09-06 — a stale AVRCP watcher survives player re-attach and can raise `Playing` against a torn-down source.** The dedup at `:2539` returns *above* the `Dispose()` at `:2542`, and `OnInterfaceRemoved` never nulls `_mediaPlayer`. — [detail](queue/AUD-14.md) | 📋 | _plan TBD_ · ⛔ **NOT auto-mergeable** — live audio path on a device event; UAT needs the owner's phone | _no spec doc — found by the `AUD-12` investigation, argued for its own row in that plan's §7.1_ · `LinuxBluetoothService.cs:2536-2542`, `:929-932` · a 2026-07-16 note already records the missing `InterfacesRemoved` cleanup | — _(no row dependency, and **`AUD-12` must NOT wait for it.** This is *why* `AUD-12` ships `(State == Stopped && HasCapturePath)` rather than bare `Stopped`; if this lands first that guard becomes belt-and-braces rather than load-bearing.)_ | — |

| AUD-15 | ⭐ **NEW 2026-09-07 — the BT audio ring buffer runs EMPTY (`buffer: 0/384000`), 55 underruns and climbing, substituting silence into playback.** PipeWire `OnProcess` execution reaches **14.2 ms** against a 10.67 ms quantum. — [detail](queue/AUD-15.md) | 📋 | [`AUD-15-the-buffer-that-runs-empty.md`](../design/plans/AUD-15-the-buffer-that-runs-empty.md) · **2 d + a ≥30 min box session with the owner present** (onset is ~12 min) · ⛔ **NOT auto-mergeable** — the fix continuously changes the sample rate of live audio and a wrong gain or sign detunes the music rather than failing loudly · ⚠ **the plan DISPROVED this row's headline**: the lock is a red herring (375-quantum ring), `buffer: 0/384000` is a tautology, and the cause is an open-loop resampler whose `SetRatio` has zero callers | _no spec doc — measured on `radio` 2026-09-07; log evidence in the dossier_ · `CLAUDE.md` § PipeWire Quantum Tuning | — _(no row dependency. ⚠ **NOT the same defect as `AUD-10`** (transport dies on pause) **nor the RotaryPhone HFP disconnect** found in the same logs — same day, same journal, three different problems. Touches the live audio path; **not auto-mergeable**.)_ | — |

| AUD-16 | ⭐ **NEW 2026-09-08 — retire the deprecated USB radio path. ⚠ The deprecation itself is ALREADY implemented, so this is a removal decision, not a fix.** — [detail](queue/AUD-16.md) | 📋 | _plan TBD — ⚠ **"keep it, documented" is an acceptable close**; the greenfield rule makes removal permitted, not required. Scope first: is the `RaddyRF320BT/` submodule in or out, and what happens to an orphaned SQLite store row?_ | _no spec doc — owner input 2026-09-07: "The Radio USB connection has been deprecated so that the SDRRTL radio is really the only one supported"_ · **already live** at `RadioFactory.cs:147` (defaults to `RTLSDRCore`), `:336-339` (blank port ⇒ RF320 never offered), `SystemConfigPage.razor:280` (ships the copy) | — _(no row dependency. **Not `AUD-13`** — that row's real defect is the wrong-jack fallback on a NON-empty port and must not be folded in.)_ | `chore/aud-16-retire-usb-radio-path` |
| AUD-17 | ⛔ **RETRACTED AND REWRITTEN 2026-09-08, the same night it was filed — AVRCP album art has NEVER worked, and this was never a regression.** `LinuxBluetoothService.cs:2761-2765` reads the MPRIS names `ArtUrl`/`mpris:artUrl` off a proxy on `org.bluez.MediaPlayer1`, which publishes **`ImgHandle`** — so `CacheAvrcpArtAsync` has never executed. — [detail](queue/AUD-17.md) | 📋 | _plan TBD — ⚠ **the first question is whether to fix it at all.** `ImgHandle` is an OBEX BIP handle and **BlueZ 5.72 ships no BIP client**, so the options are implement BIP, **delete the dead path** (probably right — SongRec already supplies art on ~99% of rows), or upgrade BlueZ. Whatever is chosen, **fix the comment and the `:339`/`:358` log strings**, which claim MPRIS about a BlueZ interface_ · ⛔ **NOT auto-mergeable** if the path is changed rather than deleted — needs the owner's phone | _no spec doc — measured read-only on `radio` 2026-09-08: **`file://` is ZERO across all 45,210 `TrackMetadata` rows, every source**, and `PlayHistoryTracker.cs:733-737` writes any BT-supplied URL raw at row creation, so one would have appeared. Of 66 `Avrcp` rows with art, **26 share a content-addressed filename with a `Shazam` row** (`AlbumArtCacheService.cs:46`/`:80` hash the image bytes, so a shared name means byte-identical), 1 with `Manual`_ · ⛔ **Do NOT "correct" `queue/AUD-1.md:26` or `ROADMAP.md:133`** — their conclusion is right, only their mechanism is incomplete | — _(no row dependency. ⚠ **`Source` records the TITLE's provenance, not the art's** — `UpdateRecentPlayHistoryCoverArtAsync` (`:1070-1112`) writes `CoverArtUrl` without touching `Source`, and its only live caller is the SongRec path. Any future row reasoning from `Source='Avrcp'` about **art** is reasoning from the wrong column.)_ | `fix/aud-17-avrcp-art-dead-path` |
| UI-8 | ⭐ **NEW 2026-09-08 — two glow tokens are consumed and never declared, so three shipped `box-shadow`s render as nothing** — including both phone call buttons. — [detail](queue/UI-8.md) | 📋 | _plan TBD — ⛔ **needs a Designer answer first**: declare the tokens (three glows switch on) or delete the declarations (codify today's actual appearance)? Both are visible changes and neither is obviously right_ | _no spec doc — verified 2026-09-08: `--signal-green-glow` consumed at `design-system.css:5186`/`:5425` and `--signal-red-glow` at `:5432`, **neither declared in `:root`**; `--accent-glow` `:74`, `--accent-glow-strong` `:75` and `--signal-amber-glow` `:88` are all declared and fine_ · ⚠ **partly known already** — the `ENC-12` comment at `:5374-5381` documents the red token accurately and deferred it deliberately, but **misses the green token entirely**, so a fix scoped to that comment leaves two thirds of the defect | — _(no row dependency)_ | `fix/ui-8-undeclared-glow-tokens` |
| UI-10 | ⭐ **NEW 2026-09-08 — the Blazor circuit times out every ~30 s.** `System.TimeoutException: Server timeout (30000.00ms)` repeatedly, plus a `JSDisconnectedException`. **Ours entirely** — RotaryPhone touched neither service. — [detail](queue/UI-10.md) | 📋 | _plan TBD — ⚠ **cheapest thing first**: check `ServerTimeout ≥ 2 × KeepAliveInterval`, the commonest cause. Then establish whether it is the kiosk, the network or the server, and whether the reconnect overlay ever appears on the panel_ · ⚠ **a unit test cannot show a wall-clock symptom on a live box** — baseline `grep -c 'Server timeout'` per day BEFORE changing anything, so "it improved" is a comparison not an impression | _no spec doc — measured in `radio-web`'s log 2026-09-08; **continued after the upstream GV fix at 15:31**, so it is a standing condition rather than a symptom of that outage. 30000 ms is the SignalR client default, so this is the **Blazor Server circuit**, not a GV call_ | — _(no row dependency. ⚠ **May be upstream of `GV-12`** — a dead circuit cannot refetch. ⚠ **Check correlation with `AUD-18`** — same box, and `CLAUDE.md` records log volume correlating with audible distortion; both could be resource pressure. **Check, do not assume** — two rows blaming each other is worse than two independent rows.)_ | `fix/ui-10-blazor-circuit-timeout` |
| UI-11 | ⭐ **NEW 2026-09-08 — an unmatched `/api/*` path returns HTTP 200 and `index.html` instead of 404.** ⭐ **Offered by us to RotaryPhone and they asked for it** — *"Yes, please"* — and they are filing the symmetric fix on their side, so the correction is two-way. — [detail](queue/UI-11.md) | 📋 | _plan TBD — ⚠ **ordering is the whole job**: claim `/api/{**rest}` BEFORE the SPA fallback so real API routes still win and only genuinely unmatched ones 404. ⛔ **Do not break the real fallback** — deep links (`/phone`, `/system`, `/radio`) must still serve `index.html`, and a fix that 404s those is worse than the bug. ⚠ **Check `/stream/audio` and `/stream/audio/mp3`** — not under `/api/`, must not be caught_ · likely auto-mergeable — no hardware, no live-audio path, testable both directions | _no spec doc — ⭐ **it has cost real time twice in ONE day, on both sides**: our original `XR-2` workaround used a raw `/`, missed the API route, fell through to the fallback and **returned `index.html` with HTTP 200**, making a broken request look successful; and RotaryPhone, verifying the `XR-2` fix **for us**, probed `/api/gvsms/` instead of `/api/gvbridge/sms/` and got our SPA shell back — their words, "your SPA-fallback trap biting us in our own house"_ · ⭐ **Same disease as everything both repos unpicked this week: a success code covering a failure** — `Succeeded:true` meaning only "the JSON parsed", `available:true` through a total outage, `NRestarts=0` on a unit being cycled, a falsification step that swapped two disjoint types | — _(no row dependency. ⚠ **Scope question**: `Radio.API` serves no static files so probably has no fallback to fix — **verify rather than assume**, and say which services were checked.)_ | `fix/ui-11-api-404-not-spa-fallback` |
| UI-13 | ⭐ **NEW 2026-09-09 — a failed broadcast still advances the change-detection cache, so the delta is NEVER re-sent.** `AudioStateUpdateService.cs:477` assigns `_lastRadioState` **before** the `await SendAsync` that may throw or be cancelled. Found by `UI-12`'s Planner and flagged as out of scope rather than folded in. — [detail](queue/UI-13.md) | 📋 | ✅ **PLANNED 2026-09-09 — and the plan FALSIFIED this row's headline. Re-tiered P2 → P3.** [`UI-13-the-cache-advance-that-only-shutdown-can-reach.md`](../design/plans/UI-13-the-cache-advance-that-only-shutdown-can-reach.md) · ⛔ **`SendAsync` CANNOT throw on a failed broadcast** — `HubConnectionContext.cs:361` catches everything else, aborts the *connection*, and returns a **successful** `FlushResult`. **Measured on a real Kestrel host with real WebSocket clients (ASP.NET Core 10.0.11): a hard-killed socket took 129 consecutive sends over 8 s with zero faults**; an unserializable payload dropped the client while the caller saw success in 8 ms · ⭐ **Only cancellation escapes, and that token is ALWAYS `ExecuteAsync`'s `stoppingToken`, which `:196-199` catches as "Normal shutdown" and breaks** — so the only exception it can produce is the one that means *stop and exit*, and the stranded cache **dies with the process** (`AddHostedService` only, no second reader) · ⚠ **KEPT for the PRECONDITION, not the bug** — safety rests entirely on "the only canceller is shutdown", so **a backplane or any different token makes this live AND silent**. ⛔ **Do not build ahead of any row with a live user-visible defect. Closing it with the correction appended is equally defensible — OWNER'S CALL** · ⛔ **SIX live sites, not five: `:249` was MISSING from this row and `:454` is a DEAD field** (assigned, read nowhere in `src/`). ⚠ **`:249` is structurally different** — unconditional assignment, send inside `if (!isFirstRun)`, so a mechanical move stops the first poll establishing a baseline · ⛔ **"The failure is silent" is half wrong** — an escaping send logs `LogError` at `:203`; ⭐ the sharper truth is that a persistent fault makes the service go **completely silent within ~3 s**, no sends, no errors, no recovery · **Re-entrancy: nothing serialises them because nothing needs to** — one caller, one instance, one writer per field_ · likely auto-mergeable — no hardware, unit-testable · _prior framing, retained: ⚠ **it is a SHAPE, not a line**: the same ordering is at `:266`, `:279`, `:453-454`, `:502`. **Fix the shape** — repairing one and leaving four is worse than none, because it makes the class look handled. ⚠ **Check re-entrancy before swapping the order** — if two updates interleave, a late assignment can write a stale value over a newer one. ⚠ **Establish whether `SendAsync` actually throws or swallows internally** — if it swallows, the cache-advance is unreachable and this is a tidy, not a fix, and a test written first would be one that cannot fail_ · likely auto-mergeable — no hardware, unit-testable | _no spec doc — ⭐ **the failure is silent AND self-erasing**: no retry, and the evidence a broadcast was missed is destroyed by the same statement that misses it. The next comparison sees "no change" against a state the clients never received_ · ⭐ **Same family as `AUD-12` and `GV-12`: state that stops tracking reality with nothing reporting a fault** — here it shows as a panel quietly out of date until something unrelated forces the next broadcast | — _(no row dependency. ⛔ **STRUCK 2026-09-09 — this cell's own instruction was a CATEGORY ERROR.** It read: *"`CLAUDE.md` § Test Timing — this service is timer-driven; use the injectable `TimeProvider` idiom."* **The service is timer-driven; the DEFECT is not.** The `Check*Async` bodies read no clock, the tests drive them directly by reflection with no `Task.Delay` on either side, and adding the seam would ship production code nothing drives. The genuinely clock-driven `_lastFingerprintBroadcast` (`:910-916`) is a **rate limiter, not a change-detection cache**, and is out of scope. ⚠ **The "do not race a wall clock" half still stands and the plan honours it** — nothing was weakened, the instrument was just pointed at the wrong thing.)_ | `fix/ui-13-cache-advance-before-send` |
| UI-14 | ⭐ **NEW 2026-09-09 — the three hub events where a dropped null is USER-VISIBLE have no test seam, and the refactor that would break them passes the entire suite.** Filed by the `UI-12` Builder from its pre-merge review finding `M6`, deferred out of that PR rather than folded in. — [detail](queue/UI-14.md) | 🚀 | ✅ **PLANNED 2026-09-09** — [`UI-14-the-null-that-must-arrive.md`](../design/plans/UI-14-the-null-that-must-arrive.md) · ⛔ **the planning FALSIFIED this row's own discriminator**: it implies a producer *can* send null on the three uncovered events and **none can** — each has exactly one sender (`AudioStateUpdateService.cs:281`, `:504`, `:1087`) and `AudioStateHub.cs` has no `SendAsync` at all, so **both families are identical in reachability**. ⭐ **The real distinction is PERMANENCE** — the service broadcasts only on a change, so a wrongly-dropped null is never re-sent and the dock and sleep screen strand indefinitely, where `UI-12`'s failure repaints wrongly once. **Conclusion survives, reason does not** · ⛔ **`NowPlayingChanged` has FIVE subscribers doing THREE different things** with a null (clear / clear / REST re-fetch / keep-and-notify / discard) — `Sleep.razor:13` injects the **hub** as `AudioState`, hiding that it is a direct subscriber · ⛔ **`UI-12`'s dossier mis-cites `AudioStateHubServiceTests.cs:148-158`, and `UI-12` MADE that comment false** — the seam it calls "no in-tree precedent" is the one `UI-12` shipped; **rewrite it in this PR** · ⛔ **ADR-033 says seven payload-carrying events; there are eight** (fourteen total) — **third counting slip in that file** · ⚠ **Two of the three have ZERO null coverage** — both existing null-firing tests fire `NowPlayingChanged` · ⭐ **`M0` measures the hole BEFORE the fix**: the realistic refactor is green across the whole assembly today; `M7` is a shape-only control that must stay 100% green **with the lint run under it** · ⭐ **Task 4 (reflection census) KEPT by coordinator** — a gate hard-coding "these three" has the same shape as the gate it replaces_ · _prior framing, retained: ⭐ **`UI-12` built the seam for the four events where a regression is UNOBSERVABLE and left the three where it is user-visible ungated — exactly backwards from where the risk lives.** ⚠ **The realistic form of the `C-404` mistake is GREEN today**: keeping `AcceptPayload` *and* adding a "defensive" null check inside `NotifyAsync<T>` passes all 1,172 `Radio.Web.Tests` while silently dropping every `NowPlayingChanged(null)`. ⛔ **The naive form fails 4 tests, so the gate looks real and is not.** ⚠ **Do NOT write the test through `HubEventFire`** — it fires the event, DOWNSTREAM of the fan-out, and passes either way_ · likely auto-mergeable — Web + tests only | _no spec doc — ⚠ **the three are NOT equivalent and `UI-12`'s first draft said they were**: `NowPlayingChanged(null)` clears the dock (**demonstrated**, `NowPlayingDockTests.cs:246-249`), `VolumeChanged(null)` is a **REST re-fetch trigger** (`NowPlayingPanel.razor:617-631`) and NOT "an absent snapshot", and `EventPlaybackChanged(null)` is **unevidenced** — no producer sends one_ · ⭐ **Same family as `UI-7` `C-213`**: a harness that reports success against a broken implementation | — _(no row dependency. Builds on `UI-12`'s seam, merged.)_ | `fix/ui-14-nullable-hub-event-seam` |
| PHN-7 | ⭐ **NEW 2026-09-08 — `BellHealthService` polls the transport that lies, so predictive-degrade cannot fire.** `SystemStatus` means two different things over SignalR vs REST, and we poll REST every 15 s. — [detail](queue/PHN-7.md) | 📋 | _plan TBD — ⛔ **needs an owner decision on one point**: their `acknowledged` flag does **NOT** survive a restart (`BellFailureTracker` is in-memory), so a nightly-restarting kiosk **does** resurrect a dismissed bell note. Treat the note as session-scoped and say so in the copy, or ask them to persist it_ · ⚠ **do NOT build the predictive-degrade rule against the REST signal meanwhile** — the rule is ours and correct, the signal is theirs and wrong until their convergence ships | _no spec doc — measured on the box 2026-09-08: two REST calls 10 ms apart returned `ht801LastCheckedUtc` of `…0867356Z` then `…0968027Z`, i.e. **`DateTime.UtcNow` every time**. Over SignalR the same field is a genuine 30 s probe of the **resolved registrar binding**; over REST it is a synchronous ICMP ping of the **configured** address_ · ⭐ **RotaryPhone's own doc says that configured-address ping "reported the CORRECT address throughout the entire 2026-07 outage while every INVITE went to a stale one"** — so predictive-degrade would not have fired during the incident it exists to prevent | — _(⚠ **Their convergence fix is ADR-written but not built**; it needs no change from us. Our half is deciding whether to wait or move onto the SignalR path we already hold open.)_ | `fix/phn-7-bell-health-transport` |
| PHN-8 | ⭐ **NEW 2026-09-08 by owner request — speak a text message from the phone DASHBOARD, without opening the thread.** ⚠ **NOT a duplicate of `PHN-3` and must not re-implement it**: that shipped a per-message play button on `MessageBubble.razor`, *inside* an open conversation. The **thread list has no speak affordance at all** (verified 2026-09-08 — `PhoneTextsPanel.razor`'s only speech matches are doc comments about the message-level feature). — [detail](queue/PHN-8.md) | 📋 | _plan TBD — ✅ **DESIGN GATE DISCHARGED BY OWNER 2026-09-08, both questions answered.** **(1) ADD, do not move** — despite the word *"rather than"*, `PHN-3`'s per-message button **stays** and nothing shipped is reverted. ⚠ The cost: **two places can now start speech**, so stop/state handling must be coherent across both and `PHN-3`'s state machine now needs to model *which origin*. ⚠ **Run a Polisher pass before merge** — two speak affordances on one surface is exactly that drift. **(2) Speak ALL UNREAD in that thread, oldest first**, falling back to the latest when nothing is unread; needs a queue and a stop control. ⚠ **"Unread" must mean what the unread dot means** — `GV-4`/`GV-6` own mark-read semantics, so do not mint a second definition. ⚠ **Whether speaking marks them read is NOT decided** — the plan must ask, and it interacts with `GV-6`'s dark-flag path. _Superseded framing, kept for the reasoning:_ `PHN-3`'s handoff already set the visual language for the per-message button and the topbar chip; extend it rather than inventing a second idiom_ | _no spec doc — ⭐ **the speech path is BUILT**: reuse `GvSpeechText.ForMessage`'s eight content rules and `EventPlaybackApiService.StartSpeechAsync`. This row is a surface/affordance question, not an audio one_ · ⚠ **the transport rejection token is `Transport`, NOT `Unreachable`** (`PhoneTextsPanel.razor:361` records an earlier draft getting this wrong) · ⚠ **never log message bodies or numbers** — `Radio.Web`'s Console sink is unrestricted, which is why `PHN-5` exists | — _(⚠ **`PHN-4` deleted the composer from this surface** and **`GV-9` shipped alignment work plus four structural tests on the conversation rows** — re-derive anchors and ⛔ **do not delete those tests as "odd assertions"**. **`GV-7` must not run concurrently**; `GV-12` touches the same panel's failure handling. ⚠ **Check `AUD-2` before judging by ear** — ducking is broken under event playback, so a speak test heard over un-ducked music reads as broken when it is not.)_ | `feat/phn-8-speak-from-dashboard` |
| PHN-9 | ⭐ **NEW 2026-09-09 — the topbar PHONE unread badge requires you to have already visited the page it points at.** `PhoneUnreadState` is a process-wide singleton starting at **0** (`Program.cs:431`) whose **only writer is `PhonePage` while mounted** (all 17 `Set` sites in `PhonePage.razor`); `MainLayout:402` seeds the badge by *reading that singleton, with no fetch*. **After every `radio-web` restart the badge is absent for the life of the process** — and deploys restart `radio-web`, so that is the normal state. — [detail](queue/PHN-9.md) | 📋 | _plan TBD — ⛔ **NOT a small fix: there is no server-side unread total to hydrate from.** `GvBridgeStatusService` exposes only `Current`/`IsAvailable`/`IsHealthy`; no count-bearing DTO or API method exists anywhere in `src/Radio.Web`, only per-item `HasUnread`/`IsRead` flags. Needs **either** a new server-side total **or** a hosted poller on the `BellHealthService` shape (`Program.cs:443-453`) — ⚠ **price the client-side option honestly: deriving it means three REST calls on every app start** on a resource-constrained box · ⚠ **CROSS-REPO — if the answer is a server-side total it is RotaryPhone's endpoint, not ours.** Follow the boundary protocol and **ask first**; do not build a Radio-side poller that duplicates a total they intend to serve_ · ⛔ **NOT auto-mergeable** — cross-repo question + user-visible surface | _no spec doc — ⭐ **the repo already names this defect, about a different badge**: `MainLayout.razor:406-408` reads *"Unlike PhoneUnread (which PhonePage publishes), BellHealthService polls on its own, **so the badge does not require the user to have already visited the page it points at**"*, and `:420-423` describes the identical failure mode as already fixed for the encoder badge. **Queue, Mute, Encoder and Bell all hydrate authoritatively; this one does not** · ⚠ **The latch is stale in BOTH directions** — `Dispose` never resets, so messages read on the handset never decrement it; the badge can be wrong high as easily as wrong low · ⭐ **Live verification needs NO code change**: tap PHONE, go Home — badge should appear, survive a kiosk reload, and die on a `radio-web` restart. **If it does not behave that way this row is wrong**_ · ⛔ **Two first guesses were both WRONG and are recorded so nobody re-runs them**: not a regression from the four PRs deployed that morning, and **not** the 30 s hub `TimeoutException` in the log — **no hub carries an unread count** (`PhoneHubService.cs:18-36` delivers individual items, never a total) and `MainLayout` injects no hub service at all | — _(no row dependency. ⚠ **Related but distinct from `PHN-7`** — that row is `BellHealthService` polling a transport that lies; this is a badge that polls nothing whatsoever.)_ | `fix/phn-9-unread-badge-hydration` |
| OPS-10 | ⭐ **NEW 2026-09-08 — give `radio-api` a readiness signal, so "active" means "the hub answers".** Split out of `OPS-3` when the premise that made it urgent turned out false. — [detail](queue/OPS-10.md) | 📋 | _plan TBD — ⚠ **the analysis already exists**: `design/plans/OPS-3-bindsto-for-radio-web.md`'s readiness sections are this row's inheritance and should move here. ⛔ **`Type=notify` was investigated and REJECTED — do not re-propose without reading the dossier**: .NET sends `READY=1` on `ApplicationStarted`, after every `IHostedService.StartAsync` returns, and `AudioEngineInitializationService.cs:107-187` awaits hardware inline and unbounded, so `READY=1` would arrive **later** than "the hub answers" and would **manufacture the very wedge `OPS-3` exists to reduce**_ · ⛔ **NOT auto-mergeable** — changes production service startup | _no spec doc — ⭐ **the real gap is the BOOT path**: `Deploy-ToLinux.ps1:452` already polls the negotiate endpoint before starting web, but **nothing polls on boot**, so this ordering has never been enforced outside a deploy_ · ⚠ **The harm was overstated and the correction is the point** — a client retry loop still on `main` heals an early start in 2–30 s, so this is a correctness improvement, not an outage fix. **Do not restore the stronger claim** | — _(**`OPS-3`** ships the coupling this makes safe. Claiming it first is possible but pointless. ⛔ **One number cannot be established without the box**: how long `radio-api` really takes from exec to the hub answering, cold — it is nowhere in the repo, and nobody has recorded whether the deploy poll has ever reached its 20th iteration.)_ | `feat/ops-10-radio-api-readiness` |
| AUD-18 | ⭐ **NEW 2026-09-08 — the fingerprint tap returned ZERO bytes for 11½ hours while the radio played, and nothing noticed.** ⭐ **First repo row for the capture-lifecycle failure** — `MEMORY.md` has carried it as HIGH PRIORITY for months and **no row existed**; `SoundFlowAudioTap` appears in no other row. — [detail](queue/AUD-18.md) | 📋 | _plan TBD — ⚠ **consider proposing the watchdog and the root cause as SEPARATE, sequenced deliverables**: eleven hours of a 15-second loop returning zero is detectable in under a minute, and a restart-after-N-empty-captures guard is unit-testable and can ship without finding the cause_ · ⚠ **the failure needs ~37 h of uptime to appear, so no test reproduces it and no green run proves the root-cause fix** — say so rather than implying otherwise · ⛔ **NOT auto-mergeable** — live audio path | _no spec doc — measured on `radio` 2026-09-08. **Onset 09-07 23:00 EDT, recovery 09-08 10:22:57, duration 11 h 23 m.** Hourly counts were **240/hour, i.e. one every 15 s — the full capture window failing back-to-back** — each with ~1,420 read attempts. **Independently confirmed in the fingerprint DB: ZERO `TrackMetadata` rows for the whole window**, resuming exactly when the warnings stop. Daily counts **0,0,0,0 → 48 → 112 → 2,490**, the last being **18.6% of every log line**, doubled because `BackgroundIdentificationService` logs a twin_ · ⭐ **Recovered on an RTL-SDR STREAM restart at 10:23:03 with `radio-api` uptime unbroken since 09-06** — so restarting the *source* suffices and the service need not be bounced, which refines `MEMORY.md`'s "restart fixes" and points below the engine | — _(no row dependency. ⚠ **Not `AUD-15`** — that is the BT ring buffer empty *during playback*; this is the tap reading zero *from the mixer*. ⚠ **Not `AUD-12`** — the source was `Playing` throughout. ⚠ **Not proven audio stopped being audible**: the tap is a separate path from playback and nobody was awake to hear it — what IS proven user-visible is album art and play history dead for 11½ hours.)_ | `fix/aud-18-capture-tap-returns-zero` |
| AUD-19 | ⭐ **NEW 2026-09-09 — after `AUD-1` ships, now-playing will obey the per-field metadata rule and History will not.** `PlayHistoryTracker` sets `MetadataSource = Fingerprinting` on *any* landed identification (`:563-573`, `:631-641`) without consulting which branch the source took. — [detail](queue/AUD-19.md) | 📋 | _plan TBD — ⚠ **the first question is whether History is even WRONG**, and it must be asked before it is assumed: there is a real argument that history records *what fingerprinting identified*, in which case this row closes as "correct, documented" and that is a legitimate outcome. If it IS wrong, it must call `AUD-1`'s shared per-field predicate rather than re-implement it — two implementations of one rule is how the rule stops being one rule_ · likely auto-mergeable once `AUD-1` lands — no hardware, unit-testable | _no spec doc — ⛔ **this row was CITED BY `AUD-1.md:113` BEFORE IT EXISTED**, as *"filed … documented in `design/FUTURE-WORK.md`"*; neither was true, and `AUD-1`'s Builder is told to name it in the PR body before merge. **Coordinator error, caught by `UI-12`'s Builder** · ⚠ **`MetadataSource` is provenance and is load-bearing elsewhere** — `AUD-17` was retracted for misreading exactly this column (`Source` records the TITLE's provenance, not the art's) · ⚠ **Beware a vacuous UAT**: with `AUD-12`'s stall or `AUD-18`'s zero-byte tap live, NO identification lands and a broken build looks correct_ · ⭐ **Why separate rather than folded in: different file, different lifecycle, and folding it in roughly doubles the blast radius of a change already on the live audio path** | **`AUD-1`** — ⛔ **do not claim first**; this row has no meaning until the per-field rule exists to diverge from | `fix/aud-19-history-per-field-precedence` |

**Shipped rows are not listed here.** All 47 of them are in [`BUILDER_QUEUE_ARCHIVE.md`](BUILDER_QUEUE_ARCHIVE.md) — with their prose intact for the rows archived before dossiers existed, and as a link to the dossier for those archived since (the `OPS-2` convention).

---

## Carried risks (baked into the plans as explicit tasks)

1. **Absolute audio URL** — the DTO `AudioUrl` is server-relative and resolves against the Web origin (`:5002`), 404ing. `GvBridgeApiService.GetVoicemailAudioUrl` rebuilds it absolute against `radio:5004`. Unit-tested in GV-1; the most likely silent-failure point.
2. **GV SMS ≠ trunk SMS** — the GV handler is `GvSmsReceived` on `PhoneHubService`/`/hub`; the pre-existing `GvTrunkHubService.SmsReceived` on `/hubs/gvtrunk` is a different product. Kept namespaced apart; cross-referenced in code comments (GV-1).
3. **Open thread to RotaryPhone** (GV-3 deliverable): request GV mark-read be pulled forward (decision 4) — ✅ **closed**, shipped as GV-4; ~~keep the voicemail audio endpoint unauthenticated/token-in-query when `X-RotaryPhone-Auth` ships (native `<audio>` can't send the header)~~ — ✅ **DISSOLVED, not resolved, by `PHN-1b` ([#534](https://github.com/mmackelprang/RTest/pull/534)).** The constraint existed only because the **browser** was the thing fetching the recording, and a native `<audio>` element cannot attach a request header. Radio.API now fetches it **server-side** through `GvMediaClient`, which attaches `X-RotaryPhone-Auth` itself (`GvMediaAuthHandler.cs:26`) — so the endpoint is free to require the header and there is nothing to ask RotaryPhone for. The `PHN-1b` commit calls this *"the mechanism that closes carried risk #3"*. ⚠ **Do not re-raise it when `PHN-2` retires the `<audio>` element** — that PR is the one that finishes removing the browser from the fetch path, which makes this *more* closed, not less; and confirm `SendSmsResponse` before wiring send — ✅ **closed by GV-5/ADR-028**, which read the as-built controller directly rather than their docs (several RotaryPhone-side handoff docs are stale and still claim the send endpoint does not exist).
4. **Trust their source over their docs for gvbridge contracts.** `D:/prj/RotaryPhone/docs/handoffs/radioconsole-gv-voicemail-sms-ui-handoff.md` still states "No `POST /api/gvbridge/sms/send` endpoint exists yet." It does. ADR-028 was written against `GvSmsController.cs` / `GvBridgeReadDtos.cs` / `GvThreadPoller.cs` directly; do the same for any future contract work.

---

## Companion notes (moved out of this file)

These three sections were measured against the index's size budget and moved rather than inlined. Nothing in them was edited; each file opens with the section verbatim.

| Section | Size | Now at |
|---|---|---|
| Dependency / ordering notes — claim order, and why each ordering is load-bearing | 48.5 KB | [`queue/ORDERING-NOTES.md`](queue/ORDERING-NOTES.md) |
| Cross-repo handoffs (RotaryPhone — NOT claimable here) | 14.3 KB | [`queue/CROSS-REPO-HANDOFFS.md`](queue/CROSS-REPO-HANDOFFS.md) |
| Documented fast-follows (NOT in these PRs) | 9.0 KB | [`queue/FAST-FOLLOWS.md`](queue/FAST-FOLLOWS.md) |

⚠ **Read `ORDERING-NOTES.md` before claiming.** Several rows have a mandatory claim order (`AUD-2` before `AUD-4`, `AUD-6` before `AUD-7`) that this index does not restate.

---

## How other documents cite this one

The restructure moved content that other documents cite. Those citations were re-pointed in the same PR, **at the file rather than at a line number** — a line anchor cannot survive the next edit, a dossier path can. `design/plans/*`, `design/decisions/*`, `docs/design-handoffs/*`, `docs/superpowers/plans/*`, `ROADMAP.md`, `FUTURE-WORK.md`, `INTEGRATIONS.md` and `HANDOFF-GA-PUNCH-LIST.md` now link to `queue/<ID>.md`, `queue/ORDERING-NOTES.md`, `queue/CROSS-REPO-HANDOFFS.md`, `queue/FAST-FOLLOWS.md` or `BUILDER_QUEUE_ARCHIVE.md` as appropriate.

⚠ **Cite a row as `queue/<ID>.md`, not as `BUILDER_QUEUE.md:<line>`.** The old form is what the restructure had to go back and repair in 21 files.

Two citations were deliberately left alone, both recorded rather than guessed:

- **`design/plans/AUD-2-one-key-per-source.md:3`** — its sentence is *about* line numbers (*"`:129` on `main`, `:131` in this working tree"*), so re-pointing it needs a rewrite, not a path swap. It was **already wrong before this change** — `:129` was `AUD-7`'s row.
- **`src/Radio.Web/Services/ApiClients/GvResult.cs:41`** — an XML doc comment citing `§ Dependency / ordering notes`. Correcting it means touching `src/`, which this documentation change deliberately does not.

**Directional words inside row prose** (*above*, *below*, *this file*) were written when every row shared one document and were left verbatim; some now point across files.

