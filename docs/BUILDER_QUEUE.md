# Builder Queue

> Work items queued by **Planner** for **Builder** to clear one PR per cycle.
> Planner appends rows + spec/plan links; Builder claims a 📋 row whose dependencies are all met, ships it as a PR, then marks it ✅.
>
> **Last updated:** 2026-09-02 (Builder) — **`ENC-12` shipped ✅ in [#535](https://github.com/mmackelprang/RTest/pull/535). `ENC-11`'s safety response finally has a voice:** an amber/red fault badge on the Settings nav pill that is correct on every route, and one Radzen notification per browser session on escalation only. A healthy boot stays completely silent. ⚠ **The defect worth not rediscovering: the badge seeded only from change-only SignalR broadcasts, and that made it dead exactly when it mattered.** On a deploy `radio-api` and `radio-web` restart together, so the API's boot config push broadcasts the tier while the web hub is still in its connect-retry loop — the broadcast lands on nobody, the cache stays null, and the badge never appears for the life of the process. Pre-merge review caught it; the fix pulls `GET /api/integrations/encoder/provisioning` (`ENC-8`'s endpoint, no new one) and treats broadcasts as the update channel, not the source. **Verified live: the tier changed before the browser existed and the badge + toast still appeared.** ⚠ **`AudioStateStore` had never been constructed in its life** — zero consumers in `src/Radio.Web`, so its hub cache had never once run. Anything else that plans to "read the cached state" from it needs a consumer first. ⚠ **How to induce the fault tiers on hardware, so nobody re-derives it:** set `RotaryEncoderConfigDefaults` TUNING `max: 99` → `max: 0`; the firmware validates `min >= max` and rejects the **entire** config atomically, so the device keeps its flashed (correct) config and the only mismatch is that one non-safety field → **`Degraded`**, with the knobs still behaving correctly. Then `PUT /api/integrations/encoder/reverse/{i}` → the rejected push also disagrees on a safety field → **`HardFault`**, live, with no redeploy and no lost browser circuit. `sudo sh -c 'echo 3-2.3:1.0 > /sys/bus/usb/drivers/usbhid/unbind'` (and `bind`) gives a genuine mid-session disconnect. ⚠ **A `Degraded` console has its volume clamped exactly as hard as a hard-faulted one** — `VolumeClampFor` returns the tight 2-unit clamp for every tier except `Configured`, while handoff §7.6's table shows it only on the Hard-fault row. The toast copy is spec-verbatim and was **not** changed; the spec defect is logged for Planner in `design/FUTURE-WORK.md`.
>
> **Prior entry — 2026-09-02 (Builder):** **`PHN-1b` shipped ✅ as [#534](https://github.com/mmackelprang/RTest/pull/534). ADR-029 PR 2 is in: `GvMediaClient`, the bounded LRU cache, the API-side `X-RotaryPhone-Auth` handler, and Radio.API's `GvMedia` config block.** Still no endpoint and nothing that plays a sound — PR 3 is the first PR a user can reach. ⚠ **Pre-merge review found 3 HIGH and 5 MEDIUM, and the two worst were in the plan's own literal code.** (1) `File.WriteAllBytesAsync` wrote straight to the final cache path, so a cancelled fetch left a **0-byte file that `TryGetPath` served forever** — and because every hit touches `LastWriteTimeUtc`, the poisoned entry became most-recently-used and was the **last** thing eviction removed. Self-preserving, and PR 3 will pass `HttpContext.RequestAborted`, so a kiosk reload was enough to trigger it. Now staged to `.tmp` + `File.Move(overwrite: true)` under the write lock. (2) The client's `try` covered only `GetAsync`, so **body-phase failures escaped the taxonomy entirely** — a mid-body reset threw raw `IOException`, and `HttpClient.Timeout` elapsing during the body (the *common* case under `ResponseHeadersRead`) threw a bare `TaskCanceledException`; neither carried a `Reason`, bypassing C-18 for the likeliest failure and making the class summary's *"failures throw … carrying a reason"* false. ⚠ **C-14 was half wrong and the box settled it.** The plan says the deploy copies **one** per-machine overlay to both services, so the auth key is *"two keys in one file"*. Measured on `radio`: `api/appsettings.Production.json` is **1057 B, mtime 2026-03-05**, `web/`'s is **75 B, mtime 2026-07-31** — different files, long diverged, and the `test -f` seed guard means neither is re-seeded. So it is **two keys in two files**, setting it is **two** hand edits plus two restarts, and `GvMediaStartupCheck`'s *"both services load the same file"* justification was corrected before merge. What C-14 got right: `src/Radio.API/appsettings.json` **is** replaced wholesale every deploy (rsync excludes only the Production overlay — confirmed live: the deployed file's mtime was that day's deploy, the overlay's six months older), so the non-secret `GvMedia` defaults do reach the appliance. ⚠ **The boot check cannot detect the failure it was written for** — it fires only on an *empty* `AuthKey`; two differing non-empty keys, which is what a mismatch means and the only state producing the 401, warn about nothing. Now stated in the code and pinned by a test rather than left as prose. 🎁 **First container guard in the repo with eager validation** (`ValidateOnBuild` + `ValidateScopes`) — but note honestly that `AddHttpClient<T>` registers the typed client through a **factory**, so `ValidateOnBuild` cannot introspect its constructor; the real constructor exercise is the `GetRequiredService` call beside it. ⚠ **"ADR-029 §17" does not exist** — the ADR's last numbered section is **§15**. The prior entry below cites "§4.2/§17" for the truncation instruction; §4.2 is real, §17 is not, and `MaxSpeechChars` ships as **rejection** deliberately (`PHN-3` owns visible client-side truncation). 📝 **Filed `PHN-5`:** `PhoneContactLookupService` logs raw phone numbers on **four** lines — including `:62` at **Information**, with the contact's real name, into the file sink that persists since `LOG-11` — and the one line it calls masked still logs `contact.Name` in full. ADR-029 §5.1 cites that file as the masking *example*, which is how it propagates. Latent today (its only caller sits behind `PhoneIntegration:Enabled=false`) but one config flip from live. ⚠ **No UAT was run and none was owed** — this PR has no route, no UI and no sound. What *was* verified live: `radio-api` boots with the new registration and `GvMediaStartupCheck` emits its Warning. **Carried to PR 3:** the real gvbridge route's status codes and whether it sends `Content-Length` — the size bound takes a different branch either way, and only the live service settles it. **Still PR 6's:** `PHN-1a`'s three device-only checks.
>
> **Prior entry — 2026-09-02 (Builder):** **`PHN-1a` shipped ✅ as [#528](https://github.com/mmackelprang/RTest/pull/528). The head of the seven-PR phone arc is in, and the ADR claim that scoped it was false.** ADR-029 §8.3 calls D4 *"a lift rather than an invention"* because `FilePlayerAudioSource` *"already implements seeking through `SoundFlowPlaybackService`"*. **Both halves are false**, re-verified against the tree: `SoundFlowPlaybackService` had **no seek method of any kind** across its complete 20-member public surface, and `FilePlayerAudioSource.SeekCoreAsync` assigns `_position = position` — the same field `Position` reads back — under an `IsSeekable => true`. So PR 1 **built** the primitive instead of lifting one; `GetPosition` had **zero callers repo-wide**, confirmed independently, so the fix carried no blast radius. `FilePlayerAudioSource` is **not** fixed — logged to `design/FUTURE-WORK.md` §14a with its own UAT requirement, and it is a candidate row of its own. ⚠ **Pre-merge review found 2 HIGH and 4 MEDIUM, and 7 of the 14 findings were defects in the PLAN, not the implementation.** Both HIGHs were in the plan's own literal code for `AudioFileEventSource`'s completion wait: a `CancellationTokenSource` disposed outside the lock that a waiter was about to read `.Token` from (`ObjectDisposedException` → `Error` state), and `SignalTransportChange()` firing *before* the base class assigns `State`, so the re-armed wait read the pre-transport state — which would have fired `EndOfContent` **while paused**, the exact defect the mechanism was written to prevent. A MEDIUM found the plan's `ValidateMediaId` deny-list defeated by a **scheme-bearing** id (`http:evil.example`): under RFC 3986 a relative reference carrying a scheme resolves as **absolute**, so `new Uri(base, id)` in PR 2 would have escaped the base — an SSRF hole inside the validator written to prevent one. All fixed; an allow-list backstop was added **after** the named checks so every pinned rejection reason is unchanged. **All four `DuckingServiceCharacterizationTests` passed as written, so PR 4's premise is intact** — ducking is still binary and transition-only, and priority still changes no duck level. **Update those tests in PR 4, never delete them.** ⚠ **No browser UAT was run and none was owed** — PR 1 ships no user-visible surface. Three device-only checks (that `Seek` repositions a real MP3, that `Time` advances, that a paused TTS source no longer reports completion) are carried to PR 6; all three degrade benignly. **PRs 2-7 remain unplanned and need a Planner pass each.** Two obligations recorded on them: PR 2 must **mask on every line** (ADR §5.1's cited example masks on one and leaks on two) and must add the first build-and-resolve DI guard in this arc; PR 3 must mint its **own** `playbackId` (`IAudioSource.Id` and `AudioFileEventSource._playbackId` are not equal, while `TTSEventSource` uses `Id` directly) and should settle whether `IEventAudioSource.SeekAsync` widens to `Task<bool>` so a player-refused seek is visible at the seam. Also for Planner: `MaxSpeechChars` ships as a **rejection**, while ADR §4.2/§17 specify **truncation** — the plan governed, but PR 3 should make that call explicitly.
>
> **Prior entry — 2026-09-02 (Builder):** **`ENC-8` shipped ✅ in [#527](https://github.com/mmackelprang/RTest/pull/527).** The encoder Settings surface is live: five cards under System Config → Integrations → Rotary Encoders, the mapping table now served from the router's own dispatch array, and nine false statements corrected in `design/INTEGRATIONS.md`. **`ENC-12` is unblocked and is the next row** — its toast copy ends *"Open encoder settings"*, and that destination now exists. ⚠ **Three defects worth not rediscovering.** **(1) Task 4's hardware gate caught a HIGH regression and is the reason it exists**: unifying the boot and maintenance read-back paths deadlocked the boot push by timeout — the read loop is now the only reader, so awaiting the push before the loop starts means nothing can complete it. It does not hang, it settles in `Degraded`, which tightens the volume clamp to 2 units per event on **every boot**. **(2) The page could never deserialize its own API response** — `Radio.API` serializes enums as strings and the Web DTO enums had no converter, so every card sat on a spinner. **No automated gate can catch this class**: the bUnit rig fails every HTTP call by design, so a null result is the expected state and a green suite is indistinguishable from a dead page. `ApiModels.cs` already documented the identical bug on `ConfidenceBucket`. **(3) Radzen renders only the selected tab's body**, so every markup-absence assertion about a non-default tab passes vacuously unless the test proves the panel rendered first. ⚠ **Two plan claims were false and are corrected in the PR**: `Radio.Web` *can* reference `Radio.Core` statics (the cabinet names are no longer duplicated), and the DTO enums do *not* cross the wire as ints. ⚠ **`Reset counters` copy deviates from the plan deliberately** — it said counters were *"zeroed on the device"*, which the protocol cannot confirm, two lines under a comment saying exactly that.
>
> **Prior entry — 2026-09-02 (Planner):** **`PHN-1a` queued 📋: ADR-029 PR 1, the head of the seven-PR phone arc.** Plan at [`design/plans/PHN-1a-event-playback-seam-contracts.md`](../design/plans/PHN-1a-event-playback-seam-contracts.md), 14 tasks, **no dependencies** — claimable now. ⚠ **Planning falsified one of ADR-029's own load-bearing claims, and it changes the work.** §8.3 calls D4 "a lift rather than an invention" because `FilePlayerAudioSource` "already implements seeking over a local file through `SoundFlowPlaybackService`". **`SoundFlowPlaybackService` has no seek method of any kind**, and `FilePlayerAudioSource.SeekCoreAsync` range-checks its argument and then assigns `_position = position` — the same field `Position` reads back — under an `IsSeekable => true`. A seek on the file player reports success, moves the readout, and changes nothing audible. **That is a fourth instance of the `CLAUDE.md` § Pre-Merge Review comment-accuracy class, and `SoundFlowPlaybackService.GetPosition`'s `// Position tracking not available in current SoundFlow API` is a fifth** — `ISoundPlayer.Time` and `bool Seek(TimeSpan, SeekOrigin)` both exist in the restored SoundFlow 1.4.1. PR 1 therefore **builds** the primitive rather than lifting one; the estimate holds. **`FilePlayerAudioSource` is deliberately not fixed in this PR** — it is a live primary-source path whose persisted resume position hangs off the same field, so it needs its own UAT; it is logged to `design/FUTURE-WORK.md` by the plan's Task 13 and is a candidate row of its own. ⚠ **Read the plan's §5 before claiming PR 4 later:** every ADR claim about the ducking subsystem is independently confirmed (`GetActiveEventsByPriority` and `StopAllDuckingAsync` have zero non-test callers; `GetPriority` is read only inside the dead one; `DuckingStateChanged` fires only on the transition), so PR 4's premise is sound — but it is the first load-bearing use of dead code, and `PHN-1a` ships characterization tests specifically so PR 4's change to it is **visible in PR 4's diff**. Those tests are to be **updated, never deleted**.
>
> **Prior entry — 2026-09-02 (Builder):** **`ENC-4` shipped ✅.** ⚠ **Read this before the next row: the implementation reached `main` WITHOUT a pull request and was therefore never reviewed pre-merge.** Four commits (`507b0d3`, `eb4005e`, `bd762d1`, `29acc01`) were committed directly to `main` and pushed, because a subagent switched the shared working tree off the feature branch mid-cycle and the branch was not re-verified before committing. **It happened a second time in the same cycle**, on the fix branch, which is why this is recorded as a process defect rather than a slip: **any Builder sharing a working tree with subagents must re-check `git branch --show-current` immediately before every commit and every push.** The owed review was run after the fact and found **three HIGH defects, all of them live on `main` and on the appliance** — the volume knob published the mute state from *before* the press toggled it (so every tap asserted the opposite of the truth for 1500 ms, on the one knob with a safety hazard behind it); `EncoderHudService.Dismiss` claimed an ENC-0 disconnect caller it did not have, and a device vanishing mid-hold left the card up **indefinitely** because a hold suspends the dismissal timer; and `StateChanged` was raised unguarded from a thread-pool timer callback, where one throwing subscriber ends the web process and every circuit in it. All three are fixed in #519 with a regression test each. 🔵 **One deviation is left for the owner and is NOT decided:** under `prefers-reduced-motion` the progress ring keeps sweeping rather than becoming handoff §6.5's "filling bar" — defensible, since freezing it hides that a 600 ms hold is arming standby, but undeclared in the plan's §0.3 table. ⛔ **The router index→handler remap was deliberately NOT done** and still belongs to `ENC-5`/`ENC-7`; a test pins the current mapping so the change has to be made on purpose.
>
> **Prior entry — 2026-09-02 (Planner):** **`ENC-8` and `ENC-12` queued 📋 as a sequenced pair**, planned together in one pass because they were about to build the same card twice. **`ENC-8` owns the entire encoder Settings page** — all four cards, the field-level `Sent` vs `Read back` table, and every action button; **`ENC-12` adds no markup to that page**, only the cross-route topbar badge and the once-per-session notification, and it links to `ENC-8`'s page. **Claim `ENC-8` first**: `ENC-12`'s toast copy ends in *“Open encoder settings”*, and shipping the sign before the destination teaches the owner the link is useless. **Five findings Builder must not rediscover.** **(1)** **A reversed knob will become a permanent hard fault with a permanently tightened volume clamp** unless the `reverse` override reaches the *verifier* and not only the wire — `reverse` is a safety field, `Classify` turns a safety mismatch into `HardFault` immediately, and `VolumeClampFor` then drops volume movement from 6 units per event to 2. `ENC-8` §2.3, with a test. **(2)** **`ENC-8` Task 4 is a hardware gate, not a formality** — four later tasks assume a `stream.Write` from a request thread reaches the device while the read loop is parked in an infinite `ReadAsync`. If it fails, **stop and report**; do not switch to a finite `ReadTimeout`, which the shipped code already rejected in writing because HidSharp may surface the expiry as `IOException` and tear the connection down once per timeout forever. **(3)** **The `SystemConfigPage` mapping table is no longer wrong** — quick win #6 ([#489](https://github.com/mmackelprang/RTest/pull/489)) corrected the text, so do not “fix” it; what survives is the second source of truth, and that fix is structural. **(4)** **There is no MudBlazor in this repository** despite `CLAUDE.md` saying so — `Radio.Web` references `Radzen.Blazor` and nothing else; `ENC-8` Task 20 corrects `CLAUDE.md`. **(5)** **`MainLayout` cannot be rendered in bUnit** — its test file is a documented stub, and nothing in `tests/` asserts `.topbar-mute-chip` or `.phone-nav-fault` in markup. `ENC-12` therefore puts every decision in a pure `EncoderFaultRules` class the way `BellHealthRules` already does. **Eleven contradictions between the punch list, the handoff and the shipped code are resolved in the two plans' §0.4 sections; five of them are statements that are false today.**
>
>
> **Prior entry — 2026-09-02 (Planner):** **`ENC-5` and `ENC-7` queued 📋 as one planning pass producing two sequenced PRs.** The punch list is explicit that these are *"one component with two lists … build them together or back to back; building them apart is how they drift"*, so `ENC-5` builds the shared selector component and `ENC-7` consumes it unchanged. Plans: [`ENC-5-source-overlay.md`](../design/plans/ENC-5-source-overlay.md) (18 tasks) and [`ENC-7-presets-knob.md`](../design/plans/ENC-7-presets-knob.md) (11 tasks). **`ENC-4`'s implementation landed on `main` at `29acc01` while these plans were being written**, so `ENC-5` is claimable as soon as that row is marked ✅; `ENC-7` is hard-blocked on `ENC-5`. `ENC-5` edits four `ENC-4` files, so **Builder should re-read them as merged before Task 1** — where the plan and the shipped code differ, the code wins and the PR should say where. ⭐ **The router remap belongs to these two rows and is sequenced so the tree is never in a state where a knob points at a handler that is not there:** `ENC-5` takes indices 1 and 3 and parks the visualiser on index 2 as a seat-warmer (leaving the old source cycler there would give two adjacent knobs two divergent copies of the source selection); `ENC-7` takes index 2. End state `0 = VOLUME · 1 = SOURCE · 2 = PRESETS · 3 = TUNING`, closing the deliberate mismatch recorded in `HANDOFF-NEXT-SESSION.md`. ⚠ **`ENC-4`'s mapping test goes red in `ENC-5`, and `ENC-5`'s goes red in `ENC-7` — both by design.** Each plan says which assertion moves and forbids chasing the red by reverting the remap. **Six findings from planning that Builder must not rediscover.** (1) ⚠ **There are no 7 slots in the preset bank** — the cap is **50**, `RadioPreset` has no `SlotNumber`, the ordinal is re-derived per request, and there is no next-free-slot logic because gaps cannot exist. The design survives intact; `ENC-7` §0.3 has the mapping. (2) **No tuner reports which bands it supports** — `IRadioControl` has no capability member and `GetAvailableBands()` is device-agnostic, so "composition resolves once per tuner" is unimplementable as written; `ENC-5` adds `SupportedBands` with a default interface implementation so the existing test double keeps compiling. (3) **Per-band last-tuned frequency does not exist** — `RadioPreferences` holds one flat `LastFrequency`, and today a band switch clamps the *previous* band's frequency into the new band, landing FM→AM at 1710 kHz every time. It gets **its own configuration section**, because `RadioPreferences` and `RadioOptions` both declare `SectionName = "Radio"` and that section already has two writers with different field sets. (4) **Recall cannot use `POST /api/radio/presets/{id}/load`** — it 400s when radio is not the active source, which is exactly the case the PRESETS knob exists to serve. (5) **`RadioControlPanel` already subscribes to `RadioStateChanged`**, so the "knob and band pills are one state" requirement is met today and the task is a regression guard, not a rebuild. (6) ⚠ **`ENC-9a` did not remove `VisualizerPanel`'s local `_currentMode`** — the punch list cites it as the template for exactly that and it added the missing *subscription* instead; the real template is "one authoritative owner + typed broadcast + a component that re-syncs without echoing". ⭐ **`ENC-7` carries a declared, deliberate deviation forward:** the on-screen bank becomes `PRESETS · n saved`, recorded in Rev 3's header, Designer §4.4 and punch-list §6 so a later consistency pass does not revert it — **Builder puts it in the PR body; Polisher does not flag it.** **One addition to the spec, surfaced rather than silently resolved:** holding PRESETS on a station already saved currently throws a duplicate error the handoff never covers, so `ENC-7` adds `ALREADY SAVED · slot NN`.
> **Prior entry — 2026-09-02 (Planner):** **`ENC-4` queued 📋** with a plan at [`design/plans/ENC-4-encoder-hud.md`](../design/plans/ENC-4-encoder-hud.md). **Both dependencies are met** (`ENC-1` and `ENC-3` shipped 2026-09-01/02), so it is claimable now and is the next row in the punch list's §2 order. **Three things came out of planning that Builder must not rediscover.** (1) **The existing `VolumeChanged` broadcast cannot carry this.** Its only call site is a 500 ms change-detecting poller (`AudioStateUpdateService.CheckVolumeAsync:453-476`) — 2 Hz against a 100 ms requirement — and it says *what the volume now is*, not *which knob moved*, which is the entire trick. The row adds a dedicated push channel with its own ≥50 ms coalescer; that does **not** contradict the "do not add a second throttle" note in `HANDOFF-NEXT-SESSION.md`, which is about `VolumeChanged` and stays true. (2) **The Sleep-variant HUD is observable today, but only on the idle path.** `idle-dimmer.js:73-81` navigates to `/sleep` *without* calling `SetSleepAsync(true)`, so `IsSleeping` is false there and a knob turn acts and renders in place; reached via the Sleep pill it is true, the input is consumed by the wake, and the browser leaves the route — so UAT via the pill would produce a false failure. (3) **The handoff's “reuse `RadioControlPanel.LongPressThresholdMs`” is not literally possible** — it is a private const in a Razor component in `Radio.Web`, while the synthesis has to run in `Radio.API`. The plan promotes the value to `Radio.Core` and repoints the component at it. ⛔ **The row does NOT remap `RotaryEncoderActionRouter`'s index→handler table** — that stays with `ENC-5`/`ENC-7`, and the plan adds a test pinning the current mapping so a later change is deliberate.
>
> **Prior entry — 2026-08-18 (Builder):** **`KIOSK-2` shipped ✅ as [#480](https://github.com/mmackelprang/RTest/pull/480). The kiosk tranche is clear.** Verified on the live box across four `setup-kiosk.sh` runs, reading the installed copies under `/usr/local/bin` rather than the checkout: the healthy path is **silent** (`TIER=silent`, no dialog), a stopped `radio-web` was repaired by one tap with the kiosk up at **4 established connections** and the dialog reading *"The console wasn't running — it's started now."*, a hard failure **held the kiosk back** (dialog up, kiosk `inactive`, **0 processes, 0 connections**) with `Try again` producing a genuine second run and `Open anyway` then launching it, and the shutdown confirm sat untouched for **90 seconds** — GNOME's own powers off at 60 — leaving `uptime -s` unchanged. **The Google Voice bridge stayed at 13 processes with CDP `:9224` answering at every step**, including across `Exit to Desktop`, which took the kiosk 10 → 0. **⚠ Two things this row found that the plan had wrong, both worth carrying forward.** (1) **The accidental-shutdown mitigation was not actually being enforced.** Clearing the persisted DING icon positions works — and DING rewrites them moments later, so the one-shot unset races a writer that always wins. `keep-arranged true` is the setting that makes it stick, and it is now set alongside `arrangeorder 'NAME'` (which was **already** `'NAME'` and had never applied, because `keep-arranged` was `false`). **The live layout before this row genuinely had `Shutdown System` and `Exit Browser` adjacent** with `Radio Console` at the far end — the exact arrangement handoff §2.1 exists to prevent, so this was not a hypothetical risk. The durable framing, since the sort *direction* proved unstable across installs: `Radio Console` is the **middle of three**, and the middle of a three-element sort is the same element whichever way the sort runs — so any future rename must keep it sorting between the other two, not merely preserve `E… < R… < S…`. (2) **`--icon-name` being dropped from zenity 4 cost nothing.** `PROBES.md` framed it as a forced choice between the right glyph and two live buttons; zenity 4 accepts **`--extra-button`** on the stock types, so `--error --ok-label="Try again" --extra-button="Open anyway"` renders the red glyph **and** both buttons in the mockup's own order. **Handoff §6.1 shipped exactly as designed** — no glyph traded, no button dropped, no design decision needed. (`--icon` still parses and is **silently ignored**: the image node is byte-identical with a valid name, a bogus one, and none at all.) **⚠ One of the two "owner's eyes" checks was retired by finding a better instrument, and it generalises well beyond this row: AT-SPI works on this box.** `python3-gi` + `Atspi` expose every dialog's widget tree, **rendered label text, screen extents in pixels, and a working `click` action** — on Wayland, with no screenshot tool. That is how the 56 px buttons became a measurement (**58 px as shipped**; GTK 4's default is 44 px, the plan's `min-height` alone overshoots to 76 px, and `GTK_THEME=Yaru-dark` adds the last 2 px) and how `Try again` / `Open anyway` / `Cancel` were actually pressed. `CLAUDE.md` currently records this box as having **no** remote UI driving at all; that is now too strong. **Only icon legibility at the panel still needs the owner.** **⚠ The 24 px inter-button gap is NOT shipped and could not be** — GTK 4's message-dialog action area keeps a 1 px hairline under three different selectors. Its intent is met by a wider margin than the spec asked for (~444 px buttons, aim point ~222 px from the seam against ~100 px in the spec's model), and it is flagged for the owner rather than quietly worked around. **⚠ `T15` could not be observed live and is not claimed as if it were:** across 17 samples spanning two full refresh cycles the maximum `psidtsAgeSeconds` was **477**, i.e. the bridge is currently refreshing on a **~10-minute** cadence and never entered the 660–1200 trough at all. The trough is verified by **direct threshold testing** instead — 17 synthetic bodies including the exact body captured during a confirmed outage (`psidtsAgeSeconds: 707`, all three liar fields healthy) classifying as `online`/silent. **Do not tighten the `>1200` test to match today's ~500 reset**; a shorter cadence makes `>1200` more conservative, and matching the observation would misreport a dead session the moment the 20-minute cadence returns. **Five pre-merge findings were fixed and two of them made a tool lie:** `--print-status` reported `TIER=silent` while a service was down (`compute_tier` had no `down` arm) while `--dry-run` said `WOULD-START` about the same run; the RotaryPhone-owned ensure script was the one **unbounded** call, so a hang would have wedged the launcher with a `--no-cancel` dialog up and the `flock` held, killing the icon until a reboot (now `timeout 20`); and `· started just now` was being claimed for units that were already running but slower than the 2 s probe. **⚠ Also closed, and it explains a latent trap: `.gitignore`'s `[Bb]in/` was swallowing `deploy/debian-x64/kiosk/bin/`** — `KIOSK-1`'s scripts are in the repo only because they were force-added, and the next helper would have shipped to the box but never to the repo. **Merging alone changes nothing — `setup-kiosk.sh` must be re-run on the box; that has already been done on `radio`.** **Gates:** build 0 errors, `bash -n` clean on all five scripts, and 5 test failures that **reproduce identically on `main`** (4 × `SrcVariableResamplerTests`, a native `libsamplerate` P/Invoke absent on Windows; 1 × a real external NWS API call) — this branch changes no C# at all, which is the `TEST-1` false-signal problem in miniature.
>
> **Prior entry — 2026-08-18 (Builder):** **`KIOSK-1` shipped ✅ as [#479](https://github.com/mmackelprang/RTest/pull/479).** Verified on the live box, not just by gates: `radio-kiosk-exit` left the Google Voice bridge at **13 → 13 procs** with CDP `:9224` still alive; `radio-kiosk-launch` brought up the transient `radio-kiosk.service` with **2 established connections to `:5002`**; and kiosk CDP `:9223` went **`NOT_REACHABLE` → returns version JSON**, which is the claim `CLAUDE.md` now makes. A full `-Runtime linux-x64` deploy printed the new green line `Kiosk is live (14 established connections to :5002, radio-kiosk.service=active)`. **Two HIGH review findings were fixed before merge and both were GV-bridge safety:** the `Exit Browser` icon still ran `pkill -f chrome` (which kills the bridge), and `radio-console.desktop` carried a second, divergent kiosk command line pointing `--password-store=basic` at the **default** profile. **`KIOSK-2` is left 🔒 for Planner to reconcile** — its blocker (the dedicated `--user-data-dir`) is now merged, so it is ready to unblock. **One-time cutover, already applied to `radio`:** a kiosk started the old way on the default profile is deliberately *not* matched by `radio-kiosk-exit` and must be stopped once by hand — never with `pkill -f chrome`. **Prior entry — `KIOSK-1` claimed 🚧 on `fix/kiosk-launch-contract`, and moved above `TEST-1` with the owner's approval.** The promotion is not a re-ranking of the 2026-08-10 tranche: `KIOSK-1` fires on **every deploy** and leaves a dead kiosk screen each time, so it is a tax on every other row's verification — you cannot confirm anything at the panel while the panel is dark. `TEST-1` keeps its place at the head of that tranche. **`KIOSK-2` is left exactly where it was** and stays 🔒 until `KIOSK-1` merges — its scoped exit and single-instance guard both match on the dedicated profile path `KIOSK-1` introduces. **Plan Task A0 is now resolved rather than a gate:** the owner read all four unknowns off the live box on 2026-08-18 (zenity **4.0.1 / GTK 4** → the 56 px CSS belongs in `gtk-4.0/`; `--timeout` / `--default-cancel` / `--width` / `--height` supported but **`--icon-name` is NOT** — zenity 4 dropped it, so per-tier iconography in `KIOSK-2` must come from the built-in `--info` / `--warning` / `--error` glyphs; `systemctl --user show-environment` **does** carry `WAYLAND_DISPLAY=wayland-0`, `XDG_RUNTIME_DIR`, `DBUS_SESSION_BUS_ADDRESS` and `DISPLAY=:0`, which is the inheritance the entire relaunch fix rests on; rotary unit is **`rotary-phone.service`**). Recorded in [`docs/uat/2026-08-18-kiosk-launcher/PROBES.md`](uat/2026-08-18-kiosk-launcher/PROBES.md) so `KIOSK-2` does not re-derive them.
>
> **Prior entry — 2026-08-18 (Planner):** **two rows out of one approved design handoff, and the split between them is the main judgement in this pass: `KIOSK-1` (the kiosk launch contract) and `KIOSK-2` (the three-icon repair-and-open desktop).** The handoff is [`HANDOFF-kiosk-desktop-launcher.md`](design-handoffs/HANDOFF-kiosk-desktop-launcher.md), now **`[APPROVED 2026-08-18]`** with all five §11 questions answered in its new §12 — **none of it is to be re-brainstormed.** **⚠ Why two rows and not one, since this arrived as one approved scope.** `KIOSK-1` is an **ops-correctness** row that stands entirely on its own: `Deploy-ToLinux.ps1:376` relaunches the kiosk with `DISPLAY=:0` on a **Wayland** box — a defect its own comment at `:369-374` names and declines to fix — **so every deploy leaves the screen dead**, which the owner hit live today. Its fix is objectively verifiable end-to-end with no UI at all, and it is also the **single riskiest change in the body of work** (a brand-new Chrome profile), which is exactly why it should not be entangled with ~600 lines of new dialog shell. `KIOSK-2` is the **UX** row. Different failure mode, different verification, different fix shape — the same test this file already applies when it keeps `AUD-6`/`AUD-7` apart. **⚠ THE ORDERING BETWEEN THEM IS LOAD-BEARING AND IS NOT A PREFERENCE: claim `KIOSK-1` FIRST.** Every piece of `KIOSK-2` — the scoped exit, the single-instance guard, the launcher — depends on the dedicated `--user-data-dir` existing. Landing `KIOSK-2` first would force matching the kiosk on the **absence** of a flag, which the design explicitly rejects as fragile, and would leave `Exit to Desktop` still able to kill the Google Voice bridge. `KIOSK-2` is therefore **blocked** until `KIOSK-1` merges. **The verified replacement, so nobody re-derives it:** `systemd-run --user --collect google-chrome --kiosk … --ozone-platform=wayland …` inherits the graphical session's environment because the transient unit is started by that session's own service manager. **Confirmed by 2 established connections to `:5002` afterwards — and that count is the meaningful liveness check, not process existence: during the 2026-08-02 outage Chrome was running and `radio-web` returned 200 for ~33 hours while the count was 0.** **⚠ One finding is worth more than either row and should not be read as a side effect: nothing has ever installed the repo's `.desktop` files to `~/Desktop`.** The in-tree `radio-console.desktop` has carried `--password-store=basic` since Aug 11; the live copy still does not. **Three separate instances of that same drift turned up in one day** — the flags, the missing icon, and the 775 mode bit — and they are one root cause, not three bugs. Closing that loop is a first-class goal of `KIOSK-1`. **⚠ A fourth instance of the comment-accuracy pattern, recorded here rather than as its own row.** `Deploy-ToLinux.ps1:146-154` explains, correctly today, why it clears `~/.cache/google-chrome/Default/Cache` and `~/.config/google-chrome/Singleton*`. A `--user-data-dir` profile keeps **both inside itself**, so `KIOSK-1` makes that comment describe paths the script no longer touches **and** silently clears the wrong cache while leaving the kiosk's own lock in place. Written onto the row as a required hunk. **⚠ Recorded so it is not "finished" by a well-meaning Builder: the in-app Blazor favicon (`App.razor:9`) is OUT OF SCOPE.** The owner initially approved it, then **reversed on evidence**: `--kiosk` hides the tab strip, address bar and title bar, so that favicon is **never visible on the appliance** — it would only ever show in a remote tab or a bookmark, which does not justify carrying the dark-surface and 16–32 px legibility risks inside a launcher cleanup. Deferred to its own pass (handoff §12, Q4). **The *desktop* icon is unaffected and fully in scope** — that is the one the owner actually taps and the one that fixes the generic-document-glyph complaint. **⚠ Cross-repo boundary, and it is narrow on purpose:** `gv-bridge-ensure.sh`, its watchdog timer and its nightly restart timer are **RotaryPhone-owned** and a Builder is bringing them under version control in `D:\prj\RotaryPhone` **right now**. `KIOSK-2` **invokes and probes only** — never owns, reimplements, edits, installs or `pkill`s bridge startup — and resolves the script from a candidate list at runtime because its canonical path may move under it. The new-consumer note for the boundary doc's Change Log is a commit in *that* repo, so per the 2026-08-10 precedent it is § Cross-repo handoffs **#8**, not a claimable row. **Also folded in and already decided:** `Exit Browser` → `Exit to Desktop` (Q1); a shutdown confirm that **never auto-proceeds**, replacing GNOME's 60-second countdown that powers the box off on inaction (Q2, overriding the earlier “leave as-is”); the **stateless `psidtsAgeSeconds > 1200`** dead-session test with **no Architect escalation** (Q3 — owner confirmed the field live at `310`); and the four reported components stand at four (Q5). **Placement: appended at the end of the Queue. `TEST-1` stays first — the owner set that — and no existing row moved.** But flagging rather than acting on it, per the standing rule that Planner appends and the owner ranks: **`KIOSK-1` fires on *every* deploy and costs a dead screen each time**, which is a stronger case for promotion than anything else currently below `TEST-1`. Say the word and it moves. **One task in the plan is a gate, not work: Task A0 probes the box and pins four unknowns** — zenity's GTK major version (which decides whether the 56 px CSS goes in `gtk-3.0/` or `gtk-4.0/`), `--timeout` / `--icon-name` / `--default-cancel` support, whether `WAYLAND_DISPLAY` is present in `systemctl --user show-environment` (which the whole relaunch fix rests on), and the `rotary-phone` unit name. **Several later choices branch on its output; it must run first and its answers get committed as `docs/uat/2026-08-18-kiosk-launcher/PROBES.md`.** _Nothing in the entry below is superseded — this pass adds rows and notes only._
>
> **Prior entry — 2026-08-11 (Planner):** **one defect from today's production deploy, queued as TWO rows rather than one: `AUD-6` (output device identity is an enumeration ordinal) and `AUD-7` (the reported active output diverges from where audio physically goes). The diagnosis is complete and neither row is to be re-brainstormed.** **The measurement, taken on the box across today's deploy restart:** on **Aug 10 16:17** MiniAudio enumerated `[0] AB13X USB Audio Analog Stereo` / `[1] Built-in Audio Analog Stereo (IsDefault=True)`; on **Aug 11 15:04** it enumerated `[0] Built-in Audio Analog Stereo (IsDefault=True)` / `[1] AB13X USB Audio Analog Stereo`. The persisted value in the config store is the string **`playback-1`** on both days. **On Aug 10 that meant the Soundbar; on Aug 11 it means USB Audio Out. Nothing in the store changed — the meaning did.** **Why two rows and not one, since this arrived as one report — the split is deliberate and is the main judgement in this pass.** `AUD-6` is a **persisted-format defect**: `SoundFlowDeviceManager.cs:517` mints `Id = $"playback-{i}"` straight off the enumeration index and that string is what reaches storage, while resolution back to hardware is **pure string parsing, not a lookup** (`SoundFlowAudioEngine.GetDeviceIndexById:1030-1043` is an `int.TryParse` and nothing else). Its fix is a stable key **plus a migration**, because every existing store holds ordinals. `AUD-7` is a **missing-call defect** with an entirely separate mechanism, and investigating it sharpened the owner's diagnosis in a way that matters: it is **not** that the two concepts "are tracked separately and not reconciled at startup" — it is that **the native device switch is reachable only from the interactive HTTP path.** `SoundFlowAudioEngine.SwitchPlaybackDevice:838` — the only code anywhere that stops, disposes and re-initializes `_playbackDevice` — has **exactly one caller**, `DevicesController.SetOutputDevice:250-254`. The startup path never calls it, and `SoundFlowDeviceManager.SetOutputDeviceAsync:213-244`, which the gate's own comment nominates as the thing that performs the swap, **does not touch the native device at all** — it validates the id against a cache, assigns a string field, and persists. **Consequence, and it is the reason `AUD-7` is not merely the second half of `AUD-6`: `AUD-7` is DETERMINISTIC, not intermittent.** It fires on **every** restart where the persisted preference is not the system default, with or without any reordering. Only `AUD-6` needs the enumeration to shuffle. Different mechanism, different trigger, different fix shape, different verification — which is the same test this file already applies when it keeps `GV-6`/`GV-8` apart. **⚠ THE ORDERING BETWEEN THEM IS LOAD-BEARING AND IS NOT A PREFERENCE: claim `AUD-6` FIRST.** `AUD-7`'s fix makes startup **act** on the persisted preference. Acting on an unstable key is strictly worse than ignoring it — shipping `AUD-7` alone against today's store would convert a **mis-report** into a **mis-route**, sending audio to USB because `playback-1` now resolves there. Full reasoning on the rows and in § Dependency / ordering notes. **⚠ RECORDED SO IT IS NOT REOPENED: this exact fragility was raised as a hypothesis during the 2026-08-10 debugging session and was CORRECTLY FALSIFIED as the cause of the soundbar silence.** The app had opened the right device; the real cause was the Cast startup mute, shipped in **#468**. **That falsification stands and must not be relitigated.** What is new is that the *fragility itself* has now been observed firing — a different claim from the one that was tested and rejected. **Thematically these are the siblings of `AUD-2` and `AUD-4`,** which turn on **source** key identity (`sdr-radio-<guid>` vs `Radio-<guid>`); this pair is **device** key identity. **Four rows now turn on an identifier that is not stable being used as if it were** — cross-referenced in both directions. **⚠ A FIFTH INSTANCE OF THE COMMENT-ACCURACY PATTERN, found while verifying these rows and recorded in § Dependency / ordering notes rather than as its own row.** `CLAUDE.md` § Pre-Merge Review currently enumerates three worked examples of a comment asserting something the code does not do; `AUD-7` supplies a fourth (`SoundFlowAudioEngine.cs:222-224` assigns a responsibility to a method that does not discharge it) and its pinning test supplies a fifth (`StartAsync_PersistedLocalDevice_RoutesThroughGate:81-83` says *"not just the device-manager swap"*, naming a swap that does not exist). **Placement: `AUD-6` then `AUD-7`, immediately after `AUD-1` and above `AUD-5`. `TEST-1` is untouched at the top — the owner set that — and no existing row moved.** Rationale on the ordering note below. **⚠ Evidence-handling note, because the owner is about to invalidate half of it on purpose: the live `/api/devices/output` capture on these rows is a snapshot as of `0b2ebcb`, not permanent state.** The owner is about to run hands-on UAT and will re-select "Soundbar" by hand, which writes `playback-0` and makes the UI truthful again. **Do not read a later truthful `/api/devices/output` as evidence that `AUD-7` is fixed** — re-check after a restart, which is the only condition under which it fires. **All citations in both rows were re-verified against `main` @ `0b2ebcb`** after PRs #463 and #464 merged; neither touched any file either row cites. _Nothing in the entry below is superseded — this pass adds rows and notes only._
>
> **Prior entry — 2026-08-11 (Planner):** **one new row: `AUD-5`, a genuine pre-existing defect surfaced by PR #473's review — plus a staleness pass, because #471, #472 and #473 have all merged since the entry below was written.** **`AUD-5`: a Cast connection that is no longer current can persist its volume as the system master volume.** `GoogleCastOutput.SyncInitialVolumeAsync` is a *read* of device status, yet its success path fires `CastVolumeChanged`, whose **sole subscriber** (`AudioStateUpdateService.OnCastVolumeChanged`) unconditionally sets **and persists** `AudioManager.MasterVolume` — and **nothing re-checks `_connectionGeneration` between the status response and the event fire.** `IsInitialSync` exists on the event and is **never branched on**; its only two uses in the codebase are log-message interpolation, so an initial status read is treated identically to the user turning the knob on the Chromecast. **Two things the fix must respect, both settled during the assessment and both written onto the row so they are not re-litigated: (1) widening `_lifecycleLock` will NOT close this** — the exposure is the *event fire*, not the field read, and widening it would hold a lock across a SharpCaster network call, i.e. re-create the multi-second output-picker hang `AUD-3` was designed to avoid; the correct shape is a **generation re-check immediately before the fire**, plus a deliberate decision on whether to branch on `IsInitialSync` at all. **(2) It PREDATES #468** — all three links landed together in `b420edc` (2026-02-11), six months earlier, verified via `git log -S "IsInitialSync"` and `git merge-base --is-ancestor`; **do not "fix" it by unpicking the Cast synchronization work.** **Also recorded on the row: what is NOT wrong.** The three unsynchronized sibling reads (`SyncInitialVolumeAsync` / `SetCastVolumeAsync` / `SetCastMuteAsync`) were assessed and found **safe** — `_client` is never nulled after assignment, each dereference sits in a `try`/`catch`, and the client-identity race self-neutralizes because `ConnectAsync` disconnects any old client; **PR #473 wrote that reasoning into `GoogleCastOutput.cs:32-104`.** The bug is *downstream* of those reads. **Placement: `AUD-5` sits immediately after `AUD-1` in the correctness band — `TEST-1` is untouched at the top, and no existing row moved.** Rationale on the ordering note below. **⚠ Three corrections to the entry beneath this one, all now stale: (a) PR #471 has MERGED (`61187bb`)** — it is no longer 🚧 in flight, and its predicted anchor churn **did not materialize**: every anchor in `AUD-1`, `AUD-2` and `AUD-4` re-verifies **byte-exact** on `0870410` (`FilePlayerAudioSource.cs:1988`/`:2112` survived #471's edits to that very file, and #471 touched **none** of the files `AUD-2` or `AUD-4` cite). **(b) PR #473 (`0870410`) shifted every anchor below `:32` in `GoogleCastOutput.cs` by exactly +66** — so `AUD-3`'s ~30 historical citations in that file are pre-#473 and must be re-sited before reuse; `AUD-5`'s are post-#473 and verified. **(c) `AUD-3`'s closing observation that the class comment "asserts an invariant the code does not hold" is now FIXED** — that is precisely what #473 corrected, and the corrected comment goes on to document `AUD-5`'s exposure at `:95-104`. **⚠ Also recorded, and it may matter more than it looks: `docs/BUILDER_PROMPT.md` has NEVER existed, and the "Handoff Protocol at the top of `docs/BUILDER_QUEUE.md`" that the Builder role definition points at is not there either — so every Builder dispatched at this project is being pointed at two documents that do not exist.** Deliberately **not** made a queue row; see § Dependency / ordering notes for the decision and its reasoning. **And for the record: PR #473's pre-merge checklist item landed in `CLAUDE.md` under a new `## Pre-Merge Review` section**, chosen because this repo has no `CONTRIBUTING.md` and no PR template, and per-plan checklists die with their plans. _Superseded by this entry: the "🚧 PR #471 is IN FLIGHT" framing throughout, and the expiry warning on the `8b1ce0a` anchor re-verification (it did not expire — it was re-checked and holds)._
>
> **Prior entry — 2026-08-11 (Planner):** **status-accuracy pass on the 2026-08-10 tranche. Not new scoping: `AUD-3` is SHIPPED, one ordering note said the exact opposite of the truth, two `AUD-1` anchors moved, and `AUD-4` gained a third layer nobody named.** The 2026-08-10 entry below was written while **PR #468 was open**. It has since been **extended and merged as `8b1ce0a`** — so that day produced **four merged PRs, not three** (#467 ✅, #468 ✅ `8b1ce0a`, #469 ✅ `c129e0d`, #470 ✅ `dea1e02` — the queue rows themselves), with **#471 🚧 still in flight** (`fix/serialize-native-device-enumeration`, native `pa_mainloop` enumeration + device-lifecycle gating; **5 commits ahead of `main`, 7 behind — verified unmerged, so no row is invented for it**; see § Dependency / ordering notes). **`AUD-3` is ✅ SHIPPED — read the code before repeating the old summary.** The owner directed the race be closed **before** #468 merged, and it was, in commits `47da897` + `1fded0c`. **Verified against the merged diff, not the merge report:** all three `_client` / `_connectedReceiver` reassignment sites the row cited are now inside a new `GoogleCastOutput._lifecycleLock` (`:39`); the NRE mechanism the row named is *structurally* gone because `ConnectAsync` resolves the receiver into a **local** and never touches the field until `TryPublishConnectionAsync` (`:599`) validates a connection generation; and the engine grew exactly the API change the row predicted — `BeginCastConnectAsync` (`:361`) / `TryCommitCastConnectAsync` (`:387`) over a `_castConnectEpoch` (`:65`) bumped inside `SetActiveOutputAsync`'s `_activeOutputLock`, so the commit re-checks **and** tears down under one acquisition. All six critical sections are await-free auto-property/field swaps that never re-enter the engine, so the stated lock order (`_fallbackLock → _activeOutputLock → _lifecycleLock`) has no cycle. **⚠ But three things the row asked for did NOT ship, and they are recorded on the row rather than quietly dropped:** (1) **the hardware UAT the row explicitly demanded never happened** — #468 added **zero** files under `docs/`, and verification is desk-only (a loopback socket + a substituted transport); this is the **GV-8 `M-1` situation again** and must not be recorded as proven; (2) **the service-level commit call is untested** — `AudioEngineInitializationServiceStartupTests` builds the service with `new Mock<IAudioEngine>()` (`:381`) while both epoch call sites gate on `_audioEngine is SoundFlowAudioEngine` (`:522`, `:569`), so `castEpoch` is `null` in **every** one of those tests and the commit branch is dead code there (the engine method itself has 4 direct tests — it is the *wiring* that is unpinned); (3) closing the test gap cost **two** new `internal` seams (`ConnectRaceHookForTests` `:54`, `ConnectTransportOverrideForTests` `:64`) — real design debt, and the **second** time this codebase has bought coverage with a seam, which is why it is now folded into `TEST-2` as a pattern rather than an incident. **⚠ `AUD-4` gained a genuinely new problem that no one anticipated, and it changes what that row has to do.** #468 also rewrote `AudioSourceBase.StopAsync` (`:100-124`) to stop gating teardown on `State`, and its new doc comment asserts that **`StopCoreAsync` is "the only code that detaches the component."** `AUD-4`'s row asserts the real detach is `SoundFlowPlaybackService.StopAsync` → `MasterMixer.RemoveComponent`. **Both cannot be the whole truth, and `AUD-4` names two layers where there are three.** Whoever claims it must reconcile that before unifying anything — unifying two layers while a third has just moved underneath them is how `03a6fea` went wrong in the first place. **⚠ Line-number citations re-verified against `main` @ `8b1ce0a`** (post-#468), exactly as the tranche was re-verified against `c129e0d` last time. **Two anchors were stale and are corrected: `FilePlayerAudioSource.cs:1968 → :1988` and `:2092 → :2112` (#468 moved that file +20 lines).** Everything else in the tranche re-verified **byte-exact and unchanged**, including the ones most at risk: `appsettings.json:91`, `AudioSourceBase.cs:28` (#468 touched the file but its hunk starts at `:97`), all nine `SoundFlowPlaybackService` anchors, all seven `AudioManager` anchors, all of `TEST-2`'s `BluetoothAudioSource` dispatch sites, `TEST-1`'s two, `OPS-2`'s seven `PackageReference` lines, and `OPS-3`'s `radio-web.service:4`/`:12-22`. **`TEST-1` stays first — the owner's ranking is untouched; only `AUD-3`'s status moved.** _Superseded by this entry: the 2026-08-10 banner's "#468 🔵 open" framing, its `AUD-3` paragraph, and § Dependency / ordering notes' "Do not read PR #468's merge as closing `AUD-3`" — which, after the extension, says the opposite of what the merged code does._
>
> **Prior entry — 2026-08-10 (Planner):** **eight rows queued out of one "no audio from the soundbar" report, which decomposed into five distinct bugs across three PRs. This is the deferred remainder — the scoping is already done and none of it needs re-brainstorming.** Three PRs came out of that day: **#467 ✅** (systemd restart limiter made functional + crash-forensics groundwork), **#469 ✅** (cross-source metadata contamination + BT state clobber), **#468** — recorded that day as *"🔵 open, awaiting an owner decision"*, **since extended and MERGED as `8b1ce0a`; see the 2026-08-11 entry above** — (Cast confirm-before-unmute + state-independent source teardown). **What is queued here is what those three deliberately did *not* fix.** **Ranked highest: `TEST-1` — the suite is producing false CI signals, and that already cost this session a wrong diagnosis.** Four tests failed under timing/load pressure and passed on retry. The bUnit one (`VisualizerPanelTests.cs:184-193`) races a real `await` and wants `WaitForAssertion` — **five files in that project already use the idiom** — but the three `Radio.API.Tests` timeouts are a **different** failure mode (30–46 s under full-suite load, 22/22 in ~5 s in isolation), and **underneath both sits the actual defect: unit tests can reach an ambient `localhost:5000`.** A unit test whose result depends on whether `radio-api` happens to be running on the self-hosted runner is the root problem. It is ranked first because a false CI signal **contaminates the verification of every other row**. **⚠ `AUD-1` carries a danger note that must not be skimmed: do NOT "fix" `UseShazamForAllSources` by setting it `false`.** The flag conflates two independent decisions — *always fingerprint BT* (**necessary**: this session proved AVRCP cannot supply album art on this box — BlueZ 5.72 ships **no BIP/cover-art implementation**, and 7 days of data show **0 AVRCP-sourced art vs 2,560 SongRec-sourced**) and *let SongRec overwrite AVRCP title/artist/album* (**not** necessary; it rewrites `Enter Sandman (Remastered)` → `Enter Sandman`). Setting it `false` makes the gate at `BluetoothAudioSource.cs:837` false and `BackgroundIdentificationService.cs:260` hard-return — **BT loses album art entirely.** The wanted behaviour already exists at `BluetoothAudioSource.cs:893-905`, on the branch the flag never takes. **`AUD-2` is investigation-first and may legitimately close as "no defect"** — an **unverified code-read inference** that SDR mints `sdr-radio-<guid>` (`SDRRadioAudioSource.cs:908`) while `AudioManager` addresses the same source as `Radio-<guid>` (`AudioSourceBase.cs:28`). If it holds, **TTS and event audio never duck the radio**, silently — none of the gain/duck setters check membership, and `SetGainOffset` still logs `"Applied gain offset …"` on a key that matched nothing. Ducking is a documented core pattern in `CLAUDE.md`, so if confirmed this is significant. **`AUD-4` is the same key mismatch seen from the teardown side** and carries the matching trap: **sweep `_activeComponents`, not `oldSource.Id`.** It also renames `SoundFlowMasterMixer` — a `List<IAudioSource>`, **not** the audio mixer — whose log line *"Removed audio source … from mixer"* is precisely why commit `03a6fea` was applied **one layer too high and silently did nothing for months**. **`OPS-3` (`BindsTo=`) is the one row here that must NOT auto-merge on green** — it changes production service coupling; its reasoning is already written into `deploy/common/radio-web.service:12-22` by #467 and needs no re-deriving. **`OPS-2` is hygiene, not a bug fix** — a floating-version theory was investigated today and **DISPROVED** as the cause of a CI failure; pin to what currently resolves, **upgrade nothing**. **`AUD-3` is 🔒 on PR #468 rather than on a row** — a deviation from the status legend, recorded rather than forced: #468's post-`StartAsync` teardown guard closes the **observable outcome** of the Cast connect/teardown race, **not the race**, and the proper fix changes the engine's output API and wants hardware UAT. _**⚠ SUPERSEDED 2026-08-11** — true of #468 **as it stood on 2026-08-10**. The owner then directed the race be closed before merge, and it was (`47da897` + `1fded0c`): the merged `8b1ce0a` closes the race itself, and **`AUD-3` is now ✅ shipped**. The "wants hardware UAT" half of this sentence is the part that survives — that UAT still has not happened. See the 2026-08-11 entry and the `AUD-3` row._ **`TEST-2` records a coverage limit so it is not mistaken for coverage** — #469's deferred-capture test reaches the acquisition through the **internal seam**, never the `capture is …` branch dispatch, because constructing either type needs a native SoundFlow `AudioEngine`; it may legitimately close as "still infeasible." **One approved item deliberately did NOT become a queue row: the uncommitted cross-repo boundary-doc edit** → § Cross-repo handoffs **#7**, because the fix is a commit in `D:\prj\RotaryPhone`, not work claimable here. **Worth knowing: the 2026-07-16 entry that established the "commit it there separately" precedent is ITSELF still uncommitted** — so this is a recurring miss of the protocol that entry documents, not a one-off. **⚠ Every line-number citation in these eight rows was re-verified against `main` @ `c129e0d`** (post-#467/#469) rather than copied from the session notes: #469 moved `BluetoothAudioSource.cs` by ~28 lines, so **the session's own numbers are pre-merge and must not be trusted** for that file.
>
> **Prior entry — 2026-07-31 (Planner):** **GV-5's plan reconciled against post-GV-8 `main`. Refresh pass, not a re-plan — and it surfaced one real gap plus one decision the owner should bless before Builder claims the row.** GV-5's plan was written 2026-07-30, before PR #461 merged; every ADR-028 decision **still holds and was re-sited, not revisited**. **(1) Line numbers moved in the three files GV-8 touched** and every anchor in Chunks 5/6/6b was re-verified against `main` — `PhonePage.razor` **+109** (all above `:686`), `PhoneTextsPanel.razor` **+51** (`+47` before `@code`), `PhoneMessagesPanel.razor` **+12**. Re-verified byte-exact and *unchanged*: `ApiModels.cs:1187-1188`, `PhoneHubService.cs:25`/`:94`, `MessageBubble.razor:38`, and **`GvBridgeSendService.cs` in full** — GV-8 never touched it, so Chunk 2's whole-file replacement stands as written. **(2) `GvResult<T>` is now adopted for the send path, and it needed a small extension first — new Chunk 0.** GV-8 built `GvResult` to be reused, but it models *"success carries a value, failure carries none,"* which is right for a read and wrong for send: **`HttpError` sets `Value = null`, and that is a documented, test-pinned invariant** (`GvResult.cs:58`; `GvResultTests` asserts `Assert.Null(result.Value)`). GV-5's mapping is driven off the **`Code` in the body** (nine codes, ADR-028 §2) and needs **two** fields off a non-2xx — `Code` and `Error` — where `GvResult` carries one. Chunk 0 adds a **separate `FailureBody` + `HttpErrorWithBody`**, deliberately leaving `Value` null so GV-8's invariant and **GV-6's branch idiom survive untouched**; widening `Value` was considered and **rejected on the record**. ~50 lines src + ~25 test, additive only, no existing factory/property/test changes. **⚠ (3) A trap for anyone adopting the idiom the obvious way, recorded loudly because it fails SILENTLY.** `GvBridgeApiService.ReadErrorCodeAsync:312` tries `{ "error", "Error", "code", "Code" }` **in that order, first match wins**. Correct for the read routes (`{"error":"upstream_error"}`) **and for GV-6** (`{"error":"markread_disabled"}`) — **wrong for `/sms/send`**, whose body carries **both with opposite meanings**: `code` is the machine taxonomy, `error` is human prose. Routing send through that helper returns the **prose**, all nine codes fall through `MapCode`'s `_ =>` arm, and the taxonomy collapses to one generic failure — the exact class of quiet bug GV-5 exists to remove. GV-5 therefore deserializes the whole typed `SendSmsResponse` on non-2xx and does **not** reuse the helper; a warning comment is added at the helper rather than reordering it (two shipped call sites depend on the current order). **🔵 (4) ONE OPEN DECISION — the composer during a failed load (GV-8 UAT `O-2`). Recommendation written, owner to bless.** `@ComposeBar()` (`PhoneTextsPanel.razor:101`) renders **unconditionally**, below whichever of GV-8's four branches won — so a pane that just said *"Couldn't load messages."* still offers a Message field and Send. GV-7 is forbidden from re-deciding composer behaviour, so **this is GV-5's to answer**. **Recommendation: disable with a stated reason** (`"Reply once this loads."`), **not hide, not leave live** — and it is **not a taste call**: `PhonePage.OnOptimisticAppend:856` does `_openThreadMessages ??= new()`, so **one send flips `Messages` non-null, GV-8's `Error && Messages == null` guard goes false, the pane falls through to the LIST branch, and the error state silently vanishes — replaced by a single outbound bubble that reads as the whole conversation.** Precedence for all four composer reasons is now specified once (most-permanent-first: un-repliable → failed load → degraded/dark → normal), extending the rule the Test Plan already had at §D2/31. **(5) `BumpThread`'s fix got a companion guard.** GV-8 added three sites keyed on `_openThreadId`; Chunk 5 Step 4 lets a row's `ThreadId` change under an open conversation, and all three would then no-op **silently**. **(6) Three pre-existing plan defects fixed** (none GV-8's doing): a test snippet referencing a `NeverCompletesHandler` class that **does not exist** (real helper is `BlockingHandler`), the plan's only prose-only step made literal, and a Test-Plan numbering collision. **(7) Duplicate-thread-row bug: STILL REPRODUCES** — `BumpThread` (`PhonePage.razor:828-849`) is **absent from GV-8's diff**, still matches on `ThreadId` alone with an unconditional insert-new `else`. Still **derived from source, not observed live** (needs `SendEnabled=true` on both sides). **Net: 8 chunks → 9, 8 tasks → 9.** Test Plan gained the live-verification preconditions this project keeps re-deriving (`192.168.86.50`, `-Runtime linux-x64`, the `psidtsAgeSeconds` blackout clock, and the binary-freshness grep) plus the note that **`auth_unavailable` is the one code observable for free** during a natural blackout.
>
> **Prior entry — 2026-07-31 (Planner):** **GV-8's follow-ups triaged: 4 candidates in, 1 new row out.** The Polisher and Tester passes on PR #461 surfaced four things that were correctly *not* fixed inside it. Triaged rather than queued wholesale, because three of the four are not GV-8-shaped: net result is **one new row (UX-1), two fold-ins, one deliberate non-row**, plus one outstanding verification recorded so it is not silently assumed proven. **GV-8 itself is not re-litigated** — it is merged, its UAT passed 10/10 with 0 HIGH / 0 MEDIUM under a genuine upstream 502. **(1) UX-1 📋 — skeleton shimmer amplitude (UAT `L-1`), design-led, app-wide.** **The shimmer is not broken and this row must not be re-filed as if it were:** `Animation.currentTime` advances 0 → 117 ms and **14.5 % of pixels change** between frames 116 ms apart against a **0 %** static control. What is marginal is the *amplitude* — `--surface-raised #141416` → `--surface-overlay #1A1A1D` is **6/255** stop-to-stop, measured peak frame-to-frame change **3/255**. `.skeleton-loading` is the primitive behind **every** skeleton in the app (27 `<Skeleton>` call sites across 6 pages plus 38 raw nodes in the phone panels), so this is a **design-token decision needing a Designer answer first**, not a GV-8 bug. **(2) `L-2` folded into OPS-1, not stood up alone.** `Deploy-ToLinux.ps1:51` defaults to `-Runtime linux-arm64` while the box is **`x86_64`** — the literal documented invocation ships ARM binaries to an x64 host, which nearly invalidated the GV-8 UAT. Folded because it is the **same file and the same failure class as OPS-1's existing scope** — *the deploy reports success without proving what it did* — and a separate row would put two PRs on one script for a ~5-line change. Recorded as splittable back out if OPS-1 slips. **(3) The thread-list `== null` guard folded into GV-9, not stood up alone.** `PhoneTextsPanel.razor:162` still has a bare `else if (Error)` where the canonical form is `Error && <collection> == null` (`PhoneMessagesPanel.razor:110`/`:78`), which is exactly what GV-8's M-1 fix shipped one branch over at `:61`. Builder deliberately left it — it is **dead code in production** (the panel's only call site mounts it in conversation mode only) and was not in Polisher's findings. **Folding it in anyway, because the file itself argues for it:** its own comment keeps the dead skeleton markup *"in sync so it isn't the next thing someone copies,"* and the same reasoning covers the guard. **Two consequences recorded on the row: GV-9 is no longer CSS-only**, and Builder's deferral note is **corrected** — no existing test sets `Error` in thread-list mode, so the behaviour is unasserted in *both* directions and the fix must **add** an assertion rather than preserve one. **(4) Playwright MCP being unusable in WSL is deliberately NOT a queue row.** It is environment/tooling, not product: the fix is either installing Chrome (needs root) or repointing the plugin at `--browser chromium` (the user's own config, which the Tester correctly declined to modify). Neither is a PR against this repo. The Tester's working substitute is recorded in full in **§ Dependency / ordering notes** so the next UAT does not re-derive it. **(5) M-1 is live-UNVERIFIED and must not be counted as proven** — see the GV-8 row and § Dependency / ordering notes. **(6) `O-1` and `O-2` were checked and are genuinely already tracked** — `O-1` (the reconnect banner cannot fire during a blackout) is § Cross-repo handoffs #6, `O-2` (composer mounted behind the error state) is GV-7's `F-3`. One tightening applied to GV-7: its composer decision was written before an error state existed on that pane, so the row now names it.
>
> **Prior entry — 2026-07-31 (Builder):** **GV-8 ✅ SHIPPED as PR #461. The texts pane can finally say "this failed to load."** UAT F-1 (HIGH) and the *state* half of F-2 are closed: `GvBridgeApiService.GetSmsThreadMessagesAsync` now returns a **`GvResult<T>`** outcome instead of a bare `null`, `PhonePage` stops coalescing failure into `?? new()`, `PhoneMessagesPanel` finally forwards the `Loading`/`Error`/`OnRetry` parameters that had existed unused on `PhoneTextsPanel` since GV-3, and the pane branches **skeleton → error → empty → list** so "Start the conversation below." is reachable only for a genuine zero-message result. **[Live UAT](uat/2026-07-31-gv8-error-state/REPORT.md): 10/10, 0 HIGH, 0 MEDIUM**, confirmed under a **real upstream 502** during a natural blackout rather than a synthesised failure — and C7 confirmed the *opposite* lie was not introduced: a genuinely empty group thread still renders **empty**. **One user-visible delta by design (Task 6): a failed open no longer marks the thread read**, and a *successful Retry* now performs the read-marking the failed open skipped. **Three durable operational facts were recorded out of this cycle, because two sessions had to rediscover the first two:** (1) **the box is `192.168.86.50`** — `radio`/`piradio` do not resolve from WSL (→ `CLAUDE.md` § Deployment); (2) **`psidtsAgeSeconds` from `:5004/api/gvbridge/status` is a live blackout clock** (`<660` healthy / `660–1200` blackout) **and the only honest field in that payload** — `available`/`degraded`/`cookiesValid` all read healthy through a confirmed outage (→ `design/INTEGRATIONS.md`, and § Dependency / ordering notes below, replacing the journald probe); (3) **`grep -ac <branch-only-symbol> /opt/radio-console/web/Radio.Web`** is the interim web-freshness gate until OPS-1 ships a real version endpoint (→ the OPS-1 row + `CLAUDE.md`). **Next up: GV-6** (adopts `GvResult<T>`), then **GV-7** (whose header/empty design now sits on a pane that *can* express failure). **Prior entry — 2026-07-31 (Planner):** **GV-8 now has a plan** ([`plans/.../gv-texts-load-error-state.md`](superpowers/plans/2026-07-31-gv-texts-load-error-state.md), 7 tasks, branch `fix/gv-texts-load-error-state`). Status stays **📋** — Builder claims it. The plan follows the row's scope verbatim and adds nothing to it silently: three additions beyond the literal row (clearing the previous thread's messages on open — *required*, or the skeleton branch stays unreachable; a stale-response guard; and **Task 6**, which skips the durable mark-read and restores the unread marker when the bodies failed to load) are listed in the plan's § "Scope additions beyond the literal queue row," each marked droppable, with **Task 6 droppable in full**. Merging with GV-6 was reconsidered while planning and **rejected on the record** — the shared shape is ~60 lines and GV-6's remainder is behaviour (a once-only Warning plus a call-suppressing latch), not plumbing; `GvResult<T>` is introduced in the reusable form GV-6 adopts later. **Prior entry — 2026-07-31 (Planner):** **UAT + root-cause pass on live GV data: F-1 turned out to be THREE defects across two repos. GV-8/GV-9/GV-10 queued; GV-7's "design blind" warning retired.** First: **the upstream parser fix (`627b928`) landed and real GV data IS now flowing on both surfaces** — 20 SMS threads, 20 voicemails with real transcripts — which **supersedes the "read data blocked upstream" status recorded below** and is what made this UAT possible at all. What the pass then found: opening a text thread frequently renders **"Start the conversation below."** with no error, no spinner and no retry ([UAT F-1](uat/2026-07-31-gv-live-data/REPORT.md), HIGH). A [root-cause pass](uat/2026-07-31-gv-live-data/F-1-DIAGNOSIS.md) decomposed that one symptom into three independent defects. **The UAT's own guessed explanation — Google Voice throttling — is FALSIFIED**, three ways: the upstream status is `401 Unauthorized` and **never 429**; failure tracks **wall-clock in a clean 20-minute square wave**, not request volume (our constant-rate 60s poller shows the identical on/off pattern); and recovery lands on fixed boundaries rather than after a variable cooldown. **(1) Defect A — theirs.** GV PSIDTS goes stale ~11 min after each refresh but their CDP refresh fires only every ~20 min with **no reactive refresh on 401** → a deterministic **~9-minute auth blackout every 20 minutes** → HTTP 502 to us; **11 of 11** of our 502s fall inside a dead window. Their `/api/gvbridge/status` also reports `available:true, degraded:false` **during** a blackout, so our "Google Voice is reconnecting" banner never fires in the exact window it exists for. **(2) Defect B — theirs.** Thread ids containing `/` arrive as literal `%2F` (Kestrel deliberately does not decode `%2F` in paths) and fail an exact string compare → **HTTP 200 with `messages: []`**. **Reproduced deterministically in a healthy window.** Framing correction worth carrying: the predicate is **"the thread id contains `/`"**, *not* "the thread is MMS" — `g.Group Message.<base64url>` ids can contain `/`, and group threads simply happen to be the MMS threads. **Not fixable client-side**; both escaping workarounds were tested and both fail. **(3) Defect C — ours → GV-8 📋.** `GvBridgeApiService.GetSmsThreadMessagesAsync` collapses every non-2xx/timeout/deserialization error to `null`, then `PhonePage.razor:632` does `?? new()`, making a 502 **byte-identical** to a genuinely empty conversation. **GV-8 ships independently of A and B and is deliberately NOT blocked on them — ours makes the failure honest, theirs makes it rare; neither subsumes the other.** **(4) GV-7 is updated, not blocked.** Its "⚠ LIVE OBSERVATIONS INCOMING" warning is retired because the observations now exist; the row points at them, most importantly **G-1 — the "2" in `Texts 2` is an UNREAD COUNT, not a thread count (there are 20 threads)** — and **G-3 — zero opaque 36-char sender IDs are reachable from this surface** (no pagination), so that case is **untested against real data, not proven absent**. **G-4 de-risked the layout concern the row was written around:** a 36-char ID measured **safe at 1920×720**, no truncation needed. **(5) GV-9 📋 / GV-10 📋** carry the LOW findings (F-4, F-7; and F-5 as *confirm-or-close*, since its mechanism is **plausible but unproven**). **F-8 deliberately got no row** — the Tester retracted it as not reproducible with trusted input. **⚠ Durable discipline, also recorded in § Dependency / ordering notes: any future UAT of this surface must record wall-clock time against the 20-minute blackout cycle**, or the results look random again — that is precisely how this pass came to hypothesise throttling. Until Defect A is fixed, test within ~10 minutes of a `CDP cookie refresh` log line.
>
> **Prior entry — 2026-07-31 (Planner):** **GV-5 UNBLOCKED; reply-ability folded in; GV-7 queued.** (1) **The "wrong tree" concern is DISPROVEN and GV-5 is 📋 again.** `D:\prj\rp-deploy\.git` is a **file** reading `gitdir: D:/prj/RotaryPhone/.git/worktrees/rp-deploy` — it is a detached-HEAD **git worktree**, same object store, same remote. **They are one repository**, so ADR-028 was derived from the deployed objects all along; that also explains the byte-identical files. A Builder has since landed the GV parser fix on that repo's `main` (`627b928`), confirming it as the development origin. The ADR-028 banner now records the investigation **and** its outcome rather than being deleted — the reasoning is worth more than a clean-looking doc. Depends-on restored to **GV-3** alone. (2) **Reply-ability folded into GV-5** (ADR-028 §8): ~a third of inbound SMS is from **non-dialable** senders (numeric short codes, opaque 36-char sender IDs). Classified **client-side** (`GvCounterparty`, mirroring the existing `GvDirection` precedent — no DTO change, no cross-service dependency) and compose is **gated before the POST**, so an un-repliable thread shows a *disabled composer with a reason* instead of their `400 invalid_number` rendered as a red "That number doesn't look right" — the same lie `send_disabled` (§5.1) already taught us not to tell. (3) **GV-7 📋** owns the **display** side, with an explicit instruction to wait for the Tester's live observations rather than design blind.
>
> **Prior entry — 2026-07-31 (Planner):** **⚠ two corrections that change what this file has been asserting.** (1) **GV-5 is now 🔒 BLOCKED.** A Tester confirmed the deployed RotaryPhone tree is **`D:\prj\rp-deploy` @ `0a86898`**, not `D:\prj\RotaryPhone` — which is the tree **ADR-028 was derived from**. The contract must be re-derived before GV-5 is claimable; GV-5 exists *because* GV-3 was built against an anticipated contract, and claiming it against a contract from the wrong tree repeats that mistake one level up. Early signal: all four source files are **byte-identical** across both trees, so expect confirmation rather than rewrite — but source-tree parity is **not** deployed-binary parity, which is exactly what OPS-1 exists to settle. ADR-028 now carries a PROVISIONAL banner. (2) **The "GV Messages read experience ships fully" claim is RETRACTED** here and in `ROADMAP.md`. RotaryPhone's `PositionalGvThreadParser` expects an object root while `alt=protojson` returns arrays, so every real response yields **0 items behind a clean HTTP 200** — **voicemail and SMS read have never returned data against live Google Voice.** Our client is complete and correct; the data never arrives. Honest status: **read UI complete, read data blocked upstream.** Defect + a wrong configured GV number are recorded in § Cross-repo handoffs; both are `rp-deploy`-side and need **no Radio Console change**.
>
> **Prior entry — 2026-07-31 (Planner):** **three items from a live UAT pass on the Ubuntu box.** (1) **ADR-024 §3.3 amended** — the mark-read response table was missing the dark-state **`409 {"error":"markread_disabled"}`** RotaryPhone returns when their `EnableMarkRead=false`; verified live. Our client degrades acceptably but cannot tell "feature dark" from "GV unreachable," so **GV-6 📋** queues the small diagnostic fix. **Rollout order matters:** flip theirs first, confirm the `409` stops, then flip ours. (2) **OPS-1 📋 — build stamp.** Investigating found this is **~80% already built**: the SHA is stamped by `Directory.Build.props`, `Deploy-ToLinux.ps1` already passes `-p:SourceRevisionId` to both publishes and already **`exit 1`s on an API mismatch**, and `Radio.API` already serves `/api/health/version`. The real gap is narrow and is precisely the 2026-07-30 incident: **`Radio.Web` has no version endpoint** (its assembly already carries the SHA — it is just unreadable), and **deploy verifies `radio-api` only**, checking `systemctl is-active` for `radio-web` and nothing more. (3) Two **cross-repo** items routed to RotaryPhone via `docs/prompts/` — see § Cross-repo handoffs. Note the CDP spam has a **concrete, testable root cause**: their `ChromeCdpPort` defaults to **9224**, but nothing on that box listens there.
>
> **Prior entry — 2026-07-30 (Planner):** **GV-5 queued 📋: reconcile the GV SMS send contract** ([ADR-028](../design/decisions/2026-07-30-gv-sms-send-contract.md), [plan](superpowers/plans/2026-07-30-gv-messages-pr5-send-contract.md)). Reading RotaryPhone's as-built endpoint against ours found the divergence is **worse than the logged response-shape mismatch**: our `SendSmsRequest` omits their required **`ToNumber`**, which binds `null` server-side and returns `400 invalid_number` on **every** send — send is **non-functional today**, not merely mis-handled. Two more defects the fast-follow note missed: we have **never subscribed to `SmsSent`**, the only channel outbound messages arrive on (so GV-3's optimistic de-dupe is unreachable dead code), and the taxonomy is **nine** codes, not the eight logged (`invalid_text` was missing). **No external blocker** — their endpoint is shipped, and their `EnableSmsSend=false` returns `409 send_disabled`, which GV-5 handles as a first-class availability state. GV-5 merges as a **user-visible no-op** (`SendEnabled` stays `false`).
>
> **Prior entry — 2026-07-30 (Builder):** **GV Messages READ experience complete: GV-4 ✅ PR #441 merged.** Durable read-state (GV write-through, ADR-024) now ships behind `RotaryPhone:Gv:MarkReadEnabled` (**default off — zero user-visible change until flipped**). PR #441 had been parked since 2026-06-21 and was merged forward onto current `main` (44 commits: PR #456 feed-row polish + contact-name resolution, the phone dark-theme/scrollbar pass, PR #460 bell-failure surfacing). Reconciliation was clean — the row-polish rewrite re-projects its feed inline per render and reads `IsRead`/`HasUnread` live off the DTOs the reconciler mutates, so the unread annotation lands in the current markup unchanged. `Radio.Web.Tests` **846 passed / 0 failed**.
>
> **RotaryPhone is no longer owner-HELD.** They have since shipped `GvVoicemailController.MarkRead` (`POST /api/gvbridge/voicemail/{id}/read`), `GvSmsController.MarkThreadRead` (`POST /api/gvbridge/sms/threads/{threadId}/read`), `ReadStateChangedDto` + the unconditional `NotifyReadStateChanged` broadcast, **and** `POST /api/gvbridge/sms/send`. Routes verified to match our call sites exactly. **Both sides are now dark by config only** — theirs `GVBridge:EnableMarkRead=false` (+ `EnableSmsSend=false`, `AllowMarkUnread=false`), ours `RotaryPhone:Gv:MarkReadEnabled=false` (+ `SendEnabled=false`). Lighting up mark-read is now a **two-side config flip, no build wait**. SMS **send is unblocked on the endpoint** (was the last external blocker) — but send wiring remains out of scope here; see fast-follows for the response-shape mismatch to resolve first. NU1903 SQLite blocker cleared (ADR-023, PR #438).

## Status legend

| Mark | Meaning |
|---|---|
| 📋 | Queued — ready to claim (all dependencies met) |
| 🔒 | Blocked — waiting on a dependency row |
| ⛔ | On hold — blocked on an **external** dependency (another repo/team must ship first); do not claim |
| 🚧 | In flight — a Builder cycle is working it on a branch |
| ✅ | Done — merged to `main` |

---

## Queue

| # | Item | Status | Plan | Spec / handoff | Depends on | Branch |
|---|------|--------|------|----------------|-----------|--------|
| GV-1 | **GV Messages PR1 — Foundation + IA shell.** DTOs + `GvBridgeApiService` voicemail/SMS read methods (delete stale "no SMS routes" comment) + absolute audio-URL builder; `PhoneHubService` `GvSmsReceived`/`GvVoicemailReceived`; `GvBridgeStatusService` (~10s poll); `RotaryPhoneAuthHandler` seam (OFF); `PhoneUnreadState`; config keys + DI; restructure `PhonePage` into the unified Messages feed (segmented filter + "More ▸" rail + missed-call badge, calls folded in). | ✅ | [`plans/.../pr1-foundation-ia-shell.md`](superpowers/plans/2026-06-20-gv-messages-pr1-foundation-ia-shell.md) | [handoff](design-handoffs/HANDOFF-phone-messages-voicemail-sms.md) · [ADR-022](../design/decisions/2026-06-20-gvbridge-voicemail-sms-integration.md) | — | `feat/gv-messages-pr1-foundation` |
| GV-2 | **GV Messages PR2 — Voicemail surface.** Voicemail rows (all list states) + inline accordion `VoicemailPlayer` (seekable Range scrubber, buffering/playing/paused/ended/audio-error incl. 502, transcript present/pending/absent) + `GvVoicemailReceived` new-arrival (never steal screen / never pause audio) + reconnecting-banner gate + UI-local mark-heard with flagged no-op `MarkVoicemailReadAsync` seam (decision 4). | ✅ | [`plans/.../pr2-voicemail-surface.md`](superpowers/plans/2026-06-20-gv-messages-pr2-voicemail-surface.md) | [handoff Screen A/B](design-handoffs/HANDOFF-phone-messages-voicemail-sms.md) · [ADR-022 D4/D5/D6](../design/decisions/2026-06-20-gvbridge-voicemail-sms-integration.md) | GV-1 | `feat/gv-messages-pr2-voicemail` |
| GV-3 | **GV Messages PR3 — Texts surface.** Thread-list rows (all states) + master-detail conversation (inbound/outbound bubbles, append-in-place on inbound when open — no toast) + `GvSmsReceived` + compose/reply + new-recipient composer + on-screen `TouchKeyboard` **all feature-flagged OFF** via `GvBridgeSendService` (`SendNotAvailableException` until `RotaryPhone:Gv:SendEnabled` + endpoint ship) with optimistic/sending/sent/failed-preserve-text + 429/in-flight/degraded guardrails. Includes the "open a thread back to RotaryPhone" deliverable (GV mark-read, audio-endpoint auth posture, send-shape confirmation). | ✅ | [`plans/.../pr3-texts-surface.md`](superpowers/plans/2026-06-20-gv-messages-pr3-texts-surface.md) | [handoff Screen C/D](design-handoffs/HANDOFF-phone-messages-voicemail-sms.md) · [ADR-022 D5/D7/§8](../design/decisions/2026-06-20-gvbridge-voicemail-sms-integration.md) | GV-1 | `feat/gv-messages-pr3-texts` |
| GV-4 | **Wire GV mark-read / durable read-state.** Fast-follow that flips read-state from UI-local to **durable via GV write-through** (Google is single source of truth; NO local store): repoint the GV-2 `MarkVoicemailReadAsync` seam at `POST /api/gvbridge/voicemail/{id}/read`, add the SMS-thread sibling (`POST /api/gvbridge/sms/threads/{threadId}/read`), subscribe to the unified `ReadStateChanged` SignalR event on the existing `/hub`, reconcile **idempotently keyed by `(id-or-threadId + isRead)`** (RotaryPhone echoes unconditionally, incl. to the originator), drop UI-local seeding (list endpoints' `isRead`/`hasUnread` are source-of-truth on reload), behind `RotaryPhone:Gv:MarkReadEnabled` (default off). No unread toggle (hidden). No new auth. | ✅ **PR #441** | [`plans/.../pr4-mark-read.md`](superpowers/plans/2026-06-20-gv-messages-pr4-mark-read.md) | [ADR-024](../design/decisions/2026-06-20-gv-mark-read-durable-readstate.md) · [ratification reply](file:///D:/prj/RotaryPhone/docs/handoffs/radioconsole-gv-markread-reply.md) (`D:/prj/RotaryPhone/docs/handoffs/radioconsole-gv-markread-reply.md`) | **GV-2** (`MarkVoicemailReadAsync` seam) + **GV-3** (SMS read surface). _Shipped flag-OFF (`MarkReadEnabled=false`) — **no network calls, no new affordances, no visual change** on merge. One intended behavior delta while off: the optimistic read flip is no longer replayed onto refetched lists, so pressing **Refresh** after hearing a voicemail restores the unread marker (ADR-024 §2 by design; resolves when both flags are on). **RotaryPhone is no longer owner-HELD**: their `MarkRead` / thread-read routes + `ReadStateChanged` broadcast are shipped and dark by config only (`GVBridge:EnableMarkRead=false`). Lighting up is a **two-side config flip**, not a build wait._ | `feat/gv-messages-pr4-mark-read` |
| GV-5 | **Reconcile the GV SMS send contract.** Replace GV-3's *anticipated* send contract with RotaryPhone's **as-built** one (ADR-028). Fixes the **request** shape — ours omits their required `ToNumber`, which binds `null` server-side and returns `400 invalid_number` on **every** send, so send is currently **non-functional**, not merely mis-handled. Adopts the complete **nine-code** `Code` taxonomy (`invalid_text` was missing from the eight previously logged), driving mapping off `Code` not HTTP status, with per-code copy from the handoff matrix; `send_disabled` (409) becomes an **availability state, not a failed send**; `invalid_number`/`invalid_text` are terminal (no Retry). Subscribes to the **`SmsSent`** `/hub` event — the only channel outbound arrives on, which we have never listened to, making GV-3's optimistic de-dupe unreachable dead code. Adds `OutboundSmsReconciler`, **idempotent keyed by exact `Id` then `(Outbound, counterparty, text, ≤120s)`** (the poller's re-surfaced copy carries a different `Id` by construction), replacing in place so the bubble never jumps. Wires `ClientCorrelationId`; `Queued:true` → existing `Sent` (no "delivered" state — neither side can honestly assert delivery). Also fixes a duplicate-thread-row bug new conversations expose. **Ships with `SendEnabled` still `false`.** **Also in scope: thread reply-ability** — ~a third of inbound SMS comes from senders that are **not dialable** (numeric short codes and opaque 36-char sender IDs, not E.164). Classify the counterparty client-side and **gate compose before the POST**, so an un-repliable thread yields a *disabled composer with an explanation*, never a red failed bubble — same reasoning as `send_disabled` (ADR-028 §5.1). _Provenance note: this row was briefly 🔒 on a "wrong tree" concern (2026-07-31) — **investigated and disproven**: `rp-deploy` is a **git worktree** of `D:\prj\RotaryPhone`, same object store, so ADR-028 was derived from the deployed objects all along. See the ADR banner._ **⚠ PLAN RECONCILED AGAINST POST-GV-8 `main` (2026-07-31) — read the plan's 🔄 banner before claiming.** The plan predates PR #461; **no ADR-028 decision changed**, but three things did. **(a) Every line-number anchor in Chunks 5/6/6b was re-sited** (`PhonePage.razor` +109, `PhoneTextsPanel.razor` +51, `PhoneMessagesPanel.razor` +12) — **do not trust any pre-refresh citation**. `GvBridgeSendService.cs`, `ApiModels.cs:1187-1188`, `PhoneHubService.cs:25`/`:94` and `MessageBubble.razor:38` were **not** touched by GV-8 and are verified exact. **(b) NEW Chunk 0 — `GvResult<T>` needs a small extension before the send path can adopt it.** `HttpError` nulls `Value` (a test-pinned invariant GV-6 relies on), but GV-5 maps off the **`Code` in the body** and needs `Code` **and** `Error` off a non-2xx. Chunk 0 adds a separate `FailureBody` + `HttpErrorWithBody`; widening `Value` was **rejected on the record**. **⚠ Do NOT reuse `GvBridgeApiService.ReadErrorCodeAsync` for send** — it tries `error` before `code`, which is right for the read routes and for GV-6 but returns this endpoint's human **prose** instead of its machine code, collapsing all nine codes into one generic failure. **(c) 🔵 ONE DECISION IS OPEN AND WANTS AN OWNER ANSWER BEFORE THE CYCLE STARTS — the composer during a failed load** (GV-8 UAT `O-2`, Chunk 6b Step 5). The plan **recommends** disabling it with a reason (`"Reply once this loads."`) rather than hiding it or leaving it live, because leaving it live is a **demonstrable defect, not a preference**: `OnOptimisticAppend` makes `Messages` non-null, which flips the pane out of GV-8's `Error && Messages == null` branch into the **list** branch, so the error state disappears and one outbound bubble renders as the whole conversation. GV-7 must not re-decide this. **(d) Also folded in:** a companion guard so `BumpThread` adopting a new `ThreadId` cannot silently desync `_openThreadId` from the three sites GV-8 keyed on it, and Test-Plan preconditions for live verification (`192.168.86.50`, `-Runtime linux-x64`, the `psidtsAgeSeconds` blackout clock, binary-freshness grep). **The duplicate-thread-row bug still reproduces** — `BumpThread` is absent from GV-8's diff — but remains **derived from RotaryPhone's source, never observed live**, since reproducing it needs `SendEnabled=true` on both sides. **9 chunks / 9 tasks** after the refresh. | 📋 | [`plans/.../pr5-send-contract.md`](superpowers/plans/2026-07-30-gv-messages-pr5-send-contract.md) | [ADR-028](../design/decisions/2026-07-30-gv-sms-send-contract.md) · [handoff §send/bubble states](design-handoffs/HANDOFF-phone-messages-voicemail-sms.md) | **GV-3** (`GvBridgeSendService`, compose/reply, `MessageBubble`, optimistic-append seam). _No external blocker: `POST /api/gvbridge/sms/send` is **shipped**. Their `GVBridge:EnableSmsSend=false` does **not** block — a dark server returns `409 send_disabled`, which this PR handles as a first-class state. **Merges as a user-visible no-op** (our flag off → compose unreachable, no new calls); the only always-on delta is the inert `SmsSent` subscription._ | `feat/gv-messages-pr5-send-contract` |
| GV-6 | **Distinguish `409 markread_disabled` from a genuine mark-read failure.** ADR-024 §3.3 (amended 2026-07-31) documents the dark-state `409` RotaryPhone returns when their `GVBridge:EnableMarkRead=false`. Our client maps **every** non-2xx to `null`, so "the feature is switched off" is indistinguishable from "GV is unreachable." Degrades acceptably today — no crash, no wrong badge, next list fetch is authoritative — so this is **diagnostic quality, not correctness**. Fix: in `GvBridgeApiService`'s two mark methods, detect `409` + `markread_disabled` and treat it as a distinct outcome — log **once** at Warning (not per call, to avoid journald churn on the N100) and latch a flag that suppresses further calls until restart, rather than re-POSTing on every mark. **No user-visible affordance** — mark-read is silent-by-design (ADR-024 §6); this is for operators. Matters in exactly one state: config skew where ours is on and theirs is off. | 📋 | _plan TBD (small)_ | [ADR-024 §3.3](../design/decisions/2026-06-20-gv-mark-read-durable-readstate.md) | **GV-4** | `fix/gv-markread-dark-409` |
| GV-7 | **Render non-dialable SMS senders (short codes + opaque sender IDs).** The **display** counterpart to GV-5's send-side reply-ability gate (ADR-028 §8). Roughly a third of inbound SMS comes from senders that are not phone numbers — **numeric short codes** and **opaque 36-char sender IDs** — and the texts surface was designed assuming a dialable counterparty throughout. Scope: **thread-list row** (what occupies the name line when there is no name and the identifier is 36 chars — truncation, `--font-mono`?, avoid layout blowout at **1920×720**); **conversation header**; **contact resolution** — `ContactResolutionService` resolves number→name and by definition **cannot** match a short code or opaque ID, so it must degrade gracefully rather than render an empty or misleading name; and the **bubble/meta** treatment for these threads. **Critical constraint Builder confirmed: there is NO `fromName` anywhere in the GV payload** — only counterparty identifiers — so display names can come **only** from local contact resolution, which structurally cannot resolve these senders. Design for "identifier is all we will ever have." Reuse GV-5's `GvCounterparty.Classify` (do not write a second classifier). Composer behavior is **GV-5's** — this row must not re-decide it. **Live observations now EXIST — design against them, not assumptions.** The prior "⚠ LIVE OBSERVATIONS INCOMING — do not design blind" warning is **retired**: the [2026-07-31 UAT](uat/2026-07-31-gv-live-data/REPORT.md) observed this surface against real GV data, and its § "Findings for GV-7" (**G-1 … G-9**) is the input. **Read it before starting.** The findings that actually change the design: **G-1 — the "2" in `Texts 2` is an UNREAD COUNT, not a thread count. There are 20 threads.** Design against a 20-row list, not the 2-row list an earlier session assumed. **G-3 — zero opaque 36-char sender IDs are reachable from this surface**: the feed has no pagination or load-more control, so it caps at 20 threads and older threads are unreachable through the UI. That case is therefore **untested against real data, NOT proven absent** — do not conclude the corpus lacks them. **G-4 de-risks the layout concern this row was written around:** a synthetic 36-char ID was injected and measured at 1920×720 — it **fits with no ellipsis and no page overflow** in both the thread-list row and the conversation header (`document.scrollWidth` stayed exactly 1920 at every length tested), so **no truncation strategy is needed for layout safety**. Also in scope from the pass: **G-6** — when no name resolves, the two-line header renders the **identical string on both lines** (`.texts-conv-name` over `.texts-conv-number`), confirmed for all 14 unresolved threads; **G-8** — MMS previews embed a sender prefix (`+1XXXXXXXXXX - <text>`), i.e. a *second* phone number inside a row that already displays a counterparty; and **F-3** — the composer is **rendered-but-disabled with no stated reason** (no tooltip, no helper text, empty `title`). Whether to hide it or label it is **a design decision for this row, not a prescription** — the UAT flagged it deliberately without deciding. **Amended 2026-07-31 — that decision now has one more context than it had when this row was written.** GV-8 shipped an **error state** on this pane, and the GV-8 UAT's `O-2` observed the composer **still mounted behind it**: under a confirmed 502 the pane renders `cloud_off` + "Couldn't load messages." + `Retry` *and*, below it, a live `Message` field with `Send`. It is **inert** (`SendEnabled=false` is honoured — same as F-3), so this is **not a new defect and deliberately gets no row of its own**; it is routed here because it is the same composer decision, not a second one. But whatever this row decides — hide, label, or leave — now has to hold in **three** states rather than two: non-dialable counterparty, genuinely empty thread, and **failed load**. The third is the weakest case for leaving it: inviting a reply into a conversation the app has just admitted it could not read. **G-5 is a PASS and needs no work** (resolution degrades gracefully — the raw identifier is used as the name, never blank, never wrong) **but recalibrates the priority: 14 of 20 (70%) of live threads render an identifier rather than a name**, so the fallback is the **common** case on this surface, not an edge case. | 📋 | _plan TBD (design-led; consult the Designer handoff first)_ | [ADR-028 §8](../design/decisions/2026-07-30-gv-sms-send-contract.md) · [handoff Screen C/D](design-handoffs/HANDOFF-phone-messages-voicemail-sms.md) · [**UAT § Findings for GV-7**](uat/2026-07-31-gv-live-data/REPORT.md) | **GV-3** (texts surface). _The "wait for observations" caveat is **discharged** — they exist and are linked in the row. Coordinate with GV-5 if both are in flight: GV-5 adds `GvCounterparty` to `ApiModels.cs` and this row consumes it. **Coordinate with GV-8 too, and prefer GV-8 first:** GV-8 rewrites the conversation pane's state branches (`PhoneTextsPanel.razor:36-68`), which this row also touches — and designing the header/empty treatment on top of a pane that **cannot express "failed"** would bake the F-1 confusion into the new design._ | `feat/gv-messages-pr7-nondialable-senders` |
| GV-8 | **Distinguish a failed conversation load from an empty one (the silently-empty texts pane).** UAT **F-1** (HIGH): opening a text thread renders **"Start the conversation below."** with **no error, no spinner, no retry** — a 502 and a genuinely empty thread are indistinguishable on screen, and the failure is invisible to browser instrumentation (0 console errors, 0 failed requests — the page is Blazor Server, so the fetch happens server-side over SignalR). **Root-caused, confirmed by source:** `GvBridgeApiService.GetSmsThreadMessagesAsync` (`src/Radio.Web/Services/ApiClients/GvBridgeApiService.cs:204-218`) wraps `GetFromJsonAsync` in `catch (Exception) { log; return null; }`, and `GetFromJsonAsync` calls `EnsureSuccessStatusCode()`, so **every non-2xx, timeout and deserialization error collapses to `null`** with no status distinction; then `PhonePage.razor:632` does `_openThreadMessages = messages?.Messages.ToList() ?? new()`, after which the two states are **byte-identical**. No `_openThreadError` field exists. **Scope:** (a) make the API client express *outcomes* (result type / status) instead of a bare `null`; (b) add `_openThreadError` / `_openThreadLoading` page state and pass `Loading` + `Error` through `PhoneMessagesPanel.razor:184-191`, which today passes **neither** although **both parameters already exist** on `PhoneTextsPanel` (`PhoneTextsPanel.razor:172-173`) — this is also why the UAT saw **no spinner**: the skeleton branch is unreachable dead code in this hosting; (c) add the missing **error branch** to `PhoneTextsPanel.razor:36-68` (today exactly three branches — skeleton / empty / list, **no error branch at all**), reusing the **F-6** pattern verbatim from `PhoneMessagesPanel.razor:110-117` — `cloud_off` + **"Couldn't load messages."** + `Retry` — a string already specified at [`HANDOFF-phone-dark-theme-and-scrollbars.md:310`](design-handoffs/HANDOFF-phone-dark-theme-and-scrollbars.md); (d) fold in UAT **F-2** (MEDIUM) — the "Start the conversation below." copy must appear **only** for a genuine empty. Today it fires on failure *and* invites a reply into a hard-disabled composer (`SendEnabled=false`), which on a short-code thread is doubly wrong. **The fix template already exists one level up:** the thread list does this correctly — `PhonePage.razor:595-616` sets `_threadsError` (and toasts "Showing the last update" when a cached list exists) and `PhoneMessagesPanel.razor:110-117` renders `cloud_off` + `Retry` off that flag. **This is mostly copying an already-designed pattern down one level.** **Test gap is explicit:** `tests/Radio.Web.Tests/Services/GvBridgeApiServiceVoicemailSmsTests.cs:81` asserts **only** the happy path — add a non-2xx case there **plus** a bUnit case asserting the pane renders the **error** state, not the empty state. **Do NOT attempt a client-side escaping workaround for the group-thread `%2F` bug** (that is RotaryPhone's — § Cross-repo handoffs #5): both double-escaping (`%252F`) and a raw `/` were tested and both fail, the latter falling through to their SPA fallback and returning `index.html` with HTTP 200. | ✅ **PR #461** | [`plans/.../gv-texts-load-error-state.md`](superpowers/plans/2026-07-31-gv-texts-load-error-state.md) | [UAT F-1/F-2](uat/2026-07-31-gv-live-data/REPORT.md) · [root-cause § Defect C](uat/2026-07-31-gv-live-data/F-1-DIAGNOSIS.md) · [handoff error-state copy](design-handoffs/HANDOFF-phone-dark-theme-and-scrollbars.md) · [**GV-8 UAT — 10/10, 0 HIGH/MEDIUM**](uat/2026-07-31-gv8-error-state/REPORT.md) | **GV-3** (texts surface: `PhoneTextsPanel`, `PhoneMessagesPanel`, `PhonePage` thread-open path). _**SHIPPED 2026-07-31.** Verified against a **genuine upstream HTTP 502** during a natural auth blackout (`psidtsAgeSeconds` 707), not a synthesised failure: the pane rendered `cloud_off` + "Couldn't load messages." + a working `Retry`. C7 confirmed the opposite failure mode is **not** introduced — a genuinely empty group thread still reads as **empty**. **One user-visible behavioural delta, by design (Task 6): a failed thread open no longer marks the thread read** — the unread dot is restored rather than silently cleared for a conversation the user was never shown, and the durable write-through is skipped (which also avoids 2-3 more upstream Google calls inside a dead auth window). A **successful `Retry` then performs the read-marking the failed open skipped**, so the dot does not linger once the conversation actually renders. A *successful* load followed by a *failed* mark-read still keeps the optimistic flip and reconciles later — ADR-024 §2, deliberately unchanged. Pre-merge review found 0 HIGH / 3 MEDIUM and a Polisher pass 1 HIGH / 1 MEDIUM / 1 LOW; all seven fixed. Two LOW deferred with justification (skeleton shimmer amplitude — a design-token question spanning every skeleton in the app; and a deploy-script runtime default), triaged 2026-07-31 into **UX-1** (shimmer amplitude — new row) and **OPS-1 (c)** (the deploy runtime default — folded, not standalone); a third item Builder flagged forward, the missing thread-list `== null` guard, is folded into **GV-9**. **⚠ ONE THING IN THIS PR IS SHIPPED BUT NOT LIVE-VERIFIED — `M-1`. Do not record it as proven.** Polisher's M-1 fix is the `Error && Messages == null` ordering (`PhoneTextsPanel.razor:61`) — *arriving content outranks a stale error flag*, so an inbound SMS landing on an open-but-failed thread renders the message rather than staying on `cloud_off`. It is **unit-covered** (`PhoneTextsPanelTests:141` — `Conversation_ShowsMessages_WhenErrorSetButMessagesArrived`) but was **never observed live**: it needs a **real inbound SMS to land while a failed thread is open**, no inbound traffic arrived during the UAT window, and the Tester **correctly declined to inject a synthetic message** rather than manufacture a pass. What *is* established live is the branch ordering around it (skeleton → error → empty → list, all four observed). **It is cheap to close opportunistically and needs no cycle:** text the GV number during a blackout (`psidtsAgeSeconds` 660–1200) with the failed thread open, and watch the pane. Also tracked in § Dependency / ordering notes. **Explicitly NOT blocked on RotaryPhone.** Their two defects (§ Cross-repo handoffs #5/#6) make this failure **rare**; this row makes it **honest**. Neither subsumes the other — shipping only theirs would leave a surface whose only defence is that the backend seldom fails. **Same bug class as GV-6** (every non-2xx → `null`) and deliberately kept separate; the reasoning is in § Dependency / ordering notes._ | `fix/gv-texts-load-error-state` |
| GV-9 | **Texts-surface polish: overflow hardening + unread-row alignment.** Two LOW findings from the same UAT, both pure CSS on the same surface, grouped into one cycle. **F-4** — `.texts-conv-number` computes to `white-space: normal; overflow: visible; text-overflow: clip`, while **both** its siblings (`.list-item-title`, `.texts-conv-name`) carry `nowrap` + `overflow: hidden` + `text-overflow: ellipsis`; beyond ~60 characters it clips **mid-character** instead of ellipsizing. _Honest severity: **no live data triggers this**, and a 36-char opaque ID does **not** (measured at 1920×720, UAT G-4). This is consistency hardening, not a fix for an observed break — do not let it be re-filed as a bug._ **F-7** — unread rows sit **20px** out of alignment with read rows, because the `<span class="unread-dot">` is a sibling placed *before* `.list-item-identity` and therefore **displaces** the text rather than occupying a reserved gutter (unread name line x=**251px**, read x=**231px**). Every row jumps horizontally the moment it is marked read — newly visible now that GV-4 wired mark-read. Fix is a reserved-width gutter that holds the text steady in both states. **Third item, FOLDED IN 2026-07-31 out of the GV-8 cycle — the thread-list branch never got the `== null` guard that GV-8 shipped in conversation mode.** `PhoneTextsPanel.razor:162` is a bare `else if (Error)`; the canonical form on this surface is `Error && <collection> == null` — `PhoneMessagesPanel.razor:110` (threads) and `:78` (voicemail) — and GV-8's M-1 fix shipped exactly that one branch over at `PhoneTextsPanel.razor:61`. **Consequence if the branch ever goes live:** a stale error flag would outrank thread rows that had actually arrived — the same lie GV-8 was written to remove, just one level up. **Builder deliberately did not fix this in PR #461, and was right not to:** it was not in Polisher's findings, and the branch is **dead in production** — the panel's only call site (`PhoneMessagesPanel.razor:184`) mounts it solely under `_openThreadId != null`, i.e. conversation mode, which the file's own comment already records. It was flagged forward to this row instead. **Fix it here, because the file argues for it in its own words:** the comment above the dead skeleton markup says it is *"kept in sync so it isn't the next thing someone copies."* That is the project's stated policy for this exact branch; the guard falls under it. **Two consequences, stated so they are not a surprise mid-cycle.** (1) **This row is NO LONGER CSS-only** — it becomes CSS plus a one-line `.razor` condition, so budget a `Radio.Web.Tests` run rather than a visual check. (2) **Correction to the deferral note this arrived on:** the behaviour is *not* covered by pre-existing tests — `PhoneTextsPanelTests` sets `Error` only with `OpenThreadId` present (`:124`, `:141`, `:197`), and the three thread-list-mode tests (`:32`, `:41`, `:68`) never set it. It is **unasserted in both directions**, so the fix must **add** an assertion, not preserve one. **Why folded here rather than a standalone row:** GV-9 is already the row that owns this surface's polish and its dead-copy sync — F-7's reserved gutter has to behave in all three `.unread-dot` sites (`PhoneMessagesPanel.razor:663` live, `PhoneTextsPanel.razor:191` dead, `VoicemailRow.razor:10`), and the dead one sits **29 lines below** the bare `else if`. A separate row would be a third queue entry against the same file for a one-line change in unreachable code. | 📋 | _plan TBD (small; **no longer CSS-only** — two CSS fixes plus one `.razor` guard and its missing test)_ | [UAT F-4/F-7](uat/2026-07-31-gv-live-data/REPORT.md) · [GV-8 UAT (guard provenance)](uat/2026-07-31-gv8-error-state/REPORT.md) · [handoff](design-handoffs/HANDOFF-phone-dark-theme-and-scrollbars.md) | **GV-3** (texts surface). _No external dependency; claimable now. GV-8 is merged, so the guard's canonical form (`PhoneTextsPanel.razor:61`) is on `main` to copy from — read it before writing the thread-list version rather than re-deriving it. Same surface as GV-7: if both are in flight expect `PhoneTextsPanel` and the thread-list row markup to have moved._ | `fix/gv-texts-polish-overflow-unread-align` |
| GV-10 | **Confirm-or-close: do conversation bubbles render the list snippet instead of the full message body?** UAT **F-5** (LOW) — **UNPROVEN. This row may legitimately close with no code change.** In the `32665` conversation a bubble ended in a literal `...` matching the truncated list preview, suggesting the conversation view renders the snippet rather than the full body. **Plausible mechanism, explicitly not proven:** RotaryPhone derives per-thread messages by **filtering the whole SMS folder list** (`GvSmsClient.ListMessagesAsync`) rather than calling a per-thread Google endpoint, and folder-list entries carry **snippets**, not full bodies. **If that is the cause it is their data, not our rendering, and this row converts into a cross-repo note rather than a Radio Console change.** **First task is confirmation, not repair:** one `curl` for a known long message, compared against the same message in Google Voice. **Two disciplines the confirming call must respect:** (1) run it inside a **healthy window** — see the 20-minute blackout note in § Dependency / ordering notes — or a 502 will masquerade as a result; (2) use a **non-group** thread (e.g. `t.32665`, the original repro) until RotaryPhone's `%2F` decode lands, since group threads return 0 messages for an entirely unrelated reason. _Provenance: the diagnosis recommended waiting for their decode fix before testing. We are **not** hard-blocking on it, because the reproducing thread is not a group thread and is curl-able today — recording the deviation rather than silently departing from the source doc._ | 📋 | _plan TBD (investigate first; scope depends on the answer)_ | [UAT F-5](uat/2026-07-31-gv-live-data/REPORT.md) · [root-cause § Scope-affecting notes](uat/2026-07-31-gv-live-data/F-1-DIAGNOSIS.md) | **GV-3** (texts surface). _Lowest priority of the three UAT rows; do not schedule ahead of GV-8._ | `fix/gv-texts-bubble-full-body` |
| OPS-1 | **Build stamp on `radio-web` + deploy verification for both services.** **Most of this already exists — scope is the gap, not a greenfield build.** `Directory.Build.props` already stamps the git SHA into `AssemblyInformationalVersion` (`_StampGitCommitHash`), `Deploy-ToLinux.ps1` already passes `-p:SourceRevisionId` to **both** publishes and already **fails loudly (`exit 1`) on an API SHA mismatch**, and `Radio.API` already serves `GET /api/health/version` → `VersionInfoDto { GitSha, GitShaShort, InformationalVersion, AssemblyVersion, BuildTimestampUtc, AssemblyName }`. **Two gaps remain, and together they are exactly the 2026-07-30 incident:** (1) **`Radio.Web` exposes no version endpoint at all** — its assembly already carries the correct SHA, it is simply unreadable at runtime; (2) **deploy verifies `radio-api` only** — for `radio-web` it checks `systemctl is-active` and nothing more, so a stale web binary passes verification silently. Add a minimal-API `GET /api/health/version` to `Radio.Web` (matching the existing `app.MapGet("/api/albumart/…")` convention — Radio.Web has no Controllers dir), returning the same shape; extend the deploy verify block to poll `:5002` and fail identically on mismatch. Also document that `BuildTimestampUtc` is file-mtime, i.e. *landed-on-disk* time, not compile time — the SHA is the authoritative signal. **Interim stopgap, proven in the GV-8 UAT — reference it in the plan and keep it documented until the endpoint ships:** grep the deployed web binary for a symbol that exists only on the branch under test — `grep -ac RetryOpenThreadAsync /opt/radio-console/web/Radio.Web` (non-zero → the new binary is live). The Tester used `OpenThreadLoading`, `RetryOpenThreadAsync` and `GvCallOutcome` to confirm freshness by hand, precisely because `systemctl is-active` had reported a healthy service while proving nothing about *which* binary was running. Now also recorded in `CLAUDE.md` § Deployment. **(c) FOLDED IN 2026-07-31 — the script's runtime default does not match the box (GV-8 UAT `L-2`, LOW, but sharp).** `deploy/Deploy-ToLinux.ps1:51` declares `[ValidateSet("linux-arm64","linux-x64")] [string]$Runtime = "linux-arm64"`, while `uname -m` on `192.168.86.50` is **`x86_64`** — so the **literal documented invocation** `Deploy-ToLinux.ps1 -TargetHost 192.168.86.50` publishes and ships **ARM64 binaries to an x64 host**. This nearly cost the GV-8 UAT its validity; the correct call is `-Runtime linux-x64` and the report records it as the rollback recipe. **Fix direction, not prescribed:** probe `uname -m` over the SSH session the script already opens and **fail before publishing** on mismatch (cheapest, and it cannot go stale), and/or derive the default from the target host instead of hardcoding one. **Note the sibling default has the identical shape** — `$TargetHost` defaults to **`piradio`**, which does not resolve from WSL (§ Dependency / ordering notes) — so the real defect is *"the script's defaults describe a box that no longer exists,"* and a `uname -m` guard fixes the half that silently produces a wrong-architecture deploy rather than an obvious connection failure. **Why this is folded here rather than standing alone:** it is the **same file** and the **same failure class** as gap (2) above — *the deploy reports success without proving what it did* — so one Builder cycle should own deploy integrity end to end; a separate row would put two PRs on one script for a ~5-line change. _Splittable back out if OPS-1 slips: it has no dependency on the version endpoint and could ship on its own in an afternoon._ | ✅ **PR [#485](https://github.com/mmackelprang/RTest/pull/485)** | _plan TBD (small; scope is fully specified in this row)_ | [ADR-024 §3.3 amendment](../design/decisions/2026-06-20-gv-mark-read-durable-readstate.md) (context: the incident that surfaced it) · [GV-8 UAT `L-2`](uat/2026-07-31-gv8-error-state/REPORT.md) | — | `feat/ops-web-build-stamp` |
| UX-1 | **Skeleton shimmer amplitude — is a 6/255 gradient delta enough on the dark theme?** **Design-led: get a Designer answer before a plan is written, and accept that this row may legitimately close as "no change."** From [GV-8 UAT `L-1`](uat/2026-07-31-gv8-error-state/REPORT.md). **The shimmer is NOT broken, and this row must not be re-filed as "the skeleton is broken"** — it demonstrably animates, on three independent kinds of evidence: `animationPlayState: running` with `Animation.currentTime` advancing **0 ms → 117 ms**; `backgroundPosition` moving `-200%` → `-174.531%`; and a CDP screencast in which **14.5 % of pixels changed** in the skeleton region between two frames **116 ms** apart, against a **0 %** change in a static control region of the same frame pair (which is what rules out compression noise and global repaint). `prefers-reduced-motion` was `false`, so the `animation: none` override was not in play. **What is marginal is the amplitude.** `.skeleton-loading` (`src/Radio.Web/wwwroot/css/design-system.css:1075-1083`) is `linear-gradient(90deg, var(--surface-raised) 0%, var(--surface-overlay) 50%, var(--surface-raised) 100%)` at `background-size: 200% 100%`, and on this theme those tokens are `#141416` and `#1A1A1D` (`design-system.css:65`/`:67`) — `rgb(20,20,22)` → `rgb(26,26,29)`, a **6/255 stop-to-stop delta**, with **measured peak frame-to-frame change of 3/255**. Side by side the two captured frames read as identical to the eye. **Scope is the whole app, not the texts pane — this is the point of the row.** `.skeleton-loading` is the shimmer primitive every skeleton composes with: `Skeleton.razor`'s seven shapes (NowPlaying, Radio, ListRow, DeviceRow, MetricTile, Visualizer, …) are mounted at **27 call sites across 6 pages** (`DeviceManagementPage`, `PlayHistoryPage`, `QueueHistoryPanel`, `RadioControlPanel`, `RadioPage`, `VisualizerPanel`), plus **38 raw `.skeleton-loading` nodes** in the phone panels. **A one-line token change lands on all of them at once — which is precisely why this is a design-token decision and not a bug fix, and why it was correctly kept out of GV-8.** **Questions for the Designer, none pre-decided here:** should the shimmer highlight get **its own token** rather than borrowing `--surface-overlay`, which exists to be a *surface* and is also used with `backdrop-filter: blur(20px)` (`design-system.css:184`) — i.e. it is not free to move? Is a wider delta still tasteful on a **wall-mounted kiosk in a dark room**, or does it start reading as a flashing band? Does the answer differ per shape — a full-bleed `Visualizer` block sweeping is a much larger moving area than a 16px `skeleton-text` bar. **Constraint any fix must honour:** the `prefers-reduced-motion: reduce` override at `design-system.css:1683` sets `animation: none`, so reduced-motion users see the **static** gradient — widening the amplitude must not make that state worse. **Judge it on the kiosk panel, not a laptop LCD:** the numbers above came from frames rendered on the box, and a 6/255 delta is exactly the range where two displays will not agree. | 📋 | _plan TBD — **do not write one until the Designer has answered**; scope depends entirely on whether the answer is "new token," "retune the existing pair," or "leave it"_ | [GV-8 UAT `L-1`](uat/2026-07-31-gv8-error-state/REPORT.md) · evidence: `uat/2026-07-31-gv8-error-state/screenshots/03-c2-frame-a-108ms.png` vs `04-c2-frame-b-224ms.png` | — _(no code dependency; it is gated on a design answer, not on a row)_ | `feat/ux-skeleton-shimmer-amplitude` |
| KIOSK-1 | **One kiosk launch line, and it works under Wayland.** **This row exists because every deploy currently leaves the screen dead, and the script itself documents why.** `Deploy-ToLinux.ps1:376` relaunches the kiosk with `DISPLAY=:0 nohup google-chrome …` on a box that runs **Wayland**, which its own comment at `:369-374` labels a *"KNOWN DEFECT, deliberately not fixed here."* The owner hit it live on 2026-08-18. **The replacement is verified by hand, not proposed:** `systemd-run --user --collect google-chrome --kiosk … --ozone-platform=wayland …`, which starts the browser from the graphical session's *own* service manager and therefore inherits that session's `WAYLAND_DISPLAY` / `DBUS_SESSION_BUS_ADDRESS` instead of an SSH shell's. **Confirmed by 2 established connections to `:5002` afterwards — and that is the meaningful check, not process existence: during the 2026-08-02 outage Chrome was running and `radio-web` returned 200 for ~33 hours while the count was 0.** Scope: a new `deploy/debian-x64/kiosk/bin/radio-kiosk-launch` that becomes the **single definition** of the kiosk command line (three callers converge on it — autostart, deploy, and the desktop icon — which today carry three different flag sets); a dedicated `--user-data-dir=~/.config/radio-kiosk-chrome`, which **pays for itself three times**: it lets `KIOSK-2`'s Exit spare the GV bridge, it **restores CDP on `:9223`** (Chrome ≥136 silently ignores `--remote-debugging-port` on the default profile), and it makes `--password-store=basic` safe **by construction** on a profile that was `basic` from first run — `CLAUDE.md` measured that flag destroying 45 `v11` → 16 `v10` cookies on an existing profile; **`setup-kiosk.sh` fixed to `install -m 755`, NOT `chmod +x`** (with the default umask `+x` yields **775**, and GNOME silently refuses to launch a group-writable `.desktop` — that mode bit is the entire reason the box's `GV-Bridge.desktop` was unlaunchable); the **missing repo→`~/Desktop` installer**, which is the root cause of the whole drift problem (the in-tree `radio-console.desktop` has carried `--password-store=basic` since Aug 11 and the live copy still does not, because nothing has ever installed it — **three separate instances of this drift turned up in one day**); and dropping **`onboard`** from `deploy/provision/packages.sh:88` + the autostart entry, which `docs/uat/2026-08-03-osk-wayland-viability/REPORT.md` already recommended (Chrome 151 on Wayland issues **zero** `zwp_text_input_v3.enable()` calls, so the OS keyboard cannot type into the kiosk at all). **⚠ Two `Deploy-ToLinux.ps1` details that will silently misfire if skipped:** the stop block's cache and `Singleton*` paths point at `~/.cache/google-chrome` and `~/.config/google-chrome`, which belong to the **default** profile — a `--user-data-dir` profile keeps both **inside itself**, so leaving them alone clears the wrong cache and leaves the kiosk's own lock in place; and its comment at `:146-154` describes exactly those paths, so it becomes a **false comment** unless rewritten with the diff (this repo's `## Pre-Merge Review` rule). **⚠ Merge-order: `KIOSK-1` BEFORE `KIOSK-2`, and it is not a preference** — every piece of `KIOSK-2` (scoped exit, single-instance guard, launcher) needs the dedicated profile to already exist; landing `KIOSK-2` first would mean matching the kiosk on the *absence* of a flag, which the design explicitly rejects as fragile. Plan Tasks **A0–A7**; A0 is a **box-probe task that pins four unknowns** (zenity's GTK major, `--timeout`/`--icon-name`/`--default-cancel` support, whether `WAYLAND_DISPLAY` is in `systemctl --user show-environment`, the `rotary-phone` unit name) and **must run first** — several later choices branch on its output. **⚠ PROMOTED above `TEST-1` on 2026-08-18 with the owner's explicit approval, and the reason is the blast radius rather than the size of the fix: this defect fires on **every single deploy** and costs a dead kiosk screen each time, so it taxes every other row in this queue — including `TEST-1`'s own verification, which needs a live panel to confirm anything on the box. `TEST-1` remains the highest-priority row of the 2026-08-10 tranche; this is one row jumping it, not a re-ordering of that tranche.** | ✅ | [`plans/.../kiosk-desktop-launcher.md` § Part A](superpowers/plans/2026-08-18-kiosk-desktop-launcher.md) | [handoff](design-handoffs/HANDOFF-kiosk-desktop-launcher.md) | [#479](https://github.com/mmackelprang/RTest/pull/479) | `fix/kiosk-launch-contract` |
| TEST-1 | **Stop the suite producing false CI signals — four flaky tests, and the ambient-`localhost:5000` defect underneath them.** **Highest priority of the 2026-08-10 tranche.** Four distinct tests failed under timing/load pressure on 2026-08-10 and passed on retry, during a debugging session that was chasing a real audio bug — **the flake was read as a regression and produced a wrong diagnosis before it was recognised as noise.** That is the cost this row exists to remove: a suite that lies intermittently makes every other row's verification untrustworthy. **Three parts, and (c) is the actual fix.** **(a) A bUnit assertion raced across a real `await`.** `ModePicker_ClickingPhase_ActivatesPhaseSegment` (`tests/Radio.Web.Tests/Components/Shared/VisualizerPanelTests.cs:184-193`) clicks the Phase segment and asserts `is-active` / `aria-selected` **immediately**, with no wait for the re-render. `phase.Click()` dispatches `VisualizerPanel.SelectMode` (`src/Radio.Web/Components/Shared/VisualizerPanel.razor:207-229`), which assigns `_currentMode` **only after** `await UnsubscribeFromCurrentMode()` — and that await **completes synchronously when the SignalR hub is disconnected but makes a real network round-trip when it is connected**, which is exactly why this passes locally and flakes on the runner. Fix: wrap the assertions in `cut.WaitForAssertion(...)`. **⚠ This is NOT loosening the assertion — it is the same condition, waited for rather than raced.** Do not re-file it as "the test was made permissive." **Five files in this project already use the idiom — copy one rather than inventing a wait:** `Components/Pages/PhonePageThreadLoadErrorTests.cs`, `Components/Pages/PlayHistoryPageTests.cs`, `Components/Shared/CastDeviceDropdownTests.cs`, `Components/Shared/RadioControlPanelTests.cs`, `Components/Shared/PhoneStatusHeroTests.cs`. **(b) ⚠ MEASURED AND RE-FILED 2026-09-01 — this part was misdiagnosed and is NOT part of this row.** The record described three `Radio.API.Tests` timing out at 30–46 s. Measured: **16–18 tests inflate and none times out** (325 passed / 0 failed both under load and alone), worst case **14.2 s**. Every inflated test is the **first-executed test of a class holding `IClassFixture<CustomWebApplicationFactory<Program>>`** — 17 such classes, 17 entries — so what is measured is host boot, which xUnit bills to the collection's first test. **`Radio.API.Tests` cannot reach the network at all**: every `HttpClient` comes from `_factory.CreateClient()` over an in-memory `TestServer`. Control run of the project alone against 40 CPU-burner processes with `:5000` closed reproduced it exactly (1.05 s → 3.43 s per first-of-class). Cause is **CPU oversubscription** — 11 assemblies as concurrent processes, each defaulting xUnit to all 32 cores. The *"22/22 in ~5 s"* also fails checking: **no class here has 22 tests**, and ~5 s is the whole project's isolation wall time. **Split out as `TEST-3` (P2, bound test parallelism).** Nothing to do in this row. **(c) The underlying defect, and the real fix: unit tests can reach an ambient `localhost:5000`.** A unit test whose result depends on whether `radio-api` happens to be running on the self-hosted runner is the root problem — **(a) and (b) are both symptoms of a test rig that is not hermetic**, and (a)'s whole flake mechanism is "the hub was actually connected." Stub the hub/config clients in the test rig so no unit test can open a socket to the box. **Ordering within the row: do (c) first, or at least alongside.** Shipping only (a) and (b) fixes two instances and leaves the class open — the next timing-sensitive test added will rediscover it. | ✅ **SHIPPED [#483](https://github.com/mmackelprang/RTest/pull/483)** | _(a) done — and widened from 1 test to 3: `Fall` and `Ring` had the identical click-then-assert shape and raced the same await; only `Phase` had lost. (b) measured and **re-filed as `TEST-3`** — not this defect. (c) done: transport seam on all four hub services + an `IHttpMessageHandlerBuilderFilter` covering every `AddHttpClient<T>()`. **Verified with a listener bound to the port during full runs: 74 connections → 0 on :5000, 0 on :5004**, 877/877 Web and 325/325 API. UAT on the live app: all four hub services connected in production config with events flowing. Pre-merge review caught a comment of mine that overclaimed safety — `GetCoverArtUrlAsync` builds its own `HttpClient`, so the stub covered one of two transports; fixed with a second seam plus a regression test proven able to fail._ | _no spec doc — the diagnosis is in this row_ · provenance: the 2026-08-10 "no audio from the soundbar" debugging session (PRs #467 / #468 / #469) | — _(no code dependency; claimable now, and **should be claimed before the other seven** — every one of them is verified by this suite)_ | `fix/test-rig-hermetic-and-flaky-waits` |
| AUD-1 | **Split `UseShazamForAllSources` into the two independent decisions it currently conflates.** **⚠ READ THIS BEFORE TOUCHING THE FLAG: do NOT "fix" the overwrite behaviour by setting `UseShazamForAllSources` to `false`. That turns off BT album art entirely.** One boolean (`src/Radio.Fingerprinting/FingerprintingOptions.cs:19`, surfaced at `src/Radio.API/appsettings.json:91` and `src/Radio.Web/Models/ApiModels.cs:788`) governs two things that have nothing to do with each other. **Decision 1 — "always fingerprint BT" — is NECESSARY and must not change.** The 2026-08-10 investigation established that **AVRCP cannot supply album art on this box**: BlueZ 5.72 ships **no BIP / cover-art implementation**, and 7 days of fingerprint-DB data show **0 AVRCP-sourced art against 2,560 SongRec-sourced**. SongRec is not an enhancement here, it is the only source. Mechanically: the flag feeds the gate at `src/Radio.Infrastructure/Audio/Sources/Primary/BluetoothAudioSource.cs:837` (`NeedsFingerprintingLookup = hasIncompleteMetadata \|\| FpOptions.UseShazamForAllSources`), and a false gate makes `src/Radio.Fingerprinting/Services/BackgroundIdentificationService.cs:260` (`if (!audioTap.NeedsFingerprintingLookup) return;`) **hard-return** — so whenever AVRCP supplies a title *and* artist, nothing is ever fingerprinted and no art is ever found. **Decision 2 — "let SongRec overwrite AVRCP title/artist/album" — is NOT necessary and is actively wrong.** It rewrites `Enter Sandman (Remastered)` to `Enter Sandman`; AVRCP is the authoritative name for what the phone is actually playing, and SongRec is guessing from audio. **The wanted behaviour already exists in the same method, on the branch that never runs when the flag is on.** `BluetoothAudioSource.OnTrackIdentified` (`:845`) forks at `:867`: **`:867-891` is the overwrite branch** (assigns Title/Artist/Album unconditionally, caches art, logs *"Shazam metadata replaced AVRCP for BT"*, `return`s) and **`:893-905` is the preserve branch** (computes `hasArt` from existing metadata and calls `CacheAndSetCoverArtAsync` **only when art is absent**, never touching title/artist/album). **The fix is to reach `:893-905`'s art-fill behaviour while keeping `:837`'s always-fingerprint gate** — i.e. two flags, not one. **⚠ Caveat that probably forces a per-source split rather than a rename:** the same flag is read by `src/Radio.Infrastructure/Audio/Sources/Primary/FilePlayerAudioSource.cs:1988` (the gate) and `:2112` (the overwrite, additionally guarded by `needsLookup`), where **ID3 tags are authoritative in a different way** and the current behaviour may well be correct as-is. A single global rename will not serve both sources — expect `FingerprintingOptions` to gain two properties and the config/DTO surfaces above to follow. **Decide deliberately whether FilePlayer's overwrite behaviour changes at all**, and say which way in the plan; silently dragging it along with BT is the failure mode here. _All line numbers verified against `main` @ `c129e0d`; **PR #469 moved this file by ~28 lines**, so pre-merge citations are stale._ **⚠ RE-VERIFIED 2026-08-11 against `main` @ `8b1ce0a` (post-#468), and TWO ANCHORS IN THIS ROW WERE STALE AND ARE NOW CORRECTED: `FilePlayerAudioSource.cs:1968 → :1988` and `:2092 → :2112`.** #468 touched that file (`StopCoreAsync` / `DisposeAsync` resume-position handling, nothing to do with fingerprinting) and pushed everything below `:872` down **+20 lines**. **Everything else in this row re-verified byte-exact and unchanged:** `FingerprintingOptions.cs:19`, `appsettings.json:91` (**checked specifically — #468 added a line to that file and it landed below `:91`, so the citation survives**), `ApiModels.cs:788`, `BluetoothAudioSource.cs:837`/`:845`/`:867-891`/`:893-905` (#468 did not touch that file at all), and `BackgroundIdentificationService.cs:260`. | 📋 | _plan TBD (small-to-medium; the behaviour already exists — the work is splitting the switch and deciding FilePlayer's side)_ | _no spec doc — the diagnosis is in this row_ · provenance: 2026-08-10 debugging session; PR #469 is the adjacent merged fix | — _(no row dependency. **Touches the same file and method region as PR #469** (`BluetoothAudioSource.OnTrackIdentified`), which is merged — rebase, don't re-derive. **Also touches `FilePlayerAudioSource.cs`, which #468 changed on 2026-08-11** — the anchors above are already re-sited, but rebase rather than trusting any earlier copy.)_ | `fix/split-shazam-fingerprint-vs-overwrite` |
| AUD-6 | **Output device identity is an enumeration ordinal, so a persisted output preference silently changes which physical device it means.** **⚠ The diagnosis is complete — do NOT re-brainstorm it. And read the falsification note before anyone "reopens" the 2026-08-10 question; this row is a different claim.** **The mechanism, verified end-to-end against `main` @ `0b2ebcb`.** `SoundFlowDeviceManager` mints output ids straight off the MiniAudio enumeration index — `Id = $"playback-{i}"` (`src/Radio.Infrastructure/Audio/SoundFlow/SoundFlowDeviceManager.cs:517`, and the sibling at `:695`), where `i` is the raw loop counter over `playbackDevices` (`:498`). **That string is what reaches storage.** It is persisted to `AudioPreferences:CurrentOutput` from two places — `SoundFlowDeviceManager.SetOutputDeviceAsync` (`:234-238`) and `SoundFlowAudioEngine.PersistActiveOutputAsync` (`src/Radio.Infrastructure/Audio/SoundFlow/SoundFlowAudioEngine.cs:425`) — and read back at startup by `AudioEngineInitializationService` (`src/Radio.API/Services/AudioEngineInitializationService.cs:210`). **And resolution back to hardware is pure string parsing, not a lookup:** `SoundFlowAudioEngine.GetDeviceIndexById` (`:1030-1043`) checks the `playback-` prefix and returns `int.TryParse(deviceId.AsSpan("playback-".Length))` — **there is no name comparison anywhere on that path.** So the persistent key *is* the ordinal, and any change in enumeration order, or a device appearing or disappearing, silently repoints the preference at different hardware. **The measurement, on the box across today's deploy restart.** Aug 10 16:17 — `Playback device [0]: "AB13X USB Audio Analog Stereo"`, `[1]: "Built-in Audio Analog Stereo" (IsDefault=True)`. Aug 11 15:04 — `[0]: "Built-in Audio Analog Stereo" (IsDefault=True)`, `[1]: "AB13X USB Audio Analog Stereo"`. The store held **`playback-1`** across both. **Aug 10 that meant the Soundbar; Aug 11 it means USB Audio Out. Nothing in the store changed — the meaning did.** **⚠ It is intermittent by nature and will NOT reproduce on demand.** It surfaced only because today's restart happened to reorder enumeration; a plan that budgets for a deterministic repro will not get one. Reordering can be *forced* for a test (unplug/replug the USB DAC, or restart PipeWire) but that is a hardware exercise, not a unit test. **⚠ THE CODEBASE ALREADY KNOWS ABOUT THIS, AND THE GUARD IT WROTE FOR IT CANNOT FIRE — read this before writing a fix, because the obvious reaction is "there is already a check."** `AudioEngineInitializationService.cs:294-296` carries the comment *"After PipeWire restarts, MiniAudio device indices may shift, causing the wrong device to be selected even though the ID matches,"* followed by a verification block at `:297-330`. **It is a tautology.** Its "expected" value, `preferredDeviceName`, is derived from the ordinal itself (`:258-262`, `outputDevices.FirstOrDefault(d => d.Id == preferredOutputId)`), and its "actual" value is `currentDevices.FirstOrDefault(d => d.Id == selectedId)` (`:300-303`) where `selectedId` is the same id just written at `:286`. **Both sides are the same lookup, by the same key, against the same `SoundFlowDeviceManager` cache** — `outputDevices` is `GetOutputDevicesAsync()` from `:142`, and `currentDevices` is the same call again at `:299` — **so `activeDevice.Name` and `preferredDeviceName` are the same string, and the mismatch branch at `:304` does not fire.** If the ordinal has gone stale, the check confirms the *wrong* device against itself and passes. _Stated precisely, because this row must not overclaim the way the comments it is criticising do: the branch is not unreachable in the strict sense — the hot-plug refresh can rewrite `_cachedOutputDevices` between those two reads, and a **reorder inside that window** would make the two sides differ. That is a narrow, unrelated race, not the restart-scale reordering this row is about, and **it is the only path on which the guard can fire at all.**_ Its repair path is dead for a second, independent reason: it calls `_deviceManager.SetOutputDeviceAsync(correctDevice.Id)` (`:317`), which performs no native switch (see `AUD-7`). **THE STABLE KEY ALREADY EXISTS, IS ALREADY COMPUTED, AND IS ALREADY TRUSTED — this row is largely about promoting it, not inventing one.** `AudioOutput:DeviceDisplay:DeviceFriendlyNames` (`src/Radio.Core/Configuration/AudioOutputOptions.cs:70`, written by `DevicesController.SetDeviceFriendlyName:1016-1021`) is a `Dictionary<string,string>` keyed by **device name**, consumed by `SoundFlowDeviceManager.ApplyFriendlyName` (`:432-451`) on `rawName`. **That mapping survived the reordering correctly** — `"Built-in Audio Analog Stereo": "Soundbar"` and `"AB13X USB Audio Analog Stereo": "USB Audio Out"` resolved to the same physical devices on both days, which is precisely why the friendly names in the live payload are right while the id underneath them is wrong. The raw name is already carried through the domain model as `AudioDeviceInfo.RawName` (`src/Radio.Core/Interfaces/Audio/IAudioDeviceManager.cs:144`, populated at `SoundFlowDeviceManager.cs:519`). **Fix direction — persist a stable identifier (the device name, or something more hardware-derived if the implementer can find one that survives a PipeWire restart), and keep `playback-N` as a transient view-model concern that never reaches storage.** The exact identifier is the implementer's call and should be argued in the plan; name-keying is the cheapest option that is already proven on this box, but it is not obviously the best one — device names are not guaranteed unique, and two identical USB DACs would collide where ordinals would not. **Say which way and why.** **⚠ THREE CONSTRAINTS ON THE FIX, all cheap to miss.** **(1) The key is polymorphic.** `AudioPreferences:CurrentOutput` holds either a device id **or** one of the virtual-output sentinels `google-cast` / `http-stream` (`SoundFlowAudioEngine.SetActiveOutputAsync:204-206`; the startup branch at `AudioEngineInitializationService.cs:226`). A format change must leave those two values untouched and still distinguishable. **(2) There are TWO read paths for this preference, not one, and they do not read the same store.** The startup service reads the **config store** (`:210`) and falls back to `IOptionsMonitor` (`:223`), while `SoundFlowDeviceManager`'s constructor restores from `IOptionsMonitor` **only** (`:115-128`, i.e. `appsettings.json:170`, which ships empty). The asymmetry is already commented at `AudioEngineInitializationService.cs:201-203`. **Both paths must handle a legacy value or the migration is half-applied.** **(3) The API DTO does not carry the raw name** — `DevicesController.MapToDeviceDto` (`:1486-1501`) projects `Id` and the friendly `Name` and drops `RawName` — so surfacing a stable id to the UI is a DTO change plus a `Radio.Web` change, not an infrastructure-only edit. **⚠ MIGRATION NOTE — REQUIRED, and it is the part with real risk.** Every existing store holds an ordinal. **A fix must be able to read a legacy `playback-N` without repointing anyone's output on upgrade** — and note the cruel detail: **on the very deploy that ships the fix, the legacy ordinal may already mean the wrong device**, so "resolve the old ordinal and rewrite it as a name" silently canonises whatever the enumeration happens to be at that moment. Decide deliberately between resolving-and-rewriting, and treating an unmigratable legacy value as *no preference* and falling back to the system default; **the second is more honest and the more defensible default, but it will move one user's output once, so say so in the plan rather than discovering it in UAT.** **⚠ RECORDED SO IT IS NOT REOPENED: this exact fragility was raised as a hypothesis on 2026-08-10 and CORRECTLY FALSIFIED as the cause of the soundbar silence.** The app had opened the right device; the cause was the Cast startup mute, shipped in **#468**. **That falsification stands — do not let this row be read as overturning it.** This row makes a strictly different claim: not *"this caused that outage"* but *"this fragility has now been observed firing,"* which the 2026-08-10 evidence neither established nor excluded. **Test gap, stated because the one test that exists gives false comfort:** `tests/Radio.Infrastructure.Tests/Audio/SoundFlowDeviceManagerTests.cs:188-207` (`SetOutputDeviceAsync_SetsSelectedDevice`) asserts only that `GetSelectedOutputDeviceId()` returns the string that was just passed in — **a bookkeeping round-trip that would pass unchanged even if no audio device were ever opened** — and it silently `return`s (i.e. passes) when the runner has no audio hardware (`:194-198`), which is the same non-hermetic pattern `TEST-1` owns. | ✅ **PR [#499](https://github.com/mmackelprang/RTest/pull/499)** | _plan TBD (medium — the identifier change is small, the **migration** and the two read paths are where the work and the risk are)_ | _no spec doc — the diagnosis is in this row_ · provenance: the 2026-08-11 production deploy; enumeration captured from the `radio-api` journal on both days · the pre-existing name-keyed precedent is `AudioOutput:DeviceDisplay:DeviceFriendlyNames` ([`AudioOutputOptions.cs:70`](../src/Radio.Core/Configuration/AudioOutputOptions.cs)) | — _(no row dependency; claimable now. **⚠ CLAIM THIS BEFORE `AUD-7` — the ordering is load-bearing, not a preference:** `AUD-7` makes startup **act** on this preference, and acting on an unstable key is worse than ignoring it. See § Dependency / ordering notes. **Same files as `AUD-7`** (`SoundFlowDeviceManager.cs`, `AudioEngineInitializationService.cs`, `SoundFlowAudioEngine.cs`, `DevicesController.cs`) — expect anchors to move if both are in flight. **Same root class as `AUD-2`/`AUD-4`** (an unstable identifier used as a persistent key) but **no file overlap with either**, so it can run alongside them.)_ | `fix/stable-output-device-identity` |
| AUD-7 | **The reported active output diverges from where audio physically goes — the output selector cannot be trusted.** **⚠ Diagnosis complete; do NOT re-brainstorm. This is the half that makes the UI lie, and any fix for `AUD-6` that does not also close this leaves a selector that is still wrong.** **The observation, live on the box at `0b2ebcb`.** `/api/devices/output` returned `{"id":"playback-0","name":"Soundbar","isActive":false}` and `{"id":"playback-1","name":"USB Audio Out","isDefault":true,"isActive":true}` — while PipeWire had the application's stream on **sink 57, `alsa_output.pci-0000_00_1f.3.analog-stereo`** (the built-in analog, i.e. the Soundbar) and the USB sink (**17449**) `SUSPENDED`. **Audio was physically reaching the Soundbar while the UI claimed USB Audio Out.** **⚠ THE MECHANISM IS SHARPER THAN "the two concepts are not reconciled at startup" — read this, because it changes the fix.** It is not that two notions drift apart; it is that **the native device switch is reachable only from the interactive HTTP path.** `SoundFlowAudioEngine.SwitchPlaybackDevice` (`src/Radio.Infrastructure/Audio/SoundFlow/SoundFlowAudioEngine.cs:838`) is the **only** code anywhere that stops, disposes and re-initializes `_playbackDevice` (`:873-883`), and it has **exactly one caller in the solution**: `DevicesController.SetOutputDevice`, fire-and-forget on the thread pool (`src/Radio.API/Controllers/DevicesController.cs:250-254`; grep-verified — the only other reference to `GetDeviceIndexById` is its own declaration). **The startup path never calls it.** What startup calls instead does nothing native. `AudioEngineInitializationService.cs:286` calls `_deviceManager.SetOutputDeviceAsync(outputToUse)`, and `SoundFlowDeviceManager.SetOutputDeviceAsync` (`:213-244`) **validates the id against the device cache, assigns the `_selectedOutputDeviceId` string, and persists the preference — and touches no audio device.** Then `:337` calls `_audioEngine.SetActiveOutputAsync(outputToUse)`, which is explicit that the swap is somebody else's job: *"The local-device switch (native MiniAudio swap) is the caller's responsibility via `IAudioDeviceManager.SetOutputDeviceAsync` — this gate only owns mute state, virtual-output lifecycle, and persistence"* (`SoundFlowAudioEngine.cs:222-224`). **That comment assigns a responsibility that the named method does not discharge.** It is the same failure class as the three worked examples in `CLAUDE.md` § Pre-Merge Review, and fixing the wording belongs in whichever PR fixes the behaviour. **So the device that is actually open is whatever `InitializeAsync` picked** — the system default, selected at `SoundFlowAudioEngine.cs:504-517` and logged as *"Selected system default playback device at index {Index}"*, which is exactly the line the startup journal shows immediately before `SetActiveOutputAsync: "<none>" -> "playback-1"` and `Local output "unmuted"`. **⚠ CONSEQUENCE THAT SEPARATES THIS ROW FROM `AUD-6`: this defect is DETERMINISTIC, not intermittent.** It fires on **every** restart in which the persisted preference is not the system default — **no reordering required**. `AUD-6` needs the enumeration to shuffle; this one does not. Do not let the two be collapsed into "the same intermittent thing." **Why the UI reports what it reports.** `isActive` is a string comparison and nothing more: `DevicesController.cs:74-81` computes `isActive = d.Id == _deviceManager.GetSelectedOutputDeviceId()`, i.e. against the same `_selectedOutputDeviceId` field the bookkeeping just wrote. (Note `isDefault` on that payload is *also* derived from the user selection when one exists, `:79-80` — which is why the live capture shows `isDefault:true` on `playback-1` while MiniAudio reports the **built-in** device as the system default. Two different meanings of "default" on one payload; worth tidying here, but it is not the defect.) **And the truth is not merely unreconciled — it is UNREACHABLE.** `_currentDeviceIndex` (`SoundFlowAudioEngine.cs:44`, assigned at `:543`, `:809`, `:883`) is the only field that reflects the device actually open, and it is **private with no accessor of any kind**, so the API layer could not report the real answer today even if it wanted to. Exposing it (or something derived from `_playbackDevice`) is likely the smallest honest fix for the reporting half. **FOUR SEPARATE NOTIONS OF "THE OUTPUT DEVICE" EXIST, AND ONLY ONE OF THEM IS REAL** — enumerate them in the plan rather than rediscovering them: **(1)** `SoundFlowAudioEngine._currentDeviceIndex` / `_playbackDevice` — where audio actually goes; set by init and by `SwitchPlaybackDevice`; **private, unexposed**. **(2)** `SoundFlowDeviceManager._selectedOutputDeviceId` (`:52`) — what `/api/devices/output` reports and what gets persisted; set by startup and by the controller. **(3)** `SoundFlowAudioEngine._activeOutputId` — the gate's local/Cast/HTTP notion, persisted to the **same** config key as (2). **(4)** `LocalAudioOutput`'s device id (`src/Radio.Infrastructure/Audio/Outputs/LocalAudioOutput.cs:166`, `:192`) — updated **only** from `DevicesController.cs:229`/`:242`, i.e. the interactive path, so it too is stale after every restart. **A THIRD SITE SILENTLY REPOINTS WITHOUT TELLING ANYONE, and it is not in the owner's report:** the engine's recovery path re-initializes `playbackDevices[0]` unconditionally and sets `_currentDeviceIndex = 0` (`SoundFlowAudioEngine.cs:805-809`) — so after a device-loss recovery, audio moves to whatever is at index 0 while (2), (3) and (4) all keep reporting the old preference. **Fold this into the fix or state explicitly that it is out of scope; do not leave it undiscovered.** **Scope, deliberately not prescribed beyond the two halves:** **(a)** make the startup path actually route to the persisted preference — the obvious shape is to call the same native switch the controller calls, but note it is currently fire-and-forget on a thread pool thread from an HTTP handler and startup wants something ordered and awaited; **(b)** make the reported active output derive from what is actually open rather than from a bookkeeping string, so the two cannot silently disagree again. **⚠ (a) WITHOUT `AUD-6` IS A REGRESSION, NOT A PARTIAL FIX — this is the single most important sentence on this row.** Making startup honour the persisted ordinal converts a **mis-report** into a **mis-route**: today's store holds `playback-1`, which now resolves to USB Audio Out, so the owner's saved intent of "Soundbar" would be honestly and confidently routed to the wrong hardware. **Claim `AUD-6` first, or ship (b) alone and hold (a) until the key is stable.** **⚠ A guard already exists for the neighbouring problem and CANNOT FIRE — do not mistake it for coverage.** `AudioEngineInitializationService.cs:294-330` compares "expected" against "actual" where both are the same lookup by the same key against the same cache; its repair path then calls `SetOutputDeviceAsync`, which does no native switch either. Full reasoning on `AUD-6`; stated here because whoever claims this row will find it first and may conclude the startup path is already verified. **It is not.** **Test gap, and it is the fifth instance of this repo's comment-accuracy failure class:** `tests/Radio.API.Tests/Services/AudioEngineInitializationServiceStartupTests.cs:79-100` (`StartAsync_PersistedLocalDevice_RoutesThroughGate`) verifies **only** `SetActiveOutputAsync("playback-2")` and asserts nothing about which device ends up open — and its own comment (`:81-83`) says the persisted device must go through the gate *"not just the device-manager swap,"* **naming a swap that does not exist.** A test whose comment describes behaviour the production code never had is worse than no test: it makes the gap look covered. Any fix here must add an assertion about the **native** target, and correct that comment. **⚠ OWNER / UAT NOTE — the evidence above is a snapshot as of `0b2ebcb`, and the owner is about to change it on purpose.** Hands-on UAT will re-select "Soundbar" by hand, which writes `playback-0` and makes the UI truthful again for the current enumeration. **Do NOT read a later truthful `/api/devices/output` as evidence this row is fixed or was never real.** The only condition under which it fires is a **restart** with a persisted preference that is not the system default — so verification means: set a non-default output, restart `radio-api`, and confirm with `pactl list sink-inputs` that the app's stream is on the sink the UI names. **Do not deploy or restart anything on the box to write this row's plan.** | ✅ **PR [#500](https://github.com/mmackelprang/RTest/pull/500)** | _plan TBD (medium — half (b) is small and shippable alone; half (a) is small in diff and sharp in blast radius, and is gated on `AUD-6`)_ | _no spec doc — the diagnosis is in this row_ · provenance: the 2026-08-11 production deploy — `/api/devices/output` compared against `pactl` sink state, plus the `radio-api` startup journal | — _(no **row** dependency in the blocking sense, but **`AUD-6` SHOULD BE CLAIMED FIRST** and half (a) must not ship before it; see the ⚠ above and § Dependency / ordering notes. **Same files as `AUD-6`** — rebase rather than re-deriving anchors. **Also touches `DevicesController.cs`, which `AUD-5` does not**, and `SoundFlowAudioEngine.cs`, where `AUD-5`'s work is confined to `GoogleCastOutput.cs` — so no conflict with `AUD-5`.)_ | `fix/reconcile-reported-and-actual-output-device` |
| AUD-5 | **A Cast connection that is no longer current can persist its volume as the system master volume.** **User-visible, silently persistent, and cheap to fix — but ⚠ the obvious fix is the wrong one, so read the "do not widen the lock" note before writing a line.** **The mechanism is verified end-to-end against `main` @ `0870410`; unlike `AUD-2` this row needs no investigation phase, and unlike most of the tranche it must not be re-brainstormed.** `GoogleCastOutput.SyncInitialVolumeAsync` (`src/Radio.Infrastructure/Audio/Outputs/GoogleCastOutput.cs:1543`) is a **read** of the Cast device's status, not a command — but its **success path fires `CastVolumeChanged`** (`:1568-1573`). That event has **exactly one subscriber in the whole solution**: `AudioStateUpdateService.OnCastVolumeChanged` (`src/Radio.API/Services/AudioStateUpdateService.cs:819`; subscribed `:118`, unsubscribed `:935` — grep-verified, no other handler anywhere in `src/` or `tests/`), which **unconditionally sets `AudioManager.MasterVolume`** (`:831`) and `IsMuted` (`:839`). **And that setter PERSISTS.** `AudioManager.MasterVolume`'s setter (`src/Radio.Infrastructure/Audio/Services/AudioManager.cs:74-82`) calls `_preferencePersistence?.ScheduleVolumePersist()` at `:80`, which debounces 500 ms and writes to the config store (`AudioPreferencePersistence.cs:46-62`). **So a stale event does not merely move a slider — it rewrites saved state that survives a restart.** **`IsInitialSync` exists on the event args (`GoogleCastOutput.cs:1829`) and is NEVER BRANCHED ON:** its only two uses in the entire codebase are string interpolation into log messages (`AudioStateUpdateService.cs:834`, `:842`). An initial *status read* is therefore treated identically to the user turning the knob on the Chromecast. **The window.** `SyncInitialVolumeAsync` has a **single** call site — `ConnectAsync:634` — and it runs **after** `TryPublishConnectionAsync` (`:614`) has already validated the connection generation, so at the moment of the call the connection genuinely *is* current. The exposure opens across the `await receiverChannel.GetChromecastStatusAsync()` network round-trip at `:1555`: **nothing re-checks `_connectionGeneration` between the status response arriving and the event fire at `:1568`.** Four sites can bump the generation inside that window — a newer connect's claim (`:539`), `DisconnectAsync` (`:712`), `InitializeAsync` (`:248`) and `DisposeAsync` (`:1720`) — after which the volume of a connection that **has already been superseded** is written to master volume and persisted. **⚠ THE OBVIOUS FIX IS BOTH WRONG AND RISKY — stated first because someone will otherwise reach for it. Do NOT widen `_lifecycleLock` to cover `SyncInitialVolumeAsync`.** The exposure is the **event fire**, not the field read, so widening the lock does not close it — and it would hold the lock across a SharpCaster network call, which is exactly the hang `AUD-3`'s fix was built to avoid. The file says so in as many words at `GoogleCastOutput.cs:49-53`: a connect can park for tens of seconds inside calls that do not observe cancellation, and a teardown queueing behind it converts a data race into a **multi-second hang on the output picker**. **The correct shape is a generation re-check immediately before the fire** — the same check `TryPublishConnectionAsync` already performs at `:675`, applied one step later. **It is cheap to plumb: `myGeneration` is already in scope at the call site** (captured at `ConnectAsync:539`, still live at `:634`), so this is a parameter plus a short await-free critical section, not new machinery. **Second half, and arguably the better one: branch on `IsInitialSync` so a status read is not treated as a user command at all.** The flag was built for precisely this distinction and has never been used for anything but logging. **Decide deliberately and say which way in the plan** — "re-check the generation," "never sync master volume from an initial read," or both; they are separable, and only the first is strictly required to close the race. **⚠ THIS PREDATES PR #468. It is NOT a regression from the Cast synchronization work and must NOT be "fixed" by unpicking it.** All three links — the event fire in `SyncInitialVolumeAsync`, the `IsInitialSync` flag, and `OnCastVolumeChanged`'s unconditional `MasterVolume` write — landed **together** in commit **`b420edc`, 2026-02-11** (*"feat: Fix Cast audio streaming and audio pipeline optimizations"*, whose own message reads *"Add bidirectional volume sync between app and Cast device"*) — **six months before #468.** Verified two ways: `git log -S "IsInitialSync" -- src/` returns `b420edc` and nothing else, and `git merge-base --is-ancestor b420edc 8b1ce0a` confirms the ordering. **#468 created the generation counter this fix will use; it did not create the hole.** **⚠ RECORDED SO IT IS NOT RE-INVESTIGATED: the three unsynchronized sibling reads in this file are NOT this bug, and were already assessed.** `SyncInitialVolumeAsync` (`:1545`), `SetCastVolumeAsync` (`:1632`) and `SetCastMuteAsync` (`:1657`) each null-check `_client` and then dereference it on a second, separate read. **Assessed and found safe** on three grounds: `_client` is **never set back to null** once assigned (exactly two assignment sites, `InitializeAsync` and `TryPublishConnectionAsync`, both non-null; `DisconnectAsync` and `DisposeAsync` deliberately leave it set), every dereference sits inside a `try`/`catch`, and the client-identity race is **self-neutralizing** because `ConnectAsync` disconnects any old client. **PR #473 wrote that reasoning into the file itself** — `GoogleCastOutput.cs:32-104`, specifically the PRECONDITION block at `:63-81` — so it is on the record and needs no re-deriving. **This row's defect is DOWNSTREAM of those reads, not in them:** the read yields a perfectly valid client; what goes wrong is that its *result* is published as a user-authored volume change after the connection stopped being current. **🔵 A HYPOTHESIS TO CHECK — deliberately NOT stated as a diagnosis, and the row does not depend on it.** The owner has been observing volume behaving oddly on this system, and the 2026-08-10 journal carries master-volume values ratcheting down through `0.03` / `0.02` / `0.01` / `6.85E-09`. **Those particular values are attributable to a slider drag during the crash investigation and are NOT evidence for this row — do not cite them as a repro.** But a superseded Cast connection that can write *and persist* master volume is a plausible source of *"my volume changed by itself"* reports in general, and that is worth **checking rather than assuming**. **The forensic signal already exists and costs nothing to look for:** `AudioStateUpdateService.cs:832-834` logs `"Synced volume from Cast device: {Volume:P0} (initial: {IsInitial})"` on every write. Grep journald for that line with `initial: True`, correlate against Cast connect/teardown churn and against play/volume history, and the hypothesis confirms or dies in one pass. **⚠ Do NOT let a negative journald result close this row** — the mechanism is real whether or not it has yet fired in production, and the fix is worth shipping either way. **Test gap, stated because it is total:** `CastVolumeChanged` and `SyncInitialVolumeAsync` have **zero test coverage anywhere in the solution** (grep-verified across `tests/`). `GoogleCastOutputConcurrencyTests.cs` covers the publish race only — its two tests (`TeardownDuringConnect_DoesNotCorruptTheInFlightConnect:35`, `ConnectThatSucceedsButLostTheRace_DiscardsItselfAndStaysUsable:97`) stop **before** the volume sync. **Note the existing seam does not reach this window:** `ConnectRaceHookForTests` (`:120`) fires at receiver resolution, which is **too early** — a test for this must interleave a teardown during the *status read*, i.e. after publish. Whether that justifies a **fourth** `internal` seam is a real question and it is the one `TEST-2` now owns as a pattern — **check `TEST-2` first, and if you do add a seam, record it there.** | 📋 | _plan TBD (small — a generation parameter, one await-free re-check, and an explicit decision on `IsInitialSync`; **the diagnosis is complete and must not be re-brainstormed**)_ | _no spec doc — the diagnosis is in this row_ · **provenance: PR #473's pre-merge review**, which found it while checking whether #473's own comment was true · the mechanism is now documented in-tree at [`GoogleCastOutput.cs:95-104`](../src/Radio.Infrastructure/Audio/Outputs/GoogleCastOutput.cs) · origin commit `b420edc` (2026-02-11) | — _(no row dependency; claimable now. **⚠ Touches `GoogleCastOutput.cs`, which PR #473 (`0870410`) reflowed: its header comment grew by exactly +66 lines, so EVERY anchor below `:32` in that file moved +66.** The citations in **this** row are already post-#473 and verified; any citation copied from the **`AUD-3`** row is pre-#473 and is not. Also touches `AudioStateUpdateService.cs`, which no other row claims. **No file overlap with `AUD-1`, `AUD-2` or `AUD-4`**, so it can run alongside any of them.)_ | `fix/cast-initial-volume-sync-generation-check` |
| AUD-2 | **Confirm-or-close: is SDR gain/ducking silently dead because the source is registered under one key and addressed by another?** **⚠ INVESTIGATE FIRST. This is an unverified inference from a code read, not an observation, and the row may legitimately close with no code change.** **The suspected mismatch.** `SDRRadioAudioSource` mints its own playback key — `_playbackId = $"sdr-radio-{Guid.NewGuid():N}"` (`src/Radio.Infrastructure/Audio/Sources/Primary/SDRRadioAudioSource.cs:908`) — and registers the component under it via `PlayComponentAsync(_playbackId, …)` (`:956-960`), which lands in `SoundFlowPlaybackService._activeComponents[sourceId]` (`src/Radio.Infrastructure/Audio/SoundFlow/SoundFlowPlaybackService.cs:423`, dictionary declared at `:25`). But `AudioManager` addresses the same source by `IAudioSource.Id`, which `AudioSourceBase` derives as `$"{Type}-{Guid.NewGuid():N}"` (`src/Radio.Infrastructure/Audio/Sources/AudioSourceBase.cs:28`) — and `SDRRadioAudioSource.Type` is `AudioSourceType.Radio` (`:204`), i.e. **`Radio-<guid>`**. **Two different keys, with two different GUIDs, for one source.** **What no-ops if it holds:** `_playbackService.SetGainOffset(source.Id, gain)` (`src/Radio.Infrastructure/Audio/Services/AudioManager.cs:292`, and the sibling at `:121`), `SetDuckingMultiplier(source.Id, multiplier)` (`:247`), `SetDuckingMultiplier(_activeSource.Id, multiplier)` (`:479`), and `ClearDuckingMultiplier(_activeSource.Id)` (`:508`, and `:240` on source switch). **Why it would fail SILENTLY, which is the part that makes this worth a row:** none of those methods checks membership. `SetGainOffset` (`SoundFlowPlaybackService.cs:620`) and `SetDuckingMultiplier` (`:643`) write into `_gainOffsets` / `_duckingMultipliers` unconditionally and then call `ApplyEffectiveVolume(sourceId)`, whose two `TryGetValue` lookups (`_activePlayers`, `_activeComponents`) simply **miss** — no exception, no warning. `SetGainOffset` then logs `"Applied gain offset {Gain:F2} to source (SourceId={SourceId})"` at Debug: **the log line asserts a success it never verified.** Same class of trap as AUD-4's mixer log, and worth fixing in the same spirit even if the key mismatch turns out not to exist. **Consequence if confirmed: TTS and event audio never duck the radio source**, and per-source gain never applies to it. Audio ducking is a documented core pattern in `CLAUDE.md` § Architecture ("Audio ducking with priority system (1-10 scale)"), so this would be a significant hole, not a cosmetic one. **First task is confirmation, not repair.** Cheapest check: play SDR radio, fire a TTS event, and observe whether radio volume actually drops; corroborate by dumping the live keys (`SoundFlowPlaybackService` already exposes `_activePlayers.Keys` / component counts around `:734` and `:752`) and comparing them against the `IAudioSource.Id` the `AudioManager` is holding. **⚠ Trap for the fix, if confirmed: do not paper over it by making `AudioManager` translate to `sdr-radio-*`.** The right answer is **one key per source, agreed by both layers** — the same conclusion AUD-4 reaches from the teardown side. Coordinate the two. _**Every anchor in this row re-verified 2026-08-11 against `main` @ `8b1ce0a` and all are byte-exact and unchanged:** `SDRRadioAudioSource.cs:204`/`:908`/`:956-960`, `SoundFlowPlaybackService.cs:25`/`:423`/`:620`/`:643`/`:734`/`:752`, `AudioManager.cs:121`/`:240`/`:247`/`:292`/`:479`/`:508`, and `AudioSourceBase.cs:28` — the last checked specifically because #468 touched that file (its hunk starts at `:97`). **The inference this row is built on is therefore intact post-#468 and still unconfirmed** — nothing in #467/#468/#469 tested it either way._ | 📋 | _plan TBD (**investigate first**; scope depends entirely on the answer)_ | _no spec doc — the diagnosis is in this row_ · `CLAUDE.md` § Architecture (ducking as a core pattern) | — _(no row dependency. **Same root as AUD-4, seen from the ducking side rather than the teardown side — prefer claiming AUD-2 FIRST**: it is investigation-cheap and it decides the key identity that AUD-4's sweep depends on.)_ | `fix/sdr-playback-id-ducking-gain` |
| OPS-2 | **Pin the six floating package versions.** Pure build-reproducibility hygiene. **Pin to the versions that currently resolve — do NOT upgrade anything as part of this row.** Six distinct packages float across **seven** `PackageReference` lines in three project files (`Serilog.AspNetCore` floats in two projects, which is why the line count and the package count differ): `src/Radio.Web/Radio.Web.csproj:23` — `Radzen.Blazor 6.*` → **6.6.4**; `src/Radio.Web/Radio.Web.csproj:24` **and** `src/Radio.API/Radio.API.csproj:35` — `Serilog.AspNetCore 8.*` → **8.0.3**; `src/Radio.Infrastructure/Radio.Infrastructure.csproj:34` — `Serilog 4.*` → **4.4.0**; `:35` — `Serilog.Extensions.Logging 8.*` → **8.0.0**; `:32` — `SoundFlow 1.*` → **1.4.1**; `src/Radio.API/Radio.API.csproj:34` — `Scalar.AspNetCore 2.*` → **resolved version was not recorded on 2026-08-10; read it off the restore graph (`dotnet list package`) rather than picking a number.** **⚠ Note for whoever picks this up, so it is not re-framed as a bug fix: a floating-version theory WAS investigated on 2026-08-10 as the cause of a CI failure and was DISPROVED.** This row is reproducibility hygiene and nothing more; if it is ever cited as the root cause of that failure, the citation is wrong. **Housekeeping trap:** `grep` for `Version="N.*"` across the repo also hits `.claude/worktrees/**` — those are **stale, untracked agent worktrees** carrying copies of the same csproj files and are **not** part of the build. Change only the three files under `src/`. **Verification for this row is a clean restore + full `dotnet test`**, not a visual diff — the point is proving the pinned graph is the graph that was already building. | 📋 | _plan TBD (small; scope is fully enumerated in this row)_ | _no spec doc — the diagnosis is in this row_ | — _(no dependency; claimable now. Best sequenced **after TEST-1**, because its only real gate is a green full-suite run and TEST-1 is what makes that signal trustworthy.)_ | `chore/pin-floating-package-versions` |
| OPS-3 | **`BindsTo=` for `radio-web` — make joint failure actually joint.** **⚠ This is a real change to production service coupling: own PR, owner review, and it is the one row in this tranche that must NOT auto-merge on green gates.** PR #467 made the systemd restart limiter functional — `StartLimitIntervalSec=300` / `StartLimitBurst=5`, correctly placed in `[Unit]` — on both units. It stopped deliberately short of this. **The gap:** `deploy/common/radio-web.service:4` declares `Requires=radio-api.service`, which **does not couple the two lifecycles the way it appears to**. systemd propagates only an **explicit** stop/restart of `radio-api` across `Requires=`; a unit that deactivates **on its own** — exactly what `radio-api` does when it exhausts `StartLimitBurst` and lands in `failed` — is **not** propagated (`systemd.unit(5)`; `BindsTo=` is the dependency type that gives joint failure). **So when `radio-api` trips its limiter, `radio-web` keeps running and serves a UI whose backend is gone.** **The reasoning is already written down in-tree and does not need re-deriving — read `deploy/common/radio-web.service:12-22` first.** #467 states the gap, names `BindsTo=` as the fix, and records why it was scoped out. This row is the follow-through, not a rediscovery. **The tradeoff being accepted is explicit:** the console going dark is a *worse-looking* failure than a UI with a dead backend, and a *more honest* one — that judgement is the owner's to confirm on the PR, which is why this does not auto-merge. **Recovery command worth recording in the runbook**, because it becomes reachable exactly when the limiter starts latching and `Restart=always` will **not** clear a tripped start limiter on its own: `sudo systemctl reset-failed radio-api.service && sudo systemctl start radio-api.service`. **Also in scope:** leave `After=` as-is, and check whether any deploy path assumes the two services restart independently — `Deploy-ToLinux.ps1` restarts them separately today, and `BindsTo=` changes what a `radio-api` failure does to an in-flight web deploy. **Do not deploy or restart anything on the box as part of writing this row's plan;** the unit files are the deliverable, and rollout is the owner's call. | 📋 | _plan TBD (small in diff, large in blast radius — the plan should be mostly test/rollout, not code)_ | [`deploy/common/radio-web.service:12-22`](../deploy/common/radio-web.service) (the gap, already documented by #467) · [`deploy/common/radio-api.service`](../deploy/common/radio-api.service) (limiter rationale) | — _(no row dependency; **PR #467 is merged**, so both `StartLimit*` blocks are on `main` to build on.)_ | `fix/systemd-bindsto-radio-web` |
| AUD-3 | **Close the Cast connect/teardown data race — not just its observable outcome.** **✅ SHIPPED 2026-08-11 in PR #468, merge commit `8b1ce0a` (commits `47da897` "close the Cast connect/teardown data race" + `1fded0c`).** _This row was written as 🔒-on-#468 while #468 was open and scoped to the observable outcome only. The owner then directed the race be closed **before** #468 merged, and it was — so #468's final merged diff is materially larger than the one this row was written against. **The diagnosis below is preserved verbatim as the record of what was wrong; the ✅ VERDICT block after it records what actually landed and what did not.**_ **The race.** The Cast connect path calls `_castOutput.InitializeAsync` / `ConnectAsync` / `SetStreamUrl` / `StartAsync` **without holding `SoundFlowAudioEngine._activeOutputLock`** (`src/Radio.Infrastructure/Audio/SoundFlow/SoundFlowAudioEngine.cs:58`), while the teardown path `TearDownCastOutputAsync` (`:303-330`) manipulates the **same** `GoogleCastOutput` instance. `GoogleCastOutput` has **no synchronisation over its connection state**: `_client` and `_connectedReceiver` (`src/Radio.Infrastructure/Audio/Outputs/GoogleCastOutput.cs:29-30`) are reassigned from at least three places (`:142-150`, `:437-444`, `:514-520`), and the type's only lock is `_debounceLock` (`:48`), which guards something else entirely. Connect and teardown can therefore interleave on an unsynchronised client handle. **⚠ Citation caveat — the method name in the session record only exists on #468's branch.** `TryConnectCastAsync` is `src/Radio.API/Services/AudioEngineInitializationService.cs:499` on `fix/cast-startup-mute-rollback-and-source-teardown`; on `main` @ `c129e0d` the same path is **still inline** at `AudioEngineInitializationService.cs:405-452`. This row is written against the post-#468 shape because that is where the fix lands — **re-site the anchors when you claim it** rather than trusting either set. _**Resolved 2026-08-11:** #468 merged, so the branch-only shape **is** `main` now — `TryConnectCastAsync` is at `AudioEngineInitializationService.cs:499` on `8b1ce0a`, confirmed. The caveat is retained because it is the reason the citation was correct in the first place._ **What #468 does and does not do, stated so its merge is not misread as closing this:** it adds a post-`StartAsync` teardown guard, which closes the **observable outcome** (the engine no longer leaves local output muted behind a Cast connect that did not survive) — **the underlying race is untouched.** **Why this is its own cycle rather than a fix-up on #468:** doing it properly means the connect path runs under the same lock the teardown path uses (or `GoogleCastOutput` grows its own state lock), which **changes the engine's output-coordination API** and ripples into `AttachOutputCoordination` (`SoundFlowAudioEngine.cs:171`) and every caller. **And it wants hardware UAT, not a desk fix** — a real Chromecast, deliberate connect/disconnect churn during startup, and the `/stream/audio/mp3` path exercised while teardown runs. Budget for time on the box. **— END OF THE 2026-08-10 DIAGNOSIS. What follows is the 2026-08-11 verdict, read off the merged diff at `8b1ce0a` rather than off the merge report. —** **✅ VERDICT: fully satisfied on the code. Recorded here so nobody re-derives it.** The fix took the **second** of the two options this row itself named — *"or `GoogleCastOutput` grows its own state lock"* — precisely because the first (running the connect under `_activeOutputLock`) would convert the race into a multi-second hang on the output picker: a Cast connect can park for tens of seconds inside SharpCaster calls that do not observe cancellation. **No lock is held across any network call.** **(1) `GoogleCastOutput` gained `_lifecycleLock` (`:39`) + `_connectionGeneration` (`:45`).** All three reassignment sites this row cited are now inside it — `InitializeAsync` (`:179-190`), the new `TryPublishConnectionAsync` (`:599-623`), and `DisconnectAsync` (`:643-655`) — plus `DisposeAsync` (`:1651-1663`) and a snapshot read in `StopAsync` (`:930-937`). **Six critical sections, all await-free**, all plain field/auto-property swaps (`Name` and `ConnectedDevice` are auto-properties with no side effects — checked, because a setter that re-entered the engine would break the stated lock order). **(2) The NRE mechanism this row named is structurally eliminated, not merely guarded.** The old code stored the resolved receiver in `_connectedReceiver` and dereferenced it after a teardown had nulled it; `ConnectAsync` now resolves into a **local** `ChromecastReceiver? receiver` and snapshots the client, so the long network work operates on values a concurrent teardown cannot touch. Teardown **preempts rather than queues**: `DisconnectAsync` bumps the generation and takes the state out from under an in-flight connect *first*, then does its network work on the snapshot. A connect that lost re-validates at publish, disconnects what it built, and hands the state machine back. **(3) The engine API change this row predicted landed** — `BeginCastConnectAsync` (`SoundFlowAudioEngine.cs:361`) / `TryCommitCastConnectAsync` (`:387`) over `_castConnectEpoch` (`:65`), bumped inside `SetActiveOutputAsync` under the existing `_activeOutputLock` (`:58`, unmoved). The commit **re-checks the epoch and tears Cast down under a single acquisition**, replacing the check-then-act (a `_localFallbackApplied` flag read, then an action) that had a window between the two steps. It did **not** ripple into `AttachOutputCoordination` (`:173`) as this row guessed it would — the epoch handshake sits beside that API rather than through it. **(4) One real defect was found in the race fix itself and fixed before merge (`1fded0c`), and it is worth knowing about because it was worse than the race:** `ConnectAsync` sets `State = Connecting` on entry and the new discard-on-supersede branch returned without resetting it — and `Connecting` is a dead end for this class (`ConnectAsync` requires Ready/Stopped, `ValidateCanInitialize` accepts only Created/Error), so the **first** teardown-during-connect would have made Cast permanently unusable until process restart. Losing that race is the *normal* outcome of the startup auto-connect rolling back, not an edge case. The branch now sets `State = Ready`. **⚠ THREE THINGS THIS ROW ASKED FOR DID NOT SHIP. Do not record them as done.** **(a) The hardware UAT this row explicitly demanded never happened.** #468 added **zero** files under `docs/` — no UAT report, no ADR — and its verification is desk-only: a deterministic `internal` hook plus a real loopback listener, and a substituted transport. No real Chromecast, no connect/disconnect churn on the box, no `/stream/audio/mp3` exercised during teardown. **This is the GV-8 `M-1` situation again** (see that row's ⚠ and § Dependency / ordering notes) — shipped, unit-pinned, **live-unverified**. It is cheap to close opportunistically and needs no cycle: switch the output picker away from Cast while a startup auto-connect is mid-flight and confirm Cast neither wedges at `Connecting` nor streams alongside the soundbar. **(b) The service-level commit call is untested.** `AudioEngineInitializationServiceStartupTests` builds the service with `new Mock<IAudioEngine>()` (`:381`), while **both** epoch call sites gate on the concrete type — `_audioEngine is SoundFlowAudioEngine` at `AudioEngineInitializationService.cs:522` (capture) and `:569` (commit). So `castEpoch` is `null` in **every** test in that file and the commit branch is unreachable there. The engine methods have four direct tests in `SoundFlowAudioEngineActiveOutputTests`; it is the **wiring between them that is unpinned**, and a refactor that dropped either `is` check would pass the whole suite. **(c) Closing the test gap cost two `internal` seams** — `ConnectRaceHookForTests` (`GoogleCastOutput.cs:54`) and `ConnectTransportOverrideForTests` (`:64`), the second added only because the first could not reach the succeeded-then-lost path offline. Genuine design debt, and the **second** time this codebase has bought coverage with a seam — folded into `TEST-2` as a pattern rather than left as an incident. **Two smaller observations, recorded because they are the kind of thing that decays into a bug:** the class comment at `GoogleCastOutput.cs:32` claims `_lifecycleLock` *"guards every read/write of `_client` / `_connectedReceiver` / `ConnectedDevice`"* — **it does not**; roughly twenty reads remain outside it (`:698`, `:710`, `:776`, `:781`, `:800`, `:1044`, `:1173`, `:1230`, `:1479`, `:1566`, `:1591`, `:1622`, …). The design is still sound *only because* `_client` is deliberately never nulled after first assignment — `DisposeAsync` says so in as many words at `:1656-1659` — so the `_client!` dereferences cannot NRE. **But the comment asserts an invariant the code does not hold, and the next person to null `_client` anywhere will believe the lock protects them.** That is the same trap as `AUD-4`'s *"Removed audio source … from mixer"* log. Second: `ConnectAsync` threads the client **snapshot** into `SubscribeToReceiverStatus(client)` — a parameter added by this very fix, documented at `:1436-1441` — and then immediately calls `SyncInitialVolumeAsync()` with no argument, which reads the `_client` **field** at `:1479`. One of three sibling call sites was left on the field. Low severity (post-publish the two agree unless something superseded again) and a one-line change; **noted, not queued.** _**⚠ BOTH of those two closing observations have since resolved — updated 2026-08-11 (second pass), and this is the only edit made to this ✅ row.**_ _**(1) The over-claiming class comment is FIXED.** PR #473 (`0870410`) rewrote it: it now states that the lock serializes the connection-state **swaps** and explicitly says an earlier revision claiming it guarded every read *"never did, and believing it makes the unlocked dereferences look safer than they actually are."* It goes further than this row asked, writing the **precondition** that licenses the unlocked reads (`_client` is never set back to null) directly into the file. **Worth knowing how it was caught, because the lesson is not the one it looks like:** #473's own **first draft of the corrected comment over-claimed in turn** — it said "never a null one," when the field is in fact null before the first `InitializeAsync` — and only a reviewer briefed to actively **falsify** the comment caught that. Reviewing a comment for plausibility is not the same as checking it against the code. **That is now a standing checklist item in `CLAUDE.md` § Pre-Merge Review**, where this row's `AUD-4` cross-reference is cited as one of its three worked examples._ _**(2) The `SyncInitialVolumeAsync()` field-read observation has been superseded by a real defect one step downstream — now `AUD-5`, and the honest note is that this row under-read it.** This row called it "low severity, a one-line change, noted not queued," and **for the field read itself that verdict still holds** — #473's precondition block establishes why the unlocked read is safe. **But the assessment stopped at the read.** What #473's review then found is that the same method's **success path fires `CastVolumeChanged`**, whose sole subscriber sets *and persists* `AudioManager.MasterVolume`, with **no generation re-check between the status response and the event fire** — so a teardown landing in that window persists the volume of a superseded connection. **That is a user-visible, persistent-state defect, not a cosmetic inconsistency, and it PREDATES #468** (`b420edc`, 2026-02-11) — so it is neither this row's residue nor a regression from this row's fix. **See `AUD-5`.** ⚠ **Note the anchor:** the `:1479` cited above is pre-#473; that read is now at `:1545`, along with every other anchor in this row's `GoogleCastOutput.cs` citations, which shifted **+66** uniformly. Those citations have been left as written because they are the historical record of what #468 fixed — **re-site them against `main` before reusing any of them.**_ | ✅ **PR #468** (`8b1ce0a`) | _no plan was written — and deliberately so: the fix landed inside #468 at the owner's direction rather than as its own cycle_ | _no spec doc — the diagnosis is in this row_ · merged evidence: `47da897`, `1fded0c`, `tests/Radio.Infrastructure.Tests/Audio/Outputs/GoogleCastOutputConcurrencyTests.cs`, `tests/Radio.Infrastructure.Tests/Audio/SoundFlowAudioEngineActiveOutputTests.cs` | **PR #468 — ✅ merged `8b1ce0a`.** _The 🔒-on-a-PR deviation this row carried is now closed; it never had to resolve either way, because the blocker and the fix turned out to be the same PR._ | `fix/cast-connect-teardown-race` _(never used — shipped on `fix/cast-startup-mute-rollback-and-source-teardown`)_ |
| AUD-4 | **Unify the two source-removal layers, and rename `SoundFlowMasterMixer` — it is not a mixer.** Two cleanups on one mechanism, deliberately in one cycle because the rename is what stops the bug from recurring. **The misnomer, and the damage it already did.** `src/Radio.Infrastructure/Audio/SoundFlow/SoundFlowMasterMixer.cs:13` is `private readonly List<IAudioSource> _sources = []` — a **registry of source objects**, not the audio mixer. The real audio mixer is SoundFlow's `playbackDevice.MasterMixer`, and the real detach is `SoundFlowPlaybackService.StopAsync` (`src/Radio.Infrastructure/Audio/SoundFlow/SoundFlowPlaybackService.cs:494`) → `MasterMixer.RemoveComponent` (`:526` / `:548`). But `SoundFlowMasterMixer.RemoveSource` (`:109-121`) logs **`"Removed audio source {SourceId} ({SourceName}) from mixer"`** (`:118`), which reads as an audio detach and is not one. **Commit `03a6fea`** ("fix: Source exclusivity, PipeWire BT auto-link cleanup, and default device selection", 2026-02-26) was applied **one layer too high** — at `AudioManager.SwitchSourceAsync`'s `mixer.RemoveSource(oldSource)` (`src/Radio.Infrastructure/Audio/Services/AudioManager.cs:214-217`, which logs the equally misleading `"Removed old source {SourceName} from mixer"`) — **and silently did nothing for months**, because the audio stayed attached one layer down. **The rename is therefore the durable half of this row, not the cosmetic half.** **⚠ ADDED 2026-08-11 — PR #468 changed the ground under this row, and it is the one thing in this tranche nobody anticipated. Read this before writing a plan.** #468 rewrote `AudioSourceBase.StopAsync` (`src/Radio.Infrastructure/Audio/Sources/AudioSourceBase.cs:100-124`): teardown is no longer gated on `State` being `Playing`/`Paused`, only `Created`/`Disposed` are skipped. The reason it gives is **exactly this row's failure class, at a layer this row does not name** — *"a source can hold an attached, audible sound component while its state reads Ready/Stopped/Error … gating teardown on the state flag meant those sources kept producing audio after a switch-away, because `StopCoreAsync` — the only code that detaches the component — was skipped."* **Two consequences.** **(1) There are THREE layers here, not two.** This row names `SoundFlowMasterMixer.RemoveSource` (registry) and `SoundFlowPlaybackService.StopAsync` → `MasterMixer.RemoveComponent` (audio); the third is `AudioSourceBase.StopAsync` → `StopCoreAsync`, whose **entry condition just changed**. Any unification written against the two-layer picture will be unifying two of three while the third has moved underneath it — which is, precisely, how `03a6fea` went wrong. **(2) #468's comment and this row make competing "the only detach" claims and they cannot both be the whole truth.** #468 says `StopCoreAsync` is *the only code that detaches the component*; this row says the real detach is `SoundFlowPlaybackService.StopAsync` → `RemoveComponent` (`:526`/`:548`). Most likely both are locally right about different sources (per-source `StopCoreAsync` implementations vs. the shared playback service), which would mean the detach is **doubly** non-unified — but that is an inference, and **resolving it is the first task of this row, before any code moves.** Do not silently pick one. **Related, and cheap to confirm at the same time:** #468's state-independent teardown may already have closed part of what this row's "no caller left able to do half the job" was aiming at. Say explicitly in the plan what remains rather than assuming the whole scope survived. **Scope:** (a) unify the layers so one call detaches both the registry entry and the audio component, with no caller left able to do half the job; (b) rename `SoundFlowMasterMixer` (and consider `IMasterMixer`, `:10`) to something that says *registry* — the exact name is the implementer's call, but **it must not contain "Mixer"**; (c) fix both log strings to state what actually happened. **⚠ TRAP FOR THE IMPLEMENTER — stated explicitly because it is the exact mistake this row exists to stop repeating: do NOT sweep by `oldSource.Id`.** SDR registers its component under `sdr-radio-<guid>` (`SDRRadioAudioSource.cs:908`) while `AudioManager` knows that same source as `Radio-<guid>` (`AudioSourceBase.cs:28`) — an `Id`-keyed sweep would miss the SDR source and, once again, log success. **Sweep `SoundFlowPlaybackService._activeComponents` (`:25`) instead**, which is keyed by whatever each source actually registered. **Coordinate with AUD-2 — it is the same key-identity problem seen from the ducking side.** If AUD-2 confirms and lands first, adopt its single-key answer here rather than coding around the mismatch a second time. **Budget note:** renaming the interface touches DI wiring and any test doubles, so this is a larger diff than the behaviour change suggests. _**Anchors re-verified 2026-08-11 against `main` @ `8b1ce0a`** and all are byte-exact and unchanged: `SoundFlowMasterMixer.cs:10`/`:13`/`:109-121`/`:118`, `SoundFlowPlaybackService.cs:25`/`:494`/`:526`/`:548`, `AudioManager.cs:214-217`, `SDRRadioAudioSource.cs:908`, and `AudioSourceBase.cs:28` — that last one checked specifically because #468 **did** touch the file, but its hunk starts at `:97`, well below the `Id` derivation._ | 📋 | _plan TBD (medium; the rename dominates the diff, the layer-unification dominates the risk — **and the first task is now the three-layer reconciliation above, not code**)_ | _no spec doc — the diagnosis is in this row_ · commit `03a6fea` is the provenance for the "one layer too high" trap · **PR #468 (`8b1ce0a`) is the provenance for the third layer** | — _(no hard row dependency. **Prefer AUD-2 first** — it decides the key identity this row's sweep must use, and doing them in the other order risks building the sweep around a mismatch that AUD-2 then removes. **Also rebase past #468**, which changed `AudioSourceBase.StopAsync`'s teardown gate — see the ⚠ above.)_ | `refactor/unify-source-removal-and-rename-mixer` |
| TEST-2 | **Close the deferred-capture branch-dispatch coverage gap left by PR #469 — if a native test harness ever becomes feasible.** **This row records a known limit so it is not mistaken for coverage. It may legitimately close as "still infeasible," and that is an acceptable outcome.** PR #469's deferred-capture test reaches the acquisition through the **internal `ApplyDeferredCaptureState()` seam** (`src/Radio.Infrastructure/Audio/Sources/Primary/BluetoothAudioSource.cs:454`, driven from `tests/Radio.Infrastructure.Tests/Audio/BluetoothAudioSourceTests.cs:917`, `:930`, `:954`) rather than by driving the real `capture is AudioCaptureDevice` / `capture is SoundComponent` branches end-to-end. **Why, and it is already recorded in the code:** constructing either type needs a **native SoundFlow `AudioEngine`**, which the method's own doc comment states at `BluetoothAudioSource.cs:449-452` — that constraint is exactly why the seam is `internal` + `InternalsVisibleTo` in the first place. **What IS pinned by #469:** the state decision (a source already `Playing` stays `Playing`; only a not-yet-playing source lands in `Ready`) and its tap consequence — demoting to `Ready` would silently kill fingerprinting while audio keeps flowing. **What is NOT pinned:** the branch dispatch around it — `TryAcquireAudioCaptureAsync` (`:462`) at `:483` / `:494`, plus the two sibling dispatch sites at `:159` / `:166` and `:684` / `:689`. Three places make the same `capture is …` decision and none is covered end-to-end. **⚠ Do not "close" this with a mock that asserts the seam again from a different angle** — that adds a test without adding coverage, and would make the gap harder to see than leaving it open. The only real close is a harness that can produce a native `AudioEngine` (or a genuine integration test on the box). **Lowest priority in the 2026-08-10 tranche** — nothing is broken; the risk is that a future refactor of the dispatch goes unnoticed. **⚠ ADDED 2026-08-11 — this stopped being a one-off, which raises the row's value without widening its scope.** PR #468 (`8b1ce0a`) closed *its* test gap the same way, and needed **two** `internal` seams to do it: `GoogleCastOutput.ConnectRaceHookForTests` (`:54`) to interleave a teardown at the exact point between receiver resolution and the network connect, and then `ConnectTransportOverrideForTests` (`:64`) because the first could not reach the succeeded-then-lost branch offline — a fake socket cannot complete a Cast handshake, so every connect diverted into the error handler instead. **So the codebase now has three `internal`+`InternalsVisibleTo` seams that exist solely because a native/hardware dependency makes the real path untestable** (`ApplyDeferredCaptureState` here, plus #468's two). **What this changes about this row: the feasibility check should ask the general question, not just the Bluetooth one.** If the honest answer is *"native `AudioEngine` construction in tests is not practical,"* then the durable output of this row is **a stated, written convention for when a seam is acceptable and how it must be labelled** — which would also retire `AUD-3`'s residue (c) — rather than a Bluetooth-specific test. That is a legitimate close for this row and a better one than another mock. **Still forbidden, unchanged:** do not close this with a mock that re-asserts the seam from a different angle. _Anchors re-verified 2026-08-11 against `main` @ `8b1ce0a` — all byte-exact and unchanged (#468 did not touch `BluetoothAudioSource.cs`): `:159`/`:166`, `:449-452`, `:454`, `:462`, `:483`/`:494`, `:684`/`:689`, and `BluetoothAudioSourceTests.cs:917`/`:930`/`:954`._ | 📋 | _plan TBD — **feasibility check first**: if a native SoundFlow test harness is not practical, close the row and say so — **and if so, prefer closing it with the seam convention described above rather than with nothing**_ | _no spec doc — the diagnosis is in this row_ · PR #469 (merged) is where the seam and its tests landed · **PR #468 (`8b1ce0a`) is where the second and third seams landed** | — _(no dependency. **Touches the same file as AUD-1** (`BluetoothAudioSource.cs`); if both are in flight, expect line anchors to move.)_ | `test/bt-capture-branch-dispatch-coverage` |
| KIOSK-2 | **Three desktop icons, one repair-and-open action.** Four hand-drifted icons become three, and **Radio Console** becomes the single *make-everything-right* tap: probe `radio-api` / `radio-web` / `rotary-phone` / the GV bridge in parallel (≤2s), start whatever is down in dependency order, then open the kiosk. **⚠ TWO RULES INVERT THE WHOLE DESIGN IF GOT WRONG, and both are the reason this can ever be silent.** (1) **The kiosk browser is the SUBJECT of the action, not a reported component.** The desktop is only visible *when the kiosk is closed*, so the kiosk is **always** down when the icon is tapped — if starting it counted as a repair, the dialog would fire on **every single tap** and the silent path would never happen. (2) **Google Voice auth is dead ~9 minutes in every 20 by design, and that is deliberately NEVER reported.** `available` / `degraded` / `cookiesValid` on `/api/gvbridge/status` are **liars** (captured reading healthy while both SMS endpoints returned hard 502s); `psidtsAgeSeconds` is the only honest field and is a live blackout clock — `<660` healthy · `660–1200` **normal trough, silent** · `>1200` or absent = the refresh cycle itself stopped. A naive "is Google OK?" check answers *no* on ~45% of taps, and **a status surface that cries wolf is worse than no status surface**. Owner approved the **stateless `>1200` probe** (§11 Q3, no Architect escalation) and confirmed the field live: `psidtsAgeSeconds: 310`. When something *did* need fixing, one zenity dialog reports **all four** components using the corpus's existing `[label][value][pill]` grammar and `--signal-*` hex values, in three tiers mapped onto `--info` / `--warning` / `--error` — **no new severity vocabulary is invented** (two new pill *labels* are: `Starting` and `Needs sign-in`, both justified in handoff §9). Started-for-you auto-closes in 10s; degraded waits for a tap; hard failure **holds the kiosk back** and offers `Try again` / `Open anyway`. **Two ordering rules that are easy to get backwards:** on the success path the kiosk launches **before** the dialog (Wayland stacking is decided by launch order, so a dialog raised first is swallowed by the fullscreen kiosk), and on the error path the kiosk is **held back** (the error dialog carries the only explanation plus the retry). Also: `Exit Browser` → **`Exit to Desktop`** (§11 Q1 approved), scoped to the kiosk profile so the GV bridge survives — today's `pkill -f chrome` kills it and nothing restarts it on demand; **`Shutdown System` gains a confirm that never auto-proceeds** (§11 Q2 approved, overriding the earlier "leave as-is") — GNOME's own confirm carries a **60-second countdown and then powers off by itself**, so an accidental tap plus walking away powers the box off anyway; and the **Anderson Console mark finally ships** (`branding/favicon.svg`, unwired until now) to `~/.local/share/icons/radio-console/` referenced by **absolute path** — a theme name is precisely what failed here (`Icon=audio-radio` exists in no installed theme and silently resolved to a generic document glyph). **⚠ The in-app Blazor favicon (`App.razor:9`) is explicitly OUT OF SCOPE** — the owner deferred it to its own pass because `--kiosk` hides the tab strip entirely, so it is never visible on the appliance. Do not "finish the job" by changing it here. **⚠ Cross-repo boundary: `gv-bridge-ensure.sh`, its watchdog and its nightly timer are ROTARYPHONE-OWNED and are being brought under version control in that repo RIGHT NOW.** This row **invokes and probes only** — it must never own, reimplement, edit, install or `pkill` bridge startup; the script is resolved from a candidate list at runtime because the canonical path may move. **Three prerequisites, not polish:** 56px GTK buttons (GTK's ~34px default is under the 48px touch floor — **if the override cannot be made to work, say so rather than shipping 34px**), the `chmod 755` fix (`KIOSK-1`), and re-running `setup-kiosk.sh` **on the box** — merging alone changes nothing. **Testability is designed in because the box cannot be screenshotted** (Shell screenshot API + `GetWindows` are `AccessDenied` on GNOME 46; `gnome-screenshot`/`grim`/`scrot` absent): `radio-console-open` ships `--print-status`, `--dry-run` and `--dialog-demo` so every tier decision is verifiable over SSH, leaving only two checks that genuinely need the owner's eyes. Plan Tasks **B1–B8**. | ✅ | [`plans/.../kiosk-desktop-launcher.md` § Part B](superpowers/plans/2026-08-18-kiosk-desktop-launcher.md) | [handoff](design-handoffs/HANDOFF-kiosk-desktop-launcher.md) | [#480](https://github.com/mmackelprang/RTest/pull/480) | `feat/kiosk-desktop-launcher` |
| ENC-4 | **`EncoderHud` — every knob visible within 100 ms, on every route.** 🔴 **P0, and the defect the whole encoder design handoff exists to fix.** Two of the four knobs currently produce **no visible evidence that anything happened**, and one changes the machine's entire behaviour from an invisible internal counter — a knob that acts silently is worse than a knob that does nothing, because the user's response to silence is to turn it further. Scope: one component, **two hosts** (`EncoderHud.razor` in `MainLayout` for normal routes, again inside `Sleep.razor` with `Variant="Sleep"`, because `/sleep` is on `EmptyLayout` and `MainLayout` is not in that tree). **Geometry is the whole trick** — the HUD divides 1920 px into quarters (centres 240 / 720 / 1200 / 1680) and renders the active knob's card in its own quarter, so something lights up above the knob that was turned. Built from three pieces already in the tree: the **unused** `.snackbar-enter` / `.snackbar-exit` primitives (`design-system.css:1218-1219`), the `GainPopoverService` layout-root hosting pattern, and `SourceBubble`. **No new design tokens** (handoff §6.9 — no `--hud-*` anything). Plus **host-side long-press synthesis** at 600 ms with a ring from 300→600 ms (the protocol has no long-press gesture; this is synthesised from press/release edges), which also moves the short press **from press to release**; and **`ENC-4b`** — turning the volume knob while muted unmutes and applies the delta in the same frame, which the Designer calls "the most important small rule in this document". **`ENC-4a` (the persistent topbar `MUTED` chip) already shipped** as [#493](https://github.com/mmackelprang/RTest/pull/493) and is the pattern to follow for cross-route persistence, **not work to redo**. ⛔ **Does NOT remap the router's index→handler table** (still `0=Volume 1=Tuning 2=Source 3=Visualization` against a cabinet reading VOLUME / SOURCE / PRESETS / TUNING) — index 0 is VOLUME under both, so the dangerous knob is already right, and the remap belongs with `ENC-5`/`ENC-7` which introduce the handlers it would point at. ⛔ Does not build the selector overlays, the three-state wake model, blanking, or the `MEMORY`→`PRESETS` rename. 17 tasks, 6 phases, ~49 new unit/bUnit tests, and a browser Test Plan Tester can drive. | ✅ | [`design/plans/ENC-4-encoder-hud.md`](../design/plans/ENC-4-encoder-hud.md) | [punch list §3.0 `ENC-4`](HANDOFF-GA-PUNCH-LIST.md) · [encoder handoff Rev 3 §6](design-handoffs/HANDOFF-rotary-encoder-mapping.md) | **✅ met** — `ENC-1` [#498](https://github.com/mmackelprang/RTest/pull/498) and `ENC-3` [#511](https://github.com/mmackelprang/RTest/pull/511) both merged | `fix/enc-4-review-findings` |
| ENC-4c | **The `EncoderHud` geometry rotation — `ENC-4` shipped it on the wrong axis.** ⚠ **Not a redesign and not a re-plan: every design question was closed before this row was claimed** (handoff Rev 4 §6.2 corrected the geometry against the owner's as-built drawing; Rev 5 closed the collisions, the centring, the keyframe exception and the phase contract). `ENC-4` divided the **1920 px width** into quarters at `left: 240 + index * 480`, which is right for four knobs in a **row beneath** the display. The drawing puts them in a **vertical column to the LEFT of the LCD**, uniform 29.63 mm pitch, 15 mm diameter, on a 406.4 × 152.4 mm panel — so the axis was wrong and all four constants with it. Cards now anchor to `left: 24px` and band down the **720 px** axis at **90 / 270 / 450 / 630**, the measured projections (93.05 / 271.02 / 448.98 / 626.95) deliberately rounded: 3.05 px of worst-case deviation is 0.508 mm on the panel against a nearest wrong band 178 px away, and *“93.05 reads as a measurement and invites the next reader to re-measure or round it; 90 reads as a layout decision.”* **The bands, the engraved names and the index→knob mapping have ONE definition** — `Radio.Core.Configuration.FrontPanelGeometry`, citing `design/hardware/front-panel-layout_4.svg` — because §6.2 names four surfaces that need them and this repo has already paid once for a value defined in three places. Three mechanics changed with the axis: `margin-left: -180px` has **no vertical twin** (card height varies — measured on the box, 178.5 px volume vs 92.5 px frequency and 113.5 px track), so the card is vertically centred on its band and **clamped ≥ 8 px inside the viewport** — load-bearing, since the measured 178.5 px card centred on band 90 would span y 0.75–179.25, i.e. **off the top of the viewport**, and on band 630 would come within 0.75 px of the bottom; the clamp engages on exactly those two bands and nowhere else, moving the card 7.25 px — expressed on the independent `translate` property so the entrance animation cannot drop it (verified mid-animation on the appliance); `snackbarSlideIn`'s `translateY(100%)` points the wrong way for a left-anchored card, so §6.1's **declared, narrow** mirrored keyframe pair is implemented as declared (⚠ **a Polisher must read `.encoder-hud-enter` as declared, not as drift**); and §6.10's phase contract is **four explicit arms, not three** — an unrecognised phase is *not holding* (it renders nothing, and a true `IsHolding` suspends the dismissal timer and strands a card forever), while **`"Value"` keeps preserving `IsHolding`** or a turn mid-hold collapses the progress ring. ⛔ **Occlusion was decided, not redesigned:** transient occlusion accepted on every band, no position / width / z-order change — per-route suppression and z-order yielding were rejected because they violate the handoff's own principle 2. ⛔ Does not remap the router's index→handler table; that is still `ENC-5` / `ENC-7`. ✅ **SHIPPED** — unit suite green, UAT on the box at 1920×720, pre-merge review clean. | ✅ | [handoff §6 Rev 5](design-handoffs/HANDOFF-rotary-encoder-mapping.md) · [`design/plans/ENC-4-encoder-hud.md`](../design/plans/ENC-4-encoder-hud.md) (geometry sections superseded) | [encoder handoff Rev 5 §6.1, §6.2, §6.7, §6.9, §6.10](design-handoffs/HANDOFF-rotary-encoder-mapping.md) · [`design/hardware/front-panel-layout_4.svg`](../design/hardware/front-panel-layout_4.svg) | **✅ met** — `ENC-4` shipped | `fix/hud-vertical-geometry` |
| ENC-5 | **The SOURCE overlay, the shared selector component, and the router remap.** 🔴 **P0.** The SOURCE knob today moves an invisible counter and logs at Debug; this row gives it a screen. Centred overlay, ~440 px, **preview-then-commit** — ⛔ do **not** "simplify" to live-commit-per-detent, which would tear down and stand up an audio source at every detent, straight into the capture-lifecycle and `autoSwitchOnConnect` bugs. **Press is one rule, not two:** press commits the highlight, so with the overlay closed it commits what is already playing — which changes nothing and opens the overlay showing you where you are. That is what makes a mis-grab free. All five states ship: A preview · B dimmed **with a reason** (reusing `SourceBubble`'s `" · offline"` idiom) · C a press on a dimmed row flashes amber 1.5 s and **leaves the overlay open**, never a silent no-op · **D in-flight spinner · E failure card** — D and E are not optional polish, because an overlay that dismisses into silence is how a person concludes the knob is broken and starts pressing repeatedly, which is the input pattern that provokes the capture-lifecycle bug. **D7 folded the radio bands in and that is where 2–3 d became 5–6 d:** committing a band while radio is *already active* is a **BAND CHANGE, not a source switch** (`SetBandAsync`, no teardown, no spinner, no fade, restore that band's last-tuned frequency); committing one from another source does both and *is* a real switch; the current-marker tracks the active **band**, so on AM row 2 is marked; and the knob and `RadioControlPanel`'s band pills must be one state. ⭐ **This row also carries the router remap** — see the ordering note below; it ends at `0 = VOLUME · 1 = SOURCE · 2 = Visualization (seat-warmer) · 3 = TUNING`. **Five deviations are declared in the plan's §0.4** and every one is a case where the handoff assumed something the tree does not have — most importantly **no tuner reports which bands it supports**, so the plan adds `IRadioControl.SupportedBands` with a default interface implementation. **Two findings correct the row's own text:** `RadioControlPanel` **already** subscribes to `RadioStateChanged`, so the one-state rule is met today and the task is a regression guard rather than a rebuild; and **`ENC-9a` did not remove `VisualizerPanel`'s local `_currentMode`** — it added the missing subscription — so the template is "one authoritative owner + typed broadcast + a component that re-syncs without echoing", not "no component may hold a copy". **Builds the shared component `ENC-7` consumes** — row model, preview machine, overlay, CSS, wire format, and the seven-row window SOURCE does not need. ✅ **SHIPPED** — [#536](https://github.com/mmackelprang/RTest/pull/536). End state `0 = Volume · 1 = SOURCE · 2 = Visualization · 3 = Tuning`; index 2 is `ENC-7`'s. ⚠ **`ENC-7` inherits three things:** the shared `EncoderSelectorOverlay` is consumed unchanged (its only parameter is the payload — adding a `ListKind` would undo "one component, two lists"); the plan's **44 px preset-row modifier was deliberately not built**, because nothing could apply it without that forbidden branch, so add it beside the caller that needs it; and the Settings page's cabinet-mismatch warning is now computed per knob, so `ENC-7` should not need to touch it. ⚠ **Pre-merge review found two HIGH defects** (the State D spinner dismissed itself at 1500 ms mid-switch; the no-tuner band list was cached permanently), both fixed with regression tests. ⚠ **UAT H2 passed — zero new xruns across 1,664 tuner calls — but measured ~52 ms per tuner step**, so an 8-step detent is ~416 ms and a hard flick tunes for ~6 s after the hand stops; logged in `design/FUTURE-WORK.md`, not changed here. | ✅ | [`ENC-5-source-overlay.md`](../design/plans/ENC-5-source-overlay.md) | [rotary-encoder-mapping §4.4 · §6.6 · §6.2](design-handoffs/HANDOFF-rotary-encoder-mapping.md) | **`ENC-4`** (hard — mounts into its seam and edits its files) | — |
| ENC-7 | **The PRESETS knob: recall and save on the existing bank, and the last remap.** 🔴 **P0.** Turn moves a highlight through the saved-station bank and **nothing plays**; press = **Recall** (switch source *and band* if needed, tune, play — **not** scoped to the active source, which is what keeps the knob alive from Bluetooth or Phono); long-press 600 ms = **Save to the next free slot, never overwrites**. Empty state is instructional — `NO STATIONS SAVED` / `hold this knob to save what's playing` — so the knob teaches its own use. ⚠ **The bank is not the shape the punch list describes, and this is the biggest finding: there are no 7 slots.** `RadioPresetService.MaxPresets` is **50** globally; `RadioPreset` has **no** `SlotNumber` and neither does the SQLite table — the slot is a per-band ordinal re-derived per request in `RadioController.GetPresets:585-589`; there is **no next-free-slot logic** because gaps cannot exist (deleting slot 2 of 3 renumbers the survivor). The design survives intact and is simpler — "never overwrites" is free because no overwrite path exists, and `PRESETS FULL` is kept verbatim while being honestly noted as a message that will essentially never fire. ⭐ **But one case the handoff does not cover fires constantly:** holding on a station already saved throws a duplicate error, and silence there is the same defect this arc exists to fix — the plan adds **`ALREADY SAVED · slot NN`** and flags it as an addition to the spec, not an interpretation. ⚠ **Recall cannot use `POST /api/radio/presets/{id}/load`** — it returns 400 `"Radio is not the active source"`, which is exactly the case the knob exists to serve; recall is implemented server-side instead. ⚠ **`IRadioPresetService` is scoped and the router is a singleton** — reach it through `IServiceScopeFactory`, never a constructor injection. ⭐ **The on-screen bank is renamed `MEMORY · n saved` → `PRESETS · n saved`** because the cabinet is engraved PRESETS. **This is a DELIBERATE, DECLARED deviation** from [`HANDOFF-saved-station-display.md`](design-handoffs/HANDOFF-saved-station-display.md), recorded in Rev 3's header, in Designer §4.4 and in punch-list §6 precisely so a later consistency pass does not revert it — **Builder must put it in the PR body and Polisher must not flag it as drift.** One functional line (`RadioControlPanel.razor:249`), one test (`RadioControlPanelTests.cs:835`) and ten stale comments; scope the verification grep with `--glob '!**/bin/**' --glob '!**/obj/**' --glob '!.claude/worktrees/**'` or it reports ~90 false positives from generated XML docs and stale agent worktrees. **Ends the remap at `0 = VOLUME · 1 = SOURCE · 2 = PRESETS · 3 = TUNING`** — matching the escutcheon and what `ENC-11` has pushed to the device since #509. **Consumes `ENC-5`'s component unchanged; the Web side of this row is one string.** | 🚀 | [`ENC-7-presets-knob.md`](../design/plans/ENC-7-presets-knob.md) | [rotary-encoder-mapping §4.3 · §4.4 · §6.6](design-handoffs/HANDOFF-rotary-encoder-mapping.md) | **`ENC-5`** (hard — consumes its component and remaps the index it parks) | — |
| ENC-8 | **The encoder Settings surface, and the stale docs behind it.** 🔴 **P0. `ENC-11`'s tiered fault model works and is verified on hardware — this is the half that lets anyone diagnose it.** Four cards under System Config → Integrations → Rotary Encoders: **Status** (connection, config tier, last verified, last saved-to-device with a staleness comparison); **Device configuration** (read-only, every field × 4 encoders, labelled by cabinet name `VOLUME · SOURCE · PRESETS · TUNING` and **never by index**, each row showing whether the device agrees); **Direction** (four `Reverse` toggles — the one editable thing; toggling pushes immediately and marks the flashed copy stale); **Actions** (`Save to device`, `Re-apply settings`, `Reset counters`). ⛔ **Factory reset is deliberately absent and a test pins it absent** — it would put the device on defaults where one volume detent spans the full range. ⚠ **The `Save to device` copy is load-bearing and a reviewer should treat a mismatch as a defect:** flash holds exactly the operating configuration, i.e. exactly what the screen shows — enforced structurally (screen, push and flash read one object from one call) and pinned by a test, not left to care. Underneath it: an `IRotaryEncoderProvisioning` facet on the same `HidRotaryEncoderService` instance, retained sent/read-back state and timestamps, **the first code in this repo that ever sends `SaveConfig` (0x01)**, staleness decided by comparing a stored SHA-256 of the flashed bytes rather than inferred from a timestamp, and a maintenance channel that writes under a lock while the read loop stays the only reader. The mapping table is served **from the router** so it cannot drift again — one array that both dispatch and display read, which makes `ENC-5`/`ENC-7`'s remap a one-line edit. Plus the doc fixes: `INTEGRATIONS.md:22-26` (wrong 8-byte report format, superseded by `ENC-1`), `:125` (swap-the-A/B-pins advice, superseded by `reverse`), `:80`/`:95` (`TuningStepKHz` — deleted from code by #490, still documented), `:88` (`Enabled` documented as defaulting `false`; it is `true` since `ENC-0`), `:5` (“all integrations are disabled by default”), and `CLAUDE.md`'s MudBlazor claim. `VolumeStepPercent`'s duplicate **editable** numeric is deleted; the value itself is kept (the router reads it every detent) and relocated read-only into card 2. **20 tasks, 5 phases, a hardware gate at Task 4, and a 16-step browser Test Plan at 1920×720.** | ✅ | [`design/plans/ENC-8-encoder-settings-surface.md`](../design/plans/ENC-8-encoder-settings-surface.md) | [punch list §3.5 `ENC-8`](HANDOFF-GA-PUNCH-LIST.md) · [encoder handoff Rev 3 §7.7–§7.8](design-handoffs/HANDOFF-rotary-encoder-mapping.md) | **✅ met** — `ENC-1`, `ENC-2` and `ENC-11` all merged (O10). Not blocked by `ENC-4`, but shares three files with it (`RotaryEncoderActionRouter.cs`, `AudioServiceExtensions.cs`, the `ApiModels.cs` tail) — expect to rebase. | [#527](https://github.com/mmackelprang/RTest/pull/527) |
| ENC-12 | **Tiered config-fault surfacing — giving `ENC-11`'s safety response a voice.** 🔴 **P0, and the row that closes the encoder arc's worst silence.** When a safety field does not read back, the host **already** drops the volume knob's per-event clamp from 6 units to 2 and holds it there until a push verifies. That response is real, correct, verified on hardware — and completely invisible. The owner experiences it as a volume knob that has quietly gone sluggish inside sealed furniture. Two surfaces: **(1)** a cross-route fault badge on the **Settings** nav pill, reusing the bell-failure §3.7 pattern verbatim (same corner, same 12 px glyph) — amber `warning` for Degraded, `--signal-red` `error` for a hard fault, amber `link_off` for absent. **Three glyphs, not one glyph in two colours:** Designer separates the tiers by colour alone, and colour alone fails WCAG 1.4.1 and this project's own §8.3 rule. **(2) One** Radzen notification, **once per session, on escalation only** — *“Knob settings couldn't be applied. The knobs still work, but they may feel wrong.”* / *“Knob safety settings couldn't be applied. Volume is limited until this is fixed.”* The latch is monotonic and **never resets**, so `Degraded → Configured → Degraded` speaks **once**; ten literal tests prove every row of the table. **A healthy boot stays completely silent** — no toast, no splash, no banner. Also adds the `ConfigStatusChanged` event and its SignalR broadcast (**the tier only** — the field detail stays on `ENC-8`'s page, which is the same `UI-2` load argument answering a different question), **resets the tier to `Unknown` on disconnect** (today an unplugged device still reports `Configured`, and the same value drives the volume clamp), and finally consumes `ENC-0`'s `WasEverConnected` flag, which nothing has used since it shipped. ⛔ **Adds no markup to the Settings page and no endpoint to `IntegrationsController`** — the punch list attributes the `Sent` vs `Read back` table and its buttons to this row; they are `ENC-8`'s, decided once in the joint planning pass. **11 tasks, 5 phases, 15-step browser Test Plan.** | ✅ **PR [#535](https://github.com/mmackelprang/RTest/pull/535)** | [`design/plans/ENC-12-config-fault-surfacing.md`](../design/plans/ENC-12-config-fault-surfacing.md) | [punch list §3.5 `ENC-12`](HANDOFF-GA-PUNCH-LIST.md) · [encoder handoff Rev 3 §7.4/§7.6](design-handoffs/HANDOFF-rotary-encoder-mapping.md) · [bell-failure handoff §3.7](design-handoffs/HANDOFF-bell-failure-surfacing.md) | **✅ met** — `ENC-8` [#527](https://github.com/mmackelprang/RTest/pull/527) and `ENC-4` / `ENC-4c` all merged. | — |
| PHN-1a | **ADR-029 PR 1 — the event-playback contracts.** 🔴 **P0, `O6`, and the head of the seven-PR phone arc.** A new **`IEventPlaybackService` in `Radio.Core`, sitting BESIDE `IAnnouncementService` rather than inside it** — the announcement service is fire-and-forget *by signature* (bare `Task`, no identity, one global `StopAsync`) and has two callers that depend on exactly that; the split it encodes is **attended vs unattended**, which is what PR 4 and PR 5 branch on. Plus the **closed discriminated `EventPlaybackRequest` with asymmetric arms** — speech carries literal text, voicemail carries a `(kind, id, duration)` **reference** — and the `EventPlaybackSnapshot` anchor (position + broadcast time + state; **no position tick**, ADR §8.2). Plus `IEventAudioSource` gaining `Position` / `IsSeekable` / `SeekAsync` / `PauseAsync` / `ResumeAsync`, copied verbatim from `IPrimaryAudioSource` and implemented on `AudioFileEventSource` and `TTSEventSource`. ⚠ **NEVER a caller-supplied URL** — that is an SSRF primitive, and "it's a LAN kiosk" is not a defence. Pinned two ways: `Validate` rejects a URL-bearing `MediaId` with a named reason, and a reflection test asserts the request type declares **no** URL-shaped property at all. ⚠ **PLANNING FOUND THE ADR WRONG ON ITS OWN LOW-RISK CLAIM, and it adds a task.** ADR §8.3 says D4 is "a lift" because `FilePlayerAudioSource` "already implements seeking through `SoundFlowPlaybackService`". **`SoundFlowPlaybackService` has no seek method at all**, and `FilePlayerAudioSource.SeekCoreAsync` (`:910-922`) assigns `_position = position` and returns — it moves a field, not any audio, under an `IsSeekable => true` at `:119`. That is a **fourth** instance of the `CLAUDE.md` § Pre-Merge Review comment-accuracy class; a **fifth** is `SoundFlowPlaybackService.GetPosition` returning `TimeSpan.Zero` behind the comment *"Position tracking not available in current SoundFlow API"* — false: `ISoundPlayer.Time` exists in the restored SoundFlow 1.4.1, along with `bool Seek(TimeSpan, SeekOrigin)`. **So PR 1 builds the primitive (Task 4) instead of lifting one; `GetPosition` has zero callers, so the fix carries no blast radius.** `FilePlayerAudioSource` is **NOT** fixed here — live primary path, own UAT, logged to `FUTURE-WORK` instead. ⚠ **Two completion traps the ADR half-missed:** a seek OR a pause makes `AudioFileEventSource`'s wall-clock `Task.Delay(_duration)` (`:205`) fire at the wrong time (ADR §14 Q4 caught only the seek half), and pausing a `TTSEventSource` would make its monitor loop read `!IsPlaying(Id)` as **natural completion** (`:169-175`) — no document mentions this one. Both fixed by re-arming on transport events; **no poll and no timer is added** (ADR §1.3). ⛔ **Do NOT use `POST /api/sources/events/{tts,file}` as a template** — re-verified live at the cited lines: `_duckingService` taken at `:44` and never read, `mixer.AddSource` at `:651` never removed or disposed, `PlayFileEvent` double-plays via `:719` then `:732`. 🎁 **Ships a gift to PR 4:** `DuckingServiceCharacterizationTests` pins that ducking is **binary and transition-only today**, so PR 4's change to it lands as an edited test in PR 4's own diff rather than as a silent shift inside a shared audio service. | ✅ | [`design/plans/PHN-1a-event-playback-seam-contracts.md`](../design/plans/PHN-1a-event-playback-seam-contracts.md) | [ADR-029](../design/decisions/2026-08-03-gv-audio-through-engine.md) D1/D2/D4 · [PHN arc PR breakdown](../design/plans/PHN-arc-pr-breakdown.md) PR 1 · [punch list §3.5 `PHN-1`, §2 `O6`](HANDOFF-GA-PUNCH-LIST.md) · [phone/audio handoff](design-handoffs/HANDOFF-phone-console-audio-and-canned-replies.md) | [#528](https://github.com/mmackelprang/RTest/pull/528) · **None — was the head of the arc.** `O6` makes it the predecessor of `PHN-2`; PRs 2-7 of the breakdown all sit behind it and are **not yet planned**. | `feat/phn-1a-event-playback` |
| OPS-5 | **Stale static assets on the kiosk — `Cache-Control` was absent entirely.** **This is the hole `OPS-1` left in deploy verification, and it silently invalidated any UAT of a styling change.** `Radio.Web`'s `UseStaticFiles()` sent `ETag` and `Last-Modified` but **no `Cache-Control` at all**, so browsers applied heuristic freshness (RFC 9111 §4.2.2) and reused `css/`, `js/`, fonts and the Radzen theme **without revalidating** — the server offered a cheap way to check and never told the client to use it. Measured 2026-09-02 while UAT'ing `ENC-4c`: the kiosk painted a `design-system.css` of 775 rules missing the whole `ENC-4` block, i.e. a copy predating a deploy earlier the same day, and those measurements only became valid after a forced reload. **A CSS-only change could land, pass the SHA check on both services, and still not be on the panel** — `OPS-1` verifies the git SHA stamped into each *assembly*, a true statement about which binary runs and a silent one about which bytes a browser paints. The kiosk relaunch does not help either: Chrome's HTTP cache lives in the profile directory and outlives the process. **Fix:** `Cache-Control: no-cache` on everything `UseStaticFiles` serves, via a testable `StaticAssetCaching.CreateOptions()` (7 unit tests). That is *revalidate before reuse*, **not** *don't store* — the existing `ETag` turns it into a `304` with **0 bytes** (measured; vs 193,069 on a full GET). **Applied uniformly on purpose:** nothing this app links is content-hashed — it is Blazor *Server*, so `_framework/` carries `blazor.web.js` at a fixed path rather than a fingerprinted set, and `_content/Radzen.Blazor/*`, `Radio.Web.styles.css`, `css/*`, `js/*`, `fonts/*` are all stable names (`js/virtual-keyboard.js?v=2` is a *hand-maintained* buster — evidence of someone patching this by hand once). A long `max-age` on any of them is this same bug with a longer fuse; caching hard needs fingerprinting first (`MapStaticAssets` + `@Assets[...]`), a larger change deliberately not taken. `Radio.API` serves no static files and needed nothing. `Deploy-ToLinux.ps1` already deleted the kiosk profile's cache for this reason (since 2026-08-18) — one profile on one box, nothing for a LAN client, nothing under `-NoRestart`; left in place, but no longer what correctness rests on. **Verified on the appliance:** header present on all 6 asset classes; `If-None-Match` → `304` **carrying the header** (so `OnPrepareResponse` runs on the revalidation path — confirmed, not assumed); and the bug itself closed — a CSS probe reached the kiosk's *computed style* over CDP after a plain `location.reload()` **with no cache clear, no restart and no relaunch**, isolating the header as the sole mechanism. | ✅ | _no plan — small operational fix, scoped in this row_ | `CLAUDE.md` § Deployment ("Verifying a deploy actually landed") | — | `fix/static-asset-cache-control` |
| PHN-1b | **ADR-029 PR 2 — `GvMediaClient`, the bounded cache, and API-side auth.** 🔴 **P0, `O6`, second of the seven-PR phone arc.** A typed `GvMediaClient` in `Radio.Infrastructure/External/` that fetches a voicemail recording **server-side** into a size-bounded LRU disk cache at `./data/gvmedia/`, a `GvMediaAuthHandler` that can carry `X-RotaryPhone-Auth` where a browser `<audio>` element never could, and Radio.API's own `GvMedia` config block. **No endpoint, no `EventPlaybackService`, nothing that plays a sound** — PR 3 is the first PR a user can reach. ⚠ **The cache is the load-bearing part and must not be reviewed as an optimisation.** GV auth is dead ~9 minutes in every 20 (`XR-3`) and their `/api/gvbridge/status` reports healthy *during* the blackout, so a replay 30 s later has ~45% odds of 502ing without it. Its correctness properties are: a hit never touches the network, eviction **actually deletes**, and `CacheMaxMegabytes = 0` is a **no-cache** path rather than an infinitely-evicting one (ADR ⟨A1·2⟩). ⚠ **PLANNING FOUND THE ADR WRONG ON THE CLAIM THAT SET THIS ROW'S RISK.** ADR §5.1/§10.1 say Radio.API has *"no `AddHttpClient`, no `IHttpClientFactory`, and no `DelegatingHandler` infrastructure at all"* and the breakdown concludes the handler infrastructure is *"genuinely net-new"*. The grep is right about the **directory** and wrong about the **container**: `Program.cs:102` → `AddSoundFlowAudio` → `AudioServiceExtensions.cs:257` `AddPhoneIntegration` (unconditional) → `:469` `AddHttpClient<PhoneContactLookupService>()`, and `Program.cs:107` → `AddRadioWeather` → two more named clients. `IHttpClientFactory` resolves in Radio.API **today**, and `Microsoft.Extensions.Http` is already a package reference. Genuinely net-new is only the handler **type**, which has a 31-line template plus tests in `Radio.Web`. ⚠ **ADR §5's registration instruction walks into the DI hazard this row exists to close** — *"beside its sibling in `AudioServiceExtensions.cs`"*, but that sibling lives inside `AddPhoneIntegration`, reachable only through `AddSoundFlowAudio`, so registering there buries the feature in the audio graph and any test resolving it initialises real audio hardware. That is exactly why `ActiveSourceAccessorRegistrationTests` can only inspect descriptors, and therefore exactly why **nothing in this repo would catch a missing registration today** — `ValidateOnBuild`/`ValidateScopes` appear **nowhere** in `src/` or `tests/`. **Scoping correction:** a standalone `AddGvMedia` following the `AddRadioWeather` precedent, which is what makes this row's **first real build-and-resolve guard** possible. ⚠ **ADR §5.1 tells this row to copy `PhoneContactLookupService`'s masking, and that file leaks on four lines against one** — raw number at `:62` (Information, **with the contact's full name**), `:78`, `:96-97`, and `:102` (Warning, so it still reaches the journal after `LOG-11`); the one masked line still logs `contact.Name` in full. **A fifth leak nobody had named: exception messages**, which every caller's catch logs. Rule here is *no raw id in a log message, a log argument, or an exception message*, masked as a **hash prefix** (`gvm:1a2b3c4d`) rather than `***last4` — nobody recognises a voicemail id, so a suffix leaks characters for zero operator benefit — pinned by a test that captures every line at every level across success/404/502/timeout/cache-hit. ⚠ **Four decisions PR 1 handed forward are settled here, so they cannot drift again:** `MaxSpeechChars` is **rejection, not truncation** (ADR §4.2 overridden — its own rule is that utterance composition belongs to `Radio.Web`, and a server that speaks less than it was asked while returning 200 is the untruth PR 1 refused when it made `SeekAsync` throw); `IEventAudioSource.SeekAsync` **stays `Task`** (widening breaks D4's verbatim-lift and drags in `FilePlayerAudioSource`, a live primary path — a refused seek is still visible as an anchor that does not move); `Label` gets `MaxLabelChars = 128` in `Validate`, **implemented by PR 3** because it would be unreachable here; and the masking rule above. ⛔ **Do NOT use `POST /api/sources/events/{tts,file}` as a template** — re-verified live at `c830fb8`: `_duckingService` taken at `:44`, never read; `mixer.AddSource` at `:651` never removed or disposed; `PlayFileEvent` double-plays via `:719` then `:732`. ⛔ **Do not touch `DuckingServiceCharacterizationTests`** — PR 4's tripwire, all four green. ⚠ **Deploy trap the ADR's D8 table depends on and does not mention:** `src/Radio.API/appsettings.Production.json` **does not exist**, the per-machine file ships from `deploy/<target>/`, and `Deploy-ToLinux.ps1:217` excludes it from rsync while `:222` seeds it **only when absent** — so a repo edit to it will **not** reach a box that already has one. Defaults therefore go in `src/Radio.API/appsettings.json` (which *is* overwritten), and flipping `AuthKey` on a live box is a documented hand edit plus a restart. | ✅ | [`design/plans/PHN-1b-gvmedia-client-cache-and-auth.md`](../design/plans/PHN-1b-gvmedia-client-cache-and-auth.md) | [ADR-029](../design/decisions/2026-08-03-gv-audio-through-engine.md) D3/D8 · [PHN arc PR breakdown](../design/plans/PHN-arc-pr-breakdown.md) PR 2 · [punch list §3.5 `PHN-1`, §2 `O6`](HANDOFF-GA-PUNCH-LIST.md) · [`PHN-1a` plan](../design/plans/PHN-1a-event-playback-seam-contracts.md) §0.4/§5 — **authority over the ADR wherever they disagree** | **`PHN-1a`** ✅ [#528](https://github.com/mmackelprang/RTest/pull/528) — merged, so this row is claimable now. Blocks PR 3 (`EventPlaybackService` + `POST /api/audio/events`), which consumes `GvMediaClient` and `GvMediaOptions`. `O6` still governs the arc. | ✅ [#534](https://github.com/mmackelprang/RTest/pull/534) — merged |
| ENC-6 | **Sleep, wake, and the three-state model — the non-blanking half.** 🔴 **P0, the last unplanned one, and the row that closes the overnight failure mode.** ⚠ **THE BLANKING HALF IS WITHDRAWN AND MUST NOT BE REINSTATED.** `ENC-15` was its hard predecessor and **its gate FAILED** on 2026-09-02 ([report](uat/2026-09-02-enc15-touch-wake-gate/REPORT.md)): the touchscreen is powered by the panel and **leaves the USB bus when it blanks**, so touch cannot wake it — there is no device left to generate the event — and `cafe:4005` has **zero evdev nodes**, so a knob cannot wake the compositor either. That is one *application-mediated* wake path where two were required, and the investigation **reproduced the brick** (a ~13 s on/off oscillation the documented recovery line could not break). **So this row builds NO DPMS, no `Ambient-Dark`, no `Standby-Dark`, no re-blank timers and no encoder-presence coupling rules.** Designer Rev 5 §8.5 and §15's first and last *Sleep, wake, blanking* checkboxes are out of scope. If a later session reads “ENC-6” and reaches for blanking, `design/INTEGRATIONS.md` §1 and `design/FUTURE-WORK.md` §7 (Sleep Mode) both carry the reason it stays off. <br><br>**What ships — and the five-state model does still cohere as three.** Rule 1 (“if the panel is dark the first input lights it”) governed *only* the two dark states, so it drops out whole; Rule 2 governs all three survivors and §8.3 already specifies both remaining columns completely. **(1) The headline defect, and it is current behaviour:** idle-at-30-min navigates to `/sleep` **without calling `SleepService`**, so `IsSleeping` stays `false` and the API never learns the clock is up. Fixed by **`Sleep.razor` reporting itself** through a new `POST /api/system/sleep-screen` — which also collapses the finding a Builder confirmed today while shipping `ENC-4`, that **`/sleep` reached by idle and `/sleep` reached by the Sleep pill are different states**. ⚠ **Do NOT “fix” this by making `idle-dimmer.js` call `SetSleepAsync(true)`** — Ambient is *defined* by audio still playing, so that converts it into Standby. **(2) The wake latch.** `TryWakeFromSleep` calls `WakeAsync` fire-and-forget and returns `true`, so a 10 ms poll discards every following event; a synchronous `TryClaimWake()` makes the state read `Awake` from the claim instant. Under the new model the window is **worse** than the handoff assumed — leaving Ambient needs a *browser round trip*, not just an await. *A fast spin must lose one detent, not twelve.* **(3) D22, settled scope, not an open question:** from Standby a **turn** lights nothing new and **does not resume audio**; a **press** (any button) or a **screen tap** does. Owner approved Designer's narrowing verbatim — *“that D8 narrowing is fine, keep it.”* **(4) Rule 2:** VOLUME **acts in place** on the lit clock and does not change screens; everything else wakes to the full UI and is consumed. **(5) The refinement that turns a loss into an answer:** a consumed input still renders that knob's **current value** — *the first detent tells you where you are, the second one moves it* — which is also what stops three knobs looking broken for a whole standby, since D22 makes every turn there consumed. Plus the Standby hint, closing the `TODO(ENC-6)` `Sleep.razor:113-116` has carried since `ENC-4`. <br><br>⚠ **Three things a reviewer should check rather than assume.** **(a)** This row **does not depend on `ENC-5`/`ENC-7` and is not blocked by them, in either order** — the gate runs *before* the dispatch tables and the only index it compares is `0`, which is VOLUME under both mappings (already pinned by `EncoderIndexZero_IsVolume_UnderBothTheOldAndTheNewPhysicalOrder`); the consumed-value readout is a **fourth parallel array beside the two `ENC-5` Task 7 already reorders**, index threaded in so no literal is left behind. If `ENC-5` lands first, re-read the router — its handlers gain a leading `int index` and **the code wins**. **(b)** Two shipped assertions **invert and must be updated, not deleted**: `WakeConsumesThePressEdge_…`'s `Assert.Empty(h.Hud.Published)` (a consumed input now publishes, by design) and `SleepService`'s class doc, which claims DPMS control the code has never performed — a **fourth** comment/code mismatch of the class `CLAUDE.md`'s pre-merge rule exists for. **(c)** `dotnet test` **cannot** cover the new HTTP round trip: `AddHermeticTestRig` fails every outbound call by design, so a null result is the *expected* state — exactly how `ENC-8` shipped a page that could not deserialize its own response with a green suite. **UAT scenario B is the only gate on it, and the plan's Test Plan exercises both `/sleep` entry paths separately** because a run through the pill produces a false pass for the idle path. <br><br>📋 **PLANNED 2026-09-02** — [`design/plans/ENC-6-sleep-wake-state-model.md`](../design/plans/ENC-6-sleep-wake-state-model.md), **10 tasks** across 4 phases, **2 declared deviations** (§0.4) and **3 mechanism decisions** the handoff does not make (§0.3). | 📋 | [`ENC-6-sleep-wake-state-model.md`](../design/plans/ENC-6-sleep-wake-state-model.md) — 10 tasks | [punch list §3.5 `ENC-6`](HANDOFF-GA-PUNCH-LIST.md) · [encoder handoff **Rev 5** §8](design-handoffs/HANDOFF-rotary-encoder-mapping.md) · [`ENC-15` gate report](uat/2026-09-02-enc15-touch-wake-gate/REPORT.md) — **withdraws the blanking half** | **✅ met** — `ENC-1` [#498](https://github.com/mmackelprang/RTest/pull/498) and `ENC-4` [#519](https://github.com/mmackelprang/RTest/pull/519) / `ENC-4c` [#526](https://github.com/mmackelprang/RTest/pull/526) all merged. **`ENC-15` was the hard predecessor of the blanking half only, and that half is out of scope.** Not blocked by `ENC-5`/`ENC-7` in either order, but **shares `RotaryEncoderActionRouter.cs` with both** — expect to rebase. | — |
| TEST-4 | **The BT capture watchdog's flaky test — a wall-clock test window racing an independent wall-clock poll loop.** `BluetoothCaptureWatchdogTests.IntermittentStall_ResetsCounter` failed `Build and Test` on `main` at `2a81f56` and passed on `c3b62d8` with no change to the file: timing-sensitive, not a regression. ⚠ **"It passed on the retry" was not the resolution** — per `TEST-1`/`TEST-3` and criterion (d), a randomly-red suite trains everyone to re-run rather than read, and five Builders were merging into `main` the day it surfaced. **The root cause is entirely in the test; the production watchdog is correct** and was not changed — which matters, because `AUD-8`'s watchdog guards a real long-uptime capture failure and this repo has a documented long-running capture-lifecycle bug. The test sequenced three phases with `await Task.Delay(60)` while `BluetoothCaptureWatchdog` polled the snapshot source on its own independent `Task.Delay(20)` tick, **with no rendezvous between the two clocks**. Nothing guaranteed the watchdog ever observed the middle, below-threshold phase — the one whose entire job is to reset `_consecutiveStalledChecks`. **Reproduced at 13/200 under 64 concurrent CPU burners on a 32-core host**, and the poll log names the mechanism exactly: the whole phase-2 window (`t=60ms → t=125ms`) elapsed **without a single poll** — ticks stretched to 42–91 ms, the reset value was set and replaced unobserved, and phase 1's two stalled polls concatenated with phase 3's three into the five consecutive checks that then **correctly** fired. Given the polls it actually received, raising was the right behaviour. **Fixed test-only.** The fake `ICaptureStreamSnapshotSource` is now a **rendezvous gate**: the watchdog parks on entry to every `GetCaptureStreamSnapshot()` until the test grants that poll, so a test states how many polls observe each value and every assertion runs while the watchdog is frozen before its next read. Polls are counted, not timed; `WatchdogTickIntervalMs` now affects only how fast the tests run, never whether they pass. **Verified 200/200 at the same 64-burner load that produced the 13/200 failure rate** — the apples-to-apples comparison — plus 200/200 and 360/360 (all six tests) at 40 burners: **760 executions under load, zero failures**, against load severe enough to slow each iteration ~12x. ⚠ **No box UAT is owed and none was run** — this is test infrastructure with no user-visible surface; `build.yml` is the gate. **Sibling tests:** three shared the shape but only in the *safe* direction (starvation could only remove polls, never flip an assertion red); they are now exact rather than "at least" assertions. **Two further defects found and fixed while there.** (1) `NullSnapshotSource_ExitsImmediatelyWithoutRaising` documented that it *"verifies the watchdog idles (never enters its main loop)"* while asserting only `ConsecutiveStalledChecksForTest == 0` — which **reads 0 on both paths**, because a null snapshot resets the counter every tick. The doc claimed a verification the assertion did not perform: a sixth instance of the `CLAUDE.md` comment-overclaim class. It now asserts `ExecuteTask` reaches `RanToCompletion`, which is what actually distinguishes the early return from the loop. (2) `AboveThreshold_RaisesAfterConsecutiveChecks` asserted `RaiseCount >= 1`; it now pins the exact count, the one-short boundary, and the post-fire reset. **The pattern is recorded in `CLAUDE.md` → "Test Timing — never race a wall clock against a wall clock"**, because it is not a one-off: the same shape is live in `Radio.Web.Tests` (see the `NowPlayingPanel` fast-follow below). | ✅ | — _(Builder-discovered; no Planner spec — the diagnosis is in this row and in the test file's class-level `<remarks>`)_ | `CLAUDE.md` → **Test Timing** · `tests/Radio.Infrastructure.Tests/Audio/Services/BluetoothCaptureWatchdogTests.cs` `<remarks>` | — _(no dependency; test-only, touches no production file)_ | `fix/flaky-bt-capture-watchdog-test` |
| PHN-5 | **`PhoneContactLookupService` logs raw phone numbers on four lines and the contact's real name on the one line it calls masked.** ⚠ **This is a PII-in-logs defect, not a style issue** — filed by `PHN-1b` rather than fixed there, because it is a different subsystem with its own callers and deserves its own reviewable change. Verified against the tree, `src/Radio.Infrastructure/External/PhoneContactLookupService.cs`: **`:62`** `LogInformation("PBAP contact found: {Number} → {Name}", phoneNumber, contact.DisplayName)` — raw number **and the contact's real name**, at **Information**, which since `LOG-11` persists to the file sink at `/opt/radio-console/logs/radio-*.txt`; **`:78`** `LogDebug("Looking up contact for {PhoneNumber}", phoneNumber)` — raw; **`:96-97`** `LogDebug("Contact lookup returned {StatusCode} for {PhoneNumber}", …, phoneNumber)` — raw; **`:102`** `LogWarning(ex, "Contact lookup failed for {PhoneNumber}", phoneNumber)` — raw, at **Warning**, so it is one of the few that still reaches `journalctl` after `LOG-11`, **and it logs `ex`**, so any exception whose `Message` carries the number leaks a second way. The single line the file treats as masked, **`:90`**, masks the number to `***1234` but still logs **`contact.Name` in full** — and ADR-029 §5.1 asks for *"voicemail ids **and caller identity**"* to be masked. So the count is **four raw-number sites against one masked, and the masked one leaks the name anyway.** ⚠ **ADR-029 §5.1 cites this exact file as the masking *example* to copy**, which is how the pattern propagates; `PHN-1b` refused the citation and masked as a hash prefix on every line instead, pinned by a test. **Exposure, stated honestly rather than inflated:** the only caller of `FindCallerNameAsync` is `Radio.API/Services/PhoneCallIntegrationService.cs:121`, whose `ExecuteAsync` returns at `:44` when `PhoneIntegration:Enabled` is false — and it is false and has never been true. So this is **latent today and becomes live the moment that flag flips**, which is a one-line config change with no code review between it and the leak. ⚠ **A second, smaller instance for the same Planner pass to scope in:** `src/Radio.Web/Services/ContactResolutionService.cs:173` logs a raw number at **Debug** and is **not gated at all** — it is injected into `PhoneMessagesPanel.razor`, the live `/phone` texts surface. Debug is normally below the configured minimum level, so it likely does not emit today; that is a level away from leaking, not a design that prevents it. **Fix shape (~15 lines):** compute the masked form once at method entry and use only it; mask `contact.Name`/`DisplayName` the same way; do not pass `ex` where its message can carry the number. | 📋 | _none — needs a Planner pass_ | [ADR-029](../design/decisions/2026-08-03-gv-audio-through-engine.md) §5.1 (the citation that propagates it) · [`PHN-1b` plan](../design/plans/PHN-1b-gvmedia-client-cache-and-auth.md) §0.3 ⓸ (the count, and the masking rule that replaced it) · `design/FUTURE-WORK.md` | **None.** Independent of the ADR-029 arc — `PHN-1b` only *found* it. Best done before `PhoneIntegration:Enabled` is ever flipped. | — |
---

## Dependency / ordering notes

- **GV-1 is the foundation.** GV-2 and GV-3 both depend on GV-1 (DTOs, the extended `GvBridgeApiService`, the new `PhoneHubService` events, `GvBridgeStatusService`, `PhoneUnreadState`, and the `PhoneMessagesPanel` shell). Builder must merge GV-1 before claiming either.
- **GV-2 and GV-3 are independent of each other** and may ship in either order after GV-1. **Recommended order: GV-1 → GV-2 → GV-3.** One shared coupling: GV-2 introduces the `FeedItem` interleave projection (PR2 Task 5) that GV-3 reuses; if GV-3 is built first, its plan instructs the implementer to introduce that projection there instead.
- **⚠ CORRECTED 2026-07-31 — the read experience is NOT functional end-to-end.** This bullet previously claimed "read experience ships fully today." **That claim was wrong** and is retracted. Our **client** work (GV-1/GV-2/GV-3/GV-4) is complete and correct against the documented contract, and it renders correctly given data — but **the server has never returned any.** RotaryPhone's `PositionalGvThreadParser.ThreadsArray()` requires a JSON **object** root with a `"threads"` property, while the request is built as positional protojson (`alt=protojson`), which returns **arrays**. The guard returns `null` on every real response → **0 items** → `Succeeded: true` (which only ever meant "the JSON parsed") → a clean, empty **HTTP 200**. So **voicemail and SMS read have never returned data against live Google Voice.** Their tests pass because every fixture hand-feeds the parser the synthetic shape it assumes, and their ADR admits the positions were synthesis with a §11 live-verification checklist that was never performed. Defect is in **`rp-deploy`**; a capture-then-fix cycle is underway on their side. **Nothing to build here** — our side needs no change — but the honest status is **"read UI complete, read data blocked upstream,"** not "shipped." **✅ SUPERSEDED 2026-07-31 (later the same day) — their parser fix (`627b928`) landed and read data IS now flowing**: a [live UAT](uat/2026-07-31-gv-live-data/REPORT.md) rendered 20 real SMS threads and 20 voicemails with real transcripts. **Current honest status: the data arrives; the read *experience* has three defects layered on top of it** — UAT F-1, split into **GV-8** here plus two RotaryPhone items (§ Cross-repo handoffs #5/#6). The correction above is left in place rather than deleted: it was right when written, and the sequence *"shipped" → "never worked" → "works, with new defects"* is itself the useful record.
- **Send status.** **`POST /api/gvbridge/sms/send` has SHIPPED on RotaryPhone** (dark behind their `GVBridge:EnableSmsSend=false`), so the endpoint is not the blocker. **Send is scoped as GV-5** (ADR-028), now **🔒 blocked** pending re-derivation against the deployed tree — see the row. GV-5 makes send *correct*; flipping `RotaryPhone:Gv:SendEnabled=true` stays a separate two-side decision afterwards (theirs first, then ours).
- **GV-8 and GV-6 are the same bug class — and are deliberately NOT merged.** Both are "`GvBridgeApiService` maps every non-2xx to `null`, destroying the distinction the caller needs." **Judgement call, recorded so it can be revisited rather than rediscovered:** they are kept as two rows because they differ on everything except that root shape — different methods (the two *mark* methods vs. the thread-*read* method), different dependencies (**GV-4** vs. **GV-3**), and different severity and audience (**GV-6 is diagnostic quality for operators with no user-visible affordance by design**; **GV-8 is a HIGH, user-visible correctness bug that currently lies to the user**). Merging would couple a HIGH fix to a low-priority operator nicety and would saddle GV-8 with an unnecessary dependency on GV-4. **What they should share is the idiom, not the PR:** whoever ships first introduces the outcome/status discrimination in `GvBridgeApiService` in a **reusable** shape, and the second row adopts it instead of inventing a second mechanism. **Preferred order: GV-8 first** — it carries the higher value and establishes the pattern; GV-6 then conforms to it. _If a Builder finds the shared shape is doing all the work and the two remainders are trivial, merging them is a defensible call — but say so explicitly rather than quietly._
- **⚠ UAT discipline for the `/phone` texts surface — record wall-clock time, or the results are noise.** Until RotaryPhone's cookie-refresh fix lands (§ Cross-repo handoffs #6), GV auth is **dead for ~9 minutes out of every 20**, on a wall-clock cycle that is independent of what the tester does. **Any UAT of this surface that does not note the time will produce results that look random** — that is exactly how the 2026-07-31 pass came to hypothesise throttling, a hypothesis the logs then falsified. **Test within the first ~10 minutes after a `CDP cookie refresh` log line, and record the timestamp in the report.** Probe, kept bounded because the Intel N100 does not tolerate heavy journald reads well: `journalctl -u rotary-phone --since '-30min' | grep 'api2thread/list returned'`. The matching client-side probe is `journalctl -u radio-web --since '-30min' | grep 'Failed to get GV SMS thread'`. **Do not tail either.**
  - **✅ BETTER PROBE, twice-confirmed (2026-07-31 root-cause pass + GV-8 UAT) — prefer this over the journal reads above.** `curl -s http://192.168.86.50:5004/api/gvbridge/status` returns **`psidtsAgeSeconds`**, which is a **live blackout clock**: **`< 660` healthy · `660–1200` blackout · resets at ~1200.** One request, no journald load, and it tells you the phase *before* you spend a test case. **It is also the only trustworthy field in that payload** — `{"available": true, "degraded": false, "cookiesValid": true, "psidtsAgeSeconds": 707}` was captured while both SMS endpoints were returning hard 502s. Full write-up in `design/INTEGRATIONS.md` § Google Voice (gvbridge) Messages → Gotchas. **Also note the box is at `192.168.86.50`; `radio`/`piradio` do not resolve from WSL** (now recorded in `CLAUDE.md` § Deployment — two sessions rediscovered it the hard way).
- **⚠ OUTSTANDING VERIFICATION — GV-8's `M-1` shipped but has never been observed live. It is NOT a pass, and nothing downstream should treat it as one.** The fix is the branch ordering at `PhoneTextsPanel.razor:61` — `else if (Error && Messages == null)`, i.e. **arriving content outranks a stale error flag** — so an inbound SMS landing on an open-but-*failed* thread renders the message instead of leaving the user on `cloud_off`. **What is proven:** the condition is in the shipped binary, the four-way ordering (skeleton → error → empty → list) was observed live in all four states, and `PhoneTextsPanelTests:141` (`Conversation_ShowsMessages_WhenErrorSetButMessagesArrived`) covers it at the unit level. **What is not:** the live transition itself. It requires a **real inbound SMS to arrive while a failed thread is open** — no inbound traffic arrived during the GV-8 UAT window, and the Tester **declined to inject a synthetic message**, which was the right call: a manufactured message would have proven the render path, not the arrival path. **Closing it costs nothing and needs no Builder cycle** — with the failed thread open during a blackout (`psidtsAgeSeconds` between 660 and 1200, per the probe below), have anyone text the GV number and watch the pane flip from `cloud_off` to the bubble. **Whoever does it: record it in the GV-8 UAT report and strike this bullet.** Until then, "GV-8 passed 10/10" means **ten cases plus one that could not be run**, exactly as its report states.
- **Playwright MCP does not work in this WSL instance — deliberately NOT a queue row; recorded here so the next UAT does not re-derive the workaround.** This is environment/tooling, not product: neither candidate fix is a PR against this repo, so there is nothing for a Builder to claim. **The failure:** the plugin is configured for the `chrome` channel and resolves `/opt/google/chrome/chrome`, which does not exist here; installing it needs root and there is **no passwordless sudo** on this WSL instance. **The substitute that worked (GV-8 UAT), reusable as-is:** Chromium **151.0.7922.10** from the Playwright bundle (`npx playwright install chromium` — **no root required**), with `libnss3` / `libnspr4` extracted from Ubuntu `.deb`s into a scratch dir and picked up via `LD_LIBRARY_PATH`; driven over **CDP** from `playwright-core`. **Nothing was installed system-wide and nothing outside the scratchpad was modified.** Two things this bought beyond mere parity, worth knowing before anyone "fixes" it back to the MCP: raw CDP is what made the `MutationObserver` animation probe and the frame-level screencast possible, and those are the only reason `L-1` could be stated as a measurement (`Animation.currentTime`, 14.5 % pixel delta) rather than an impression. **Also note the viewport must be asserted in-page** (`window.innerWidth/innerHeight`), not assumed from a launch flag — the screencast run came back **1920×577**, not 1920×720. **The permanent fix is an owner decision, not a Builder task:** either install Chrome on the WSL box, or point the plugin at `--browser chromium`. **The plugin config was deliberately left untouched** — it is the user's environment, and a Tester silently editing it would be exactly the kind of unrequested config change this project's agents are told not to make.
- **GV-5 is claimable now and is independent of GV-4.** It touches `GvBridgeSendService`, `PhoneTextsPanel`, `MessageBubble`, `PhoneHubService`, and `PhonePage`. The only overlap with GV-4 is `PhoneHubService` (a sibling `.On<>` registration) and `PhonePage` (a sibling handler beside `OnGvSmsReceived`); GV-4 is merged, so there is no conflict — but a Builder rebasing an old branch should expect both files to have moved.
- **The 2026-08-10 tranche (`TEST-1`, `AUD-1…4`, `OPS-2`, `OPS-3`, `TEST-2`) has a priority order, and the order is not arbitrary.** These eight rows are the deferred remainder of one debugging session, so nothing forces them into sequence the way the GV arc is sequenced — but the ranking is deliberate and should be respected absent a reason. **`TEST-1` first, and by a wide margin.** It is not the most user-visible row; it is the row that makes every other row's verification mean something. Four tests flaked under load on 2026-08-10 and the flake was **read as a real regression before it was recognised as noise** — an unreliable suite does not merely waste a cycle, it produces confidently wrong diagnoses. Every other row in this tranche is gated on a green full-suite run, so clearing `TEST-1` first is the cheapest way to make seven downstream verdicts trustworthy. **Then the correctness rows** — `AUD-1` (an active metadata defect with a booby-trapped "obvious fix"), `AUD-2` (potentially a silently dead core pattern), `OPS-2`, `OPS-3`. **Then the deferred four** — `AUD-3`, `AUD-4`, `TEST-2` — which are real but none of which is currently lying to a user. **The owner set this ranking; do not reshuffle it without asking.** _**Updated 2026-08-11 — status change only, ranking untouched.** `AUD-3` shipped inside PR #468, so the tail of this order is now **`AUD-4`, `TEST-2`** (the "deferred four" only ever listed three — a pre-existing miscount, left as written). **`TEST-1` remains first and nothing else moved.** Both remaining tail rows gained scope-shaping notes from #468 — `AUD-4` a third teardown layer, `TEST-2` two more internal seams — which **raises what each has to reckon with without raising its rank.**_ _**Updated 2026-08-11 (second pass) — one row ADDED into the correctness band; nothing existing moved.** **`AUD-5` slots immediately after `AUD-1`**, making the order **`TEST-1` → `AUD-1` → `AUD-5` → `AUD-2` → `OPS-2` → `OPS-3` → `AUD-4` → `TEST-2``. **`TEST-1` is untouched at the top — the owner set that and this pass did not go near it**, and `AUD-1` keeps its position for the same reason. **Why `AUD-5` lands where it does, stated so it can be argued with rather than rediscovered.** It ranks **above `AUD-2`** on three counts: its mechanism is **confirmed by construction** and needs no investigation phase, where `AUD-2` is an explicitly unverified code-read inference that may close as "no defect"; it is **user-visible** in the most direct way in this tranche (the volume changes and stays changed); and it is the **only row here that can silently write wrong state to *persistent* config**, so its blast radius outlives the process. It ranks **below `AUD-1`** because `AUD-1` is an *observed, currently-firing* metadata defect with a booby-trapped obvious fix, while `AUD-5`'s mechanism is proven but its firing in production is still a hypothesis. It is also **the cheapest fix in the band** — a parameter, one await-free re-check, and one deliberate decision — which is a tie-breaker, not the reason._ _**Updated 2026-08-11 (third pass) — two rows ADDED as a pair; again, nothing existing moved.** **`AUD-6` then `AUD-7` slot immediately after `AUD-1` and above `AUD-5`**, making the order **`TEST-1` → `AUD-1` → `AUD-6` → `AUD-7` → `AUD-5` → `AUD-2` → `OPS-2` → `OPS-3` → `AUD-4` → `TEST-2`**. **`TEST-1` is untouched at the top — the owner set that and this pass did not go near it.** **Why the pair lands above `AUD-5`, stated so it can be argued with rather than rediscovered.** `AUD-5` sits where it does partly because *"its mechanism is proven but its firing in production is still a hypothesis"* — **that qualification does not apply to either of these.** Both were **measured on the box today**: `AUD-6` by comparing two days' enumeration against an unchanged stored value, `AUD-7` by comparing `/api/devices/output` against live PipeWire sink state. They share `AUD-5`'s persistent-state blast radius (`AUD-6` is *worse* on that axis — the stored value does not change, its **meaning** does, so no inspection of the store can detect it) and they exceed it on user-visibility: a volume that moves is noticeable and correctable, whereas an output selector that names the wrong device is **actively misleading about where sound is going** and gives the user no way to tell. **Why they land below `AUD-1` — and this one is discipline, not severity.** A case can be made that `AUD-7` outranks `AUD-1` outright: misrouted audio is a larger failure than a rewritten track title. **`AUD-1` keeps its position because this file's convention is that a pass adds rows and does not reshuffle the owner's ranking**, and because `AUD-1` is fully scoped and cheap, so nothing is gained by displacing it. **If the owner wants `AUD-7` above `AUD-1`, that is a defensible call and needs only a word — it was not taken unilaterally.**_
- **`AUD-2` and `AUD-4` are two symptoms of ONE root cause — claim `AUD-2` first.** The root is key identity: a source registers its audio component under a key it mints itself (`SDRRadioAudioSource.cs:908` → `sdr-radio-<guid>`) while `AudioManager` addresses it by `IAudioSource.Id` (`AudioSourceBase.cs:28` → `Radio-<guid>`). `AUD-2` sees it from the **ducking/gain** side (setters that write to a key nothing reads back); `AUD-4` sees it from the **teardown** side (a sweep that would miss the SDR component). **`AUD-2` is investigation-first and cheap, and it decides the key**, so doing it first prevents `AUD-4` from engineering a sweep around a mismatch that `AUD-2` may simply remove. _Judgement recorded so it can be revisited rather than rediscovered: they are kept as two rows because `AUD-2` may close as "no defect" after one observation, while `AUD-4`'s rename-and-unify work stands on its own merits either way. If a Builder finds the single-key fix does all the work and `AUD-4`'s remainder is just the rename, merging them is defensible — but say so explicitly rather than quietly._ **⚠ Added 2026-08-11: these two now have SIBLINGS, and the pattern is worth naming.** `AUD-6` and `AUD-7` are the same failure at the **device** layer that `AUD-2` and `AUD-4` are at the **source** layer — *an identifier that is not stable being used as if it were*, then addressed by a second layer that assumes a different key. **Four of the eleven rows in the 2026-08-10 tranche now turn on that one idea.** They are **not** being merged and share no files, but whoever claims any of them should read the other three's traps first: the recurring shape is that **the mismatch fails silently and something logs or reports success anyway** (`SetGainOffset`'s *"Applied gain offset …"*, `RemoveSource`'s *"Removed audio source … from mixer"*, and now `/api/devices/output`'s `isActive`). _Not queued as a fifth "unify all identity" row, deliberately: three of the four still need their own investigation or migration, and a grand unification written before `AUD-2` reports back would be exactly the premature abstraction `03a6fea` already demonstrated._
- **`AUD-6` and `AUD-7` are one report split into two rows, and the ORDER BETWEEN THEM IS LOAD-BEARING — claim `AUD-6` first.** One deploy produced one symptom (*the output the app names is not the output the audio uses*) with **two independent causes**, and they are queued separately on the same test this file already applies to `GV-6`/`GV-8`: different mechanism, different trigger, different fix shape, different verification. `AUD-6` is a **persisted-format** defect — the id is an enumeration ordinal, so the stored preference changes meaning when enumeration reorders; its fix is a stable key **plus a migration over every existing store**, and it is **intermittent** (it needs a reshuffle). `AUD-7` is a **missing-call** defect — the native device switch (`SoundFlowAudioEngine.SwitchPlaybackDevice:838`) has exactly one caller, the interactive HTTP path, and startup never reaches it; its fix is a call plus an honest `isActive`, and it is **deterministic** (every restart where the preference is not the system default, reordering or not). **Merging them would gate a cheap deterministic fix behind a data migration**, which is the opposite of what this file usually optimises for. **⚠ The ordering constraint, which is the reason this bullet exists rather than a one-line cross-reference: `AUD-7`'s fix makes startup ACT on the persisted preference, and acting on an unstable key is strictly worse than ignoring it.** Shipping `AUD-7`'s routing half against today's store would send audio to USB Audio Out because `playback-1` now resolves there — converting a **mis-report** into a **mis-route**. Today's mis-report is, perversely, protective: it falls back to the system default. So either land `AUD-6` first, or land only `AUD-7`'s **reporting** half (make the UI honest) and hold the routing half. **What is NOT a reason to reorder them: that `AUD-7` is the more serious of the two.** It is — and it still goes second. _If a Builder finds the stable-key change and the startup call are genuinely one edit and the split is costing more than it saves, merging them is defensible — but say so explicitly rather than quietly, and do not merge them by simply claiming `AUD-7` first._
- **⚠ A FOURTH AND FIFTH INSTANCE OF THE COMMENT-ACCURACY FAILURE CLASS, found while verifying `AUD-7`. Recorded here, not queued — the fixes belong inside `AUD-7`'s PR, not in a row of their own.** `CLAUDE.md` § Pre-Merge Review enumerates three worked examples of a comment asserting something the code does not do. `AUD-7` supplies two more, and the second is the more instructive. **(4) Production code.** `SoundFlowAudioEngine.cs:222-224` states that *"The local-device switch (native MiniAudio swap) is the caller's responsibility via `IAudioDeviceManager.SetOutputDeviceAsync`"* — **`SoundFlowDeviceManager.SetOutputDeviceAsync` (`:213-244`) performs no native swap at all**; it validates an id against a cache, assigns a string, and persists. The comment does not merely overclaim about itself, it **assigns a responsibility to a named collaborator that the collaborator does not discharge**, which is why every reader of the gate concludes the swap happens somewhere. **(5) A test.** `AudioEngineInitializationServiceStartupTests.cs:81-83` says the persisted local device must route through the gate *"not just the device-manager swap"* — **naming a swap that does not exist** — while the test body (`:97-99`) verifies only the gate call. **This is the one worth generalising: a test comment can carry the same lie as a production comment, and it is more dangerous there,** because a green test reads as evidence. The existing checklist item says to check that comments assert only what the code does; **it is worth extending to test comments and test names, which currently escape it** — `RoutesThroughGate` is an accurate name for what the test asserts, and its comment is not.
- **⚠ Both `AUD-2` and `AUD-4` exist because a log line asserted a success it never verified — treat that as the pattern, not the incident.** `SoundFlowMasterMixer.RemoveSource` logs *"Removed audio source … from mixer"* while removing only a `List<IAudioSource>` entry (`SoundFlowMasterMixer.cs:118`), which is why commit `03a6fea` landed one layer too high and **did nothing for months without anyone noticing**. `SoundFlowPlaybackService.SetGainOffset` logs *"Applied gain offset …"* (`:620`) after a dictionary write whose corresponding lookup may have matched nothing. **Neither log is wrong about what it did; both are wrong about what they imply.** Whoever fixes either should fix the log wording in the same PR — a correct fix behind a misleading log line is one refactor away from being undone again.
- **~~`AUD-3` is 🔒 on PR #468, which is a PR and not a queue row.~~ RESOLVED 2026-08-11 — #468 merged (`8b1ce0a`) and `AUD-3` is ✅ shipped inside it.** The deviation this note recorded (🔒 pointing at a PR rather than a row, because neither 🔒 nor ⛔ fits an in-repo PR pending an owner decision) never had to resolve either way: **the blocker and the fix turned out to be the same PR.** The practical rule it stated — *"if #468 merges, re-site the anchors and flip to 📋"* — was followed except for the mark, which went to ✅ rather than 📋. The re-siting was done and confirms this note's own prediction: `TryConnectCastAsync` is now `AudioEngineInitializationService.cs:499` on `main`. _Kept rather than deleted, because the reasoning behind the deviation is worth more than a tidy-looking legend — the next in-repo-PR blocker should be recorded the same way._
- **⚠ REVERSED 2026-08-11 — this file previously said "Do not read PR #468's merge as closing `AUD-3`," and once #468 was extended that instruction became FALSE. #468's merged diff DOES close `AUD-3`.** The original note was correct about #468 *as it stood on 2026-08-10*: a post-`StartAsync` teardown guard closing the observable outcome and nothing more. The owner then directed the race itself be closed before merge, and commits `47da897` + `1fded0c` did exactly that — `GoogleCastOutput` grew a lifecycle lock plus a connection generation, the resolved receiver moved from a field to a local, and the engine grew `BeginCastConnectAsync`/`TryCommitCastConnectAsync` over an epoch bumped under `_activeOutputLock`. **The replacement rule is the inverse of the old one: "the Cast race is still open" is now the wrong summary, and the right one is "the race is closed in code and has never been exercised on hardware."** The `AUD-3` row carries the evidence and the three things that did *not* ship — the missing hardware UAT above all. _Recorded loudly because a stale "do not read the merge as closing this" is worse than no note at all: it would have kept a shipped fix looking open indefinitely._
- **~~🚧 PR #471 is IN FLIGHT and deliberately has NO row.~~ ✅ MERGED 2026-08-11 as `61187bb` — and the anchor churn this note predicted DID NOT HAPPEN.** `fix/serialize-native-device-enumeration` (native `pa_mainloop` enumeration serialization + playback-device lifecycle gating) landed with `SoundFlowAudioEngine.cs`, `SoundFlowDeviceManager.cs`, `FilePlayerAudioSource.cs`, `USBAudioSourceBase.cs`, `WindowsBluetoothService.cs` and `NowPlayingPanel.razor` touched, plus new `NativeAudioDeviceGate.cs` + `SerializedMiniAudioEngine.cs` and five new test files. **The note above warned that the `8b1ce0a` re-verification on `AUD-1`/`AUD-2`/`AUD-4` was "good until #471 merges and not after." It merged, the anchors were re-checked against `0870410`, and they all hold — so the warning is discharged rather than triggered.** Specifically: **`AUD-1`'s two `FilePlayerAudioSource.cs` anchors survived #471's edits to that very file** — `:1988` is still the `needsFingerprinting` gate and `:2112` is still the overwrite branch, because #471's hunks in that file net out above and below them — and its other six (`FingerprintingOptions.cs:19`, `appsettings.json:91`, `ApiModels.cs:788`, `BluetoothAudioSource.cs:837`/`:845`/`:893`, `BackgroundIdentificationService.cs:260`) are in files #471 never opened. **`AUD-2` and `AUD-4` are entirely unaffected: #471 touched NONE of the files they cite** — `SoundFlowPlaybackService.cs`, `AudioManager.cs`, `SoundFlowMasterMixer.cs`, `SDRRadioAudioSource.cs` and `AudioSourceBase.cs` are all untouched, and every one of their ~20 anchors re-verifies byte-exact. **No row has been invented for what #471 leaves open**, which is the same discipline this note originally recorded — re-triage is deliberate work, not an assumption, and nothing in #471's diff has been read as closing or opening a row without checking. **⚠ One file DID move, and it is not one this note anticipated: `GoogleCastOutput.cs`.** PR #473 (`0870410`) grew its header comment by exactly **+66 lines**, so **every anchor below `:32` in that file shifted +66** — `_lifecycleLock` `:39 → :105`, `_connectionGeneration` `:45 → :111`, the two test seams `:54 → :120` / `:64 → :130`, `TryPublishConnectionAsync` `:599 → :665`, `DisconnectAsync`'s critical section `:643 → :709`, `StopAsync`'s `:930 → :996`, `DisposeAsync`'s `:1651 → :1717`. **`AUD-3`'s citations in that file are all pre-#473 and must be re-sited before reuse** (they are historical record on a ✅ row, so they have deliberately been left as written rather than mass-edited); **`AUD-5`'s are post-#473 and verified.**
- **⚠ #468 and #469 each shipped with ZERO documentation, and this reconciliation pass exists because of it.** Neither merge touched a single file under `docs/` or any `.md` (#467 at least updated `CLAUDE.md`). The consequence was concrete: `BUILDER_QUEUE.md` **and** `ROADMAP.md` both went on asserting *"#468 🔵 open, awaiting an owner decision"* and *"the race is untouched"* after the opposite had become true, and `AUD-3` sat 🔒 against a blocker that had already delivered the fix. **A PR that closes or invalidates a queue row must update that row in the same PR.** Stated here rather than as a row because it is a process correction, not claimable work — but it is the cheapest defect in this tranche to prevent, and it has now cost a session.
- **⚠ THE BUILDER ROLE POINTS AT TWO DOCUMENTS THAT DO NOT EXIST. Recorded here as an ordering note rather than a queue row — the decision and its reasoning are below, because this one is genuinely arguable.** PR #473's Builder found that **`docs/BUILDER_PROMPT.md` has never existed** — not deleted, not moved; there is no commit in this repository that ever created it — and that the Builder role definition additionally references a **"Handoff Protocol at the top of `docs/BUILDER_QUEUE.md`"** which **is also not there**: the top of this file is a *Last updated* banner followed by § Status legend, and no section by that name has ever existed either. **#473's Builder deliberately left both alone, and was right to** — inventing its own operating instructions mid-cycle is exactly the kind of unrequested scope this file keeps warning about. **Why this is NOT a queue row, stated so the decision can be overturned rather than rediscovered.** It would be a docs-only change to this repo, so on its face it is claimable — but the *content* of both documents is **the specification of Builder's own behaviour**, and a Builder dispatched to write it would be authoring the instructions it was dispatched under. That is circular in a way that no amount of review fixes: the reviewer would be checking the loop against the document the loop just wrote. **The precedent is already set twice in this section** — the Playwright-MCP note ("environment/tooling, not product … nothing for a Builder to claim") and the zero-documentation note directly above ("a process correction, not claimable work"). This is the same shape. **⚠ But do not read "not a row" as "not important," because the blast radius here is wider than either precedent.** Every Builder ever dispatched at this project has been pointed at a prompt file that does not exist and told to resume at "step 1 of the Handoff Protocol" that does not exist. **The reason nothing has visibly broken is that each Builder has silently reconstructed the loop from this file's shape** — the § Status legend, the 📋/🚧/✅ marks, and the one-PR-per-cycle convention in the header quote — and they have so far reconstructed it **compatibly**. That is luck holding, not a protocol working, and it is precisely the failure class this repo has now shipped four times: **a document asserting something that is not there** (`SoundFlowMasterMixer`'s log line, `BluetoothAudioSource`'s unreachable `Playing` branch, `GoogleCastOutput._lifecycleLock`'s over-claiming comment, and now the Builder role's own two citations). **The concrete deliverable, so whoever picks it up is not starting from nothing:** (1) a `## Handoff Protocol` section at the top of this file, above § Status legend, stating the claim→branch→implement→review→UAT→docs→PR→merge→loop cycle and the rule for picking the next eligible row; (2) `docs/BUILDER_PROMPT.md` as the paste-trigger that points at it. **Owner or Planner authors both; a Builder may then be pointed at them, but must not be the one to write them.** Until they exist, § Status legend plus this note *are* the protocol, and that is worth saying out loud rather than leaving implicit.
- **For the record — where the pre-merge checklist lives, and why it is in `CLAUDE.md` rather than anywhere more obvious.** PR #473's checklist item (*do comments, log messages and XML docs assert only what the code actually does?*) landed in **`CLAUDE.md` under a new `## Pre-Merge Review` section**. **The location was a deliberate choice among poor options:** this repo has **no `CONTRIBUTING.md` and no PR template**, so there is no conventional home for a standing review convention; and a checklist written into an individual plan **dies with that plan** the moment its cycle closes — which is how the three comment-accuracy defects it enumerates came to be *rediscovered* three separate times rather than caught once. `CLAUDE.md` is the one file every agent in this project reads unprompted, which makes it the only location where the checklist is actually load-bearing. **Noted here because the queue is where cross-cycle conventions get looked for**, and a reviewer hunting for the checklist should not have to guess.
- **`OPS-2` is hygiene and must not be re-labelled as a bug fix.** A floating-package-version theory was investigated on 2026-08-10 as the cause of a CI failure and was **disproved**. Pinning is worth doing for reproducibility; it fixes no known defect, and citing it as the root cause of that CI failure would be wrong.
- **`OPS-3` is the only row in this tranche exempt from the auto-merge-on-green policy.** Switching `Requires=` to `BindsTo=` changes how two production services fail together on the box. Green gates are necessary but not sufficient; the owner confirms the tradeoff (a dark console instead of a UI with a dead backend) on the PR itself.

- **`KIOSK-1` sits above `TEST-1` from 2026-08-18, by the owner's explicit call, and the ordering note above about the 2026-08-10 tranche is unchanged by it.** `TEST-1` is still the head of that tranche; `KIOSK-1` is one row jumping the whole list because it is the only queued defect that fires on **every deploy** — each one leaves the panel dark until someone relaunches the browser by hand. That makes it a tax on every other row's verification rather than a competing priority: several rows in this queue can only be confirmed at the glass, and the glass is dead after a deploy. **This is not a precedent for reshuffling the queue** — Builder read top-down as normal; the promotion was an owner decision recorded before the claim.
- **`KIOSK-1` before `KIOSK-2`, and the reason is mechanical rather than editorial.** `KIOSK-2`'s `Exit to Desktop` distinguishes the kiosk Chrome from the Google Voice bridge Chrome **by profile path**. Until `KIOSK-1` gives the kiosk its own `--user-data-dir`, the only distinguishing feature is the *absence* of the bridge's flag — a negative match the design rejects, and one that would leave the current `pkill -f chrome` behaviour (which kills the bridge, and nothing restarts it on demand — its watchdog is a 2-minute cadence) in place for the interval. The same profile is what `KIOSK-2`'s single-instance `pgrep` guard matches on. `KIOSK-2` is **blocked** until `KIOSK-1` merges.
- **The GV Bridge desktop entry is deleted in `KIOSK-2`, not `KIOSK-1`, deliberately.** Deleting it in `KIOSK-1` would remove the owner's on-demand way to start the bridge before `KIOSK-2` supplies the replacement. The bridge watchdog timer is enabled and active, so the exposure is bounded — but there is no reason to open it at all.
- **Plan Task A0 is a gate.** Four later decisions branch on what the box reports (zenity's GTK major, `--timeout`/`--icon-name`/`--default-cancel` support, `WAYLAND_DISPLAY` in the user manager's environment, the `rotary-phone` unit name). Guessing any of them ships either a dialog that cannot be tapped or a `PHONE` row that is a **permanent false red** — which is precisely the cry-wolf failure the design's Google Voice rule exists to prevent.
- **Two checks in `KIOSK-2` genuinely need the owner's eyes, and that is a property of the box, not a gap in the plan.** Shell's screenshot API and `GetWindows` are `AccessDenied` on GNOME 46 and `gnome-screenshot` / `grim` / `scrot` are absent, so the 56 px button size and the icon's legibility at the panel cannot be measured remotely. Everything else is an objective probe — established-connection counts, process flags, CDP reachability, file modes, and the launcher's own `--print-status` / `--dry-run` / `--dialog-demo` affordances, which exist specifically so tier decisions are verifiable over SSH. **Do not substitute plausibility for measurement on the two that are not.**
- **`radio-refresh-browser` is NOT fixed by these rows and no row claims it is.** `KIOSK-1` restores CDP on `:9223` as a side effect of the dedicated profile, which makes a CDP-based refresh *possible*; the shipped helper still drives `xdotool`, which does not work under Wayland. Recorded as a fast-follow, not folded in — it is a different mechanism with its own verification.
- **`ENC-8` and `ENC-12` were planned in one pass, and the reason is not convenience.** `ENC-12`'s third surface (*“the status card carries the field-level `Sent` vs `Read back` table plus retry / restore”*) and `ENC-8`'s card 1 (*“Status — connection, config tier, last verified, last saved-to-device with a staleness comparison”*) **are the same card, described twice by two rows written at different times.** Planned apart it gets built twice and the second build fights the first. **`ENC-8` owns it — the whole page, all four cards, every action button, every provisioning endpoint. `ENC-12` owns the topbar badge and the notification, and adds nothing to that page.** The two transports differ on purpose and that is not an inconsistency: the page **pulls** at 2 Hz only while it is open (handoff §7.9 and the `UI-2` load rule — a background diagnostic poll on this N100 is exactly the incidental load that correlates with audible distortion), while the badge must be right on `/queue`, so it gets **one push event carrying the tier and nothing else**.
- **Order: `ENC-8` before `ENC-12`, and it is a real ordering rather than a preference.** `ENC-12`'s notification copy ends in an action — Designer §7.6 writes it as *“→ Open encoder settings”*. A toast whose entire payoff is a page that does not exist yet teaches the owner that the link is useless. Build the destination, then the sign that points at it. `ENC-12` also edits the same `HidRotaryEncoderService.ConfigStatus` property that `ENC-8` adds retained state around, so taking them in the other order means resolving that twice.
- **Neither row remaps the router, and `ENC-8` is shaped so that `ENC-5`/`ENC-7` can.** `ENC-8` Task 3 replaces the router's `switch` blocks with **one array that dispatch and display both read**, and serves the Settings page's mapping table from it. The remap then becomes an edit to that array with the page following automatically. ⚠ **If `ENC-5`/`ENC-7` land first, `ENC-8` Task 3 converts their switch into the array rather than adding an array beside it** — a descriptive list kept next to a `switch` is two sources of truth wearing one name, which is the defect the task exists to remove. Note also that the mapping table's *contents* are already correct (#489); what this fixes is the cause, not the symptom.
- **The encoder arc is a chain, not a set — punch list §2 `O5` and `O10`.** `ENC-1` (the decoder rewrite) gates every other encoder row, and `ENC-1` → `ENC-2` → `ENC-11` → the rest is an ordering, not a preference. All four are shipped, which is why **`ENC-4` is claimable now**. What `ENC-4` in turn gates: `ENC-5` (SOURCE overlay) and `ENC-7` (PRESETS) both mount into the HUD host it builds, and `ENC-12` (the config-fault notification) needs somewhere to speak — `ENC-11`'s tiered fault model works and is verified on hardware, but a Degraded or Hard-fault outcome is currently **visible only in the log and the API**. The safety response is real and silent until `ENC-4` lands the surface.
- **`ENC-5` and `ENC-7` should be built together or back to back**, per the design handoff §6.6: they are one component with two lists, on purpose — *learn one, you have learned both*. They are also where the `RotaryEncoderActionRouter` index→handler remap belongs, since they introduce the SOURCE and PRESETS handlers the remap would point at. **`ENC-4` deliberately leaves the router mapping alone** and adds a test pinning it, so the change has to be made on purpose rather than drifted into.
- **⚠ `ENC-4` → `ENC-5` → `ENC-7`, and the ordering is mechanical, not editorial.** `ENC-5` mounts into the seam `ENC-4` §2.5 declares and edits four of its files (`EncoderFeedbackService`, `EncoderHudService`, `EncoderHud.razor`, `IEncoderFeedbackSink`); `ENC-7` consumes the selector component `ENC-5` builds and takes the encoder index `ENC-5` parks. Neither can be claimed before its predecessor merges.
- **⭐ The router remap is split across `ENC-5` and `ENC-7` so no index is ever left without a handler.** `RotaryEncoderActionRouter` maps `0=Volume · 1=Tuning · 2=Source · 3=Visualization`; the cabinet reads `VOLUME · SOURCE · PRESETS · TUNING`, and `ENC-11` has pushed device configuration in the cabinet's order since #509. Index 0 is VOLUME under both, so **the knob with a safety hazard behind it has been correct all along** — indices 1–3 were deliberately left wrong because remapping earlier would have pointed encoder 2 at a PRESETS handler that did not exist. `ENC-5` moves SOURCE to index 1 and TUNING to index 3, retires the old source cycler, and parks the visualiser on index 2; `ENC-7` replaces it with PRESETS. **Leaving the old cycler on index 2 during the interim was rejected**: it would give two adjacent knobs two divergent copies of the source selection, which is the defect class Designer §4.4 spends a paragraph forbidding.
- **Two mapping tests are expected to go red, one per PR, and neither is a regression.** `ENC-4` Task 5 pins the current index→handler table *on purpose* so that changing it has to be deliberate; `ENC-5` Task 8 rewrites it to `(0 VOLUME, 1 SOURCE, 2 VISUALIZER, 3 TUNING)` and `ENC-7` Task 5 changes the third row to `PRESETS`. Each plan names the assertion and forbids chasing the red by reverting the remap. **`ENC-5` also removes a live trap:** `ENC-4` hard-codes the encoder index in each handler's `PublishHud` call, so after a remap the tuning card would render in the SOURCE quarter — the card above the wrong knob, which is the one thing `ENC-4` exists to get right. `ENC-5` threads the real event index through instead.
- **`ENC-5` is where tuning acceleration goes live for the first time.** Device config already pushes the tuning tiers `(150 ×2 / 80 ×4 / 40 ×8)` to encoder 3, which today drives the visualiser; after the remap they reach the tuner, `TuningClamp = 8` stops being theoretical, and `StepRadioFrequencyAsync` awaits **one hardware call per step** — up to eight per detent on a box where incidental load correlates with audible distortion. It is the designed feel (§5.5) and the clamp bounds it, but `ENC-5`'s UAT check H2 exists because this is where it first happens.
- **`ENC-7` removes the visualiser's knob, and `ENC-9` is the row that was supposed to replace it — it is P1 and not queued.** The capability survives today on `VisualizerPanel`'s six-segment picker and the System Config dropdown, both shipped, so `ENC-7` does not block on it. But the punch list's own justification for `ENC-9` is *"removing a knob must not remove a capability"*, and `ENC-7` is the moment the knob goes — so this is the moment to notice `ENC-9` is unqueued rather than after the cabinet is closed.
- **The phone arc is seven PRs, and `PHN-1a` is only the first — do not read the punch list's `PHN-1` row as one claimable item.** The punch list scopes `PHN-1` at 1.5-2 weeks for "the seam + Feature A". That is not one PR, and the pieces carry sharply different risk, so `design/plans/PHN-arc-pr-breakdown.md` sequences it into seven: **1** core contracts (this row, `PHN-1a`) · **2** `GvMediaClient` + bounded LRU cache + API-side auth · **3** `EventPlaybackService` + `POST /api/audio/events` · **4** priority becomes load-bearing · **5** server-owned playback state + the three stop conditions · **6** `PHN-2`, retire the `<audio>` element · **7** Feature C, canned replies. **Only PR 1 is planned.** PRs 2-7 have a scope and an estimate in the breakdown but no plan file, and each needs a Planner pass before it is queued. `O6` (`PHN-1` before or with `PHN-2`/`PHN-3`) is satisfied by claiming this row first.
- **⚠ `PHN-1a` registers nothing in DI, and that is deliberate — but PR 2 is where the DI hazard actually lands.** Checked while planning: `RotaryEncoderRegistrationTests` builds a real `ServiceProvider` and resolves from it, but only over `AddRotaryEncoders`; `ActiveSourceAccessorRegistrationTests` covers `AddSoundFlowAudio` by **descriptor inspection only**; and **no test anywhere calls `BuildServiceProvider` with `ValidateOnBuild` or `ValidateScopes`** — zero matches across `tests/`. So a new service registered in `AudioServiceExtensions.cs` with a missing or mis-scoped dependency would be caught by **nothing**, and would surface as a service that will not start on the appliance. `PHN-1a` is exempt because it adds no registration and changes no constructor signature; **PR 2 and PR 3 must each add a build-and-resolve guard test** under `tests/Radio.Infrastructure.Tests/DependencyInjection/`.
- **⚠ Carried into PR 2 from `PHN-1a`'s planning pass: ADR-029 §5.1's masking instruction is wrong about its own example.** It tells `GvMediaClient` to follow `PhoneContactLookupService`'s *"log-masking discipline (it masks numbers to `***1234`)"*. That file masks on exactly **one** line — the success-path debug at `:87-90` — and logs the **raw** number at `:78` and again in the REST catch at `:100`. The rule for `GvMediaClient` is **mask on every line**, voicemail ids included; copying the cited file verbatim would leak.

## Carried risks (baked into the plans as explicit tasks)

1. **Absolute audio URL** — the DTO `AudioUrl` is server-relative and resolves against the Web origin (`:5002`), 404ing. `GvBridgeApiService.GetVoicemailAudioUrl` rebuilds it absolute against `radio:5004`. Unit-tested in GV-1; the most likely silent-failure point.
2. **GV SMS ≠ trunk SMS** — the GV handler is `GvSmsReceived` on `PhoneHubService`/`/hub`; the pre-existing `GvTrunkHubService.SmsReceived` on `/hubs/gvtrunk` is a different product. Kept namespaced apart; cross-referenced in code comments (GV-1).
3. **Open thread to RotaryPhone** (GV-3 deliverable): request GV mark-read be pulled forward (decision 4) — ✅ **closed**, shipped as GV-4; keep the voicemail audio endpoint unauthenticated/token-in-query when `X-RotaryPhone-Auth` ships (native `<audio>` can't send the header) — **still open**; and confirm `SendSmsResponse` before wiring send — ✅ **closed by GV-5/ADR-028**, which read the as-built controller directly rather than their docs (several RotaryPhone-side handoff docs are stale and still claim the send endpoint does not exist).
4. **Trust their source over their docs for gvbridge contracts.** `D:/prj/RotaryPhone/docs/handoffs/radioconsole-gv-voicemail-sms-ui-handoff.md` still states "No `POST /api/gvbridge/sms/send` endpoint exists yet." It does. ADR-028 was written against `GvSmsController.cs` / `GvBridgeReadDtos.cs` / `GvThreadPoller.cs` directly; do the same for any future contract work.

## Cross-repo handoffs (RotaryPhone — NOT claimable here)

> These live in `D:\prj\RotaryPhone` and are **not** Radio Console queue rows. Routed via the documented protocol (boundary doc § "Passing Work Between Sessions" → create a file in `D:\prj\RotaryPhone\docs\prompts\`). Request files: **`D:/prj/RotaryPhone/docs/prompts/radioconsole-cdp-spam-and-build-stamp-request.md`** (items 1-2) and **`D:/prj/RotaryPhone/docs/prompts/radioconsole-gv-threadid-decode-and-auth-blackout-request.md`** (items 5-6).
>
> The boundary doc's Change Log was deliberately **not** touched — it is scoped to BT/audio ownership, and neither item is a BT/audio boundary change. The prompts channel is the right lane.

1. **`CdpCookieExtractor` log spam — `CDP: Cannot reach Chrome on port 9224` every ~20 min with a ~20-line stack trace.** Non-fatal (cookies stay valid via another path), but per project memory heavy journald churn on the Intel N100 **competes with the audio pipeline and correlates with audio distortion** — so this is a performance issue wearing a log-noise costume. **Concrete finding to hand them:** `GVBridgeConfig.ChromeCdpPort` defaults to **9224** (`GVBridgeConfig.cs:23`) and the extractor expects a Chrome with a live Google Voice tab; the only Chrome our deploy launches on that box is the **kiosk on port 9223** pointed at `http://localhost:5002` (`Deploy-ToLinux.ps1:363`) — a different instance for a different purpose. So nothing is listening on 9224 unless someone separately starts a GV-session Chrome. **May be absorbed into a larger bug** — a Tester is investigating whether this same CDP failure explains GV read endpoints returning **empty lists while reporting healthy**; see the request file for the causal chain.
2. **Build stamp for `rotary-phone`.** The 2026-07-29 incident (stale `rotary-phone` binary after a deploy restarted only `radio-api`/`radio-web`) is the same class of failure OPS-1 fixes on our side. Recommended: mirror our approach — `SourceRevisionId` → `AssemblyInformationalVersion` → a `/version` endpoint → deploy-time verification. ~~**Open question:** which tree is authoritative.~~ **✅ SETTLED 2026-07-31 — the deployed tree is `D:\prj\rp-deploy` @ `0a86898`, NOT `D:\prj\RotaryPhone`.** Confirmed on the live box by a Tester. This has real blast radius on our side: **ADR-028 was derived by reading `D:\prj\RotaryPhone`**, so GV-5 is now 🔒 blocked pending re-derivation. It also raises the priority of the stamp — source-tree parity is not deployed-binary parity, and until a stamp exists nothing proves the running binary came from `0a86898` either.

3. ~~**GV read has NEVER worked — positional parser defect (`rp-deploy`).**~~ ✅ **STALE — CORRECTED 2026-09-01 (`XR-1a`). Do NOT file a request for this; there is no cross-repo defect here.** The owner, answering D17: *"I can see voicemail and text messages in the Radio Console UI today. I can listen to the voicemail and read the texts on the screen."* **GV read works.** The original finding — that `PositionalGvThreadParser.ThreadsArray()` required a JSON object root while `alt=protojson` returns arrays, yielding 0 items behind a clean HTTP 200 — was accurate when written on 2026-07-31 and has since been fixed upstream. It is kept here struck through rather than deleted because the *shape* of the bug is worth remembering: `Succeeded: true` meant only "the JSON parsed", never "data was returned", so the failure was invisible to every caller. ⚠ Left uncorrected, this entry would send the next session chasing a defect that no longer exists, in a repo it does not own.

4. **Configured GV number does not match the live session.** The `GvPhoneNumber` value in RotaryPhone's config is **a different number** from the one the owner's live Google Voice session is actually bound to. _(Both values are deliberately **not recorded here** — this is a public repo. The configured value is in the box's `GVBridge` config; the live value is visible in the Google Voice session. Compare them there.)_ Independent of the parser defect — and note that **nothing validates configured identity against the live session**, so a mismatch fails silently. Two distinct faults could each present as "empty lists, healthy status," so this should be corrected *before* the parser fix is evaluated, or the fix will be assessed against a confounded baseline.

5. **Thread ids containing `/` are never decoded → HTTP 200 with `messages: []`; every group/MMS conversation is permanently unreadable.** **Confirmed by direct reproduction** in a verified-healthy window, 2026-07-31. We call with `Uri.EscapeDataString(threadId)`; **Kestrel deliberately leaves `%2F` encoded in the path** so it cannot forge a segment boundary, so their route value keeps the literal `%2F` while `%20` decodes normally — visible in their own log line: `Listed 0 SMS for thread g.Group Message.d5Mri%2FNrDUQgXNXNQehOfw (of 149 parsed)`. `GvSmsClient.ListMessagesAsync` then exact-string-compares (`all.Where(m => m.ThreadId == threadId)`), matches nothing, and `GvSmsController.GetThreadMessages` returns **200 + empty**. **Framing correction worth carrying: the predicate is "the thread id contains `/`", NOT "the thread is MMS."** GV group threads are `g.Group Message.<base64url>` and the base64url alphabet includes `/`; group threads merely *happen* to be the MMS threads, which is why the symptom looked MMS-shaped in the UAT. **Ask:** decode the route value in **both** `GetThreadMessages` **and** `MarkThreadRead` (`Uri.UnescapeDataString`), or move the id to a query parameter — **plus a per-thread sanity check**, because *their existing honest-status guards structurally cannot catch this*: `ShapeIsSane` and the `Succeeded` flag both pass, since the fetch and the parse genuinely succeeded and only the **filter** matched nothing. **We cannot fix it here:** double-escaping (`%252F`) still yields 0 messages, and a raw `/` misses the API route entirely and falls through to their SPA fallback, returning `index.html` with HTTP 200. **Ask them to do this one first** — it is cheap, and until it lands group conversations stay unreadable even in a perfectly healthy window, which also confounds any test of item 6.

6. **GV PSIDTS staleness → a deterministic ~9-minute auth blackout every 20 minutes.** **Confirmed from server logs**, 2026-07-31: `api2thread/list returned Unauthorized for folder Sms` **271 times in one day**, in a clean 20-minute square wave, surfacing to us as **HTTP 502**. Mechanism captured at a boundary: a `CDP cookie refresh` at 15:00:02 restores service and the next `Unauthorized` lands **11m42s later** — **Google's PSIDTS is good for ~11 minutes, their refresh fires every ~20, and there is no reactive refresh on 401.** **11 of 11** of our 502s fall inside a dead window. **Throttling is FALSIFIED three ways** — this was the UAT's own first guess, so it is worth stating plainly: our constant-rate 60-second poller shows the identical on/off pattern, so **failure tracks wall-clock, not request volume**; the status is **401, never 429**; and recovery lands on fixed 20-minute boundaries rather than after a variable cooldown. **Concrete lead to hand them:** deployed `appsettings*.json` declares **`CookieRefreshIntervalMinutes: 5`** (matching the default at `GVBridgeConfig.cs:23`) while the **observed** cadence is 20 minutes — so either the value is not being read or something downstream sets its own interval; a genuine 5-minute refresh would make most of this disappear. **Ask:** align the interval with the ~11-minute observed lifetime, add **refresh-and-retry on the first 401** (a purely time-based fix stays fragile if Google shortens the lifetime), and **make `/api/gvbridge/status` honest** — measured at 15:13:03 while both SMS endpoints were returning 502, it still reported `{"available":true,"cookiesValid":true,"degraded":false}`, which is why our **"Google Voice is reconnecting"** banner (`PhoneMessagesPanel.razor:14-20`) never fires during the exact window it exists for. _Same lesson as `Succeeded` meaning only "the JSON parsed": a health field derived from a probe rather than from "did the last real call return data" will report healthy straight through an outage._ **Our GV-8 does not wait on this** — theirs makes the failure rare, ours makes it honest.

7. **Two boundary-doc `Change Log` entries are sitting UNCOMMITTED in the RotaryPhone repo — including the one that established the rule they are both breaking.** Verified 2026-08-10: `D:\prj\RotaryPhone` is on branch **`diag/gv-srtp-receive`** with `docs/prompts/RADIO-CONSOLE-BT-AUDIO-BOUNDARY.md` modified, **1 file / +2 lines**, both additions to the Change Log table. **Deliberately NOT a Radio Console queue row** — there is nothing to build here; the fix is a commit in the other repo, per that doc's own § "Passing Work Between Sessions." **(a) The 2026-08-10 entry — a shared-system change that reaches RotaryPhone.** It records that **`systemd-coredump` (255.4-1ubuntu8.16) now handles core dumps machine-wide** on `192.168.86.50`, replacing `apport`. The motivation was ours: `core_pattern` piped to apport, which retains exactly **one** `.crash` per executable, so **two `radio-api` SIGABRTs on 2026-08-10 (16:16 and 16:42) produced a single file — the second silently overwrote the first.** `apport-core-dump-handler` was APT-removed to satisfy apport's alternative dependency; the `apport` package itself remains. **Consequence for RotaryPhone: their crashes now land in `coredumpctl` / `/var/lib/systemd/coredump/` (zstd-compressed) instead of `/var/crash/*.crash`** — strictly more forensic data, not less (`coredumpctl list` / `info <exe>` / `debug <exe>`). Retention is bounded in `/etc/systemd/coredump.conf` (`Compress=yes`, `ProcessSizeMax=2G`, `ExternalSizeMax=2G`, `MaxUse=2G`, `KeepFree=10G`). **Action required of them: none** — unless some RotaryPhone tooling greps `/var/crash/`, in which case switch it to `coredumpctl`. Verified at install time that `rotary-phone.service` stayed `active (running)` with MainPID unchanged and `NRestarts=0`. **(b) ⚠ The 2026-07-16 entry is uncommitted TOO — and it is the precedent.** That entry (the IAC "save" that relocated the boundary-owned WirePlumber rules into `deploy/common/`) is the one that wrote down *"this Change Log edit lives in the RotaryPhone repo and must be committed there separately — it is NOT part of the Radio Console PR."* **It was never itself committed.** So this is not one missed commit but the **second consecutive miss of the protocol the entry documents**, which is the part worth acting on: the boundary doc is the cross-service contract, and an uncommitted contract is invisible to the other session by construction. **Ask:** commit both rows in `D:\prj\RotaryPhone` (they belong on that repo's own branch/PR flow, not ours), and consider whether the "commit separately" step needs to become a checklist item in § "Passing Work Between Sessions" rather than a note inside individual rows.

8. **Record Radio Console's kiosk launcher as a SECOND CONSUMER of `gv-bridge-ensure.sh`.** `KIOSK-2` adds a caller that depends on that script's **path** and its **exit code** — it runs it to repair a down bridge and reads the result to decide whether the `VOICE` row is amber. It does **not** own, reimplement, edit, install or `pkill` any part of bridge startup; the watchdog timer and the nightly restart timer stay entirely RotaryPhone's. **Why this matters now rather than later: a Builder in `D:\prj\RotaryPhone` is bringing that script and its timers under version control as this is written**, and a relocation would silently break the new consumer. `KIOSK-2` therefore resolves the script from a candidate list (`~/bin/` → `/usr/local/bin/` → `/opt/rotary-phone/bin/`) rather than hard-coding one path, so a move degrades to a reported failure instead of a wrong one. **The ask is one Change Log entry** in `RADIO-CONSOLE-BT-AUDIO-BOUNDARY.md` naming the new consumer and pinning the contract as *path + exit code*. Per the 2026-08-10 precedent this is a commit in the other repo, so it is not claimable here — **and note that entry also recorded two boundary-doc Change Log edits sitting uncommitted in that repo, one of which established the very rule they were breaking, so this is a recurring miss of the protocol rather than a one-off.** Also worth passing along, already handled on the box today and needing no work from us: `gv-bridge-ensure.sh` now carries `--remote-debugging-port=9224 --remote-allow-origins=*`, whose absence was breaking cookie extraction and producing empty SMS/voicemail lists.

## Documented fast-follows (NOT in these PRs)

- **Voicemail Call back / Text back quick actions** (decision 3) — deferred; markers left in the player.
- **GV mark-read / durable read-state** — ✅ **SHIPPED as GV-4 (PR #441)**, flag-OFF. Durable via GV write-through per ADR-024; Google is single source of truth, no local store. `MarkVoicemailReadAsync` (repointed) + `MarkSmsThreadReadAsync` + the unified `ReadStateChanged` SignalR event on `/hub`, reconciled idempotently by `(id-or-threadId + isRead)`.
  - **Remaining work is a config flip, not a build.** RotaryPhone's side is **shipped, not owner-HELD**: `GvVoicemailController.MarkRead`, `GvSmsController.MarkThreadRead`, `ReadStateChangedDto` + unconditional broadcast all exist and their routes match our call sites verbatim. Both sides are dark by config: theirs `GVBridge:EnableMarkRead=false`, ours `RotaryPhone:Gv:MarkReadEnabled=false`. **Flip theirs first, confirm the routes stop returning the dark rejection, then flip ours** in `appsettings.Production.json` (deploy overwrites `appsettings.json`).
  - **Unread support** (informational) — their `GVBridge:AllowMarkUnread` is also `false`, so `isRead:false` returns `400 unread_unsupported`. Our UI never sends `false` and the toggle stays hidden (ADR-024 §6). No change needed on either side unless unread is wanted.
  - **Path (b), phone→kiosk LIVE push** (informational) — their poller-flip fast-follow. Until it lands, externally-originated reads reconcile on our next list refresh rather than instantly. Same `ReadStateChanged` handler covers both; no RadioConsole-side change when it ships.
- **SMS send contract reconciliation** — ✅ **SCOPED AND QUEUED as GV-5** ([ADR-028](../design/decisions/2026-07-30-gv-sms-send-contract.md)). The earlier note here framed this as a response-shape mismatch; investigating the as-built endpoint found it is **larger on three axes**:
  - **The request is wrong and fails 100% of sends.** Ours is `SendSmsRequest(string ThreadId, string Text)`; theirs is `SendSmsRequest(string ToNumber, string Text, string? ThreadId, string? ClientCorrelationId = null)`. We POST `{"threadId":…,"text":…}`; `ToNumber` binds **`null`**, their `PhoneNumberNormalizer.TryNormalize(null)` returns false, and the route returns **`400 invalid_number`** — in both reply and new-recipient mode. Send was never functional, only silent.
  - **The taxonomy is nine codes, not eight.** `invalid_text` (400) was missing from the list previously recorded here. Full set: `queued` (200) | `send_disabled` (409) | `rate_limited` (429) | `invalid_text` (400) | `invalid_number` (400) | `auth_unavailable` (502) | `upstream_error` (502) | `timeout` (504) | `error` (500).
  - **We never subscribe to the outbound echo channel.** Outbound messages are broadcast on **`SmsSent`**, never on `SmsReceived`. `PhoneHubService` does not handle it, so GV-3's optimistic→confirmed de-dupe in `PhonePage.OnGvSmsReceived` is **unreachable dead code** and the optimistic bubble would linger un-reconciled. RotaryPhone's poller also re-surfaces the message later with a **different `Id`** by construction, so exact-id matching cannot be the only tier — their own source calls the fuzzy `(text + counterparty + ≤120s)` match "REQUIRED, not optional."
  - **Their `EnableSmsSend=false` is not a blocker.** A dark server returns `409 send_disabled` with no GV call; GV-5 treats that as an **availability state, not a failed send** (no red bubble, no Retry — nothing was sent). Flipping `RotaryPhone:Gv:SendEnabled` remains a separate, later two-side decision; GV-5 ships with it `false`.
- **Audible new-text chime** — belongs in the Radio.API audio layer (ducking-aware), not Blazor (handoff).
- **`NowPlayingPanel` volume-debounce tests carry `TEST-4`'s exact defect, and closing it needs a production seam — so it is filed here rather than folded into `TEST-4`'s PR.** Found while fixing `TEST-4`; **not fixed**, and deliberately not fixed, because the two PRs would not have been reviewable together. `TEST-4` was test-only; this one is not. Three tests in `tests/Radio.Web.Tests/Components/Shared/NowPlayingPanelVolumeDebounceTests.cs` — `VolumeDrag_PersistsThePreferenceOnce` (`:129`), `VolumeDrag_PersistsTheFinalValue` (`:165`, the one the `OPS-5` Builder saw fail under whole-solution contention) and `SeparatedVolumeChanges_EachPersist` (`:192`) — sequence with `await Task.Delay(1500)` against `NowPlayingPanel`'s own hardcoded 300 ms `System.Threading.Timer` (`src/Radio.Web/Components/Shared/NowPlayingPanel.razor:1054-1078`), with no rendezvous. **Be precise about which assertion is at risk:** `Assert.Equal(88d, pending)` is *safe* — `_pendingVolumePreference` is assigned synchronously inside `QueueVolumePreferenceSave` before the awaited call returns, so starvation cannot break it. It is `Assert.Equal(1, CountConfigWrites(handler))` that is unsafe, and it fails in **both** directions: **undershoot to 0** if the timer callback plus the *two* HTTP hops it makes (`ConfigurationApiService.cs:83-97` does a GET before the POST) do not all drain inside the 1500 ms; and **overshoot to 2** if a stall inserts >300 ms between the un-slept setup invokes, re-arming the trailing edge. The overshoot mode is the one `TEST-4` did not have. **Lower flake rate, same shape** — the margin is 5x (1500/300) rather than 3x on a 20 ms poll. **The fix needs a seam that does not exist:** `VolumePreferenceDebounce` is `private static readonly` and the timer is a raw `System.Threading.Timer`, so `NowPlayingPanel` must take an injectable `TimeProvider` first. The house idiom is already in `Radio.Web` (`EncoderHudService.cs:32,45-48`) and `FakeTimeProvider` is already used in this same test project (`EncoderHudServiceTests`, `SleepTests`). ⚠ Advancing a fake clock is **not sufficient on its own** — the callback is `async void` over two awaited HTTP hops, so the fix still needs a completion rendezvous before asserting on the write count. Sibling `VolumeDrag_StillAppliesEveryTickToTheAudioEngine` (`:150`) is genuinely safe and needs no change. The panel carries a second hardcoded timer with the same exposure (`_gainDebounceTimer`, 200 ms, `:891-909`) — worth pulling into the same seam. **Planner: this is a row, not a note.**
