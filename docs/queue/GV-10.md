# GV-10 — ASSESSED AGAINST `D31` 2026-09-05 — content unchanged, ARGUMENT STRENGTHENED, tier deliberately NOT promoted, and its priority within the GV set goes from LOWEST to HIGHEST.

> Queue dossier for row **`GV-10`** of [`BUILDER_QUEUE.md`](../BUILDER_QUEUE.md).
> The detail below was moved verbatim out of that row's Item cell on 2026-09-06; only
> whitespace, the table's `\|` escapes and docs-relative link prefixes changed.

| Field | Value |
|---|---|
| Status | 📋 |
| Plan | _plan TBD (investigate first; scope depends on the answer)_ |
| Spec / handoff | [UAT F-5](../uat/2026-07-31-gv-live-data/REPORT.md) · [root-cause § Scope-affecting notes](../uat/2026-07-31-gv-live-data/F-1-DIAGNOSIS.md) |
| Depends on | **GV-3** (texts surface). _Lowest priority of the three UAT rows; do not schedule ahead of GV-8._ |
| Branch | `fix/gv-texts-bubble-full-body` |

## Detail

⬆ **ASSESSED AGAINST `D31` 2026-09-05 — content unchanged, ARGUMENT STRENGTHENED, tier deliberately NOT promoted, and its priority within the GV set goes from LOWEST to HIGHEST.**

⭐ If the console can only ever *read* texts, rendering the whole message is not polish — it is the feature, and *"it's done, except it may silently truncate the texts it shows you"* fires the P1 honest-sentence test cleanly.

⛔ **It stays P2 anyway, because the row is UNPROVEN** — one bubble ending in a literal `...`, with a plausible **upstream** mechanism.

**Promoting an unconfirmed row on a strengthened premise is tiering a hypothesis**, which is the failure `AUD-6`'s parked row exists to record.

⬆ **It moves to P1 the moment the confirming `curl` shows the truncation is OURS**, and becomes a cross-repo note if it is theirs.

⚠ **The line below saying *"do not schedule ahead of GV-8"* is spent — `GV-8` shipped as #461.**

**Run this row's one confirming `curl` before anything else in the GV set**; it is the only one that might be a content defect rather than polish.

**Confirm-or-close: do conversation bubbles render the list snippet instead of the full message body?** UAT **F-5** (LOW) — **UNPROVEN. This row may legitimately close with no code change.** In the `32665` conversation a bubble ended in a literal `...` matching the truncated list preview, suggesting the conversation view renders the snippet rather than the full body.

**Plausible mechanism, explicitly not proven:** RotaryPhone derives per-thread messages by **filtering the whole SMS folder list** (`GvSmsClient.ListMessagesAsync`) rather than calling a per-thread Google endpoint, and folder-list entries carry **snippets**, not full bodies.

**If that is the cause it is their data, not our rendering, and this row converts into a cross-repo note rather than a Radio Console change.**

**First task is confirmation, not repair:** one `curl` for a known long message, compared against the same message in Google Voice.

**Two disciplines the confirming call must respect:** (1) run it inside a **healthy window** — see the 20-minute blackout note in § Dependency / ordering notes — or a 502 will masquerade as a result; (2) use a **non-group** thread (e.g. `t.32665`, the original repro) until RotaryPhone's `%2F` decode lands, since group threads return 0 messages for an entirely unrelated reason. _Provenance: the diagnosis recommended waiting for their decode fix before testing. We are **not** hard-blocking on it, because the reproducing thread is not a group thread and is curl-able today — recording the deviation rather than silently departing from the source doc._
