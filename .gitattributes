# Default: auto-detect and normalize line endings
* text=auto

# Shell scripts must always use LF (Unix) line endings
*.sh text eol=lf

# PowerShell can use either, but keep CRLF for Windows dev
*.ps1 text eol=crlf
